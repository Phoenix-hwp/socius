---
description: 读取历史对话流程：按日期时段检索并展示摘要，支持日常与项目双源
alwaysApply: false
glossary:
  layer: 3-工作流执行层
  module: Conversation
  depends: [mod-conversation-framework, flow-conversation-routing]
guard_replaceable: false
Lifecycle: long
---

# 读取历史对话流程

## 触发

- 主指令：`读取 X年X月X日，上午/下午/晚上的对话`
- 别名：`查对话`

口语兼容
- `读取4。
3日下午的对话`（同年份可省略年份）
- `继续X月X日的对话`（缺少时段时先追问）

---

## 执行步骤（固定四步）

### 步骤 1：时间解

1. **年份处理**：若未写年份，默认当前年份（仅同年可省略
2. **时段处理**
   - 上午 14:00
   - 下午4:00 - 19:59
   - 晚上= 20:00
3. **缺失追问**：若口语指令缺少时段，先追问"上午、下午还是晚上？"

### 步骤 2：索引检索（双源

`flow-conversation-routing.mdc` 判断归属后，检索对应索引：

| 归属 | 检索来| 说明 |
|:---|:---|:---|
| 无归| `Daily-Backups/日常备份索引.md` | 按日时段匹配 |
| 有项目归| `<P>/对话备份/` 目录 Glob | 按文件名日期+时段匹配 |

#### 检索结果处

- **命中 1 *：直接进入步。
3
- **命中多条**：展示主题列表，请用户回复编号选择
- **未命*：明确告知，给出相邻时段建议

### 步骤 3：明细读

读取选中的明细文件，提取
- 核心结论
- 未完成事
- 待确认事
- 下轮衔接点（如有

### 步骤 4：摘要展

回执格式（三段）

```
【主题】XXX
【时间】YYYY-MM-DD 时段

=== 核心结论 ===
...

=== 未完待确===
...

=== 下轮衔接===
...
```

---

## 多条命中处理

当同日期同时段命中多条记录时

1. 展示编号清单~n
2. 每行显示：编+ 主题 + 时间
3. 请用户只回复数字编号
4. 选定后进入步。
3

---

## 未命中处

明确告知
- "未找YYYY-MM-DD 时段 的备份记
- 给出建议：相邻时/ 相邻日期

---

## 参考文

- 统一框架定义：`mod-conversation-framework.mdc`
- 归属判断：`flow-conversation-routing.mdc`
