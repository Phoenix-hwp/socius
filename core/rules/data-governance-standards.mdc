---
description: 数据治理宪法：JSON 不存长文本 + 单一权威源 + 格式一致性第一公民。Guard 轨道上 JSON/JSONL 格式校验由 SchemaValidator 自动执行
alwaysApply: true
severity: safety
task_triggers:
  - data_write
  - data_create
  - data_modify
requires:
  - script-coding-constraints.mdc
  - pre-change-impact-enumeration.mdc
Lifecycle: 长期
Created: 2026-05-13
Updated: 2026-05-26
glossary:
  layer: 横切
  purpose: 数据治理宪法 设计原则层（不可代码化）+ 格式校验层（Guard SchemaValidator 自动执行
  downstream:
    - script-coding-constraints.mdc
    - pre-change-impact-enumeration.mdc
    - kernel-runtime.mdc
  guard_implemented_in: "guard/src/p008/constraint_applier.py (SchemaValidator)"
guard_replaceable: partial
---
description: 数据治理宪法：JSON 不存长文本 + 单一权威源 + 格式一致性第一公民。Guard 轨道上 JSON/JSONL 格式校验由 SchemaValidator 自动执行

# 数据治理宪法

> **Guard 轨道**：原。
3 中的 JSON/JSONL schema 校验`SchemaValidator.validate()` 自动执行，不再依Agent 自觉。原1 2JSON 不存长文"单一权威）为设计原则，无法代码化

## 适用范围

凡对仓库中以下数据对象做**新增或实质修*，须遵守本宪法三条原则：
- `.json`（配置、注册表、索引、任务追踪）
- `.jsonl`（日志、关联数据）
- `.md` / `.mdc`（知识正文、规则、备忘、文档）
- `.txt`（白名单、一次性说明）

---

## 原则 1：JSON 不存人类阅读的长文本（不可代码化

- **JSON / JSONL 中任何字符串字段，若超过 50 个中文字或超过一句话的长度，必须外迁到独`.md` 文件**，JSON 仅保留短标识 + 文件指针
- JSON 数组字段仅存**原子*（单个关键词、标签名、ID），不存句子
- JSON 正文字段（如 `body`、`content`、`description`）不超过 50 字；超过则引`.md` 路径

> 机器元数据豁免见 §附录 1.1

---

## 原则 2：一份数据只有一个权威源（不可代码化

- **任何数据只在仓库中一处定*。索引文件只存指针，不存正文
- **可运行时计算的数据不持久*。派生数据、自动生成结果、中间缓存不Git
- 禁止同一份数据的两个副本同时存在（含重复目录、重复行、镜像文件）

---

## 原则 3：格式一致性是第一公民

1. **同一文件内所有行共享同一字段集合**。可选字段用 `null` 表示不存在；不创建使用不同键集合的变体类型
2. **所有脚本生成的 JSON/JSONL 必须通过 schema 验证**后才写入磁盘*Guard 轨道上由 `SchemaValidator.validate()` 自动执行*
3. **`.md` frontmatter 字段命名统一**：全库统一使用**首字母大* `Title`/`Created`/`Updated`/`Lifecycle`（已2026-05-16 完成）
4. **JSONL 行式存储**：每行必须是合法的单JSON（不含换行符 `\n`），不跨行

### 违规案例（审计发现，已全部修复）

| 文件 | 问题 | 修复日期 |
|:---|:---|:---|
| `Behavior-Fit-Log.jsonl` | legacy/compact 两种格式字段集合不同 | 2026-05-15 |
| `Round-Behavior-Log.jsonl` | hot/warm 字段零交| 2026-05-15（已拆分|
| `.md` 文件 frontmatter | `title`/`Title` 混用 | 2026-05-16 |

---

## 原则 1-bis：按读者分层（2026-05-26 新增）

**系统内部定义不再使用 .md 承载。.md 仅面向人类读者——人类在这个系统里的阅读需求极少。**

| 读者 | 优先格式 | 示例 |
|:---|:---|:---|
| **LLM / Agent** | JSON | 实体说明书（`core/knowledge/entities/*.json`）、索引、激活元数据、配置、注册表 |
| **Guard** | `.py`（直接 import）| P008 维度枚举、FSM 状态、dataclass |
| **人类用户** | `.md`（Markdown）| 知识脑协议正文、用户创建的规则/工作流、产品说明 |

- 面向 Agent 的系统内部定义不写 `.md`——`.md` 对 LLM 的解析开销高于等价的 `.json`
- 当某实体既需人类阅读又需 Agent 解析时，人类版放独立用户文档，Agent 版放 `.json`；两者不互相包裹
- 原有原则 1（JSON 不存长文本 → 外迁 .md）仅适用于人类读者场景；面向 Agent 的数据优先用 JSON 自包含

## 原则 3-bis：LLM 检索优先（2026-05-26 新增）

**面向 Agent 的数据文件设计以 LLM 检索效率为第一优先级。**

- 一份 JSON 文件应**自包含**——LLM 读一个文件就能完整理解一个实体，不需要跳转
- 索引文件仅存指针和最小检索信息——LLM 先用索引定位文件，再读完整说明书
- 高频检索字段（`cp_type`、`task_types`、`concept_anchor`、`type_id`）放在 JSON 顶层——不嵌套，方便 LLM 第一眼提取
- 实体说明书放在 `core/knowledge/entities/` 目录，索引放在 `core/knowledge/entity-index.json`

---

## 格式分配

| 格式 | 适用场景 | 禁止 |
|:---|:---|:---|
| **JSON** | 配置、注册表、索引、实体说明书（Agent 主格式）| 不存人类散文长文本（>50 字，除非为 Agent 自包含设计）。机器元数据豁免 §1.1 |
| **JSONL** | 行为日志、关联数行式追加，固schema，脚本写 | 不混用多schema 变体、不存可运行时计算的数据、不跨行 |
| **`.md` / `.mdc`** | **仅面向人类**：知识脑协议正文、用户规则、备忘 | 系统内部定义不写 .md（原则 1-bis）；不 JSON 包裹完整 Markdown |
| **`.txt`** | 白名单、一次性说明 | 不替 `.md` 的知识沉淀功能 |

---

## 写入自检清单

| # | 检查项 | 执行方式 |
|:---:|:---|:---|
| 1 | JSON/JSONL 格式合规（schema 校验、字段集合一致、单JSON）| **Guard 自动**（`SchemaValidator`）|
| 2 | JSON 字符串字段是否人类散文 >50 字？| Agent 判断——若为 Agent 自包含设计（LLM 检索优先，原则 3-bis）则 50 字限制不适用。**机器元数据豁免 §1.1** |
| 3 | 数据是否已在他处存在？（原则 2：单一权威源） | Agent 判断 |
| 4 | 是否为可运行时计算的数据？（不应持久化） | Agent 判断 |
| 5 | **新增 JSON 是否为 Agent 读的实体说明书？**（原则 1-bis）| Agent 判断——Agent 用 JSON 自包含；人类用 .md。不混用 |
| 6 | **字段是否放在顶层方便 LLM 检索？**（原则 3-bis）| Agent 判断——高频字段不嵌套，索引文件只存指针 |

---

## §附录 1.1：结构化机器元数据豁免（2026-05-23 修订

> **背景**：TEP 流水线（任务分解→排期→待办）引入了 LLM 自动生成的子任务元数据字段（`method`、`input_from`、`output_to`、`deadline`、`planned_completion`、`estimated_minutes`、`tool_lock` 等）。这些字段是**机器间通信结构化数*——由 LLM#2 写入、连载恢复时Agent 程序化读取——人类不直接阅读 JSON 原文来理解这些字段，而是通过"待办"弹窗的格式化展示来消费。与 `description`/`next_action` 等人类散文有本质区别

**豁免条件**（三条缺一不可）：

| 条件 | 说明 |
|:---|:---|
| **schema 已声* | 字段必须在对JSON 文件`meta` 块中显式声明其语义（`Pending-Plan-Tracker.json` `meta.pending_fields`|
| **机器间通信** | 写入端是系统内部模块（TEP/LLM#2/Agent），读取端也是系统内部模块（连载恢复/待办弹窗），**非人类直接从 JSON 原文阅读理解** |
| **结构化程序数* | 字段内容是结构化数据（时间、路径、数组、短声明）而非叙述性散文段|

**豁免字段示例**（`Pending-Plan-Tracker.json`）：

| 字段 | 类型 | 用|
|:---|:---|:---|
| `method` | `string` | 锁定方法声明——一句话核心方法，连载恢复时注入上下|
| `input_from` | `array[{from_task, asset}]` | 上游依赖声明 |
| `output_to` | `string` | 产出物路径或|
| `deadline` | `string`（日期） | 最终截止日|
| `planned_completion` | `string`（日期） | TEP 计划完成|
| `estimated_minutes` | `number` | 预估耗时 |
| `tool_lock` | `string` | 锁定工具关键|

**约束（防滥用*

- 凡豁免字段，写入自检清单 #20 字检查）**跳过**，由 Agent 判断字段归属（人类散vs 机器元数据）
- 若豁免字段开始承载叙述性内容（`method` 变成 200 字操作说明）已蜕变为人类散文，应外迁
