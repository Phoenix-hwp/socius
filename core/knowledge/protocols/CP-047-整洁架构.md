---
Title: CP-047：整洁架构——依赖倒置的四层洋葱模型
Lifecycle: 阶段
Created: 2026-05-17
status: candidate
cp_type: "conceptual"
cp_subtypes: []
concept_anchor: "ARCH.clean"
validated_count: 0
capability_id: ""
applicable_roles: []
source_origin: "personal"
source_access: "public"
sources:
  - { system: "conversation", title: "整洁架构：依赖倒置的四层洋葱模型", date: "2026-05-10" }

activation:
  self_recital: "整洁架构四层洋葱：Entities→UseCases→Adapters→Frameworks，外层依赖内层不可逆"
  task_types: ["system-architecture", "code-structure"]
  concept_anchor: "ARCH.clean"
  decision_signal: "设计系统整体架构风格、评估模块间依赖方向是否合理时"
  anti_pattern: "内层依赖外层——依赖倒置原则禁止Entity知道有人在用它下单"
  capability_hint: "系统架构设计"

judgment:
  verdict: "可信"
  self_check: "LLM 自检通过，四层洋葱模型与Robert C. Martin Clean Architecture一致"
  web_check: null
  doubts_resolved: []
  note: null

---

# CP-047：整洁架构——依赖倒置的四层洋葱模型

## 四层洋葱

```text
外层
├── Frameworks & Drivers（框架与驱动层）
│      Web 框架、数据库驱动、UI、外部设备
│   ┌── Interface Adapters（接口适配层）
│   │      Controller、Presenter、Gateway
│   │   ┌── Use Cases（用例层 / 应用业务规则层）
│   │   │      具体业务规则、应用逻辑
│   │   │   ┌── Entities（实体层 / 企业业务规则层）
│   │   │   │      核心领域模型，最抽象的规则
│   │   │   └────────────────
│   │   └────────────────
│   └────────────────
└────────────────
依赖方向：外层 → 内层（单向，不可逆）
```

## 核心原则

1. **依赖只能从外向内**：内层不知道外层的存在
2. **Entity 不知道有人在用它下单**——最内层只有纯业务规则，不引用基础设施
3. **Use Case 只知道 Entity**——不关心数据怎么存的、请求怎么来的

## 关联概念

| 关系 | 概念锚 | 说明 |
|:---|:---|:---|
| 对比 | `ARCH.three_layer` | 整洁架构以依赖倒置为核心，三层架构以职责分离为核心（CP-045） |
| 对齐 | `ARCH.DDD` | DDD 的领域层/应用层/基础设施层可映射到整洁架构的 Entity/UC/Adapter+Frameworks（CP-048） |
