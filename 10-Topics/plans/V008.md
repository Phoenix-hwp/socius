# V008：去 Cursor 绑定

**状态**：vision  
**first_seen**：2026-05-19  
**source**：系统全面评价 — 单点故障（Cursor 绑定）+ 独立 Agent 路线

## 描述

当前四层架构的网关层、能力层、执行层全部寄生在 Cursor Agent 框架上。Cursor 服务宕机/模型切换/API 变更 → 系统直接瘫痪。远期目标：Guard 落地后，main.py CLI 可直接接收用户任务描述 → P008 评估 → 注入上下文 → 调用任意 LLM API（OpenAI / Anthropic / 本地模型）→ 执行 → 反馈，不再依赖 Cursor 作为唯一运行时。认知层（Knowledge-Brain .md 协议）保持独立，仅需 Guard 提供检索接口。

## 触发条件

Guard v0.3 全链路通过后，启动独立 Agent 运行时开发。
