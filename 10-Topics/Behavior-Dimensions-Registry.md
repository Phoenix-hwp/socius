---
Lifecycle: 阶段
Title: 行为维度注册表
Created: 2026-05-09
---

# 行为维度注册表

> **用途**：定义 §6 轮级行为快照的维度与值域。仅支持新增维度 + 启用/禁用维度。不支持修改值域或删除维度。
> **引用来源**：`Round-Behavior-Log.md`（快照）→ `Cursor-usage-profile-and-templates.md` §6（归档）。

---

## 维度定义

| 维度名 | 字段 key | 状态 | 值域 | 版本 | 说明 |
|--------|---------|------|------|------|------|
| 指令风格 | style | 启用 | `直接执行` / `方案先于执行` / `问答为主` | v1 | 本轮主要执行模式 |
| 确认模式 | confirm | 启用 | `跳过确认` / `关键确认` / `逐步确认` | v1 | 对高风险操作的确认程度 |
| 输出结构 | output_structure | 启用 | `表格化` / `分段叙述` / `混合` | v1 | 回复的主要组织形式 |
| 用户输入 | user_input | 启用 | `直接文本` / `选项交互` / `混合` | v1 | 用户输入方式偏好 |
| 工具通道 | tool_channel | 启用 | `文件操作` / `Shell` / `MCP` / `脚本直达` / `混合` | v1 | 主要使用的工具类型 |
| 特殊行为 | special | 启用 | （自由文本，≤10字） | v1 | 本轮显著的非标准行为 |

---

## 操作日志

| 日期 | 操作 | 维度 | 说明 |
|------|------|------|------|
| 2026-05-09 | 新增 | 全部 | 初版六维度定义，v1 |
