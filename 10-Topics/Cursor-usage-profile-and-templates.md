---
title: Cursor 个人使用档案与快捷话术
type: cursor-profile
created: 2026-04-18
updated: 2026-05-08（会话收束：Earth Library 原子入库与归档 md 补导入）
sync_playbook: 10-Topics/Behavior-Preferences-Sync-Playbook.md
tags:
  - cursor
  - meta
  - 使用习惯
aliases:
  - Cursor个人档案
---

## 0. 会话工作流（开端 + 收束）

### 开端（尽量自动化）

- 已在 **项目级** Cursor Hook（**工作区根** `.cursor/hooks.json` → Windows 默认：`cmd /c python .cursor/hooks/session_start_profile_launch.py || node .cursor/hooks/session_start_profile_launch.mjs`；启动器内依次尝试 `python` / `python3` /（本机 Windows）`py -3` 执行 `session_start_profile_context.py`，再尝试 `node` 执行 `session_start_profile_context.mjs`，避免双 `sessionStart` 重复注入）注册 **`sessionStart`**：当你把 **Obsidian 工作区根**（含 `Cursor_Knowledge/`）或 **仅 `Cursor_Knowledge` 文件夹**作为 Cursor 工作区根打开并新建 Composer / Agent 会话时，会把「档案全文」注入为 `additional_context`（过长会截断并标注）。注入块会以「Injected by sessionStart hook」开头。
- **默认档案位置**（脚本按顺序查找，先命中者用）：`<工作区根>/Cursor_Knowledge/10-Topics/Cursor-usage-profile-and-templates.md`，其次 `<工作区根>/10-Topics/Cursor-usage-profile-and-templates.md`。Cursor 会提供 **`CURSOR_PROJECT_DIR`**；若档案在别处，可设置 **`CURSOR_OBSIDIAN_PROFILE`** 为档案 `.md` 的**绝对路径**（优先），然后重启 Cursor。
- 若你未看到档案要点：请让 Agent **Read** 上述相对路径之一（存在哪个读哪个）。

### 收束（需你发送触发语）

Cursor 的 `sessionEnd` 钩子**不能**把分析结果写回笔记（官方为 fire-and-forget，输出不被使用）。因此「契合度分析 + 写回档案」在每次会话结束时由你 **主动触发**：

**触发语（任选其一，单独发或放在消息开头）：**

- `/收束`
- `会话收束：`
- `结束会话`
- `结束对话`
- `结束`（口语收尾，视同「结束对话」）

**收到触发后，Agent 必须按顺序执行：**

1. Read 本文件（若上下文里尚无最新全文）。
2. 用简短要点概括**本轮对话里你的实际行为**（任务类型、指令风格、偏好是否一致）。
3. 对照本文件 **§1–§3**（用途 / 语义意图 / 行为偏好），给出 **契合度**（高/中/低 + 一句理由）。
4. 在 **§6 表格**中 **追加一行**（不要删改历史行）；若同时需要更新 §1–§4 的表述，在 §5「迭代记录」追加一条，并对正文做**最小必要**编辑。
5. 写回本文件（UTF-8，无 BOM），保存后简要回复「已追加 §6 / 已更新 §x」。

---

# Cursor 个人使用档案与快捷话术

> 用途：记录你使用 Cursor 的目的、希望被理解的「语义习惯」、行为偏好；并沉淀可复制的 Chat 话术模板。  
> 使用方式：随用随改；在对话中 `@本笔记` 或说「先读个人档案再回答」。成熟后可把摘要写入 Cursor **User Rules** 作全局默认。

## 1. 我主要用 Cursor 做什么（用途）

<!-- 例：写业务代码 / 脚本自动化 / 读代码库 / 写文档 / 学习新技术 / 与 Obsidian 知识库联动 -->

- 
- 

## 2. 语义意图习惯（希望 AI 如何理解我）

<!-- 例：说「优化一下」默认指性能而非排版；「简单」= 最少改动；「上线」= 要考虑回滚 -->

- **省略主语时**：通常指 ___
- **我说「快一点」时**：优先 ___ 而非 ___
- **我说「稳一点」时**：指 ___（如类型安全、测试、错误处理）
- **领域词或个人黑话**：

| 我说的词 | 我真正的意思 |
|----------|----------------|
|          |                |

### 常用错别字语义对照与纠偏

> **双端**：本表为错别字 **主本**（与 Notion 第 4 节冲突时以本表为准）。因长文档多在 Notion 编辑，对方可能更全；**同步时请与 Notion 对比合并** 后再写回两侧，流程见 `Behavior-Preferences-Sync-Playbook.md` **§3.1**。  
> 目的：当输入存在错别字/近音字/漏字时，优先结合上下文自动纠偏，降低反复澄清成本。

| 可能输入（含错写） | 期望纠偏为 | 语义说明 |
|---|---|---|
| 竟彩 / 竟足 | 竞彩 / 竞足 | 统一为竞彩业务语境 |
| 竞建 / 竟荐 | 竞荐 | 高手方案分类词 |
| 北建 / 背荐 | 北荐 | 高手方案分类词 |
| 组窜 / 组串串 | 组串 | 指智能组串能力 |
| 赛矿 / 赛况况 | 赛况 | 指赛程-赛况页 |
| 资科库 / 资料苦 | 资料库 | 指 M10 资料库模块 |
| 备份当前会话 / 备份当期对话 | 备份当前对话 | 指令归一化 |
| 读取x月x日对话（缺年份） | 读取当年x月x日对话 | 缺省年份=当前年 |
| 行事历 / 形势历 / 形式历 | 行事历 | 个人任务/日程语境 |
| 分账 / 分帐 | 分账 | 财务拆分 |
| 彩店 / 菜店 / 采店 | 彩店 | 线下门店语境 |
| 口经 | 口径 | 规则/说法一致 |
| 叠代 | 迭代 | 版本推进 |
| 需球 | 需求 | 口误 |
| 拥金 | 佣金 | 口误 |
| 提线 | 提现 | 口误 |
| 对起 | 对齐 | 达成一致 |
| 沉店 | 沉淀 | 文档/知识固化 |

纠偏规则（默认）：
1. 先按上下文与历史主题做语义纠偏，再执行动作。
2. 若存在两个以上合理含义且影响执行结果，先给 1 句确认问题再继续。
3. 指令词纠偏后，回显“按 <纠偏词> 执行”以便用户感知。

## 3. 行为偏好

> **双端结构**：下列 **第 1–9 节** 与 Notion《行为偏好（供 Notion AI 参考）》及 `TMP_Behavior-Preferences-Unified-Template.md` **同级编号一致**。同步流程、锚点与检查清单见 **`10-Topics/Behavior-Preferences-Sync-Playbook.md`**。  
> **优先级**：用户当轮明确指令 > 静态偏好。

### 1. 使用说明与冲突处理

- 本节与 Notion 行为偏好页 **第 1 节** 同义；冲突时以 **当次对话明确指令** 为准。
- 大改版式或合并冗长历史时，可先按 **TMP** 模板由 Notion AI 整理，再与本节 **1–9** 对拍（见同步手册 §4）。

### 2. Skill 与自动化（Notion）

- **执行记录 / 调用执行记录 Skill**：对话偏好做可复用总结，写入 **固定页**——Notion：行为偏好页 **「执行记录（Skill）」** 等区块；Cursor：**§6** 契合度日志，必要时在本节或 §5 补记。**默认不另建子页面**（除非当次明确要求）。
- Skill 输出建议含：概览、观察到的偏好、约束/禁忌、后续交互建议、置信度、证据摘录（可选）；与 Cursor 规则冲突时以用户当次说明为准，并在修改侧简要注明。

### 3. 沟通与交互

- **回复语言**：**中文为主**；需要英文或中英混排时我会说明。
- **篇幅与表达**：**结论先行**、表达直接、**少寒暄**；篇幅在极简 / 适中 / 可长但要分层标题之间按任务选用。
- **不确定时**：先给 **2–5 个短选项**（尽量简短），再继续执行。
- **假设与约束**：重要处用 **「假设：…」「约束：…」** 标注。

### 4. 语义、措辞与纠偏

- **错别字与领域词**：运行时以 **§2 对照表为主本**；与 Notion 同步时 **对比合并**（Notion 常更全，但 **同输入不同纠偏** 以 §2 为准）。允许结合上下文纠偏；关键处标注 **「纠偏：A → B」**；影响 **金额 / 规则 / 权限 / 口径** 等时 **先确认再执行**。
- **任务措辞 → 交付标准**（与 Notion 第 4 节子表对齐）：

| 常用说法 | 我指的交付标准 |
|----------|----------------|
| 优化 / 调整优化 / 补缺补漏 | 在原结构上 **增量** 完善：补约束、消矛盾、统一术语；**不大改结构/版式** |
| 根据建议文档…优化当前文档 | 先对齐建议点，再给出 **可落地** 方案；建议未写清处由你 **补逻辑闭环** |
| 用蓝色凸显 / 凸显优化内容 | **新增与关键改动用蓝色** 标出，便于 diff；纯错别字可轻标或不标 |
| 美容 / 美化 / 整理 / 排版 | 结构与可读性（标题层级、列表、Callout），**不改业务含义** |
| 执行记录 / 调用执行记录 Skill | 见 **§3 第 2 节** |
| 不要放在 X 下 / 放到 Y | **信息架构硬约束**：移动或合并到指定位置，而非只加链接 |
| 固定存储 / 无特殊说明不变更 | 同类输出 **默认沿用同一页面/区块** |

### 5. 输出与格式

- 默认 **清单/表格**，避免堆长段落；提供可复制标题、要点、字段、模板。
- **日期/时间**：**Asia/Shanghai**，**YYYY-MM-DD**。
- **代码风格**：先给方案再改代码 / 直接改并说明差异。
- **解释深度**：默认不展开原理 / 需要时展开。

### 6. 工具与环境

- **工作区 Git / Gitee**：口语 **`提交git`**（及别名 **Git同步** / **推仓库** / **提交远端**）走 **`10-Topics/Gitee-Workspace-Git-Workflow.md`** 与规则 **`git-workspace-commit.mdc`**；与 **Notion 写入**（默认对话内六步）相互独立。
- **工具使用**：尽量自己跑终端与读文件，少让我手动复制。
- **钩子 / 脚本**：**新建命令型钩子及相关启动脚本时，默认须同时支持 Python 与 Node.js**（互备实现或统一启动器；Windows 为主要环境时可采用与本库 `session_start_profile_launch` 相同思路：`cmd /c python … || node …`；注入类输出保持 UTF-8 一行 JSON）。
- **指令创建与别名**：**当我要求“创建指令”时，默认即创建执行级规则（`.cursor/rules/*.mdc`）并登记到 `10-Topics/Cursor-command-aliases.md`；执行需同时支持完整指令与别名触发，且每个别名长度为 2-10 个字。**
- **长记忆开场确认**：**默认按“下轮起点”轻确认后直接执行；仅在冲突/歧义时才完整询问“上一轮位置 + 本轮范围（章/阶段/环节）”；范围外内容仅登记待处理。**
- **Notion 内执行**：先搜索是否已有页面/模板/数据库；新建放在当前相关页面体系下（除非另有指定）；对页面/数据库改动尽量 **一次合并**；标题用 **清晰可检索的中文**（可加 `{行事历}` 等前缀）。
- **Notion 执行细则（2026-04-25 确认）**：
  - 每次调用前先读取对应 MCP 工具 schema，再执行工具调用。
  - 标准链路：`search -> fetch(schema) -> create/update -> fetch(verify)`，避免盲写。
  - 写数据库时严格使用 `fetch` 返回的真实字段名，不猜测字段。
  - 内容默认写入页面正文（详情）；仅在你明确指定时写入属性字段。
  - 涉及正文 Markdown 时，先对齐增强 Markdown 规范后再写入。
  - 目标或映射不明确时先确认，不做高风险猜测写入。
  - 删除策略：优先从数据库移除（移出父级）；彻底删除按你确认后再执行或引导 UI 操作。
  - 入口切换阈值：同类写入 >=5 条、或涉及 >=2 个数据库/数据源、或要求 dry-run/审计留痕时，默认改用脚本；其余单条/少量交互优先插件。
  - 首条 Notion 指令确认：每个对话中，执行第一条 Notion 操作指令前，先询问“是否开启网络环境（Y/N）”；你回复 `Y` 才继续执行，回复 `N` 则不执行该任务。
- **Notion 写入通道（约定）**：**大段**正文、操作说明、配置留底优先 **本地 Markdown + 仓库脚本**（如 `run_notion_workflow.py`）；**小条、随手测试**优先 **MCP**（先 `notion-fetch` 取数据源与属性名，再 `notion-create-pages` 等）。

### 7. 决策与待确认

- **关键决策**（结构调整、批量改动、可能影响历史数据）：先给 **影响范围 + 备选方案** 再动手。
- 回复中的待确认项集中在 **「待确认」** 小节（Notion 同惯例）。

### 8. 沉淀、归档与长文档增量

- **固定存储**：无特殊说明则 **不变更** 默认落点；「放到 Y」表示 **移动/合并**，不是仅加链接。
- **长期背景页**：目标页底部 **「迭代暂存区」** 粘贴新版 → 比对新增/修改/删除 → **删除类先确认** → 更新迭代日志 → 清空粘贴区；冲突大时先给 **差异摘要**。
- **偏好迭代（可选）**：每次任务后，助手可在回复末给出 **「新增/修订的行为偏好建议」**（无则写「无」）；你确认后再写入本节或 Notion 对应节，避免误记。

### 9. 附件失败与异常兜底（Notion）

- 附件无法直接读取时 **不空转**：先建目标页骨架 + **黄色 callout 粘贴区**；回复说明原因与需你配合的动作；粘贴完整后再排版并清理占位。

### 其他（仅 Cursor 工作区）

- **清理临时/草稿时**默认保留 **备忘录** 类文件（Memo/备忘录）；仅在我 **明确点名** 时才删除。

## 4. 常用快捷话术模板（复制到 Cursor Chat）

| 场景 | 话术（复制使用） |
|------|------------------|
| 开场（档案已在 Hook 时可选） | 若档案未载入：先 Read `10-Topics/Cursor-usage-profile-and-templates.md` 再继续。 |
| 短指令分发 | 先查 `10-Topics/Cursor-command-aliases.md` 是否命中已登记指令，再执行对应动作。 |
| **会话收束（写回 §6）** | `/收束` 或 `会话收束：` 或 `结束会话` 或 `结束对话` 或 `结束` |
| 开场对齐上下文 | 先读 `10-Topics/Cursor-usage-profile-and-templates.md`，再回答下面问题： |
| 只要结论 | 用要点列表回答，不要扩写背景；不确定处标「待确认」。 |
| 深度实现 | 阅读相关代码后给出最小可行改动，并说明风险与测试建议。 |
| 与知识库同步 | 把本次结论写入 Obsidian：`路径/文件名.md`，用 UTF-8。 |
| 长记忆续聊开场 | 先读主控文件/备份并回显下轮起点，优先一句轻确认后直接执行；仅在歧义时再完整询问章/阶段/环节。 |
| 行为偏好双端同步 | 先 Read `10-Topics/Behavior-Preferences-Sync-Playbook.md`，再按其中 1–9 节对拍 Cursor §3 与 Notion 行为偏好页。 |
| 复盘一次对话 | 总结：目标 / 已做 / 未做 / 建议下一步；若有可沉淀条目写入知识库。 |
| 自定义 1 | |
| 自定义 2 | |

### 纯文本块

```
/收束
```

## 5. 迭代记录

| 日期 | 改了什么 |
|------|----------|
| 2026-04-18 | 创建模板 |
| 2026-04-18 | 增加 §0 工作流、§6 契合度日志、sessionStart Hook |
| 2026-04-18 | 将 sessionStart Hook 从项目级迁移为用户级（`~/.cursor/hooks`） |
| 2026-04-19 | 改回**项目级** sessionStart（随库复制）；解析路径用 `CURSOR_PROJECT_DIR` / `CURSOR_OBSIDIAN_PROFILE`；清空用户级 `hooks.json` 中的 sessionStart 以免重复注入 |
| 2026-04-19 | §0/§4 增补收束触发语「结束会话」；§6 追加本轮一行 |
| 2026-04-19 | §6 收束追加：店主端 PRD 审阅产出、Notion×Cursor 集成问答 |
| 2026-04-19 | §0/§4 增补收束触发语「结束对话」；§6 追加 Notion 清理与收束一行 |
| 2026-04-19 | sessionStart：用 `session_start_profile_launch.mjs` 串联 Python 与 Node 实现；§0 更新钩子路径说明 |
| 2026-04-19 | sessionStart：Windows 下钩子 JSON 经 UTF-8 字节写 stdout（`.mjs` 的 `writeSync`、`launch` 用 buffer 解码子进程、`.py` 的 `stdout.buffer`），避免档案中文在注入链路乱码 |
| 2026-04-19 | §6 追加：Cursor×Obsidian 插件/集成问答收束一行 |
| 2026-04-20 | §6 追加：行为监测（钩子）实测与单字「结束」收束一行 |
| 2026-04-21 | sessionStart：新增 `session_start_profile_launch.py`；`hooks.json` 改为 Windows `cmd /c python …` 失败则 `node …launch.mjs`；`launch.mjs` 增补 `py -3`；§0 同步说明；可选保留 `session_start_profile_launch.sh` 供非 Windows |
| 2026-04-21 | §3 增补「钩子/脚本」偏好：新建钩子默认双运行时 Python + Node；新增 `.cursor/rules/hooks-dual-runtime.mdc`（alwaysApply） |
| 2026-04-21 | §6 追加：用户确认偏好落库后以「好的，结束」收束一行 |
| 2026-04-21 | 修复“查看首页截图分析”未触发：新增 `.cursor/rules/command-alias-dispatch.mdc` 与 `10-Topics/Cursor-command-aliases.md`，建立短指令映射与兜底流程 |
| 2026-04-21 | 新增“指令创建与别名”偏好：创建指令默认落执行级规则；完整指令与别名双触发；别名长度约束 6-50 字符，并同步更新分发规则与别名清单 |
| 2026-04-21 | 469 背景补全：入库盈钻长期有效（暂定）、全盈钻抵扣完整价格拆分、分佣基数按实付；新增“项目备忘录/记录备忘”执行级规则与备忘清单，并清理失效指令映射 |
| 2026-04-21 | 调整别名长度偏好：由 6-50 改为 2-10 个字；同步更新分发规则、备忘录规则与别名清单，并启用短别名 `备忘录`/`记下` |
| 2026-04-21 | 新增删除保护偏好：清理草稿/临时文件时默认保留备忘录类文件（Memo/备忘录），仅在明确点名时删除 |
| 2026-04-23 | §2 新增“常用错别字语义对照与纠偏”区块；§3 新增“错别字纠偏”偏好（上下文优先，歧义先确认） |
| 2026-04-23 | 新增长记忆开场确认偏好：每轮先读取主控并确认“上一轮位置 + 本轮范围（章/阶段/环节）”；新增执行级规则 `.cursor/rules/round-start-checkpoint-confirmation.mdc` |
| 2026-04-23 | 将长记忆开场改为“下轮起点轻确认优先”：默认一句确认即执行，仅在冲突/歧义时才完整询问 |
| 2026-04-23 | 多轮长记忆流程重构：新增 `long-memory-round-workflow` 规则、根目录 `Knowledge-Assets/Control-File-Template.md`、`总结本轮对话/确认合并` 指令映射 |
| 2026-04-23 | 对话备份从项目目录迁移为根目录 `Daily-Backups` 体系；同步更新备份/读取/续聊规则与别名映射 |
| 2026-04-23 | 新增根目录 `Command-Help-Index.md`，支持 `help/查看帮助` 调取；清理失效指令映射 |
| 2026-04-24 | 合并 Notion《行为偏好（供 Notion AI 参考）》与档案 §3：扩充沟通/格式/Notion 执行/决策/沉淀等维度；§2 增补通用错别字行；新增 §3.1 措辞语义表与 §3.2；Notion 页追加「Cursor 工作区同步」块 |
| 2026-04-24 | 新增 `Behavior-Preferences-Sync-Playbook.md`；将 §3 重组为与模板一致的 **第 1–9 节**，frontmatter 增加 `sync_playbook` 指向；§4 增补「双端同步」话术行 |
| 2026-04-24 | 错别字表：§2 / §3.4 与 Playbook §3.1 统一为 **§2 主本 + 与 Notion 对比合并**（Notion 可能更全）；TMP §4.2 增加整理提示 |
| 2026-04-25 | 记录 Notion 执行规则：先读 schema；search->fetch->写入->回查；字段名以 schema 为准；正文优先；不明确先确认；删除优先移出数据库 |
| 2026-04-25 | 增补 Notion 入口切换阈值：写入 >=5 条或跨 >=2 数据源、或需 dry-run/审计时切脚本；其余场景插件优先 |
| 2026-04-25 | 增补 Notion 首条指令前置确认：每个对话首个 Notion 操作前先问是否开启网络环境；Y 执行，N 不执行 |
| 2026-05-02 | Notion 写入六步流程规则化；澄清 Notion 仍以对话为主并恢复 GUI 钩子 | 更新 `notion-write-workflow-confirmation`、`script-option-ids`、别名与帮助索引；恢复 `notion_gui_menu.ps1` 与 `notion_gui_popup`；§3「工具与环境」明确 Git 与 Notion 写入相互独立 | 已写入 §6 / §3 |
| 2026-05-02 | Notion「改」分支：`1`/`2` 更新策略、执行通道 MCP 优先；统一入口 §4.2.2 与索引同步 | 落盘 `notion-unified-crud-workflow.mdc`、`Notion-统一入口规范.md`、`Cursor-command-aliases.md`、`Command-Help-Index.md`、`NOTION_WORKFLOW_README.md` | 本次收束记入 §6 |
| 2026-05-04 | 跨设备 AI Shim、换机 bootstrap、Git 密钥基线与别名「新设备初始化」 | 见 §6 同日行；工具链与仓库基线改造；§6 修正 2026-05-03 两行表首列 `||` 为 `|` | 已写入 §6 |
| 2026-05-04 | 三层架构 Git 对齐、横切说明、换机脚本与规则跨路径自检 | 收敛 gateway/mod-git/git-workspace/flow-git-commit；删 `bootstrap.cmd`；`bootstrap-on-pull` 绝对路径与 Key 检测；收束后执行 `提交git` | 已写入 §6；Git 由 Agent 提交 |
| 2026-05-04 | Notion 通道策略口头确认落库 | 用户确认「大段留底走脚本+md、小条测试走 MCP」；收束写入 §3.6 与 §6 | 已写入 §3 / §6 |
| 2026-05-05 | 固化 `post-git-pull-three-layer-awareness.mdc`；Git 备忘与 flow-git-commit / gateway / git-workspace / git-cross-device 衔接 | 拉取并入远端后须重温网关索引、别名表、Help Index，再新增规则/钩子/别名相关脚本，降低跨设备重复与冲突 | 已写入规则与 §6 |
| 2026-05-06 | 多设备别名边界、盈钻更名、Notion 模板写入与 469 M02 口径；收束写 §6 | 网关/post-git/flow-git 补强；469 与 Earth 术语；Notion 六步与备忘；用户「结束对话」收束 | 已写入 §6 |
| 2026-05-06 | Notion 幂等 upsert 脚本与 flow 衔接、README 规则配合、Git 推送；收束追加 §6 | 落盘 upsert 工作流与 `flow-notion-create`/`flow-notion-update` 自动通道；用户确认后提交并 push | 已写入 §6 |
| 2026-05-08 | 收束追加第六节：档案与 Git 提交关系溯源 | 对照 `16c6ce7` 夹带档案全文大 diff 属误伤致乱码，正向修正多在工作区；本轮仅问答与收束落盘 | 已写入 §6 |
| 2026-05-08 | 全库脚本审计、去耦合优化与编码约束规则固化 | 审计 28 项冗余/耦合/硬编码/三层越界问题，完成 el_parsers/config 提取、4 规则存根化、subprocess 解耦、阈值同源；新增 `script-coding-constraints.mdc` 防复发；全量语法/导入验证通过 | 已写入 §6 |
| 2026-05-08 | Earth Library 原子入库与迁移收束 | §6 追加一行：Notion 一页一卡迁移、归档汇总卡、relation 链接补导入及 Windows 批量 subprocess 输出解析风险说明 | 已写入 §6 |

## 6. 会话契合度分析日志（追加写入，勿删历史）

| 日期(ISO) | 本轮主题摘要 | 观察到的行为摘要 | 与档案契合度 | 偏差 / 风险 | 建议的档案微调 | 采纳情况 |
|-----------|--------------|------------------|--------------|-------------|----------------|----------|
| 2026-04-19 | Obsidian 笔记问答；工作区 Hook/规则审计与可移植改造；全局规则片段修正；偏好自动/手动说明；会话收束 | 指令明确（直接修改/自检删除）；主动 Read/Glob/Shell；中文、结构化答复；收束用语为「结束会话」 | 中 | §1–§3 未填写，契合度难细评；原档案未列「结束会话」为触发语 | 将「结束会话」写入 §0/§4 与 `/收束` 并列 | 已采纳（本次写入） |
| 2026-04-19 | 469 体育店主端 PRD 交叉审阅（产出审阅 md）；Cursor 与 Notion 创建/修改内容的集成路径；收束 | 读项目语境与长 PRD、写结构化审阅并落盘；检索官方 Notion MCP 文档并归纳步骤；中文、分层标题 | 中 | §1–§3 仍多为占位，难比对「语义习惯」细节 | 可选：§1 补「PRD/需求文档评审与落盘」 | 待定 |
| 2026-04-19 | 中止 Notion 写入；清理工作区临时/过程 json 与分片文件；结束对话 | 明确停止远端写入；Glob+Delete+Remove-Item 清 `_tmp`/`_notion*`/`_mcp*` 等；说明可迁移性 | 中 | §1–§3 未细化 | 将「结束对话」与「结束会话」并列为收束触发语 | 已采纳（本次写入） |
| 2026-04-19 | 档案钩子可执行性检查；Python 不可用则配置 Node；Python 与 Node 同时保留 | 要求自检并直接配置；Agent 用 Glob/Read/Shell 验证；追问双运行时并存；中文致谢并以「结束对话」收束 | 中 | §1–§3 多为占位，难做细粒度比对 | §0 写明 launch + py/mjs；§5 记本次钩子改造 | 已采纳（本次写入） |
| 2026-04-19 | 测试行为偏好记录、分析与收束执行是否正常；Windows 注入编码 | 要求实测钩子与链路；Read/Glob/Shell；修正 sessionStart 输出为 UTF-8；用 buffer 解析校验「行为偏好」「收束」 | 中 | §1–§3 仍为占位；口语「结束」原未写入触发列表 | §0/§4 增「结束」；§5 记 UTF-8 钩子修复 | 已采纳（本次写入） |
| 2026-04-19 | 确认行为分析链路说明；会话收尾 | 简短肯定「好」后以「好，结束」收尾；触发收束写回 | 中 | §1–§3 未填，契合度粗评 | 无 | 无 |
| 2026-04-19 | Cursor 与 Obsidian 的插件/集成路径问答；结束对话 | Web 检索 + 中文分层说明（无官方专用插件、VS Code 市场扩展与 Obsidian 侧桥、MCP 进阶）；收束触发语为「结束对话」 | 中 | §1–§3 多为占位；本轮未要求先 Read 档案 | 可选：§1 增补「工具链/集成问答」 | 待定 |
| 2026-04-20 | 测试工作区用户行为监测（sessionStart 与档案收束链路）是否正常 | 要求实测；Read hooks/档案与钩子脚本；Shell 跑 Python 校验注入 JSON 与 UTF-8 中文；说明 `hooks.json` 入口依赖本机 `node`；删除临时 `_hook_py_out.txt`；单字「结束」触发收束 | 中 | §1–§3 仍为占位，细粒度契合难评；与 §3「尽量自己跑终端」一致 | 无 Node 时可考虑将 sessionStart 改为直调 `python …session_start_profile_context.py` | 已由 2026-04-21 改造：外层先 python 再 node |
| 2026-04-20 | 雷速体育「重要」赛事分类分析；会话结束并请求开启新 Agent 对话 | 基于截图做分层归纳（热门/五大/洲际/分区）并推断筛选维度与产品理由；中文结构化输出；用户以「结束」语义收束 | 中 | §1–§3 仍未填写，难做个性偏好细评；仅截图推断存在信息不完全风险 | 可选：§1 增补「产品信息架构/分类策略分析」 | 待定 |
| 2026-04-20 | 工作区调用能力实跑与移动硬盘可用性验证；本机 Python 安装故障排查；469 项目背景架构化重构与持续补全机制落库 | 指令偏“直接执行+持续写回”；Agent 高频使用 Read/Glob/Shell/ApplyPatch，完成可运行验证、安装诊断与知识库双文件联动维护；中文、结构化、迭代式问答 | 中 | §1–§3 长期未填写，导致契合评估与偏好拟合仍偏粗粒度；规则中“先方案再改代码/直接改”偏好未显式固化 | 可选：在 §1 增补“项目背景建模与长期迭代维护”，在 §3 明确“偏好直接改写知识库并逐轮提问补全” | 待定 |
| 2026-04-21 | sessionStart 同时支持 Python 与 Node；钩子脚本实测；结束对话 | Glob/Read 定位钩子；Shell 验证本机 Python/Node；新增 `session_start_profile_launch.py`，`hooks.json` 用 `cmd /c` 先 `python` 再 `node`，`launch.mjs` 增补 Windows `py -3`；用户表示主要在 Windows、不需改用 Unix `sh` 入口；以「结束对话」收束 | 中 | §1–§3 仍为占位，细评难；与 §3「尽量自己跑终端」一致 | §0 同步钩子入口；§5 记本次改造 | 已采纳（本次写入） |
| 2026-04-21 | 钩子双运行时偏好落库；口语收尾 | 用户要求「记住」新建钩子默认 Python+Node；已写入 §3 与 `hooks-dual-runtime.mdc`、§5；用户回复「好的，结束」触发收束 | 中偏高 | §1–§2 仍为占位；§3 已含钩子偏好，与本轮一致 | 无 | 无 |
| 2026-04-21 | 469 体育背景新增 M12「智能组串」占位；结束对话 | 检索并编辑 `469_Sports_ProjectContext.md` 与 `469_Sports_Background_Pending_and_Progress.md`：模块地图、占位卡片（逻辑暂缺）、关联表、迭代日志与进度快照；中文要点回复；用户以「好的，结束对话」收束 | 中 | §1–§2 仍为占位，细粒度契合难评；与 §3「工具自跑、中文、知识库落盘」一致 | 可选：§1 补「469 项目背景迭代维护」 | 待定 |
| 2026-04-21 | 469 体育项目背景持续补全；首页截图分析草稿独立保存；结束对话 | 连续按指令增补 `469_Sports_ProjectContext.md`（VIP 折扣、无中退、M13/M14、免费/付费口径等）；基于截图输出“可记录/待确认/不记录”并新建 `469_Sports_Homepage_Screenshot_Analysis_Draft.md`，约定以“查看首页截图分析”继续；全程中文、直接落库、结构化回复 | 中偏高 | §1–§2 仍为空，个性化语义偏好缺少可对照项；截图仅单张，部分判断待二次确认 | 可选：§1 增补“截图驱动需求抽取与知识库沉淀”；§2 增补“先独立保存草稿再入主文档”的习惯语义 | 待定 |
| 2026-04-21 | 469 项目背景收敛整理、误删恢复与保护规则固化；流程图识别能力确认；结束 | 连续执行规则与知识库维护：先按“仅保留主背景”删除，再按用户要求恢复 `Project_Memo`；新增“删除临时文件默认保留备忘录”规则并写入行为偏好；确认可识别流程图/状态机并支持后续 PRD 归档流程；用户以「好的，结束」收束 | 高 | 无明显偏差；执行中出现一次与用户预期不一致（备忘录被删）但已即时恢复并加防护 | 可选：§1 增补“PRD+流程图+状态机归档建模”；§2 增补“删除指令默认保护关键文档”语义 | 已采纳（本次写入） |
| 2026-04-22 | 资料库 PRD 解读并回写 469 背景；建立“后续新版 PRD 持续更新”备忘；结束对话 | 读取外部 PRD 与项目背景文件，提炼业务逻辑与底层逻辑并更新 `M10`；明确资料库归属赛程子版块、入口暂定“数据资料”；按指令将后续持续更新约定写入项目备忘；全程中文、直接编辑、结构化反馈 | 高 | 无明显偏差 | 可选：§1 增补“PRD 差异分析与持续回写项目背景”用途 | 待定 |
| 2026-04-23 | 长记忆协作方法沉淀、对话备份规则建立与时段边界修订、错别字纠偏偏好落库；结束对话 | 新建 PRD 长记忆模板与会话备份；新增“备份当前对话/读取对话”执行级规则与索引；将下午/晚上时段调整为 14:00-20:00 / >=20:00 并增加 14:00/20:00/00:00 前后10分钟确认；在个人档案新增错别字映射与纠偏规则 | 高 | 无明显偏差；时间回显工具偶发异常已用“未记录”兜底 | 可选：后续补一版“极简字段模板”以进一步贴合高频用法 | 已采纳（本次写入） |
| 2026-04-23 | 店主端 PRD 优化落地与蓝色标注；审阅建议文档删除；Markdown 转 Word 嵌图；软著可独立性评估；结束对话 | 按审阅建议与项目背景（M07）对店主端 PRD 做可实施改写并高亮改动；按指令删除指定审阅建议文件；将外部 PRD Markdown 转为 docx 并嵌入同目录图片；基于 `ProjectContext` 输出前台/后台/后端软著拆分建议；全程中文、直接执行与结果回传 | 高 | 无明显偏差；档案 §1–§2 仍偏占位，细粒度语义偏好可继续补全 | 可选：§1 增补“软著拆分评估/申报清单产出”；§2 增补“优先要可落地方案并落盘”语义 | 待定 |
| 2026-04-23 | 长记忆流程再收敛、日常备份独立化与帮助系统建立；结束对话 | 连续完成规则迭代：项目索引初始化、轮次轻确认、项目/日常备份分离；迁移历史备份到根目录 `Daily-Backups`；删除 469 下临时索引与模板；新增 `help/查看帮助` 调取指令帮助索引 | 高 | 无明显偏差；指令体系变更较多，初期可能记忆混淆 | 可选：后续补一条“本周新增指令变更摘要”自动回显 | 已采纳（本次写入） |
| 2026-04-23 | Notion 接入与读写实操；根目录英文化重命名；生命周期分层与自动清理机制落地；结束对话 | 完成 Notion MCP 便携配置与联通验证；修复分页遗漏与编码检索问题；按指令执行目录删除/重命名并同步规则引用；新增生命周期规则、长期沉淀目录、临时15天软删除脚本、阶段文件白名单+二次确认删除脚本（Python+Node）并接入 sessionStart 自动清理 | 高 | 个别 PowerShell 中文编码导致中间结果乱码，已通过 Unicode/ID 方式修正并验证 | 可选：后续将“临时备份创建”进一步强制化（自动注入 `Lifecycle: 临时`） | 已采纳（本次写入） |
| 2026-04-24 | 行为偏好双端同步手册与 §3 结构对齐；469 资料库 PRD→M10（含搜索页）；去除溯源依赖并删除 STAGE 稿；结束对话 | 多轮 Read/Write/Notion API 与桌面 md 接入；直接改 `469_Sports_ProjectContext` 与 Playbook；按「直接帮我删」执行 Delete；中文分层回复；触发语「结束对话」收束 | 高 | §1–§2 仍为占位，用途/语义习惯细评仍粗 | 无 | 无 |
| 2026-04-28 | 469 体育项目背景补录与修订；同步更新 Notion 项目器条目背景；结束对话 | 先在本地知识库增量改写 `469_Sports_ProjectContext.md`（M12 组串数据口径、M02 大咖列表跳转口径）并补齐迭代日志；Notion 操作前先做网络确认与 MCP 鉴权；按 `search -> fetch -> update -> fetch(verify)` 更新 Notion「项目器→469体育App」页面内容；全程中文、直接执行并回查确认 | 高 | Notion 页面内容更新采用“精确替换”依赖旧文段完全匹配，若后续人工改动导致不匹配需调整更新锚点 | 可选：在 §3 增补“Notion 同步优先小块替换+回查”已实践为默认；在 §2 增补“同步=本地+Notion 双端一致”语义 | 待定 |
| 2026-04-29 | 469 体育软著《产品说明书（草案）》模板生成并写入 Notion；列表信息流广告删除后重算策略建议；结束对话 | 先基于 `469_Sports_ProjectContext.md` 产出 C 端软著说明模板；按网络确认后执行 Notion MCP 链路（search/fetch/create），处理中途 401 与 data_source_id 参数校验并完成写入；随后针对“删除已保存内容是否重算广告”给出触发场景与稳定性策略建议 | 高 | Notion 授权链路在会话中存在波动（401），依赖用户端重新授权后才能继续自动写入 | 可选：§2 增补“存为备忘=执行收束+日常备份”语义别名，减少口语触发歧义 | 待定 |
| 2026-04-29 | Notion 授权异常排查、错误日志自动化、每日预检+一键修复落地；行为偏好双端同步与无损清洗；结束对话 | 新增项目级错误日志体系（`postToolUseFailure/afterShellExecution` 自动记录 + 跨设备环境字段）；落地 Notion 每日首次预检与失败自动修复（Python+Node 双实现）；完成 Notion 行为偏好页对拍、同步记录追加与分块无损清洗（仅清格式不改语义） | 高 | Notion 页“Cursor/Obsidian 同步区”仍可能有少量历史格式噪音；授权窗口不弹问题受本机浏览器/会话环境影响 | 可选：§3 增补“Notion 首次任务前默认预检并失败自修复”偏好；保留 Notion 独有内容不回写 Cursor 的边界说明 | 待定 |
| 2026-04-30 | Notion 双通道路由确认与命名改造；前台操作型脚本清单梳理并写入原始需求池；会话备忘收束 | 按用户要求将本地脚本 MCP 路由改名为 `notion-local`，并保留插件通道作为主路由；完成“前台操作型”脚本盘点，输出可原型化表格；按网络确认后在 Notion「原始需求池-需求清单」创建条目 `cursor图形界面原型设置` 并写入表格详情 | 高 | 存在“后台守护脚本是否需图形化”边界，若不先定义会造成实现范围膨胀 | 可选：§2 增补“图形化=先确认范围、暂不落地”语义约束；§3 增补“插件优先、本地脚本兜底”执行口径 | 待定 |
| 2026-04-30 | Notion 脚本性能优化、一级/二级结构拉取、级联枚举文件生成与两项一级节点补充；结束对话 | 连续完成 `run_notion_workflow.py` 提速改造（重试策略收敛、replace 并行归档、read 类型提示）；按用户指定读取多个一级页的二级页面/数据库；生成供 Cascader 使用的 `notion_cascader_options.json`，并对 `page/database` 做类型区分，新增 `悦读笔记/参考文摘` 仅一级节点 | 高 | Notion 结构依赖实时内容，若上游页面调整需定期刷新枚举；`悦读笔记/参考文摘` 当前按一级直挂，后续若要二级需单独扩展拉取策略 | 可选：§3 增补“级联来源文件优先 ID 直达，标题检索仅兜底”执行偏好 | 已采纳（本次写入） |
| 2026-05-02 | Notion 写入交互收敛；结束对话 | 固定对话内六步（网络→编号目录→标题策略→标题落地→正文预览→**确认写入**→API）；禁止代码式一行拼接；实测任务筐写入与厦门天气摘要；恢复 `notion_gui_menu.ps1`/钩子；精简 `notion_write_menu.cmd`、删示例 json 等；§3 明确 Git 与 Notion 写入相互独立 | 高 | §1–§2 仍为占位 | 无 | 已采纳 |
| 2026-05-02 | 会话收束：对齐 §5 迭代说明与 §6「采纳」列 | 用户再次触发「结束对话」；将 §5 当日迭代注明 §3 增补；§6 同日行「建议微调」改为已完成 §3、「采纳」改为已采纳 | 高 | 无新增偏差 | 无 | 已写入 §5 / §6 |
| 2026-05-02 | Notion 统一流程：「改」双策略（清空重写/局部合并）、MCP 优先与脚本兜底落盘；目录 JSON 条数核对；Shell 任务复盘；结束对话 | 扩展 `notion-unified-crud-workflow.mdc` 与 `Notion-统一入口规范` §4.2.2；同步别名与帮助索引、`NOTION_WORKFLOW_README`； Ask 模式对齐插件优先思路；Agent 模式执行 Write/StrReplace；用户「结束对话」收束 | 高 | §3 仍有一处「对话内六步」口径与统一 CRUD「改」分型并存，可按需改为互链说明 | 可选：§3 §6 一行改为「写入见创建六步；更新见统一规则改分支」 | 待定 |
| 2026-05-02 | Notion 规则三层架构重构整理；结束对话 | 分析现有规则问题→设计三层架构（网关/模块框架/工作流）→创建7新文件、删5旧文件、同步8引用→测试发现脚本缺陷→按用户要求转存日常备份；全程中文、直接落盘、用户「结束对话」收束 | 高 | 无显著偏差；系统性重构与 §3「规则落盘」偏好一致 | 可选：§1 增补「规则架构设计与重构」用途 | 待定 |
| 2026-05-03 | Notion 查询脚本缺陷修复；方案分析；Git 同步；结束对话 | 恢复 5月2日备份上下文；方案1/方案2对比分析（成本/效率/效果）→选定方案1；增强 `run_notion_workflow.py` 支持 `query` 参数、修复 `parse_notion_id()` 标准UUID；测试验证命中「双钻模型」；提交并推送到 Gitee；全程中文、先给方案再改代码、用户「结束对话」收束 | 高 | 无显著偏差；与 §3「先方案再改代码」「工具自跑」「Git 提交走工作流」一致 | 无 | 无 |
| 2026-05-03 | 三层架构扩展；脚本重构整合；Notion MCP 调通测试；结束对话 | 完成 Git/对话备份/项目备忘录/Earth Library 模块的三层架构化；提取公共模块 notion_sdk/lifecycle_manager/hook_framework，重构业务脚本消除重复代码；调通 MCP notion-update-page 工具调用（需 page_id + command + content_updates[] 格式）；验证行为偏好更新；全程中文、直接落盘、用户「结束对话」触发收束+Git同步 | 高 | 无显著偏差；与 §3「高内聚松耦合」「规则落盘」偏好一致 | 可选：§1 增补「三层架构扩展与脚本重构」用途；§3 增补「MCP 工具参数需严格匹配 schema」注意 | 无 |
| 2026-05-04 | Cursor AI 模型 Shim（Kimi/DeepSeek）与 ngrok；跨设备可运行基线与换机流程；结束对话 | 维护 `.cursor/ai-model-shim`（转发、`reasoning_content`、DeepSeek 图过滤、Moonshot 上游域名等）、简化 `auto-switch.cmd` 与 shim 侧 `bootstrap.cmd`；根目录 `bootstrap-on-pull.cmd` 从模板生成 `config.json`/`notion.env`；弃用 cloudflared 并清理冗余脚本；更新 `.gitignore`、`git-cross-device-and-secrets.mdc`、`模型配置说明.md`、Gitee 流程与 README；别名登记「新设备初始化」；约定密钥不入库、对话不校验 Token；批处理避免 CMD 中文编码问题 | 高 | §1–§2 仍为占位；隧道与 Cursor Key 开关依赖本机网络与环境 | 可选：`Command-Help-Index.md` 补「新设备初始化」；§1 补「多设备 API 代理与换机初始化」 | 待定 |
| 2026-05-04 | 三层架构 Git 对齐、横切说明、换机脚本与规则自检；结束对话并提交 Git | 修正网关 `提交git`→`flow-git-commit`；合并 Git 阶段 C 去掉虚构 flow；精简 `git-workspace-commit`；`flow-git-commit` 增变体说明；`bootstrap-on-pull`/`auto-switch` 路径与格式修复；`hooks-dual-runtime`/`pre-edit-script-change-brief`/`git-cross-device` 补跨路径；文档同步；收束写 §5/§6 并执行 add/commit/pull/push | 高 | 无显著偏差 | 无 | 已写入 §6；已 push `main`（bd2f251） |
| 2026-05-04 | bootstrap-on-pull 冗余清理与 Install 路径确认；结束对话 | 讲解密钥保护两层机制（.gitignore + bootstrap-on-pull.cmd 脚本级检测）；确认 D 盘 Install\nodejs 探测已就绪（priority 1b）；审查并清理 bootstrap-on-pull.cmd 4 处冗余（死变量 ERRORS=0、Notion Token 空值误报、[2c/5] Undici 与 [2b/5] 重复、[2e/5] Final Key Check 与 Phase 1 重复），Phase 2 从 5 步缩为 3 步；两轮全量测试通过；用户「结束对话」收束 | 高 | 无显著偏差；与 §3「先改动说明再落盘」「测试验收」一致 | 可选：§1 补「换机初始化脚本维护与冗余清理」用途 | 待定 |
| 2026-05-04 | 换机初始化、生活向问答、Notion 行为偏好局部合并、任务筐测试、MCP 读写测通与通道效率；结束对话 | 执行 `bootstrap-on-pull`；说明 example 模板用途；厦门 SM/日料闲聊；行为偏好 `Y`+策略2+`确认更新` 后脚本局部替换同步区并保留增量补丁；`run_notion_workflow` 在任务筐建「测试测试」行；解释未走 MCP 原因；确认「大段脚本、小条 MCP」效率策略后口头 OK；`notion-search` 与 `notion-fetch`+`notion-create-pages` 测通 MCP；中文回复 | 高 | §1–§2 仍为占位；生活类话题与知识库主线无关 | §3.6 增补 Notion 写入通道约定 | 已采纳（本次写入） |
| 2026-05-05 | 三层架构审计：4 旧版 alwaysApply 存根化 + depends 补全；DeepSeek 模型名排查与撤回（不影响功能）；「运行模型」指令别名登记 | 审查 37 个 .mdc 规则文件，整改 3 个旧版文件为引用存根 + 补 flow-git-clone 的 depends；别名登记 + Git 提交；DeepSeek 子代理恢复报 Model Not Found，经改名/映射/回测后确认 API 正常、仅 Cursor 白名单限制，撤回全部修改；全程中文、测试驱动、接受限制果断撤回 | 高 | 报错不影响实际推理，只影响子代理完成通知 | 无 | 已写入 §6 |
| 2026-05-05 | Git 拉取；「运行模型」指令别名登记并提交；三层架构审计与整改；DeepSeek 模型名报错排查与三轮回测后撤回 | Git pull --rebase + stash 处理未暂存文件；登记「运行模型/切换模型/启动模型」别名关联 auto-switch.cmd；审查 37 个 .mdc 规则文件，整改 3 个旧版 alwaysApply 文件为引用存根 + 补 depends；DeepSeek 子代理恢复报 AI Model Not Found，经改名→映射→回测后确认 API 调用正常、仅 Cursor 内部白名单校验失败，撤回全部修改；全程中文、直接执行、测试驱动、接受限制果断撤回 | 高 | 深层原因：Cursor 自定义 Provider 模型名白名单不可控；报错不影响实际推理 | 可选：§1 补「规则架构审计与整改」用途 | 待定 |
| 2026-05-05 | Git 提交 + Notion 悦读笔记 19 篇批量入库 Earth Library + 图书馆巡检 | 三层架构审计整改 4 文件已提交；Notion API 批量读取悦读笔记 19 篇产品思维类文章，生成知识卡片、更新 Library_Index 与 Relations_Index；主动执行图书馆巡检，整理 11 项标签近邻关联入 Review_Queue；验证 19 篇标题全部匹配（PowerShell GBK 编码显示误报） | 高 | PowerShell stdout GBK 编码导致标题显示异常，已用文件写入方式验证全部匹配 | 无 | 已写入 §6 |
| 2026-05-05 | Git 拉取；运行模型与 `auto-switch.cmd` 对齐；三层架构文档重温；pull 后防重复横切规则；Earth Library 检索 5W2H；收束 | 执行 `git pull --rebase`（本地有改动时 stash→pull→pop）；按 `Cursor-command-aliases` 用 `cmd /k` 启动 `auto-switch.cmd`；说明并撤销「绕过脚本直连 node/ngrok」的临时改动；阅读并归纳 gateway / mod / flow 与 `Command-Help-Index`；固化 `post-git-pull-three-layer-awareness.mdc` 并衔接 `flow-git-commit`、Gitee 备忘、gateway、`git-workspace-commit`、`git-cross-device`；grep+Read 核对「运行模型」相关改动已恢复仓库约定；检索 Earth Library 简述 5W2H | 高 | §1–§2 仍为占位，细粒度语义习惯难评 | 可选：§1 补「拉取后先对齐全库规则与别名再扩展本地约定」 | 待定 |
| 2026-05-06 | 新设备初始化；运行模型与别名对齐反思；多设备/Git 规则补强；盈钻更名；Notion 产品说明书空模板与重复行备忘；469 M02 高手/大咖玩法；厦门天气；Git 提交；结束对话 | 执行 `bootstrap-on-pull`；讨论「运行模型」须先读别名表、勿自建启动链并撤回 `.current_model` 等；落盘 `post-git-pull`/`gateway`/`flow-git-commit`/`git-cross-device` 多设备与别名边界；全库「赢钻→盈钻」与 469 背景/备忘/Earth 示例同步；Notion `Y`+`确认写入` 写入模板、说明 DB 必填属性与分块续传、记入 `TMP_日常备忘`；修正高手三玩法不含让球、大咖可含让球；`提交git` 至 Gitee（pull 无新并入）；收束触发「结束对话」 | 高 | §1–§2 仍为占位；Notion 同名多行需用户晚间自行清理；本次 commit 曾用一次性 `workspace@local` 作者信息 | 可选：§3 增补「Notion 建库行先对齐 schema 必填、POST 慎重重试防重复」 | 待定 |
| 2026-05-06 | Notion 幂等 upsert、flow 衔接、Git 与架构自检；结束对话 | `拉取git`（脏工作区 stash→pull→pop）、`安全拉取`（fetch 无差）；备忘检索与 Notion 重复条目根因、幂等与 MCP/脚本通道；`运行模型` 按别名 Start-Process `auto-switch.cmd`；新增 `notion_upsert_workflow.py`+`notion_workflow.upsert.json`+README；`flow-notion-create`/`flow-notion-update` 步骤5/6 长文自动通道与中断续传；说明别名表与 `Command-Help-Index` 登记边界；用户确认后 `提交git` 推送 `a6115fd`；三层架构与 upsert 高内聚低耦合自检 | 高 | §1–§2 仍为占位；upsert 与 `run_notion_workflow` 的 `md_to_blocks` 重复、`load_state` 后定义覆盖前定义等技术债 | 可选：Markdown 解析与状态辅助收敛到共享模块 | 待定 |
| 2026-05-07 | Notion 下钻归纳落地 Earth Library（B 方案三层分离） | 落地 `notion_drill.py`（child_page 与 relation 遍历、DrillNode、防环）、`notion_drill_earth_library.py`（渲染并委托 `store_to_library.py` 入库）、`run_notion_workflow.py` 新增 `drill` mode；本轮另含 relation 类型说明、知识库写入 cascader、「个人素养」下钻查阅归纳 | 高 | 档案第1–3节仍为占位；drill 的 consumer 预留未接线 | 可选：Command-Help-Index 登记 drill；示例 notion_workflow.drill.json | 待定 |
| 2026-05-08 | 档案改动是否与上午有关：Git 溯源；结束对话 | 用户追问上午修改是否涉及档案正文修正；Agent 用 `git log`/`git show`/`git status` 核对：`16c6ce7`（5/8 00:09）夹带档案全文大 diff 属误伤致乱码，正向语义修正多在后续工作区修复；区分「凌晨提交」与口语「上午」时间语义 | 高 | 档案第1–3节仍为占位；未提交修复若未 push 则远端仍可能为乱码版本 | 可选：大功能提交避免夹带个人档案全文；或档案单独 commit | 待定 |
| 2026-05-08 | 全库脚本审计、去耦合优化与编码约束规则固化；结束对话 | 审计 28 项并执行 10 项优化：提取 el_parsers/config 共享模块、删除硬编码默认值、subprocess 解耦、规则存根化、阈值同源；新增 `script-coding-constraints.mdc`（alwaysApply）防复发；18 项因「保护功能」约束判定不改；全量语法/导入验证通过；用户「结束对话」收束 | 高 | 无显著偏差；与 §3「先改动说明再落盘」「测试验收」「规则落盘」一致 | 可选：§1 补「代码库审计与持续重构」用途；§3 增补「脚本编码约束自检」偏好 | 已写入 §6 |
| 2026-05-08 | Earth Library 原子入库改造、汇总卡拆分迁移、归档 md 中 relation 链接补导入；结束对话 | Ask 模式厘清一页一卡与网状边粒度；Agent 落盘 `notion_atomic_ingest.py`、`flow-library-ingest-notion-atomic.mdc`、架构对比说明；用户确认网络后 dry-run 并正式拆分「商业管理」「流程建模」Notion 树，归档原汇总卡并更新 `Library_Index`；解释 child_page 与 relation 差异；按归档 md 链接批量补导入时 PowerShell/subprocess 抓 stdout 致 JSON 解析误报，以文件系统核验卡片已生成；用户「结束对话」收束 | 中高 | §1–§2 仍为占位；批量脚本宜改为写结果文件或强制 UTF-8/分离 stderr，避免误判失败 | 可选：§1 补「Obsidian/Earth Library 知识粒度与迁移」用途 | 待定 |
> 说明：每次收束 **新增一行**；档案正文的小幅优化写在 §5 并改对应章节。
