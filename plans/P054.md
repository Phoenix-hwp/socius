# P054：Decision-Log — 透明性增强（诚实策略）

**状态**：pending  

## 描述

当前 Decision-Log 记录决策结果，但未纳入芒格的"诚实是最好的策略"准则——如实记录不确定性、错误和犹豫，而非维持表面一致性。需在 decision_log.py 的输出模板中增加 uncertainty_flag 和 honest_note 字段。来源：CP-122（《穷查理宝典》消化）

## 执行计划

1) 在 decision_log.py 的 DecisionLogEntry 中增加 uncertainty_flag(bool) + honest_note(str) 字段→ 2) 修改 LLM#3 fill 阶段的 system prompt（坦诚记录不确定性）→ 3) 确保 JSONL 格式一致（新字段 null 表示未提供）→ 4) 测试（检查现有决策日志格式兼容性）
