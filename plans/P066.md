# P066：CP-128 机会成本 — TIP Step 3 + P008 V 维度量化

**状态**：completed (2026-05-22)

## 描述

CP-128 机会成本计算已在 dimensions.py（compute_opportunity_cost()）和 fsm.py（升级前检查）落地。下一步需集成到两个上游节点：(1) TIP Step 3 提案阶段——备选方案对比输出 opportunity_cost 字段和放弃声明；(2) P008 V 维度——将模糊的"价值冲突"判断替换为机会成本量化（方案A收益 vs 方案B收益 × 置信度）。前置：CP-128 核心函数已就绪（dimensions.py）。

## 结果

context_builder.py LLM#2 输出 schema 增加 opportunity_cost 字段 +"放弃声明：选 X 意味着放弃 Y 的 Z% 收益"；engine.py V 维度评估调用 compute_opportunity_cost() 替代当前模糊判断；test_integration.py 全链路验证通过。
