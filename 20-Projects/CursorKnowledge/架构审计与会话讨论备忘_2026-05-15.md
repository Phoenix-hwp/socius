---
Project: CursorKnowledge
Lifecycle: 阶段
Created: 2026-05-15
Type: 备忘
Purpose: 跨设备继续讨论用，记录架构审计全部17项 + 修复清单 + 本轮延伸讨论结论
---

# 架构审计与会话讨论备忘（2026-05-15）

> 来源：架构审计总报告 + 修复清单 + 本轮会话（5/15 下午）。供跨设备继续讨论。

---

## 第一部分：架构审计总报告与修复清单（全量 17 项）

### 🔴 严重（阻断正确运行）

| # | 问题 | 修复 | 状态 |
|:---|:---|:---|:---|
| 1 | `Capability-Registry.md` 不存在 | Step 4 统一指向 `skill-registry.json`，删引用 | 待执行 |
| 2 | `Decision-Log.jsonl` 不存在 | 创建文件，含 header + schema | 待执行 |
| 3 | 两张 Task-Type-Registry 分裂（JSON 9 vs MD 18） | JSON 补全 18 个 type_id | 待执行 |
| 4 | 2 个孤儿 type_id（`todo_reminder`、`tts_speak`） | 两表补建「提醒类任务」「内容朗读」 | 待执行 |

### 🟠 高（架构一致性受损）

| # | 问题 | 修复 | 状态 |
|:---|:---|:---|:---|
| 5 | 6 个规则文件缺 `layer` 字段（用了 `related:` 而非 `glossary:`） | 补 frontmatter | 待讨论（D） |
| 6 | `layer` 命名不一致（Layer 2 两种写法、Layer 3 两种写法） | 统一命名 | 待执行 |
| 7 | `method-reliability` 4 条记录用中文标签而非 `type_id` | 改为 type_id 值 | 待执行 |
| 8 | `Behavior-Fit-Log` 混合两种 schema（49 legacy + 4 compact） | 统一为 compact，legacy 独有字段设 null | 待讨论（A） |
| 9 | `Round-Behavior-Log` hot/warm 字段不重叠 | 统一字段全集，互缺字段设 null | 待讨论（B） |
| 10 | 7 个活跃技能的 type_id 在 JSON 注册表中缺失 | 补全 JSON 注册表 | 待执行 |

### 🟡 中（设计质量）— 本轮已展开讨论

| # | 问题 | 讨论结论 | 状态 |
|:---|:---|:---|:---|
| 11 | `flow-capability-encapsulate`（L3）直接依赖 gateway 数据文件 | **路径 1**：gateway 加别名登记协议 → flow 引用协议而非裸写文件。Path 1→2 阈值：封装冲突 ≥3 次 | ✅ 方案已定 |
| 12 | `task-init-protocol` ↔ `mod-decision-framework` 双向引用 | 待讨论（E） | ⚠ 待讨论 |
| 13 | 三规则无显式执行顺序 | 待讨论（F/G） | ⚠ 待讨论 |
| 14 | 4 个 stub 缺 `Lifecycle` | 分三类：2 个直接删、1 个改引用后删、1 个改引用后删。自动化链路已验证零依赖 ✅ | ✅ 方案已定 |

### ⚪ 低（完善度）

| # | 问题 | 修复 | 状态 |
|:---|:---|:---|:---|
| 15 | `skill-performance-log.jsonl` 零记录 | 等首次任务完成后写入 | 观察中 |
| 16 | `Candidate-Protocols/` 仅 README | 等知识脑学习产出新协议后写入 | 观察中 |
| 17 | `temp/` 有 stale git 追踪 | 清理 git 追踪 | 待执行 |

---

## 第二部分：核心诊断 + 能力分层（修复清单后半段）

| # | 内容 | 状态 |
|:---|:---|:---|
| 8 | `skill-registry.json` 每个技能加 `tier`：system / general / role | 待执行 |
| 9 | `task_type` 语义从"归属"改为"响应"——系统级可不声明 task_type | 待执行 |
| 10 | 补建 `todo_reminder`、`tts_speak` 两个 type_id（同 #4） | 待执行 |

---

## 第三部分：待确认讨论点

### 审计报告原讨论点

| 点 | 问题 | 说明 |
|:---|:---|:---|
| **A** | `Behavior-Fit-Log` 统一时，`adopted`/`deviation`/`suggestion` 要不要保留？ | compact 里砍了，但可能对 L3 分析有价值 |
| **B** | `Round-Behavior-Log` 的 hot/warm 要不要拆成两个文件？ | 字段集几乎不重叠，放一个文件违反原则 3 |
| **C** | 两张 Task-Type-Registry 谁是权威源？ | JSON 机器读？MD 人类读？还是保持镜像双写？ |
| **D** | 6 个缺 `layer` 的规则文件要不要一并补 frontmatter？ | — |

### 本轮会话新增讨论点

| 点 | 问题 | 说明 |
|:---|:---|:---|
| **E** | `task-init-protocol` ↔ `mod-decision-framework` 双向引用怎么解？ | 选项：① `trigger_points` 改抽象事件名；② 保留并加 `cross-layer-coupling: intentional`；③ `trigger` 字段改自描述措辞 |
| **F** | 三规则执行顺序编排方式？ | ① 新建横切编排规则；② 各规则 frontmatter 加 `sequence` + `consumes` 字段 |
| **G** | 三规则执行顺序本身？ | 建议：影响枚举 → 脚本预告 → 编码自检 → 落盘 → §5 差异审查 |
| **H** | 过渡方案登记系统？ | A) `CursorKnowledge_Project_Memo.md` 建 §过渡方案登记表；B) `Pending-Plan-Tracker.json` 建待办项（`resume_keywords` 提醒） |
| **I** | 待办提醒交互化 | 先保持现状，后续另建备忘讨论 |

---

## 第四部分：可立即执行（方案已定）

| 序号 | 操作 | 涉及文件 |
|:---:|:---|:---|
| 1 | 删除 `conversation-backup-commands.mdc` | 1 |
| 2 | 删除 `project-memo-commands.mdc` | 1 |
| 3 | 修正 `Earth_Library_Architecture.md` L329 + 删除 `earth-library-commands.mdc` | 2 |
| 4 | 修正 `script-coding-constraints.mdc` L45 + `script-option-ids.md` + `Cursor-usage-profile-and-templates.md` + 删除 `git-workspace-commit.mdc` | 4 |
| 5 | `gateway-command-router.mdc` 新增 §别名登记协议 | 1 |
| 6 | `flow-capability-encapsulate.mdc` 步骤 2 改引用 + frontmatter 改 `depends` | 1 |
| 7 | 创建 `Decision-Log.jsonl` | 1 |
| 8 | 两表补建 `todo_reminder`、`tts_speak` | 2 |
| 9 | JSON Task-Type-Registry 补全 18 个 type_id | 1 |

---

## 第五部分：过渡方案登记位（迁移阈值）

| ID | 方案 | 当前状态 | 升级触发条件 |
|:---|:---|:---|:---|
| T01 | `flow-capability-encapsulate` 路径 1（gateway 别名协议） | 待实施 | 封装冲突/重复登记 ≥3 次 → 升级路径 2 |
| T02 | 别名路由 → P008 迁移 | 设计注记 | Task-Type 连续 ≥5 成功 |
| T03 | 脚本重构整合 | 待启动 | `Scripts_Refactor_Plan` 复审 |
| T04 | GitHub Skills 接入 | 待首次接入 | 首次接入后验证 |
| T05 | 技能评估体系 | 待复审 | 1-2 天后复核 |
| T06 | `Active-Task-Tracker` schema 预留 | 预留中 | 任务分解系统接线 |
| T07 | `Decomposition-Log` 预留 | 预留中 | 自拆解能力上线 |
| T08 | 4 个 stub 文件清理 | 本轮执行 | 引用修正后立即 |
