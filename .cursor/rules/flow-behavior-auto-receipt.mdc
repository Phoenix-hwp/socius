---
description: 行为偏好自动收束工作流 — 双层记录（轮级快照 + 任务级归纳），任务完成自动触发 Behavior-Fit-Log.jsonl
glossary:
  layer: 3-工作流执行层
  module: BehaviorPreferences
  depends: [mod-behavior-crud-framework, Behavior-Dimensions-Registry]
---

# 行为偏好自动收束工作流

## 触发条件

每轮对话结尾（Agent 准备退出前）强制执行；无用户触发语。

---

## 步骤 A：轮级行为快照（每轮必须）

1. 读取 `10-Topics/Behavior-Dimensions-Registry.md`，获取当前启用的维度及值域。
2. 评估本轮行为，按启用维度填值。取值规则：
   - `指令风格`：本轮主要执行模式（多数操作属于哪种）。
   - `确认模式`：高风险操作是否等确认 → `关键确认`；几乎不等 → `跳过确认`；每步都等 → `逐步确认`。
   - `输出结构`：表格/结构化为主 → `表格化`；段落为主 → `分段叙述`；兼有 → `混合`。
   - `用户输入`：纯打字 → `直接文本`；用了 AskQuestion → `选项交互`；兼有 → `混合`。
   - `工具通道`：以本轮使用的工具类型组合判断；单类取具体值，≥3 类取 `混合`。
   - `特殊行为`：自由文本 ≤10 字。无显著特殊行为则填 `—`。
   - **值域强制校验（写入前）**：对照 `Behavior-Dimensions-Registry.md` 的维度定义列「值域」，逐字段检查取值是否在合法范围内：
     - `style`（指令风格）：仅允许 `直接执行` / `方案先于执行` / `问答为主`
     - `confirm`（确认模式）：仅允许 `跳过确认` / `关键确认` / `逐步确认`
     - `output_structure`（输出结构）：仅允许 `表格化` / `分段叙述` / `混合`
     - `user_input`（用户输入）：仅允许 `直接文本` / `选项交互` / `混合`
     - `tool_channel`（工具通道）：仅允许 `文件操作` / `Shell` / `MCP` / `脚本直达` / `混合`
     - `special`（特殊行为）：自由文本，无约束
     - 若任一字段值不在合法值域内，**拒绝写入**本轮热快照，输出一行 `⚠ 轮级热快照值域越界：<字段>=<非法值>，请修正为合法值域后重试`，本轮步骤 A 中断。
3. 确定快照归属：
   - 若本轮与某个活跃任务（`Active-Task-Tracker.json`）相关 → 归属该任务 ID。
   - 若与多个任务相关 → 归属主要任务。
   - 若无明确归属 → 归属 `其他`。
4. 追加一行 JSON 到 `10-Topics/Round-Behavior-Log.jsonl`，字段：`{"type":"hot","date":"<ISO>","task":"<ID|null>","style":"<值>","confirm":"<值>","output_structure":"<值>","user_input":"<值>","tool_channel":"<值>","special":"<值>","consumed":false}`。
   - 温快照同时追加一行 `{"type":"warm",...}`（见步骤 A.2）。

### 步骤 A.2：认知维度快照（每轮必须）

1. 从 `Behavior-Dimensions-Registry.md` 读取组别为「认知-A」「认知-B」的已启用维度及值域。
2. **取值策略（自评回忆，不扫描全轮对话全文）**：
   - **认知-A（auto — 结构类）**：问题拆解方式、认知路径。Agent 基于对本轮首条用户提问的 framing 判定——用户第一个问题问的是整体/框架/结构 → `自顶向下` / `先框架后细节`；问的是具体细节/某个字段/某个函数 → `自底向上` / `先细节后框架`。两维度默认取值一致但不强制（可一为自顶向下、一为先细节后框架，取决于用户是否有「先问整体但立刻追问细节」的模式）。
   - **认知-B（semi_auto — 语言类）**：语言风格、表达密度、情感基调。Agent 基于对**自己本轮回复内容**的元认知做自评判定，每条维度 10-15 字内完成：
     - `语言风格`：Agent 自问「我本轮回复用了多正式的措辞？是否使用了比喻或类比？是否以结构化层级组织？」→ 取值 `正式` / `口语` / `比喻` / `结构化`。
     - `表达密度`：Agent 自问「我本轮回复的信息量和修饰程度如何？」→ 取值 `简洁` / `适中` / `细腻`。若本轮既有简洁交互又有长篇分析 → 取主导风格。
     - `情感基调`：Agent 自问「我本轮回复的驱动力偏理性论证还是感性表达？」→ 取值 `逻辑` / `中性` / `情绪`。
   - B 组每个维度取值时**必须附带置信度**（`高` / `中` / `低`），由 Agent 自评本轮该维度信号是否足够明确。置信度为 `低` 的维度 L3 分析时跳过当轮。
3. **冷启动标记**：检查温快照表中已完成快照行数：若 ≤3 行 → 整行标记 `sample_phase: warmup`，B 组维度强制置信度 `低`。从第 4 行起进入正常采集阶段。
4. 追加一行 JSON 到 `10-Topics/Round-Behavior-Log.jsonl`（与热快照为同一文件），字段：`{"type":"warm","date":"<ISO>","task":"<ID|null>","decomposition":"<值>","cognitive_path":"<值>","language_style":"<值>","expression_density":"<值>","emotional_tone":"<值>","confidence":"<高|中|低>","sample_phase":"<warmup|active>","consumed":false}`。
   - 冷启动阶段 B 组维度强制置信度 `低`。

### 步骤 A+：方法可靠性注册表更新（每轮强制）

在步骤 A 写入快照后，更新 `.cursor/method-reliability-registry.json`：

1. 读取注册表，获取当前设备（`hostname`）的所有记录。
2. 对本轮快照中涉及的「任务类型 + 方法路径」，匹配现有行：
   - **已存在**：`总调用 += 1`；若本轮成功 → 重置连续失败为 0、连续成功 += 1；若本轮失败 → 连续失败 += 1、重置连续成功为 0。更新 `最后状态` 和 `最近上下文`。
   - **不存在**：新增一行（`总调用=1`，根据成功/失败初始化计数器）。
3. 重新计算状态：
   - 连续失败 ≥ 3 → `⚠ 降权`
   - 连续成功 ≥ 5（从降权恢复后）→ `✅ 正常`
   - 总失败率 > 70% 且总调用 ≥ 10 → `🚫 禁用`
4. 写回 JSON 文件（不改其他设备和任务类型的行）。
5. **冷启动跳过**：若本次温快照标记为 `sample_phase: warmup`，则跳过步骤 A+ 的注册表更新（避免冷启动期噪声污染方法统计）。

### 步骤 A++：设备执行规则自学习（每轮强制）

在步骤 A+ 更新方法可靠性注册表后，检查本轮是否有 Shell 命令执行失败：

1. 若本轮无 Shell 命令执行或全部成功 → 跳过本步。
2. 若存在 Shell 命令执行失败：
   - 分析失败根因，判断是否属于**设备平台约束**（OS/Shell 语法差异、环境变量缺失、路径分隔符等），而非方法本身的问题。
   - **若属于设备平台约束**：生成一条规则候选（`id` 自增，填 `scope`/`os`/`shell`/`condition`/`match`/`action`/`from`/`to`/`rationale`），但**不直接写入**。
   - 通过 AskQuestion 选项确认：
     - `[确认添加规则]` — 追加到 `.cursor/device-execution-rules.json` 的 `rules` 数组
     - `[跳过]` — 不添加，Agent 口头提醒即可
   - **禁止自动写入**：设备级规则会影响后续所有命令执行，必须经用户确认后再落盘。
   - 确认写入时检查是否已有相同 `from` + `to` + `os` + `shell` 的规则存在 → 若已存在则跳过，提示「规则已覆盖」。
3. **若不属于平台约束**（如网络超时、认证失败、业务逻辑错误）→ 不生成本步规则，仅依赖方法可靠性注册表（步骤 A+）的降权/恢复机制处理。

---

## 步骤 B：纠偏候选检测（每轮）

1. 若本轮做了语义纠偏操作（如将"需球"纠偏为"需求"）：
   - 检查该输入是否已在档案 §2 对照表中。
   - 若**不在**对照表中 → 追加到档案 §2 附近「待确认纠偏」队列。
2. 队列格式：

```
| 输入 | 当前纠偏为 | 出现上下文 | 日期 |
|------|-----------|-----------|------|
| 需球 | 需求 | 讨论功能迭代 | 2026-05-09 |
```

3. 任务完成时提示用户确认队列内容，确认后写入 §2 并清空队列。

---

## 步骤 B+：冲突检测（三层）

### B+1 L1 即时检测（本轮回复中）

1. 若本轮用户指令与档案 §3 声明的偏好存在显性冲突（如档案写"结论先行"，用户要求先展开推理），在回复结尾轻标注：
   ```
   ⚠ 本轮 X 维度与 §3 偏好不一致：<具体差异>。照常执行。
   ```
2. 不中断流程，不做额外写入。

### B+2 L2 记录检测（写入快照时）

1. **操作维度（旧 6 维度）**：在步骤 A 写入热快照时，对照档案 §3 对应偏好，逐维度比对。若某维度实际行为与档案声明不一致 → 在 `特殊行为` 列追加 `冲突:维度名`。
2. **认知-A 维度（问题拆解方式、认知路径）**：第 5 轮起启用。在步骤 A.2 写入温快照时，对照档案 §3 认知偏好声明，逐维度比对。冲突时在温快照 `阶段` 列追加 `冲突:维度名`。
3. **认知-B 维度（语言风格、表达密度、情感基调）**：第 10 轮起启用，且仅当该维度的滚动 5 轮众数 ≥3 时才参与比对。冲突时在温快照 `阶段` 列追加 `冲突:维度名`。
4. 若多项冲突，逐项列出。

### B+3 L3 统计检测（任务收束时）

1. 在步骤 C 任务完成自动收束时，统计该任务期间所有快照中冲突标注：
   - 热快照（操作维度）→ `特殊行为` 列的 `冲突:` 标注
   - 温快照（认知维度）→ `阶段` 列的 `冲突:` 标注
2. 若同维度冲突出现 ≥2 次 → 在 Behavior-Fit-Log.jsonl 的日志行末尾（或口头回执中）提示：
   ```
   该任务期间确认模式与档案冲突 3/5 轮，建议确认：该偏好是否已变化？
   ```
3. 不自动修改档案 — 用户决定。冲突标注可作为 L3 模式发现的输入之一。

---

## 步骤 C：任务完成自动收束（仅当任务状态变更时）

触发条件：`10-Topics/Active-Task-Tracker.json` 中任一任务状态从 `进行中` → `已完成`。

1. **收集快照**：从 `Round-Behavior-Log.jsonl` 中筛选该任务 ID 的所有行（`type: "hot"`）。
2. **合并归纳**：
   - 若该任务只有 1 轮 → 直接使用该轮快照。
   - 若跨多轮 → 合并为综合描述：各维度取多数值，特殊行为合并去重。
3. **追加到 `10-Topics/Behavior-Fit-Log.jsonl`**（精简版）：
   - JSON 字段：`{"date":"<ISO>","topic":"<摘要>","behavior":"<六字段管道分隔>","fit":"<高|中|低>","format":"compact"}`
   - 行为摘要格式：`指令风格 \| 确认模式 \| 输出结构 \| 用户输入 \| 工具通道 \| 特殊行为`
   - 契合度：对照档案 §3 评估偏差，高/中/低。
4. **归档任务**：在 `Active-Task-Tracker.json` 中将该任务从 `active` 移到 `archive`。
5. **标记已消费**：在 `Round-Behavior-Log.jsonl` 中将已消费的快照行 `consumed` 改为 `true`。
6. **提示下游**：若有其他任务的依赖字段指向本任务，提示用户。
7. **保留手动收束**：用户仍可用 `/收束` 触发完整版（含建议微调/采纳情况列）。
8. **待办自动完成检测**（步骤 C 末尾，无论任务是否状态变更）：
   1. Read `10-Topics/Pending-Plan-Tracker.json`，取所有 `status: "in_progress"` 的项。
   2. 检查本轮对话是否涉及该待办的 `resume_keywords`。
   3. 若命中 → Agent 自行判断 `next_action` 是否已实际完成（结合本轮 `special` 快照字段）。
   4. 若判定完成 → AskQuestion `[确认完成 Pxxx] [仍进行中]`。
   5. 确认后更新 tracker.json：`status: "completed"` + `completed_date: "<ISO日期>"`，从 `pending` 移到 `archive`。
   6. **不做自动静默完成**（防误判），不自动写备忘文件。
9. **§5 事后审查兜底**（步骤 C 末尾强制，无论任务是否状态变更）：
   1. 检查本轮写入文件数（从本轮快照的 `special` 字段或 `git diff --stat` 推断）。
   2. 若 ≥3 文件写入 → 回查 `pre-change-impact-enumeration.mdc` §5 是否完整执行：
      - §5a：基线 commit 是否已记录？
      - §5b：内核路径（`.cursor/rules/`、`10-Topics/`）的详细 diff 是否执行？
      - §5c：三类展示（预期内/连锁影响/非预期）是否输出？
      - §5d：确认回执是否给出？
   3. 若任一缺失 → **阻断，补执行对应步骤后放行**。
   4. 若全部执行 → 记录到本轮收束回执末尾「✅ §5 审查已通过」。

---

## 步骤 C+：技能表现评估（任务完成时）

### 触发条件

步骤 C 执行完毕后，检查本轮或本任务期间是否使用了 `Skills_Library/skills/` 或 `Skills_Library/scripts/` 中的技能完成了任务。

> 触发范围：注册表中 `status: active` 的**外部技能**（注册表条目含 `source_url`）和**内部技能**（注册表条目无 `source_url` 但被本轮调用）。
> 本轮如未使用任何技能 → 跳过 C+，直接进入步骤 D。

### 评估来源

1. 本轮对话的上下文（Agent 对表现的自评回忆）。
2. 步骤 C 中已收集的快照数据（轮次、Token 消耗）。
3. 用户的明确纠正反馈（如有）。

### 评估流程

#### C+1 识别本轮使用的技能

- 搜索本轮对话中调用的技能路径（`Skills_Library/skills/<name>/` 或注册表 ID）。
- 对照 `skill-registry.json` 确认技能 ID 和 `task_types`。

#### C+2 自评五维

按 `mod-skill-evaluation.mdc` §3 的五维评分标准自评：

| 维度 | 评分 | 自评方式 |
|:---|:---|:---|
| `correctness`（0-5） | 正确性 | 参考 §3.1 用户纠正行为检测 |
| `autonomy`（0-5） | 自主度 | 用户纠正次数 0→5，1次→4，2次→3，显著错误→2，3+次→1，完全失败→0 |
| `side_effects`（0-5） | 副作用 | `git diff --stat` 检查是否产生了预期外写入；无→5，有小副作用但已清理→3，预期外且未清理→0 |
| `efficiency_turns`（0-5） | 效率（轮次） | 参考 §3.2，首次使用时取 3（默认中位） |
| `efficiency_tokens`（0-5） | 能耗（Token） | 参考 §3.2，首次使用时取 3（默认中位） |

`total = correctness + autonomy + side_effects + efficiency_turns + efficiency_tokens`（满分 25）。

#### C+3 AskQuestion 轻确认

弹窗展示评估结果：
- 标题：`技能表现评估 — <技能名称>`
- prompt：展示五维得分 + total + 简短理由（1-2 句）
- 选项：`[确认评估] [修改评估值]`
  - `[确认评估]`：以当前得分写入日志。
  - `[修改评估值]`：Agent 逐一询问用户需调整的维度（可单选或多选），修改后重新展示。

若本任务使用了多个技能，逐技能弹出 AskQuestion（先全部自评完，再逐个确认）。

#### C+4 写入表现日志

追加一行 JSON 到 `Skills_Library/skill-performance-log.jsonl`：

```json
{"date":"<ISO>","skill_id":"<注册表ID>","task_type":"<任务类型>","correctness":<0-5>,"autonomy":<0-5>,"side_effects":<0-5>,"efficiency_turns":<0-5>,"efficiency_tokens":<0-5>,"total":<0-25>}
```

#### C+5 更新技能注册表

在 `skill-registry.json` 中更新该技能条目：

1. `last_used` → 当前 ISO 时间。
2. `performance_history` 数组追加一条 `{"date":"<ISO>","task_type":"<类型>","total":<分数>}`。
3. `performance_score` → 更新为 `performance_history` 中最近 5 次 `total` 的算术均值（< 5 次取全量）。见 `mod-skill-evaluation.mdc` §3.3。

### 降级策略

- **`skill-performance-log.jsonl` 不存在** → 创建该文件（含头部说明行）。
- **同一技能在同任务中被多轮调用** → 仅在任务收束时合并评估一次（避免重复评分）。
- **`git diff --stat` 不可用（非 Git 环境）** → `side_effects` 维度标记为 `null`，不参与 total 计算，total 满分降为 20。
- **用户说「跳过评估」** → 本轮不写入日志，但 Agent 口头提醒「该技能缺少本轮表现数据」。

### 与已有规则的关系

| 规则 | 触发时机 | 分工 |
|:---|:---|:---|
| `mod-skill-evaluation.mdc` §3 | — | 五维评分标准定义 |
| `mod-skill-evaluation.mdc` §8 | — | 本步骤的顶层设计 |
| **本步骤（C+）** | **任务收束时** | **执行落地** |
| `flow-skill-execute.mdc` | Skill 执行前/中/后 | 执行级安全约束 |
| `flow-behavior-auto-receipt.mdc` 步骤 C | 任务完成时 | 行为快照合并与归档（C+ 的上游） |

---

## 步骤 D：任务被取消时

1. 在 `Active-Task-Tracker.json` 中将该任务从 `active` 移到 `archive`，状态=`取消`。
2. 已积累的快照行 `consumed` 保持 `true`（备查，不写入 Behavior-Fit-Log.jsonl）。
3. 提示用户：如有依赖本任务的下游，需确认是否继续。

---

## 步骤 E：`/收束` 手动触发（保留）

1. 用户发送 `/收束` / `结束对话` / `结束会话` / `结束` 时：
2. **⚠ 强制阻断 — 进行中任务确认**：扫描 `10-Topics/Active-Task-Tracker.json` `active` 数组中所有状态为「进行中」的任务。对每条任务**必须使用 `AskQuestion` 工具**弹出可点击选项，**不得用纯文本替代**：
   - 选项 `[已完成]`：用户确认后，Agent 将该任务状态改为「已完成」，触发步骤 C（任务完成自动收束），再继续本步骤下一条。
   - 选项 `[继续]`：保留「进行中」状态，跳过该任务。
   - 若同时存在多条进行中任务 → 逐条逐个弹出 AskQuestion（如上一条选「已完成」则等待步骤 C 完成后再弹出下一条）。
   - 此步为强制阻断：未完成所有进行中任务的 AskQuestion 确认前，**不得进入后续步骤 3**。
3. 执行完整收束（含「建议的档案微调」「采纳情况」列），追加 `10-Topics/Behavior-Fit-Log.jsonl` 完整行。
4. 写回档案 UTF-8（无 BOM）：`Cursor-usage-profile-and-templates.md` + `Behavior-Fit-Log.jsonl`。

---

## 与 L3 分析的关系

本工作流产出的标准化 Behavior-Fit-Log.jsonl 日志行，供 L3 分析消费。

### L3 触发机制

> **闸门阻断（强制）**：不在收束时自动创建规则。达到阈值后，由 `kernel-runtime.mdc` 在下次 sessionStart 执行 L3 闸门检查并强制阻断确认。见该文件「开端（闸门）」行。

| 层级 | 触发条件 | 动作 |
|:---|:---|:---|
| **闸门阻断（4a）** | `Behavior-Fit-Log.jsonl` 中 `format: "compact"` 的行数 ≥20 且 `.cursor/rules/flow-behavior-l3-analysis.mdc` 不存在 | sessionStart 强制阻断 → AskQuestion `[确认创建 L3 分析规则] [稍后（下次提醒）]` → 创建规则 + 首次分析输出 |
| **静默刷新（4b）** | 规则已存在且自上次分析后 `Behavior-Fit-Log.jsonl` 新增 `format: "compact"` 的行 ≥5 行 | 自动更新 `10-Topics/L3_Analysis_Output.md`（不阻断） |
| **档案更新（4c）** | 连续 3 次分析同维度趋势方向一致 | AskQuestion `[确认更新档案 §§1-3] [暂缓]`（附变更预览） |

### L3 分析规则自动构建规格（创建时落地）

> 以下规格在 `.cursor/rules/flow-behavior-l3-analysis.mdc` 首次创建时落地。

**数据来源**：
- **操作维度**：`Behavior-Fit-Log.jsonl` `format: "compact"` 行的 `behavior` 字段（六字段管道分隔格式）
- **认知维度**：`Round-Behavior-Log.jsonl` `type: "warm"` 行

**各维度独立阈值**：

| 维度组 | 最少有效快照 | 取值方式 | 平滑方式 |
|:---|:---|:---|:---|
| 操作（旧 6 维度） | 10 行 | 行为摘要列直接读取 | 单轮值（噪声低） |
| 认知-A（拆解方式 / 认知路径） | 15 行 | 温快照列直接读取 | 单轮值（噪声低） |
| 认知-B（语言风格 / 表达密度 / 情感基调） | 20 行（含 ≥12 有效行，即置信度 ≠ 低的占比 ≥60%） | 温快照列读取，跳过置信度 = 低的轮次 | 滚动 5 轮众数 |
| 认知-C（价值排序） | 20 行 | 从操作维度 + 认知维度多信号推断 | 滚动 5 轮 |

**落地内容**：`.cursor/rules/flow-behavior-l3-analysis.mdc` 包含：
- **步骤 1：模式发现**（≥10 行即执行）——各维度频次统计 + 组合模式识别（同轮多维度联合分布）
- **步骤 2：趋势检测**（≥20 行才执行）——按时间排序后拆分前半/后半各维度分布对比，检测方向性变化
  - 拆分方式：按创建时间排序，取前 50% 为「前半段」、后 50% 为「后半段」
  - 判断规则：任一维度前后半占比变化 ≥30 个百分点且后半样本数 ≥3 → 标记趋势；多维度同方向 → 标注「偏好漂移」
- **步骤 3：滚动众数平滑**（B 组维度）——取最近 5 轮有效快照的众数作为当前有效认知值；离群检测（单轮值与滚动众数差距极大 → 标记离群，不参与统计，追加到待确认队列）
- **步骤 4：维度独立性自检**——若两个维度取值总是联动（如「正式」总伴随「细腻」），标记「需人工复核维度独立性」
- **步骤 5：合并报告**——统一输出建议，人工确认后更新档案 §§1-3

**输出格式**：`10-Topics/L3_Analysis_Output.md`（趋势检测报告，供 Agent 后续对话时读取）

**首次分析**：由闸门阻断确认后执行。后续分析由静默刷新触发。
