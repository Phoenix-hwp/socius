---
description: 行为偏好自动收束工作流：双层记录（轮级快照 + 任务级归纳），任务完成自动触发 Behavior-Fit-Log.jsonl
glossary:
  layer: 3-工作流执行层
  module: BehaviorPreferences
  depends: [mod-behavior-crud-framework, Behavior-Dimensions-Registry]
guard_replaceable: false
  pipeline_markers:
    rule: "Agent 在自动收束的**每个步骤(A-E)完成*必须在回复末尾输`[Step_ID]` 标记。Step A(轮级快照)Step E(任务级归日志落盘)required=true，Step B/C/D 可选——Guard 下次启动时自动扫描缺失标记并阻断
    close_check: "**收束前自检（强制）**：在最后一E（落盘）前，Agent 必须回溯检查本A/D 是否均有完成标记。若有缺补充 `[Step_ID]（补标）` 后落盘
    format:
      done: "[Step_ID]"
      skip: "[Step_ID: reason]"
    definitions: ".cursor/workflow-definitions.json behavior_auto_receipt"

> **Guard 轨道**：任务级反馈评分`feedback.py` 执行。收束时Decision-Log 写入`decision_log.py` 执行。收束流程本身的触发/交互/AskQuestion Agent 执行，无 Guard 代码覆盖，保留本工作流完整步骤---

# 行为偏好自动收束工作
## 触发条件

每轮对话结尾（Agent 准备退出前）强制执行；无用户触发语
---

## 步骤 A：轮级行为快照（每轮必须
1. 读取 `docs/Behavior-Dimensions-Registry.md`，获取当前启用的维度及值域2. 评估本轮行为，按启用维度填值。取值规则：
   - `指令风格`：本轮主要执行模式（多数操作属于哪种）   - `确认模式`：高风险操作是否等确`关键确认`；几乎不`跳过确认`；每步都`逐步确认`   - `输出结构`：表结构化为`表格化`；段落为`分段叙述`；兼`混合`   - `用户输入`：纯打字 `直接文本`；用AskQuestion `选项交互`；兼`混合`   - `工具通道`：以本轮使用的工具类型组合判断；单类取具体值， 类取 `混合`   - `特殊行为`：自由文0 字。无显著特殊行为则填 `—`   - **值域强制校验（写入前*：对`Behavior-Dimensions-Registry.md` 的维度定义列「值域」，逐字段检查取值是否在合法范围内：
     - `style`（指令风格）：仅允许 `直接执行` / `方案先于执行` / `问答为主`
     - `confirm`（确认模式）：仅允许 `跳过确认` / `关键确认` / `逐步确认`
     - `output_structure`（输出结构）：仅允许 `表格化` / `分段叙述` / `混合`
     - `user_input`（用户输入）：仅允许 `直接文本` / `选项交互` / `混合`
     - `tool_channel`（工具通道）：仅允`文件操作` / `Shell` / `MCP` / `脚本直达` / `混合`
     - `special`（特殊行为）：自由文本，无约     - 若任一字段值不在合法值域内，**拒绝写入**本轮热快照，输出一`轮级热快照值域越界字段>=<非法，请修正为合法值域后重试`，本轮步A 中断。
3. 确定快照归属   - 若本轮与某个活跃任务（`Active-Task-Tracker.json`）相归属该任ID   - 若与多个任务相关 归属主要任务   - 若无明确归属 归属 `其他`4. 追加一JSON `core/data/Round-Behavior-Hot.jsonl`，字段：`{"type":"hot","date":"<ISO>","task":"<ID|null>","style":"<","confirm":"<","output_structure":"<","user_input":"<","tool_channel":"<","special":"<","consumed":false}`   - 温快照同时追加一`{"type":"warm",...}` `core/data/Round-Behavior-Warm.jsonl`（见步骤 A.2）
### 步骤 A.2：认知维度快照（每轮必须
1. `Behavior-Dimensions-Registry.md` 读取组别为「认A」「认B」的已启用维度及值域2. **取值策略（自评回忆，不扫描全轮对话全文*   - **认知-A（auto 结构类）**：问题拆解方式、认知路径。Agent 基于对本轮首条用户提问的 framing 判定——用户第一个问题问的是整体/框架/结构 `自顶向下` / `先框架后细节`；问的是具体细节/某个字段/某个函数 `自底向上` / `先细节后框架`。两维度默认取值一致但不强制（可一为自顶向下、一为先细节后框架，取决于用户是否有「先问整体但立刻追问细节」的模式）   - **认知-B（semi_auto 语言类）**：语言风格、表达密度、情感基调。Agent 基于*自己本轮回复内容**的元认知做自评判定，每条维度 10-15 字内完成     - `语言风格`：Agent 自问「我本轮回复用了多正式的措辞？是否使用了比喻或类比？是否以结构化层级组织？」→ 取`正式` / `口语` / `比喻` / `结构化`     - `表达密度`：Agent 自问「我本轮回复的信息量和修饰程度如何？」→ 取`简洁` / `适中` / `细腻`。若本轮既有简洁交互又有长篇分取主导风格     - `情感基调`：Agent 自问「我本轮回复的驱动力偏理性论证还是感性表达？」→ 取`逻辑` / `中性` / `情绪`   - B 组每个维度取值时**必须附带置信*（`高` / `中` / `低`），Agent 自评本轮该维度信号是否足够明确。置信度`低` 的维L3 分析时跳过当轮。
3. **冷启动标*：检查温快照表中已完成快照行数： 整行标记 `sample_phase: warmup`，B 组维度强制置信度 `低`。从4 行起进入正常采集阶段4. 追加一JSON `core/data/Round-Behavior-Warm.jsonl`（与热快照分离存储），字段：`{"type":"warm","date":"<ISO>","task":"<ID|null>","decomposition":"<","cognitive_path":"<","language_style":"<","expression_density":"<","emotional_tone":"<","confidence":"<高|中|","sample_phase":"<warmup|active>","consumed":false}`   - 冷启动阶B 组维度强制置信度 `低`
### 步骤 A+：方法可靠性注册表更新（每轮强制）

在步A 写入快照后，更新 `.cursor/method-reliability-registry.json`
1. 读取注册表，获取当前设备（`hostname`）的所有记录2. 对本轮快照中涉及的「任务类+ 方法路径」，匹配现有行：
   - **已存*：`总调+= 1`；若本轮成功 重置连续失败0、连续成+= 1；若本轮失败 连续失败 += 1、重置连续成功为 0。更`最后状态` `最近上下文`   - **不存*：新增一行（`总调1`，根据成失败初始化计数器）。
3. 重新计算状态：
   - 连续失败 。
3 `降权`
   - 连续成功 5（从降权恢复后）`正常`
   - 总失败率 > 70% 且总调10 `🚫 禁用`
4. 写回 JSON 文件（不改其他设备和任务类型的行）5. **冷启动跳*：若本次温快照标记为 `sample_phase: warmup`，则跳过步骤 A+ 的注册表更新（避免冷启动期噪声污染方法统计）
### 步骤 A++：设备执行规则自学习（每轮强制）

在步A+ 更新方法可靠性注册表后，检查本轮是否有 Shell 命令执行失败
1. 若本轮无 Shell 命令执行或全部成跳过本步2. 若存Shell 命令执行失败   - 分析失败根因，判断是否属*设备平台约束**（OS/Shell 语法差异、环境变量缺失、路径分隔符等），而非方法本身的问题   - **若属于设备平台约*：生成一条规则候选（`id` 自增，填 `scope`/`os`/`shell`/`condition`/`match`/`action`/`from`/`to`/`rationale`），*不直接写*   - 通过 AskQuestion 选项确认     - `[确认添加规则]` 追加`.cursor/device-execution-rules.json` `rules` 数组
     - `[跳过]` 不添加，Agent 口头提醒即可
   - **禁止自动写入**：设备级规则会影响后续所有命令执行，必须经用户确认后再落盘   - 确认写入时检查是否已有相`from` + `to` + `os` + `shell` 的规则存若已存在则跳过，提示「规则已覆盖」。
3. **若不属于平台约束**（如网络超时、认证失败、业务逻辑错误）→ 不生成本步规则，仅依赖方法可靠性注册表（步A+）的降权/恢复机制处理
---

## 步骤 B：纠偏候选检测（每轮
1. 若本轮做了语义纠偏操作（如将"需纠偏需）：
   - 检查该输入是否已在档案 §2 对照表中   - *不在**对照表中 追加到档§2 附近「待确认纠偏」队列2. 队列格式
```
| 输入 | 当前纠偏| 出现上下| 日期 |
|------|-----------|-----------|------|
| 需| 需| 讨论功能迭代 | 2026-05-09 |
```

3. 任务完成时提示用户确认队列内容，确认后写§2 并清空队列
---

## 步骤 B+：冲突检测（三层
### B+1 L1 即时检测（本轮回复中）

1. 若本轮用户指令与档案 §3 声明的偏好存在显性冲突（如档案写"结论先行"，用户要求先展开推理），在回复结尾轻标注   ```
   本轮 X 维度§3 偏好不一致：<具体差异>。照常执行   ```
2. 不中断流程，不做额外写入
### B+2 L2 记录检测（写入快照时）

1. **操作维度（旧 6 维度*：在步骤 A 写入热快照时，对照档§3 对应偏好，逐维度比对。若某维度实际行为与档案声明不一`特殊行为` 列追`冲突:维度名`2. **认知-A 维度（问题拆解方式、认知路径）**：第 5 轮起启用。在步骤 A.2 写入温快照时，对照档§3 认知偏好声明，逐维度比对。冲突时在温快照 `阶段` 列追`冲突:维度名`。
3. **认知-B 维度（语言风格、表达密度、情感基调）**：第 10 轮起启用，且仅当该维度的滚动 5 轮众 时才参与比对。冲突时在温快照 `阶段` 列追`冲突:维度名`4. 若多项冲突，逐项列出
### B+3 L3 统计检测（任务收束时）

1. 在步C 任务完成自动收束时，统计该任务期间所有快照中冲突标注   - 热快照（操作维度）→ `特殊行为` 列的 `冲突:` 标注
   - 温快照（认知维度）→ `阶段` 列的 `冲突:` 标注
2. 若同维度冲突出现  Behavior-Fit-Log.jsonl 的日志行末尾（或口头回执中）提示   ```
   该任务期间确认模式与档案冲突 3/5 轮，建议确认：该偏好是否已变化？
   ```
3. 不自动修改档用户决定。冲突标注可作为 L3 模式发现的输入之一
---

### C0：收束前自我诊断（心理表征比对，CP-067
在填写快照之前，Agent 在脑中快速运行一次「预vs 实际」比对（CP-067 功用⑤）
1. 我预期的任务完成路径是什么？（如 步完成，用户直接接受」）
2. 实际情况是什么？（如「花4 步，用户纠正2 次」）
3. 差距暴露了我的哪些心理表征缺陷？
   - 是拆解粒度不对？（该拆的更细/更粗   - 是领域知识不足？（对该任务域的模式积累不够）
   - 是策略选择偏差？（用错了方法路径）

此诊断不出现在快照字段中，仅作为 Agent 的内部反思工具。诊断结果影响后续同类型任务的策略选择
---

## 步骤 C：任务完成自动收束（仅当任务状态变更时
触发条件：`core/data/Active-Task-Tracker.json` 中任一任务状态从 `进行中` `已完成`
1. **收集快照**：从 `Round-Behavior-Hot.jsonl` 中筛选该任务 ID 的所有行（`type: "hot"`）2. **合并归纳**   - 若该任务只有 1 直接使用该轮快照   - 若跨多轮 合并为综合描述：各维度取多数值，特殊行为合并去重。
3. **追加`core/data/Behavior-Fit-Log.jsonl`**（精简版）   - JSON 字段：`{"date":"<ISO>","topic":"<摘要>","behavior":"<六字段管道分","fit":"<高|中|","format":"compact"}`
   - 行为摘要格式：`指令风格 \| 确认模式 \| 输出结构 \| 用户输入 \| 工具通道 \| 特殊行为`
   - 契合度：对照档案 §3 评估偏差，高/低4. **归档任务**：在 `Active-Task-Tracker.json` 中将该任务从 `active` 移到 `archive`5. **标记已消*：在 `Round-Behavior-Hot.jsonl` `Round-Behavior-Warm.jsonl` 中将已消费的快照`consumed` 改为 `true`6. **提示下游**：若有其他任务的依赖字段指向本任务，提示用户7. **保留手动收束**：用户仍可用 `/收束` 触发完整版（含建议微采纳情况列）8. **待办自动完成检*（步C 末尾，无论任务是否状态变更）   1. Read `core/data/Pending-Plan-Tracker.json`，取所`status: "pending"` `status: "in_progress"` 的项   2. 检查本轮对话是否涉及该待办`resume_keywords`   3. 若命Agent 自行判断 `next_action` 是否已实际完成（结合本轮 `special` 快照字段、写入文件内容）   4. 若判定完AskQuestion `[确认完成 Pxxx] [仍进行中]`   5. 确认后更tracker.json：`status: "completed"` + `completed_date: "<ISO日期>"`，从 `pending` 移到 `archive`   6. **不做自动静默完成**（防误判），不自动写备忘文件9. **§5 事后审查兜底**（步C 末尾强制，无论任务是否状态变更）   1. 检查本轮写入文件数（从本轮快照`special` 字段`git diff --stat` 推断）   2.  文件写入 回查 `pre-change-impact-enumeration.mdc` §5 是否完整执行      - §5a：基commit 是否已记录？
      - §5b：内核路径（`.cursor/rules/`、`docs/`）的详细 diff 是否执行      - §5c：三类展示（预期连锁影响/非预期）是否输出      - §5d：确认回执是否给出？
   3. 若任一缺失 **阻断，补执行对应步骤后放*   4. 若全部执记录到本轮收束回执末尾「✅ §5 审查已通过」10. **知识脑缺口复*（步C 末尾，无论任务是否状态变更）   1. Agent 自问：「如果重做一次这个任务，我缺什么知识就不需要试这么多轮？   2. 检测信号：
      - 本轮多轮试错（工具切 次或参数调整  次才成功      - 任务涉及的知识领域在 `core/knowledge/framework.md` Earth Library 中无覆盖
   3. 若信号命中且无覆在收束回执末尾追加：
      ```
      > 💡 知识脑缺口：本轮 [任务简述] [领域] 上试错较多      > 如果你有相关阅读笔记/文章/书籍，可以让我读一读，下次类似任务我会更准      ```
   4. 不阻断收束，仅追加提示
11. **周期知识脑轻*（步C 末尾，低频触发）   1. 检`Active-Task-Tracker.json` 最近完成的任务类型
   2. 若连 个非重复任务未触发知识脑 + 累计  个新领域缺口 在回执末尾：
      ```
      > 💡 最近遇到了 [领域A]、[领域B] 两个新知识域，知识脑尚未覆盖      > 你有相关阅读资料可以让我补一补吗      ```

12. **瞬态文件自动清*（步C 末尾强制，无论任务是否状态变更）   1. 扫描全库 `Lifecycle: 瞬态` 的文件（Grep 搜索 `Lifecycle: 瞬态` + Glob 匹配 `TEMP_*` 前缀）   2. 若结果为跳过，回执末尾标`🗑 无瞬态文件需清理`   3. 若非逐文件物理删除（`Remove-Item` / `rm`），跳过软删除，不弹 AskQuestion   4. 删除后回执末尾标`🗑 瞬态文xN 已清理（全库扫描）`   > **设计意图**：瞬态文件不限存放路径，frontmatter `Lifecycle: 瞬态` 标记识别；收束时自动清空，避免散落堆积
13. **协议有效性反*（步C 末尾强制，无论任务是否状态变更）   1. 从本轮对应的 `Decision-Log.jsonl` 条目中读`kb_protocols_activated` 字段   2. 若为空或不存跳过，标`📋 KB 协议无激活，跳过有效性评估`   3. 若非对每条激活协议执行四态判断：

   | 状| 判定信号 | 结果 | 动作 |
   |------|----------|------|------|
   | 有效 | 任务 `result: "success"` + Agent 在执行中参考了该协+ 无用户纠正涉及该协议的主| `effective: true` | 写入 `activation-log.jsonl`，该协议 `effective_count += 1` |
   | 协议错误 | 用户纠正Agent，且纠正内容**直接反驳**协议核心主张（如协议说「用 A 标准判断」，用户说「不对，应该B」） | `effective: false, miscue: "protocol"` | 写入 `activation-log.jsonl`，记`miscue_type: "protocol_wrong"`，附 `pended_review: true` |
   | 场景误用 | 用户纠正Agent，但纠正的是**场景选择**（如「这个场景不该用这份协议」），而非协议内容本身 | `effective: false, miscue: "scene"` | 写入 `activation-log.jsonl`，记`miscue_type: "scene_mismatch"`，附 `pended_review: true` |
   | 未采| 协议被激活但 Agent 本轮执行中未明显参考该协议（既无引用也无纠正） | `effective: null` | 写入 `activation-log.jsonl`，`effective: null`，该协议 `bystander_count += 1`。同 task_type `bystander_count 5` `effective_count = 0` 下次同一 task_type 时该协议不进入候选池（由激活引擎过滤器 2 降权|

   4. **的区分规*（Agent 自判，不弹窗）：
      - Agent 检查用户纠正发生时，该协议是否处于激活触发态（activation engine 标记      - 在触发+ 用户纠正直接反驳协议主张 ②（协议错误      - 在触发+ 用户纠正的是操作方式而非主张 ③（场景误用      - 不在触发③（场景误用，协议未被正确激活）
   5. **统一标记 `pended_review: true`**，不在本轮自动定案。下次对话开头轻问一条：
      ```
      > 💡 上次执行中协[CP-xxx] 被标记为 `pended_review`：[简述纠正内容]      > 是协议本身有问题，还是我用错了场景？
      ```
   6. **写入 `core/knowledge/activation-log.jsonl`**（新建或追加一行）      ```json
      {"date":"<ISO>","task_id":"<ID>","protocol_id":"<CP-xxx>","task_type":"<type>","concept_anchor":"<anchor>","effective":<true|false|null>,"miscue_type":<null|"protocol_wrong"|"scene_mismatch">,"pended_review":<false|true>,"bystander_count":<number>,"note":"<0字简"}
      ```
      - `activated_count` 对①②③④均 `+1`（协议被激活）
      - `effective_count` 仅①`+1`
      - `bystander_count` 仅④`+1`
      - `bystander_count` 重置条件：若本轮评估后同 task_type `effective_count` 0 变为 ，则task_type `bystander_count` 重置0（该协议终于被用上了，之前累积的旁观不再有意义）
   7. 全部协议评估完毕后，更新 `Decision-Log.jsonl` 对应条目      - `kb_effective`：全部协`effective=true` `true`；任一 `effective=false` `false`；全`effective=null` `null`
      - `kb_miscued`：`effective=false` 的协ID 列表
   8. 评估后在回执末尾标注 `📊 KB 协议反馈 xN 已写activation-log`   
   8-bis. **写入 `core/data/experience-index.jsonl`**（M0 统一经验索引 步骤 C 末尾强制，与 activation-log 同步）：
      1. 取本轮任务的基础信息：`task_id` / `task_type` / `date` / `p008_level`（从 Decision-Log 取） / `result`
      2. 取本轮写入的各日志源的行号范围：
         - `behavior_log_line`：本轮最后写入的 Behavior-Fit-Log 行号（若步骤 C 未写行为日志，填 `null`         - `decision_log_line`：本轮最后写入的 Decision-Log 行号（若本轮无决策，`null`         - `hot_log_lines`：本Round-Behavior-Hot 的行号范`[start, end]`（若无，`null`         - `warm_log_lines`：本Round-Behavior-Warm 的行号范围（同上         - `activation_log_lines`：本activation-log 的行号范围（同上      3. 提取 `keywords`：从任务描述 + 本轮执行的操作中。
3-7 个关键词（用于后续续接检索匹配）
      4. 追加一行到 `experience-index.jsonl`（单JSON，不含换行符   
   9. **待消化池检*（步C 末尾强制，无论任务是否状态变更）      1. 检`core/knowledge/pending-digestion.json` 是否存在`candidates` 数组非空      2. 若为空或不存跳过，标`📋 待消化池为空，跳过`      3. 若非输出待消化池摘要         ```
         📋 待消化池：本轮任务中获取N 条候选知识，即将进入消化管线            [立即消化]  [稍后处理]
         ```
      4. `[立即消化]`：逐条走消化管线（Step 0 源分Step 1.5 诠释自检 Step 1-4 分类路由 P2 四问闸门 Step R 读后总结 Step S 全域巡检）。消化完成后清空 `pending-digestion.json`      5. `[稍后处理]`：保`pending-digestion.json`，下次收束时再次提醒。标`📋 待消化池保留（N 条待处理）`
14. **跨设备上下文移交摘要**（步C 末尾强制，无论任务是否状态变更）   1. Agent 自评本轮对话的核心上下文状态：
      - `active_task`：本轮主要讨论的任务/主题（≤30 字）
      - `pending_items`：本轮未完成的待处理事项（若有多轮项目，Master_Control 待处理池取前 3 条；若无归属，取本轮讨论中已提出但未解决的事项， 条）
      - `behavioral_tone`：本轮对话的行为模式简述（如「方案设计模式，结构化讨论「快速问答模式，用户主导「探索调试模式」）
   2. 获取当前设备标记（`hostname`）   3. 从本`Decision-Log.jsonl` 条目中提`kb_protocols_activated` 作为 `active_protocols`；提取最近的决策简述作`last_decision`   4. 生成移交摘要 JSON **覆盖写入** `core/data/context-handoff.json`（单 JSON 对象，非 JSONL）：
      ```json
      {
        "date": "<ISO>",
        "last_device": "<hostname>",
        "active_task": "<0字简",
        "pending_items": ["<事项1>", "<事项2>"],
        "behavioral_tone": "<行为模式简",
        "active_protocols": ["<CP-xxx>"],
        "last_decision": "<最近关键决策简述，若无null>"
      }
      ```
   5. 写入后在回执末尾标注 `📦 上下文移交摘要已写入（TTL 24h）`
   > **设计意图**：跨设备同日续接 用户在设A 上结束会话后换到设备 B，Agent 在会话开端自动读取移交摘要。若距今 <24h 则直接注入上下文；≥24h 降级为手`继续对话`。文件覆盖写入（非追加），始终只存最后一次会话的上下文快照
---

## 步骤 C-bis：经验提取与入库（任务收束时强制，前期每轮执行）

> 触发条件：步C 执行完毕（无论任务是否状态变更）。当前强制每轮执行。后期切换阈值由 `mod-decision-framework.mdc` 定义（待落地）
### C-bis 总体原则

Agent 扫描本轮对话，按三层策略提取经验
| 层级 | 名称 | 写入策略 | 判定标准 |
|:---|:---|:---|:---|
| **T1** | 硬事| 静默自动写入 | Agent 可独立验证因+ 有错误码/异常类型 + 已被规避验证 |
| **T2** | 轻经| 收束时一`AskQuestion` 批量确认 | 用户明确分享的操作经流程偏好，表达明确、已被任务验|
| **T3** | 审慎经验 | 逐条 `AskQuestion` 确认 | 用户仍在探索的方法论/架构级认知、或表达不确|

**T2/T3 判定信号**- T2：用户语气斩钉截铁（"直接...最一般我.."先改....."）、正在纠Agent 操作、属工具操作/流程步骤/偏好声明领域、本轮任务中已被实际使用且成- T3：用户语气试探（"可能..."我觉.."我也很难讲明）、正在和 Agent 一起思讨论、属方法架构范式/认知模型领域、尚未落地验证或属于远期方向

### C-bis.0 认知纠正行为约定（天赋归因，CP-064/CP-072
若本轮对话中用户用「天赋」「天生」「基因决定」等解释某项能力差异，Agent 在收束时应标`special` 字段中追`天赋归因纠正`，并在经验提取时引用 CP-064 的归因链（基因只在极少数生理极限上起作用 技能是习得追问训练方法）
### C-bis.1 T1 硬事静默写入 `known_issues`

1. 扫描本轮方法调用记录，识别满足以下全部条件的硬事实：
   - 有明确的错误异常类型（如 COM -2146824090   - 同一任务中换了方路径后成   - Agent 可独立归因（非用户主观判断）
2. 生成 `known_issues` 条目：`{condition, symptom, mitigation, mitigation_success_rate, discovered}`
3. **静默写入** `method-reliability-registry.json` 对应方法记录`known_issues` 数组*不弹窗确*（Agent 编码层强项）
4. 若对应方法记录不存在则先创建记录
5. 写入后在回执末尾标注 `🧠 T1 硬事xN 已入known_issues`

### C-bis.2 T2 轻经批量确认 写入 `User-Knowledge-Registry`

1. 收集本轮所T2 类经验（用户明确分享的操作经流程偏好），以及**从外部知识源消化并经 `classifier.md` Step R 标记为「可转化为工作流规则」的、可映射Agent 决策点的认知模式**2. T2 列表非空 一`AskQuestion` 展示全部 T2 条目   - 标题：`经验提取 轻经验（批量确认）`
   - prompt：列出本轮所T2 条目（编+ 摘要 + 领域），每条 1 行简   - 选项：`[确认全部写入] [逐条查看] [跳过本轮]`
3. `[确认全部写入]` 逐条写入 `User-Knowledge-Registry.jsonl`（`type: "T2_轻经`，`source: "user_shared"` `"task_review"`4. `[逐条查看]` 逐条弹出选项：`[写入此条] [跳过此条] [返回全选]`
5. 写入后在回执末尾标注 `📝 T2 轻经xN 已入User-Knowledge-Registry`

### C-bis.3 T3 审慎经验 逐条确认 写入 `User-Knowledge-Registry`

1. 收集本轮所T3 类经验（用户仍在探索的方法论/架构级认知）
2. T3 列表非空 逐条弹出 `AskQuestion`   - 标题：`经验提取 审慎经验 [N/M]`
   - prompt：展示该条经验内容与上下   - 选项：`[确认写入此条] [暂不记录] [跳过剩余全部]`
3. `[确认写入此条]` 写入 `User-Knowledge-Registry.jsonl`（`type: "T3_审慎经验"`；`validated` 按实际验证状态填写）
4. 写入后在回执末尾标注 `🤔 T3 审慎经验 xN 已入User-Knowledge-Registry`

### C-bis.4 任务拆解复盘 写入 `Decomposition-Log`

1. 若本轮涉*任务拆解**（Agent 将复杂任务分解为多步执行，且拆解步骤 ）：
   - 收集子任务序列、工具链、IO 约束
   - 生成一条复盘拆解记录：`{source: "manual_review", task_type, io_constraint, subtasks, tools_used, iteration_count}`
2. **若拆解由 Agent 复盘提取（非 Agent 自主拆解* 静默写入 `Decomposition-Log.jsonl`（复盘产物，不驱动自动行为，低风险）
3. **若拆解为 Agent 自主拆解（远X1→X2* AskQuestion 确认后写入（`source: "auto_decompose"`4. 写入后在回执末尾标注 `🔧 任务拆解模式已入Decomposition-Log`

### C-bis.5 回执汇
收束回执末尾追加经验提取汇总：
```
📊 本轮经验提取  🧠 T1 硬事xN
  📝 T2 轻经xN
  🤔 T3 审慎经验 xN
  🔧 任务拆解模式 x1
  📊 KB 协议反馈 xN（含 pended_review xM  跳过 xN
```

---

## 步骤 C+：技能表现评估（任务完成时）

### 触发条件

步骤 C 执行完毕后，检查本轮或本任务期间是否使用了 `Skills_Library/skills/` `Skills_Library/scripts/` 中的技能完成了任务
> 触发范围：注册表`status: active` *外部技*（注册表条目`source_url`）和**内部技*（注册表条目`source_url` 但被本轮调用）> 本轮如未使用任何技跳过 C+，直接进入步D
### 评估来源

1. 本轮对话的上下文（Agent 对表现的自评回忆）2. 步骤 C 中已收集的快照数据（轮次、Token 消耗）。
3. 用户的明确纠正反馈（如有）
### 评估流程

#### C+1 识别本轮使用的技
- 搜索本轮对话中调用的技能路径（`Skills_Library/skills/<name>/` 或注册表 ID）- 对照 `skill-registry.json` 确认技ID `task_types`
#### C+2 自评五维

`mod-skill-evaluation.mdc` §3 的五维评分标准自评：

| 维度 | 评分 | 自评方式 |
|:---|:---|:---|
| `correctness`-5| 正确| 参§3.1 用户纠正行为检|
| `autonomy`-5| 自主| 用户纠正次数 0次→4次→3，显著错误→2+次→1，完全失败→0 |
| `side_effects`-5| 副作| `git diff --stat` 检查是否产生了预期外写入；无→5，有小副作用但已清理，预期外且未清理 |
| `efficiency_turns`-5| 效率（轮次） | 参§3.2，首次使用时。
3（默认中位） |
| `efficiency_tokens`-5| 能耗（Token| 参§3.2，首次使用时。
3（默认中位） |

`total = correctness + autonomy + side_effects + efficiency_turns + efficiency_tokens`（满25）
#### C+3 AskQuestion 轻确
弹窗展示评估结果- 标题：`技能表现评<技能名`
- prompt：展示五维得+ total + 简短理由（1-2 句）
- 选项：`[确认评估] [修改评估值]`
  - `[确认评估]`：以当前得分写入日志  - `[修改评估值]`：Agent 逐一询问用户需调整的维度（可单选或多选），修改后重新展示
若本任务使用了多个技能，逐技能弹AskQuestion（先全部自评完，再逐个确认）
#### C+4 写入表现日志

追加一JSON `Skills_Library/skill-performance-log.jsonl`
```json
{"date":"<ISO>","skill_id":"<注册表ID>","task_type":"<任务类型>","correctness":<0-5>,"autonomy":<0-5>,"side_effects":<0-5>,"efficiency_turns":<0-5>,"efficiency_tokens":<0-5>,"total":<0-25>}
```

#### C+5 更新技能注册表

`skill-registry.json` 中更新该技能条目：

1. `last_used` 当前 ISO 时间2. `performance_history` 数组追加一`{"date":"<ISO>","task_type":"<类型>","total":<分数>}`。
3. `performance_score` 更新`performance_history` 中最5 `total` 的算术均值（< 5 次取全量）。见 `mod-skill-evaluation.mdc` §3.3
### 降级策略

- **`skill-performance-log.jsonl` 不存* 创建该文件（含头部说明行）- **同一技能在同任务中被多轮调* 仅在任务收束时合并评估一次（避免重复评分）- **`git diff --stat` 不可用（Git 环境* `side_effects` 维度标记`null`，不参与 total 计算，total 满分降为 20- **用户说「跳过评估* 本轮不写入日志，Agent 口头提醒「该技能缺少本轮表现数据」
### 与已有规则的关系

| 规则 | 触发时机 | 分工 |
|:---|:---|:---|
| `mod-skill-evaluation.mdc` §3 | | 五维评分标准定义 |
| `mod-skill-evaluation.mdc` §8 | | 本步骤的顶层设计 |
| **本步骤（C+* | **任务收束* | **执行落地** |
| `flow-skill-execute.mdc` | Skill 执行| 执行级安全约|
| `flow-behavior-auto-receipt.mdc` 步骤 C | 任务完成| 行为快照合并与归档（C+ 的上游） |

---

## 步骤 D：任务被取消
1. `Active-Task-Tracker.json` 中将该任务从 `active` 移到 `archive`，状`取消`2. 已积累的快照`consumed` 保持 `true`（备查，不写Behavior-Fit-Log.jsonl）。
3. 提示用户：如有依赖本任务的下游，需确认是否继续
---

## 步骤 E：`/收束` 手动触发（保留）

1. 用户发`/收束` / `结束对话` / `结束会话` / `结束` 时：
2. **强制阻断 进行中任务确*：扫`core/data/Active-Task-Tracker.json` `active` 数组中所有状态为「进行中」的任务。对每条任务**必须使用 `AskQuestion` 工具**弹出可点击选项*不得用纯文本替代**   - 选项 `[已完成]`：用户确认后，Agent 将该任务状态改为「已完成」，触发步骤 C（任务完成自动收束），再继续本步骤下一条   - 选项 `[继续]`：保留「进行中」状态，跳过该任务   - 若同时存在多条进行中任务 逐条逐个弹出 AskQuestion（如上一条选「已完成」则等待步骤 C 完成后再弹出下一条）   - 此步为强制阻断：未完成所有进行中任务AskQuestion 确认前，**不得进入后续步骤 3**。
3. 执行完整收束（含「建议的档案微调」「采纳情况」列），追加 `core/data/Behavior-Fit-Log.jsonl` 完整行4. 写回档案 UTF-8（无 BOM）：`Cursor-usage-profile-and-templates.md` + `Behavior-Fit-Log.jsonl`
---

## L3 分析的关
本工作流产出的标准化 Behavior-Fit-Log.jsonl 日志行，L3 分析消费
### L3 触发机制

> **闸门阻断（强制）**：不在收束时自动创建规则。达到阈值后，由 `kernel-runtime.mdc` 在下sessionStart 执行 L3 闸门检查并强制阻断确认。见该文件「开端（闸门）」行
| 层级 | 触发条件 | 动作 |
|:---|:---|:---|
| **闸门阻断a* | `Behavior-Fit-Log.jsonl` `format: "compact"` 的行0 `.cursor/rules/flow-behavior-l3-analysis.mdc` 不存| sessionStart 强制阻断 AskQuestion `[确认创建 L3 分析规则] [稍后（下次提醒）]` 创建规则 + 首次分析输出 |
| **静默刷新b* | 规则已存在且自上次分析后 `Behavior-Fit-Log.jsonl` 新增 `format: "compact"` 的行  | 自动更新 `docs/L3_Analysis_Output.md`（不阻断|
| **档案更新c* | 连续 3 次分析同维度趋势方向一| AskQuestion `[确认更新档案 §§1-3] [暂缓]`（附变更预览|

### L3 分析规则自动构建规格（创建时落地
> 以下规格`.cursor/rules/flow-behavior-l3-analysis.mdc` 首次创建时落地
**数据来源**- **操作维度**：`Behavior-Fit-Log.jsonl` `format: "compact"` 行的 `behavior` 字段（六字段管道分隔格式- **认知维度**：`Round-Behavior-Warm.jsonl` `type: "warm"` 
**各维度独立阈*
| 维度| 最少有效快| 取值方| 平滑方式 |
|:---|:---|:---|:---|
| 操作（旧 6 维度| 10 | 行为摘要列直接读| 单轮值（噪声低） |
| 认知-A（拆解方/ 认知路径| 15 | 温快照列直接读取 | 单轮值（噪声低） |
| 认知-B（语言风格 / 表达密度 / 情感基调| 20 行（2 有效行，即置信度 低的占比 0%| 温快照列读取，跳过置信度 = 低的轮次 | 滚动 5 轮众|
| 认知-C（价值排序） | 20 | 从操作维+ 认知维度多信号推| 滚动 5 |

**落地内容**：`.cursor/rules/flow-behavior-l3-analysis.mdc` 包含- **步骤 1：模式发*（≥10 行即执行）——各维度频次统计 + 组合模式识别（同轮多维度联合分布- **步骤 2：趋势检*（≥20 行才执行）——按时间排序后拆分前后半各维度分布对比，检测方向性变  - 拆分方式：按创建时间排序，取50% 为「前半段」、后 50% 为「后半段  - 判断规则：任一维度前后半占比变0 个百分点且后半样本数  标记趋势；多维度同方标注「偏好漂移- **步骤 3：滚动众数平*（B 组维度）——取最5 轮有效快照的众数作为当前有效认知值；离群检测（单轮值与滚动众数差距极大 标记离群，不参与统计，追加到待确认队列）
- **步骤 4：维度独立性自检**——若两个维度取值总是联动（如「正式」总伴随「细腻」），标记「需人工复核维度独立性- **步骤 5：合并报*——统一输出建议，人工确认后更新档案 §§1-3

**输出格式**：`docs/L3_Analysis_Output.md`（趋势检测报告，Agent 后续对话时读取）

**首次分析**：由闸门阻断确认后执行。后续分析由静默刷新触发