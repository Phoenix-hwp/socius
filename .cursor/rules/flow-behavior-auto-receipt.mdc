---
description: 行为偏好自动收束工作流 — 双层记录（轮级快照 + 任务级归纳），任务完成自动触发 Behavior-Fit-Log.md §2
glossary:
  layer: 3-工作流执行层
  module: BehaviorPreferences
  depends: [mod-behavior-crud-framework, Behavior-Dimensions-Registry]
---

# 行为偏好自动收束工作流

## 触发条件

每轮对话结尾（Agent 准备退出前）强制执行；无用户触发语。

---

## 步骤 A：轮级行为快照（每轮必须）

1. 读取 `10-Topics/Behavior-Dimensions-Registry.md`，获取当前启用的维度及值域。
2. 评估本轮行为，按启用维度填值。取值规则：
   - `指令风格`：本轮主要执行模式（多数操作属于哪种）。
   - `确认模式`：高风险操作是否等确认 → `关键确认`；几乎不等 → `跳过确认`；每步都等 → `逐步确认`。
   - `输出结构`：表格/结构化为主 → `表格化`；段落为主 → `分段叙述`；兼有 → `混合`。
   - `用户输入`：纯打字 → `直接文本`；用了 AskQuestion → `选项交互`；兼有 → `混合`。
   - `工具通道`：以本轮使用的工具类型组合判断；单类取具体值，≥3 类取 `混合`。
   - `特殊行为`：自由文本 ≤10 字。无显著特殊行为则填 `—`。
3. 确定快照归属：
   - 若本轮与某个活跃任务（`Active-Task-Tracker.md`）相关 → 归属该任务 ID。
   - 若与多个任务相关 → 归属主要任务。
   - 若无明确归属 → 归属 `其他`。
4. 追加一行到 `10-Topics/Round-Behavior-Log.md` 的 `## 活跃快照` 表。

### 步骤 A+：方法可靠性注册表更新（每轮强制）

在步骤 A 写入快照后，更新 `.cursor/method-reliability-registry.md`：

1. 读取注册表，获取当前设备（`hostname`）的所有记录。
2. 对本轮快照中涉及的「任务类型 + 方法路径」，匹配现有行：
   - **已存在**：`总调用 += 1`；若本轮成功 → 重置连续失败为 0、连续成功 += 1；若本轮失败 → 连续失败 += 1、重置连续成功为 0。更新 `最后状态` 和 `最近上下文`。
   - **不存在**：新增一行（`总调用=1`，根据成功/失败初始化计数器）。
3. 重新计算状态：
   - 连续失败 ≥ 3 → `⚠ 降权`
   - 连续成功 ≥ 5（从降权恢复后）→ `✅ 正常`
   - 总失败率 > 70% 且总调用 ≥ 10 → `🚫 禁用`
4. 写回注册表（不改其他设备和任务类型的行）。

---

## 步骤 B：纠偏候选检测（每轮）

1. 若本轮做了语义纠偏操作（如将"需球"纠偏为"需求"）：
   - 检查该输入是否已在档案 §2 对照表中。
   - 若**不在**对照表中 → 追加到档案 §2 附近「待确认纠偏」队列。
2. 队列格式：

```
| 输入 | 当前纠偏为 | 出现上下文 | 日期 |
|------|-----------|-----------|------|
| 需球 | 需求 | 讨论功能迭代 | 2026-05-09 |
```

3. 任务完成时提示用户确认队列内容，确认后写入 §2 并清空队列。

---

## 步骤 B+：冲突检测（三层）

### B+1 L1 即时检测（本轮回复中）

1. 若本轮用户指令与档案 §3 声明的偏好存在显性冲突（如档案写"结论先行"，用户要求先展开推理），在回复结尾轻标注：
   ```
   ⚠ 本轮 X 维度与 §3 偏好不一致：<具体差异>。照常执行。
   ```
2. 不中断流程，不做额外写入。

### B+2 L2 记录检测（写入快照时）

1. 在步骤 A 写入轮级快照时，对照档案 §3 对应偏好，逐维度比对。
2. 若某维度实际行为与档案声明不一致 → 在 `特殊行为` 列追加 `冲突:维度名`。
   - 例：`特殊行为=冲突:确认模式`
3. 若多项冲突，逐项列出（如 `冲突:确认模式,输出结构`）。

### B+3 L3 统计检测（任务收束时）

1. 在步骤 C 任务完成自动收束时，统计该任务期间所有快照中 `特殊行为` 列的 `冲突:` 标注。
2. 若同维度冲突出现 ≥2 次 → 在 Behavior-Fit-Log.md 的日志行末尾（或口头回执中）提示：
   ```
   该任务期间确认模式与档案冲突 3/5 轮，建议确认：该偏好是否已变化？
   ```
3. 不自动修改档案 — 用户决定。冲突标注可作为 L3 模式发现的输入之一。

---

## 步骤 C：任务完成自动收束（仅当任务状态变更时）

触发条件：`Active-Task-Tracker.md` 中任一任务状态从 `进行中` → `已完成`。

1. **收集快照**：从 `Round-Behavior-Log.md` 的「活跃快照」中筛选该任务 ID 的所有行。
2. **合并归纳**：
   - 若该任务只有 1 轮 → 直接使用该轮快照。
   - 若跨多轮 → 合并为综合描述：各维度取多数值，特殊行为合并去重。
3. **追加到 `10-Topics/Behavior-Fit-Log.md` §2「契合度分析日志」**（精简版，4 列）：

```
| 日期 | 任务主题 | 行为摘要（六字段） | 契合度 |
```

   - 行为摘要格式：`指令风格 \| 确认模式 \| 输出结构 \| 用户输入 \| 工具通道 \| 特殊行为`
   - 契合度：对照档案 §3 评估偏差，高/中/低。

4. **归档任务**：将该行从「活跃任务」移到「任务归档」。
5. **清理快照**：将已消费的快照行移到「已消费」区，或直接移除。
6. **提示下游**：若有其他任务的依赖字段指向本任务，提示用户。
7. **保留手动收束**：用户仍可用 `/收束` 触发完整版（含建议微调/采纳情况列）。

---

## 步骤 D：任务被取消时

1. 任务从「活跃任务」移到「任务归档」，状态=`取消`。
2. 已积累的快照保留在「已消费」区（备查，不写入 Behavior-Fit-Log.md）。
3. 提示用户：如有依赖本任务的下游，需确认是否继续。

---

## 步骤 E：`/收束` 手动触发（保留）

1. 用户发送 `/收束` / `结束对话` / `结束会话` / `结束` 时：
2. **⚠ 强制阻断 — 进行中任务确认**：扫描 `Active-Task-Tracker.md`「活跃任务」表中所有状态为「进行中」的任务。对每条任务**必须使用 `AskQuestion` 工具**弹出可点击选项，**不得用纯文本替代**：
   - 选项 `[已完成]`：用户确认后，Agent 将该任务状态改为「已完成」，触发步骤 C（任务完成自动收束），再继续本步骤下一条。
   - 选项 `[继续]`：保留「进行中」状态，跳过该任务。
   - 若同时存在多条进行中任务 → 逐条逐个弹出 AskQuestion（如上一条选「已完成」则等待步骤 C 完成后再弹出下一条）。
   - 此步为强制阻断：未完成所有进行中任务的 AskQuestion 确认前，**不得进入后续步骤 3**。
3. 执行完整收束（含「建议的档案微调」「采纳情况」列），追加 `10-Topics/Behavior-Fit-Log.md` §2 完整行。
4. 写回档案 UTF-8（无 BOM）：`Cursor-usage-profile-and-templates.md` + `Behavior-Fit-Log.md`。

---

## 与 L3 分析的关系

本工作流产出的标准化 Behavior-Fit-Log.md 日志行（六字段格式），供 L3 分析消费。

### L3 触发机制（混合触发）

| 层级 | 触发条件 | 动作 |
|:---|:---|:---|
| **软提示** | 每次任务收束时，若自上次 L3 后 Behavior-Fit-Log.md §2 新增标准化行 ≥10 行 | 轻提示："Behavior-Fit-Log.md 新增 N 行，建议执行 L3 偏好分析" |
| **硬分析** | 用户指令触发（如"分析偏好"、"偏好统计"、"L3 分析"） | 完整扫描 Behavior-Fit-Log.md §2 → 频次/趋势/组合模式检测 → 生成建议 → 人工确认 |
| **自动构建** | Behavior-Fit-Log.md §2 标准化行累计 ≥20 行时 | Agent 主动落地 L3 分析规则文件（频次统计逻辑 + 建议生成模板），并告知用户 |

### 自动构建阈值说明

- **Behavior-Fit-Log.md §2 标准化行**：指 2026-05-09 后按四列精简格式写入 `10-Topics/Behavior-Fit-Log.md` §2 的行。
- **阈值 20 行**：基于统计有效性（各维度至少出现 3-5 次方能排除偶发噪声）。
- **落地内容**：新建 `.cursor/rules/flow-behavior-l3-analysis.mdc`（前向引用，达到阈值时自动生成），包含：
  - **步骤 1：模式发现**（≥10 行即执行）——各维度频次统计 + 组合模式识别（同轮多维度联合分布）。
  - **步骤 2：趋势检测**（≥20 行才执行）——按时间排序后拆分前半/后半各维度分布对比，检测方向性变化。
  - **步骤 3：合并报告**——统一输出建议，人工确认后更新档案 §§1-3。
  - **建议生成模板**——发现 → 建议格式。
- **达到阈值后不自动执行分析**：仅落地规则。首次 L3 分析需用户确认触发。

### 趋势检测设计规格

以下规格在 L3 规则自动构建时落地。

**数据要求**：Behavior-Fit-Log.md §2 标准化行 ≥20 行方可激活。

**拆分方式**：按创建时间排序，取前 50% 为「前半段」、后 50% 为「后半段」。

**对比维度**：各枚举维度（指令风格/确认模式/输出结构/用户输入/工具通道）在前后半的分布。

**判断规则**：
- 任一维度前后半占比变化 ≥30 个百分点且后半样本数 ≥3 → 标记为「向上/向下」趋势。
- 多个维度同方向变化 → 综合标注为「偏好漂移」，提示更新档案。
- 变化 <30 个百分点或后半样本 <3 → 标记为「无显著趋势」。

**报告格式**：

| 维度 | 前半分布 | 后半分布 | 趋势 |
|:---|:---|:---|:---|
| 确认模式 | 关键确认 80%，跳过确认 20% | 关键确认 30%，跳过确认 70% | ↓确认 向上跳过 |
| 输出结构 | 表格化 90%，分段叙述 10% | 表格化 85%，分段叙述 15% | 无显著趋势 |
