---
description: 对话备份与读取指令规则（含时段与年份省略）
alwaysApply: true
---

# 对话备份与读取指令规则

## 指令 1：备份当前对话

- 当用户输入 `备份当前对话` 或其合规别名（如：`备份对话`）时：
  1. 先获取当前本地时间（24 小时制），再生成一条对话备份记录，必须包含：
     - 日期（YYYY-MM-DD）
     - 时间（24小时制）
     - 时段（上午/下午/晚上）
     - 简短主题（8-20字，基于本轮主要内容自动生成）
     - 核心结论与后续衔接点（简要）
  2. 备份写入：
     - 索引：`Daily-Backups/日常备份索引.md`
     - 明细：`Daily-Backups/TMP_YYYY-MM-DD_时段_主题.md`
  3. 明细文件 frontmatter 必须包含 `Lifecycle: 临时`。
  3. 若目录不存在，可按需创建（仅创建备份相关目录与文件）。

## 指令 2：读取对话

- 当用户输入形如 `读取 X年X月X日，上午/下午/晚上的对话`（或合规别名）时：
  1. 若未写年份，默认当前年份（仅当查询日期与当前时间同一年时可省略年份）。
  2. 时段判定规则固定为：
     - 上午：当天 14:00 前
     - 下午：当天 14:00 至 20:00 前（即 14:00-19:59）
     - 晚上：当天 20:00 及以后
  3. 按日期+时段检索索引与明细：
     - 若命中 1 条，直接读取并返回摘要。
     - 若命中多条，先返回“主题列表”供用户选择，再读取对应条目。
     - 若未命中，明确告知并给出相邻时段建议。

## 指令 3：继续备忘对话（恢复并续聊）

- 当用户输入 `继续备忘对话` 或其合规别名（如：`续聊备忘`）时，必须执行：
  1. 先解析用户消息中的时间条件（年月日/时段）；若未写年份且同年，按当前年份补全。
  2. 再按“读取对话”规则检索索引与明细，并恢复命中备份的核心结论与下轮衔接点。
  3. 若命中多条，先让用户选主题；选定后再继续交流，不得跳过选择。
  4. 恢复后默认输出三段：`已恢复上下文`、`当前可继续事项`、`建议下一步`。

## 口语指令兼容（继续X月X日...）

- 对以下口语表达，按“继续备忘对话”处理，不要求用户重说：
  - `继续4月23日下午的对话`
  - `继续X月X日（上午/下午/晚上）的对话`
- 若口语指令缺少时段，先追问时段后再检索。

## 边界时间确认规则（必须询问）

- 在执行“备份当前对话”时，若当前时间位于以下边界点的正负 10 分钟内（含端点），必须先询问用户确认时段归属再落盘：
  - 14:00（上午/下午分界）
  - 20:00（下午/晚上分界）
  - 00:00（跨日分界）
- 询问优先给出可选项，待用户确认后写入索引与明细，避免边界误判。

## 时间异常兜底（必须询问）

- 若“当前时间”获取失败、异常或为空，不得默认写入“晚上”。
- 必须先询问用户确认时间或时段，再写入索引与明细。
- 若用户暂时无法确认，可暂存：
  - 时间：`待确认`
  - 时段：`待确认`
  并在下次补录时修正索引与明细一致性。

## 凌晨记录规则（补充）

- 在执行“备份当前对话”时，若当前时间处于凌晨（00:00-05:59）：
  - 必须先询问用户该条记录归属：
    1) 记为“当前日期的上午”
    2) 记为“前一日晚上”
  - 用户确认后再落盘，避免时段归档歧义。

## 别名约束

- 别名长度必须为 2-10 个字。
- 用户提供别名不满足长度时，先提示并给出合规替代后再登记。

