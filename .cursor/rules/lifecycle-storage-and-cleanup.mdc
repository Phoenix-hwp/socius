---
description: 生命周期分层存储与清理规则
alwaysApply: true
glossary:
  layer: 横切
  purpose: 文件生命周期管理 — 长期/阶段/临时三分级 + 软删除 + 模板定级确认
---

# 文件生命周期规则

- **长期类**：存放在根目录 `Knowledge-Assets/`，默认不删除。
- **阶段类**：存放在具体项目目录，删除须二次确认。
- **临时类**：存放在 `Daily-Backups/`，默认保留 15 天并自动清理。

**项目归属备份**：`<项目目录>/对话备份/` 文件 `Lifecycle` 为 `阶段`，由 `Master_Control` 管理，多轮完结后手动确认清理。

## 状态字段（强制）

- 新增或改写生命周期文件时，必须在 frontmatter 中声明 `Lifecycle: 长期` / `阶段` / `临时`。
- 临时文件推荐前缀 `TMP_`，阶段文件推荐前缀 `STAGE_`。

## 删除策略

### 软删除（回收站）— 默认行为

- **所有 Agent 发起的文件删除 / 移动操作，默认走软删除**：移动到 `.trash/`（回收站），禁止直接调用 `Remove-Item` / `rm` / `unlink` 等永久删除命令。
- **仅以下情况可永久删除**（且须先经过 AskQuestion 确认）：
  - 用户在确认弹窗中选择了「永久删除」
  - 自动清理脚本按过期规则执行（仅限 `TMP_` 前缀文件，15 天）
- 软删除时**保留原始路径结构**，避免同名文件冲突与上下文丢失：
  - 例：`10-Topics/foo.md` → `.trash/10-Topics/foo.md`
  - 例：`.cursor/rules/bar.mdc` → `.trash/.cursor/rules/bar.mdc`

### 回收站清空规则

- 软删除文件默认保留 **30 天**，30 天后自动物理删除（比临时文件 15 天延长一倍，给误删恢复缓冲）。
- 用户说「清空回收站」或「清空 trash」时，Agent 须：
  1. 列出 `.trash/` 下所有待清文件
  2. 弹出 AskQuestion `[确认清空] [取消]`
  3. 物理删除后追加一行操作记录到 `.trash/purge-log.md`（日期、文件名、原始路径、触发方式）
- **回收站不受 Git 版本控制**（`.gitignore` 已排除 `.trash/`），清空时不可提交到 Git。

### 歧义保护（防 Agent 误判用户意图）

当用户原话具有以下语义特征时，视为**陈述意图**而非委托执行，Agent **必须先通过 AskQuestion 确认后再动作**，不可直接动手：

| 触发词 | 例句 | 处理方式 |
|:---|:---|:---|
| `我会……` / `我打算……` / `我准备……` | 「我会手动移出」 | 弹出 AskQuestion `[确认执行] [忽略（不操作）]` |
| `我自己……` | 「我自己删」 | 同上 |
| 对已有文件状态的口头假设 | 「这个文件不需要同步」 | 同上 |

仅在用户给出明确的执行委托意图时（如 `帮我删` / `你删一下` / `直接……`），方可跳过歧义确认。

### 原有清理路径（保留）

- 临时清理：软删除到 `.trash/soft-delete/daily-backups/`（15 天自动物理删除）。
- 阶段清理：白名单 + 交互确认，软删除到 `.trash/soft-delete/stage-files/`。细则见 `flow-behavior-auto-receipt.mdc` 步骤 D 及 `.cursor/config/stage-delete-whitelist.txt`。

## 模板定级确认（强制）

- 产出"可复用结论模板"时不得直接定级为长期，必须先通过 AskQuestion 询问用户；仅在用户确认后执行迁移、标记和引用更新。
