---
description: 项目备忘录相关指令规则（三层对齐存根）
alwaysApply: true
---

# 项目备忘录指令规则

> **三层对齐**：本文件为 **alwaysApply** 的触发摘要；**阶段分型**以第二层 `mod-project-memo-framework.mdc` 为准，**具体步骤**以第三层 `flow-project-memo-read.mdc`、`flow-project-memo-append.mdc` 为准，避免与上述文件重复维护。

## 触发词

- 主指令：`项目备忘录`
- 别名：`备忘录`、`XX项目备忘录`
- 记录：`记录备忘`、`记下`、`记录XX备忘`

## 执行动作（强制）

1. **Read** `mod-project-memo-framework.mdc`，解析项目名，完成阶段A/B/C
2. **Read** 对应第三层文件：
   - 查看：`flow-project-memo-read.mdc`
   - 追加：`flow-project-memo-append.mdc`

## 文件保护（删除防护）

- 当用户要求"删除草稿/临时文件"时，默认**不删除**包含"备忘录"语义的文件（如 `*Memo*`、`*备忘录*`）
- 只有在用户**明确点名**要删除该备忘录文件时，才允许执行删除
