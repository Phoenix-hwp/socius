---
description: 按项目名快速创建长记忆备份索引
alwaysApply: true
---

# 项目索引初始化指令

## 指令：创建XX项目索引

- 当用户输入 `创建XX项目索引` 时：
  1. 将 `XX` 解析为项目文件夹名（如 `469_Sports`）。
  2. 在工作区根目录检查是否存在 `<XX>/` 项目目录：
     - 若不存在，先提示用户确认项目名或是否需要创建该目录。
     - 若存在，继续执行初始化。
  3. 初始化长记忆备份结构：
     - 索引文件：`<XX>/<XX>_对话备份索引.md`
     - 备份目录：`<XX>/对话备份/`
  4. 若索引文件不存在则新建，使用以下标准骨架；若已存在则仅补齐缺失字段，不覆盖已有记录。

## 索引骨架（标准）

- 标题：`# <XX> 对话备份索引`
- 说明区包含：
  - 用途：按“日期 + 时段 + 主题”检索历史对话备份
  - 时段规则：上午<14:00；下午14:00-20:00；晚上>=20:00
  - 边界提示：14:00/20:00/00:00 前后10分钟需人工确认
- 表头固定：
  - `| 日期 | 时间 | 时段 | 主题 | 明细文件 |`

## 回执要求

- 初始化完成后，回执必须包含：
  - 已创建/已复用的索引文件路径
  - 已创建/已复用的备份目录路径
  - 一条可直接使用的示例指令：`备份当前对话`

## 脚本运行时约束（Python + Node.js）

- 若该指令后续实现为脚本化自动执行（而非纯文档/文件操作），必须同时支持 Python 与 Node.js：
  - Python 实现：`*.py`
  - Node.js 实现：`*.mjs` 或 `*.js`
- 推荐使用统一启动器优先尝试 Python，失败后回退 Node.js（Windows 示例：`cmd /c python ... || node ...`）。
- 两个实现的输入参数、输出字段与行为必须一致，避免同一指令在不同运行时出现差异。
- 若本机缺少其中一个运行时，不得阻塞指令执行；应自动回退到可用运行时并给出简短提示。

## 失败兜底

- 若项目名包含非法路径字符或无法定位目录，先返回可修正建议（候选项目名），待用户确认后再创建。
