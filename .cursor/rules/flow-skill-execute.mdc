---
description: 技能执行工作流 — 执行前快照 + 写路径限制 + 执行后差异审查 + 确认/回滚
alwaysApply: false
Lifecycle: 长期
Created: 2026-05-12
related:
  - mod-skills-library-framework.mdc（Skills Library 框架）
  - external-dependency-boundary.mdc（外部依赖边界）
  - pre-change-impact-enumeration.mdc（§5 执行后差异审查通用原则）
  - flow-skill-acquire.mdc（技能获取工作流）
---

# 技能执行工作流

## 触发条件

- 用户要求使用某已部署的 Skill 执行任务
- 用户在项目/任务上下文中调用 Skills_Library 中的技能

## 原则

在真实工作目录中运行外部技能时，强制执行「三部曲」：
1. **执行前快照** — 保存当前状态基线
2. **执行中隔离** — 限制写入路径，监控越权
3. **执行后审查** — 差异展示 + 确认保留/回滚

**与获取阶段的区别**：`flow-skill-acquire.mdc` 在 `git worktree` 隔离副本中试运行；本工作流在**真实工作目录**中运行，需要更强的防污染措施。

---

## §A 执行前快照

### A1 检查工作区清洁度

```
git status --porcelain
```

- 工作区干净 → 进入 A2
- 工作区有未提交变更 → AskQuestion `[暂存后继续] [先提交再继续] [取消执行]`
  - `[暂存后继续]` → `git stash push -m "skill-exec-pre-<skill-name>"`
  - `[先提交再继续]` → 引导用户完成提交后继续
  - `[取消执行]` → 终止

### A2 记录基线

```
git rev-parse HEAD  # 记录基线 commit
```

Agent 内部记录：`基线: <commit_hash>`

### A3 读取技能声明

Read `Skills_Library/skills/<skill-name>/SKILL.md`，确认：
- 声明的写入范围（`write_scope`）
- 依赖项（`dependencies`）
- 风险信号（`risk_signals`）

若写入范围包含 `.cursor/rules/`、`.git/`、`10-Topics/` 等内核路径 → 强制 AskQuestion `[确认授权越权写入] [取消执行]`。

---

## §B 执行中隔离

### B1 路径限制（Agent 强制执行）

技能运行时，Agent 监控其文件操作：

- **允许**写入：`Skills_Library/skills/<skill-name>/`、`声明的 write_scope`、`.trash/`
- **禁止**写入：`.cursor/rules/`、`.cursor/hooks/`、`.cursor/hooks.json`、`.git/`、`10-Topics/Cursor-command-aliases.md`、`kernel-runtime.mdc`
- **需确认**写入：`10-Topics/`（非别名表）、`Earth_Library/`、`Daily-Backups/`、项目目录

### B2 越权拦截

若技能在运行时尝试写入禁止路径：
- Agent 立即中断该写入操作
- 输出 `⚠ 技能 <skill-name> 试图写入禁止路径 <path>，已拦截`
- AskQuestion `[授权本次写入] [跳过本次写入继续] [终止技能执行]`

### B3 执行

- 按技能声明的方式运行（Python/Node/Shell）
- 捕获 stdout/stderr 用于后续审查
- 记录执行耗时

---

## §C 执行后差异审查

### C1 文件变更概览

```
git diff --stat
```

### C2 逐路径详细审查

按路径分类展示：

| 路径 | 变更类型 | 分类 | 动作 |
|:---|:---|:---|:---|
| `Skills_Library/skills/<name>/` | 新增/修改 | 预期内 | ✅ |
| `声明的 write_scope` | 修改 | 预期内 | ✅ |
| 其他非禁止路径 | 修改 | 连锁影响 | 🔗 |
| 内核路径（`.cursor/rules/` 等） | 修改 | ⚠ 越权 | 拦截 |

对每个 ⚠ 越权变更，逐文件展示 `git diff -- <path>`。

### C3 变更摘要

```
技能执行审查：<skill-name>
- 预期内变更：N 个文件
- 连锁影响：M 个文件
- 越权变更：K 个文件 ⚠
- 基线：<commit_hash>
```

---

## §D 确认/回滚

### 无非预期变更

简短回执「✅ 技能 `<skill-name>` 执行完毕，N 个文件变更均为预期/授权范围」，不阻断。

### 有连锁影响但无越权

展示 🔗 文件清单，AskQuestion `[确认保留] [回滚连锁文件] [回滚全部]`

### 有越权变更

强制展示 ⚠ 文件 diff，AskQuestion `[授权保留越权变更] [回滚越权文件] [回滚全部]`

> 回滚越权文件：`git checkout -- <越权文件路径>`
> 回滚全部：`git reset --hard <基线commit>`（仅限本轮未提交时）

---

## §E 执行后清理

- 回滚 stash（如果有）：`git stash pop`（若 A1 中执行了 stash）
- 更新 `skill-registry.json` 中 `last_used` 字段
- 记录执行结果到 `method-reliability-registry.json`（成功/失败/越权次数）

---

## 降级与兜底

- **git 不可用**：跳过 git 快照，Agent 手动记录文件清单作为基线，手动对比
- **stash 冲突**：保留 stash，提示用户手动处理
- **技能崩溃**：自动回滚所有变更到基线（`git reset --hard` + `git stash pop`），输出崩溃日志

## 与通用原则的关系

本工作流是 `pre-change-impact-enumeration.mdc` §5 在 Skills 场景下的**最高防控等级**实现。日常小范围变更（≤2 个文件）走通用 §5 即可；Skills 执行必须走本工作流的三部曲。
