---
description: Confirm network availability before first Notion action
alwaysApply: true
---

# Notion 首条操作前置网络确认

## 触发条件
- 每个对话中，准备执行第一条 Notion 相关操作（插件、MCP、脚本、API）之前。

## 强制流程
1. 先询问用户：是否开启网络环境（Y/N）？
2. 仅当用户明确回复 `Y` 时，才执行 Notion 操作。
3. 若回复 `N`，本次不执行 Notion 操作，并给出可继续的离线替代方案（如仅准备参数、生成待执行计划）。

## 约束
- 不得跳过该确认直接执行写入或读取。
- 若用户在当次消息中已明确授权“直接执行/无需再确认”，可按用户最新明确指令覆盖本规则。

## 写入类操作的后续规则

对用户 **`Y`**（或等价授权）之后，凡 **写入/新建** Notion：还须遵守 **`notion-write-workflow-confirmation.mdc`** 的 **固定步骤**（目录编号 → 标题策略 → 正文预览 → **`确认写入`** 等）。网络确认不能替代后续步骤。
