---
description: Skills Library 统一框架 — 技能获取/部署/开关/移除的阶段结构与异常边界
alwaysApply: false
Lifecycle: 长期
Created: 2026-05-12
related:
  - external-dependency-boundary.mdc（外部依赖边界）
  - flow-skill-acquire.mdc（技能获取工作流）
  - flow-skill-toggle.mdc（技能开关工作流）
---

# Skills Library 统一框架

## 适用范围

通过 Skills Library 管理的外部能力（GitHub Skills、第三方脚本）的 **获取 / 部署 / 开关 / 移除**。

**执行通道（默认）**：
- **获取**：`flow-skill-acquire.mdc`（安全闸门 + 隔离试运行）
- **开关**：`flow-skill-toggle.mdc`（单技能开关 + 全开/全关）
- **移除**：归档到 `.trash/Skills_Library/<skill-name>/`，30 天后物理删除

## 抽象阶段结构

| 阶段 | 名称 | 核心职责 | 下层工作流引用 |
|:---:|:---|:---|:---|
| **A** | 技能发现 | 用户指定来源（GitHub URL / 本地路径 / 名称检索） | `flow-skill-acquire.mdc` §A |
| **B** | 安全闸门 | 依赖声明 → 配置检查 → 功能范围 → 安全信号 | `flow-skill-acquire.mdc` §B |
| **C** | 试运行 | git worktree 隔离 → 部署 → 验证 → 报告 | `flow-skill-acquire.mdc` §C |
| **D** | 部署确认 | AskQuestion [确认部署] [回滚] | `flow-skill-acquire.mdc` §D |
| **E** | 生命周期管理 | 开关 / 休眠 / 归档 / 移除 | `flow-skill-toggle.mdc` |

## 技能注册表结构

`Skills_Library/skill-registry.json` 中每条记录：

```json
{
  "id": "skill-unique-id",
  "name": "显示名称",
  "source": "github|local|custom",
  "source_url": "https://github.com/...",
  "frequency": "常用|高频|低频|特定",
  "status": "active|dormant|archived",
  "locked": false,
  "agent_scope": ["default", "agent-x"],
  "acquired": "2026-05-12",
  "last_used": "2026-05-12",
  "dependencies": ["python>=3.10", "requests"],
  "write_scope": ["Skills_Library/skills/<name>/", ".trash/"],
  "risk_signals": []
}
```

## 异常边界

### 获取失败
- GitHub 不可达 → 提示用户检查网络，保留 URL 待重试
- 仓库不存在 / 权限不足 → 提示用户确认 URL 或授权

### 安全闸门不通过
- 任一检查项不通过 → 展示风险清单，AskQuestion [强制继续] [放弃]
- 包含高风险模式（eval/exec）→ 强制阻断，除非用户明确授权

### 试运行失败
- git worktree 创建失败 → 检查磁盘空间 / git 版本
- 部署后验证失败 → 自动回滚，保留失败日志

### 技能冲突
- 与已有技能同名 → 提示用户确认覆盖 / 重命名

### 降级策略
- Skills_Library 目录不存在 → 视为未初始化，不影响核心流程
- skill-registry.json 损坏 → 从备份重建，或降级为仅内核运行

## Agent 自检（Skills 操作前）

- [ ] 是否已读取 `external-dependency-boundary.mdc` 确认边界合规？
- [ ] 是否已核对 `skill-registry.json` 当前状态？
- [ ] 安全闸门各项是否均已检查并通过？
- [ ] 试运行是否在隔离环境（git worktree）中执行？
- [ ] 部署/移除前是否已通过 AskQuestion 确认？
