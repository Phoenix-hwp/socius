---
description: Git 直接拉取流程 — 一句到位，git pull --rebase
glossary:
  layer: 3-工作流执行层
  module: Git
  depends: [mod-git-crud-framework]
---

# Git 直接拉取流程

## 触发词

- 主指令：`拉取git`
- 别名：`拉取远端`、`直接拉取`

## 意图

从远端直接拉取最新内容，不做差异审查，一句到位。适用于对远端无疑虑的日常同步场景。

---

## 执行步骤

### 步骤 1：环境确认

1. 确保工作目录在 **工作区根**
2. 执行 `git rev-parse --abbrev-ref HEAD` 获取当前分支名
3. 执行 `git remote get-url origin` 确认远程仓库已配置

### 步骤 2：直接拉取

```bash
git pull --rebase origin <BRANCH>
```

### 步骤 3：回执结果

- 若 `Already up to date` → 提示「本地已是最新」
- 若有新提交并入 → 回执并入的提交数量与摘要
- 若拉取失败 → 回执错误信息

---

## 冲突处理

若 `pull --rebase` 遇到冲突：

1. 回执冲突文件清单
2. 通过 **AskQuestion** 弹出选项：
   - `[协助解决冲突]` — Agent 协助解决后继续
   - `[改用merge]` — 中止 rebase，改用 `git pull --no-rebase`
   - `[取消拉取]` — 取消，保留本地内容

---

## 拉取后对齐

若实际并入远端新提交，回执末尾须指向 `post-git-pull-three-layer-awareness.mdc`，并按其中「拉取完成后的立即动作 A/B/C」执行全部拉取后对齐流程。

---

## 失败兜底

- `.git` 不存在 → 提示「当前目录非 Git 仓库」
- `remote` 未配置 → 提示参考 `10-Topics/Gitee-Workspace-Git-Workflow.md` §首次绑定
- 认证失败 → 给出命令模板，不替用户保管 Token/密码
