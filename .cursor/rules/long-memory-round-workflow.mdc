---
description: 多轮长记忆项目的开场、执行、收束与合并流程
alwaysApply: true
---

# 多轮长记忆流程规则

适用场景：用户明确某主题将进行多轮长对话（项目型或主题型）。

## 1) 初始化阶段（一次性）

- 当用户要求创建项目索引后：
  1. 先确保项目目录存在。
  2. 调用索引初始化流程（索引文件 + 对话备份目录）。
  3. 基于根目录通用模板 `Knowledge-Assets/Control-File-Template.md` 与用户共创项目主控文件。
  4. 自动创建并初始化：
     - 进度看板
     - 待处理池
     - 决策清单
     - 轮次日志
     - 术语/口径表

## 2) 推进模式

- 主控文件必须声明 `推进模式`：
  - `章节式`
  - `开放式`
- 未声明时默认 `章节式`，并在当轮询问确认是否改为开放式。

## 3) 每轮开场

- 先读取主控文件与最近一轮日志。
- 若存在清晰“下轮起点”，执行一句轻确认：
  - `按上轮起点继续 <章节/阶段/环节>，是否直接开始？`
- 若存在冲突/歧义（多条命中、起点缺失、目标不一致），再完整询问。

## 4) 每轮输出结构（固定）

- 问题清单
- 可执行方案
- 建议改写
- 本轮结论 / 下轮入口

## 5) 轮次收束指令

- 当用户输入 `总结本轮对话`（或同义表达）时，必须：
  1. 生成本轮小结（已完成/未完成/待确认/下轮起点）。
  2. 写回主控文件中的轮次日志与相关看板区域。
  3. 开放式模式下，额外询问：
     - `是否同意本轮结论作为下一轮默认起点？`

## 6) 合并闸门

- 当用户输入 `确认合并`（或明确表示合并）时：
  1. 先做一致性快检：术语、口径、规则、依赖。
  2. 仅将“已确认”内容写入主文档。
  3. 在轮次日志追加“合并记录”（时间、范围、摘要）。

