---
description: 多轮长记忆项目的入场、推进、收束与合并约定
alwaysApply: true
---

# 多轮长记忆流程规则

适用场景：用户明确某主题将进行多轮长对话（项目型或主题型）。

## 初始化：见 `flow-multi-round-upgrade.mdc`

- 升级流程（创建 Master_Control + Project_Memo + 对话备份目录 + R0 初始化）由该文件定义。

## 推进模式

- 主控文件 `Master_Control.md` §1 必须声明 `推进模式`：`章节式` / `开放式`。
- 未声明时默认 `章节式`，并在当轮询问确认是否改为开放式。

## 每轮开场：见 `round-start-checkpoint-confirmation.mdc`

- 先读 `Master_Control` → 轻确认下轮起点；`Master_Control` 不存在时触发 `flow-multi-round-upgrade.mdc`。

## 每轮输出结构（固定）

- 问题清单 / 可执行方案 / 建议改写 / 本轮结论 + 下轮入口。

## 轮次收束：见 `flow-behavior-auto-receipt.mdc`

- 触发语 `总结本轮对话` → 按该文件执行小结生成与写回。

## 合并闸门

- 用户明确表示合并时，通过 AskQuestion `[确认合并] [暂缓]` 确认，先做一致性快检后写入主文档并追加合并记录。
