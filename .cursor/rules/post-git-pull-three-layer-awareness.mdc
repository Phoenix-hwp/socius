---
description: git pull 或多设备等价场景并入远端约定后，在改规则/钩子/别名相关脚本前须重温网关与别名表，防冲突与分叉
alwaysApply: true
---

# Git 拉取后与三层架构对齐（强制）

## 目的

远端或其它设备可能已新增 **规则（`.cursor/rules`）**、**钩子（`.cursor/hooks`）**、**仓库根或 `.cursor` 下的自动化脚本**、**`Earth_Library/scripts`** 等与「指令路由、模块流程」相关的约定。若在 **未重新阅读** 当前仓库中的三层架构与别名说明前，又在本地新建重叠能力的规则或脚本并推送，易导致 **合并冲突、功能冗余或行为分叉**。

**多设备说明**：你在其它电脑 **push** 了上述路径的变更后，本机即使由你 **手动** `git pull`（对话里未出现 Agent 执行 pull），只要 **随后在本轮对话中** 即将改动同类对象，仍须先完成下方必读；若你 **口头说明**「刚在别的设备提交 / 刚拉完远端 / 别名或脚本可能已在别处更新」，亦视为已满足「须先重温」的触发前提（Agent 未必能感知静默拉取，**依赖你一句同步声明** 即可对齐）。

## 触发条件（满足任一即适用本条）

1. 本轮对话中 **刚完成** `git pull` / `git pull --rebase` / `git merge`（来自远端的分支更新已成功写入工作区）。
2. 用户明确表示 **刚同步远端**、**换机拉完仓库**、**合并了他人推送** 等同等语义。
3. **多设备 / 协作等价语义**（与「并入远端新约定」效果相同，且即将动手改本条所列对象时）：例如 **另一台设备已 push**、**刚合并同事改动的 `.cursor` / 别名相关文件**、**在别处新建了指令或脚本并已进远端**、**别名表或钩子可能刚被更新** 等表述。

## 在后续架构相关改动之前（强制执行）

在准备 **新增或实质性修改** 下列对象之前，须先完成下方 **必读文档**（可分批 Read，但 **不得**跳过网关索引与别名表）：

- `.cursor/rules/**/*.mdc`
- `.cursor/hooks*`、`hooks.json` 及其垫片/启动脚本
- 被 **`Cursor-command-aliases.md`** 或 **`Command-Help-Index.md`** 引用的仓库脚本路径
- `Earth_Library/scripts/` 内与 **入库 / 检索 / 巡检 / 启停** 指令链路相关的脚本

### 必读文档（最小集合）

1. **`gateway-command-router.mdc`** — 「路由流程」「三层架构速查」「规则索引」。
2. **`10-Topics/Cursor-command-aliases.md`** — 已有指令与别名；**禁止**在未核对映射的前提下复制登记同质指令。
3. **`Command-Help-Index.md`** — 各模块「框架 / 工作流」列与三层对齐情况。

### 建议补充（改动面较大时）

- 用 **Glob / 列表** 浏览 `.cursor/rules`，确认是否出现本轮尚未知晓的新增 `mod-*` / `flow-*`。
- 若变更触及日常 Git 同步约定：再 Read **`mod-git-crud-framework.mdc`**、**`flow-git-commit.mdc`**。

## 与人工操作的衔接

操作者在本机执行 pull 后，建议在动手加规则或脚本前同样浏览上述必读文档（至少 **`Cursor-command-aliases.md`**）。**`bootstrap-on-pull.cmd`** 仍按 **`模型配置说明.md`** 与 **`git-cross-device-and-secrets.mdc`** 执行，与本条互补。

## 与其它规则的关系

- **`flow-git-commit.mdc`**：步骤 5 在拉取 **实际并入远端新提交** 时，须在回执中简要指向本条。
- **`git-cross-device-and-secrets.mdc`**：跨设备密钥与模板约束仍以该文件为准；本条侧重 **指令体系与脚本入口防重复**。
