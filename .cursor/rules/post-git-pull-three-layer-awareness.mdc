---
description: git pull 或多设备等价场景并入远端约定后，在改规则/钩子/别名相关脚本前须重温网关与别名表，防冲突与分叉
alwaysApply: false
---

# Git 拉取后与规则文件分层对齐（强制）

## 目的

远端或其它设备可能已新增 **规则（`.cursor/rules`）**、**钩子（`.cursor/hooks`）**、**仓库根或 `.cursor` 下的自动化脚本**、**`Earth_Library/scripts`** 等与「指令路由、模块流程」相关的约定。若在 **未重新阅读** 当前仓库中的规则文件分层（gateway/mod/flow）与别名说明前，又在本地新建重叠能力的规则或脚本并推送，易导致 **合并冲突、功能冗余或行为分叉**。

> **注**：本文中「分层」指 `.cursor/rules/` 内部的文件组织约定（gateway/mod/flow），与系统四层能力架构（网关层/能力层/执行层/认知层）正交。四层架构定义见 `Knowledge-Brain/framework.md` §一。

**多设备说明**：你在其它电脑 **push** 了上述路径的变更后，本机即使由你 **手动** `git pull`（对话里未出现 Agent 执行 pull），只要 **随后在本轮对话中** 即将改动同类对象，仍须先完成下方必读；若你 **口头说明**「刚在别的设备提交 / 刚拉完远端 / 别名或脚本可能已在别处更新」，亦视为已满足「须先重温」的触发前提。

---

## 触发条件（满足任一即适用本条）

1. 本轮对话中 **刚完成** `git pull` / `git pull --rebase` / `git merge`（来自远端的分支更新已成功写入工作区）。
2. 用户明确表示 **刚同步远端**、**换机拉完仓库**、**合并了他人推送** 等同等语义。
3. **多设备 / 协作等价语义**（与「并入远端新约定」效果相同，且即将动手改本条所列对象时）：例如 **另一台设备已 push**、**刚合并同事改动的 `.cursor` / 别名相关文件**、**在别处新建了指令或脚本并已进远端**、**别名表或钩子可能刚被更新** 等表述。

---

## 拉取完成后的立即动作

### A. 回执新增规则/脚本

当远端变更中包含 **新增的 `.cursor/rules/*.mdc`** 或 **被 `Cursor-command-aliases.md` 引用的新增脚本** 时，Agent 须在拉取回执末尾列出：

```
远端新增规则/指令体系文件：
- <文件名> — <一句话用途>
```

### B. 自动对齐别名表

若远端变更了 **`10-Topics/Cursor-command-aliases.md`**，Agent 须在当轮回执中简短确认「别名表已更新」，并在执行 **任何** 短指令前 **重新 Read** 该文件对应段落。

### C. 待办计划同步检查（交互式）

> **统一入口**：待办扫描与状态更新已封装为 `skill-todo-reminder`（Shell：`python Skills_Library/scripts/todo-reminder.py`）。本节仅保留触发逻辑，完整流程与内核 §1.2b 步骤 2 一致。

> **每日去重**：每日仅执行一次。由 `todo-reminder.py --scan` 内置的 `last_reminded` 去重保证，同日后续 pull 自动跳过。

#### 步骤 C1：待办扫描与第一层弹窗

1. 执行 Shell：`python Skills_Library/scripts/todo-reminder.py --scan`，获取 JSON 输出。
2. 若 `"skipped": true` → **跳过**待办检查，进入一致性检查。结束本节。
3. 若 `"skipped": false`：
   - 解析 `overdue`（到期）、`upcoming`（即将到期，days_until ∈ [1,3]）、`in_progress`（进行中）
   - 三组合并，弹出 **第一层 AskQuestion**（`allow_multiple: true`）：

```
📋 待办提醒（pull 同步）

prompt: "以下待办已到期或即将到期，选择要处理的项："

options（按 days_until 升序排列）：
  [Pxxx] ◀ 今天/进行中 — <topic>
  [Pxxx] 📅 <N>天后 — <topic>
  [跳过全部] 稍后处理
```

- 若用户选了 `[跳过全部]` → 执行 Shell：`python Skills_Library/scripts/todo-reminder.py --set-reminded`，结束本节。
- 若无到期也无即将到期 → 不弹窗，直接执行 Shell：`python Skills_Library/scripts/todo-reminder.py --set-reminded`，结束本节。
- 若用户选了一个或多个待办 → 进入步骤 C2。

#### 步骤 C2：逐项操作弹窗

对用户选中的每个待办，**依次**弹出 **第二层 AskQuestion**（`allow_multiple: true`）：

```
📋 <Pxxx> — <topic>

prompt: "plan: <planned_date> | next_action: <全文（截断至 120 字）>"

options：
  [查看备忘] → 若 source_memo 存在
  [标记完成]
  [推迟一天] → planned_date += 1
  [推迟三天] → planned_date += 3
  [推迟一周] → planned_date += 7
  [标记进行中] → status = "in_progress"
  [跳过此项]
```

操作行为：

|| 选项 | 动作 |
||:---|:---|
|| `[查看备忘]` | Read `source_memo.path` 对应 `entry` 段落，展示后重新弹出本项操作弹窗 |
|| `[标记完成]` | Shell `python Skills_Library/scripts/todo-reminder.py --mark-done <Pxxx>` |
|| `[推迟 N 天]` | Shell `python Skills_Library/scripts/todo-reminder.py --postpone <Pxxx> <new_date>` |
|| `[标记进行中]` | Shell `python Skills_Library/scripts/todo-reminder.py --start <Pxxx>` |
|| `[跳过此项]` | 不做改动，进入下一项 |

- 每项操作后立即执行 Shell 写回（逐项落盘，防止链中断丢失）。
- 全部已选待办处理完毕后，进入步骤 C3。

#### 步骤 C3：结束

执行 Shell：`python Skills_Library/scripts/todo-reminder.py --set-reminded`。结束待办检查，进入一致性检查。

---

#### C-bis. 关键词命中提示（对话中进行）

> 独立于 C1-C3，在任意对话回复的**末尾**，若本轮对话命中了某待办的 `resume_keywords`（且该待办 `status !== "completed"`），追加醒目标示：

```
> **⏰ 待办关联**：<Pxxx> — <topic>（plan: <planned_date>，状态：<pending|in_progress>）
> 输入 `待办` 查看详情或调整
```

- 此提示不阻断、不弹窗，仅追加在回复末尾。
- 不受 `last_reminded` 每日去重约束（关键词驱动，每次命中都提示）。
- 若同时命中多个待办 → 逐行列出。

---

> **与 sessionStart 的分工**：sessionStart 自动 `git fetch` → 有远端更新则 pull（走本步骤 C）；无更新则 sessionStart §1.2b 兜底（同样走交互弹窗）。手动词「拉取git」触发 pull 后同样走本步骤 C。三者都受 `last_reminded` 每日去重约束。

---

## 在后续架构相关改动之前（强制执行）

在准备 **新增或实质性修改** 下列对象之前，须先完成下方 **必读文档**：

- `.cursor/rules/**/*.mdc`
- `.cursor/hooks*`、`hooks.json` 及其垫片/启动脚本
- 被 **`Cursor-command-aliases.md`** 或 **`Command-Help-Index.md`** 引用的仓库脚本路径
- `Earth_Library/scripts/` 内与 **入库 / 检索 / 巡检 / 启停** 指令链路相关的脚本

### 必读文档（最小集合）

1. **`gateway-command-router.mdc`** — 「路由流程」「规则文件内部分层（三层）」「规则索引」。
2. **`10-Topics/Cursor-command-aliases.md`** — 已有指令与别名；**禁止**在未核对映射的前提下复制登记同质指令。
3. **`Command-Help-Index.md`** — 各模块「框架 / 工作流」列与规则文件分层对齐情况。

### 建议补充（改动面较大时）

- 用 **Glob** 浏览 `.cursor/rules`，确认是否出现本轮尚未知晓的新增 `mod-*` / `flow-*`。
- 若变更触及日常 Git 同步约定：再 Read **`mod-git-crud-framework.mdc`**。

---

## 与其它规则的关系

- 本条是 post-pull 对齐的 **唯一权威源**。其它文件如需引用，只留一句指针（如「拉取后须遵守 `post-git-pull-three-layer-awareness.mdc`」），**不在此重复罗列操作细则**。
- **`git-cross-device-and-secrets.mdc`**：跨设备密钥与模板约束仍以该文件为准；本条侧重 **指令体系与脚本入口防重复**。
- **`bootstrap-on-pull.cmd`** 仍按 **`模型配置说明.md`** 执行，与本条互补。

---

## 别名映射执行边界（与网关路由的衔接）

以下约束原本散落在多个文件中，现收敛于此：

- **Read 别名表行**：命中映射后，须 **Read `10-Topics/Cursor-command-aliases.md` 中该指令对应行（及映射列出的关联文件）**，再执行。
- **禁止擅自替换入口**：映射中写明的执行方式（例如某目录下 `auto-switch.cmd`）即为权威路径；**禁止**用临时脚本或平行启动链替代，除非用户 **明确要求** 修改仓库内的标准入口。
- **多设备 / 远端已变**：用户若表示「在其它设备已改并提交」「刚同步完多设备仓库」等，在执行别名相关动作前须 **重新 Read** `Cursor-command-aliases.md`（至少相关段落）。

---

## 拉取后一致性检查（第三层：变更影响兜底）

> 本段为 **`pre-change-impact-enumeration.mdc`**（变更前影响枚举）的远端兜底 —— 拉取后自动检查 diff 涉及的模块是否有未同步引用。

### 触发条件

拉取实际并入新提交后（`Fast-forward` 或有合并），强制执行本检查。

### 检查步骤

1. 执行 `git diff --name-only HEAD@{1}..HEAD`（或等效：`git show --name-only --format="" HEAD`）获取本次拉取的文件变更列表。
2. 从变更列表中提取受影响的关键词：
   - 删除的目录名（如 `Knowledge_Cards/`）
   - 变更的数据文件字段名（如 `card_path` → 已移除）
   - 重命名的文件
3. 对每个关键词，执行一次全库搜索：
   - **优先用 Grep 工具**（内置，速度最快）。
   - 搜索范围：`.cursor/rules/**/*.mdc`、`Earth_Library/**/*.md`、`**/*.py`、`**/*.mjs`、`10-Topics/**/*.md`。
4. 排除以下「有意保留」的引用：
   - `.trash/` 下的文件
   - `Daily-Backups/` 下的对话备份/复盘文件（历史记录，无需回改）
   - 变更日志/迭代记录中的历史记载行
5. 输出格式：

```
## 拉取后一致性检查

| 关键词 | 残留引用数 | 位置 | 处理 |
|:---|:---|:---|:---|
| Knowledge_Cards | 2 | .cursor/rules/xxx.mdc L42, Earth_Library/Templates/QnA_xxx.md L15 | 需同步修改 |
| card_path | 0 | — | ✅ 无残留 |

### 需同步
- [ ] <文件路径> — <引用内容>
```

6. 若残留引用 > 0 但不影响功能（如文档中的兼容说明），标记「可忽略」。
7. 若残留引用会导致运行时错误（如脚本引用已删除路径），标注「⚠ 阻断级遗漏，须在本地修复后提交」。

### 若发现阻断级遗漏

说明另一台设备执行变更时**未完成影响枚举**。Agent 应：
1. 提示用户「远端变更存在阻断级引用遗漏，需手动修复或通知原设备补交」
2. 列出须修复的文件清单
3. 是否在**本轮**修复（由用户选择）

### 与第一层规则的关系

本检查是 **兜底机制**，理想情况下第一层（`pre-change-impact-enumeration.mdc`）会在落盘前消除所有遗漏。若本检查发现遗漏，说明第一层规则在所涉设备上未被遵守或第二层影响面注册表不够完整，须提示用户考虑完善注册表。
