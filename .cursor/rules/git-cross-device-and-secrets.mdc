---
description: Git 跨设备拉取后基础可运行；密钥与模板分离；新建脚本与提交时的强制约束
alwaysApply: true
glossary:
  layer: 横切
  purpose: Git 跨设备密钥安全 — 密钥不入 Git、bootstrap 占位、路径跨设备兼容
---

# Git 跨设备与本地密钥基线（强制）

## 目标（优先级）

1. **跨设备 `git pull` 后**：仓库内**入口脚本与常规脚本**不得因「缺少未跟踪文件」而**无法启动或立即崩溃**（无模板、无说明即失败视为不达标）。
2. **跨设备路径差异**：脚本必须适配**不同盘符**（如 `C:` vs `D:`）和**不同上级目录结构**（如 `C:\Users\Alice\Projects\Repo` vs `D:\Work\Tools\Repo`）。禁止硬编码绝对路径、盘符或固定父目录结构。
3. **真实密钥、Token、付费 Key**：**永不**写入 Git；仅存在于本机/各设备私有路径（含 **`notion.env`**、**`config.json`** 中的 API Key）。
4. **「能用」与「功能全开」区分**：无密钥时允许**降级**（明确提示 + 退出码）或**占位配置可解析**；禁止静默失败或乱码崩溃。

### Notion Token 说明

- 从 Notion **我的集成**创建集成后复制 **Internal Integration Secret**（常见前缀 `secret_` 或 `ntn_`），粘贴到本机 **`.cursor/mcp/notion.env`** 的 `NOTION_TOKEN=` 后。
- **勿将**含真实 Token 的 `notion.env` 提交到 Git；跨设备请用密码管理器或安全渠道单独传递。

## 仓库内必须随 Git 存在的「可运行基线」

| 类型 | 要求 |
|:---|:---|
| 需要本地密钥的目录 | 必须提供 **`*example*` / `config.example.json` / `notion.env.example`** 等模板，占位值可被解析。 |
| 首次使用文档 | 根目录 `模型配置说明.md` 写明：**拉取后第一步** `bootstrap-on-pull.cmd`。 |
| 大二进制 | 可不进 Git，但须给出可执行的获取方式。 |

## 拉取后一键占位（仓库根）

- 运行：**`bootstrap-on-pull.cmd`**
- 作用：若不存在则从模板生成 **`config.json`**、**`notion.env`**，并视情况 `npm install`。

## 新建或修改脚本时（Agent 自检）

- [ ] **跨设备路径**：脚本是否使用 `%~dp0`（批处理）、`__dirname`（Node.js）等获取自身位置？禁止硬编码盘符或固定父目录。
- [ ] **跨盘符切换**：批处理中 `cd` 是否使用 `/d` 参数（如 `cd /d "%~dp0"`）支持从任意盘符启动？
- [ ] **路径含空格**：所有路径引用是否用引号包裹（如 `"%REPO_ROOT%\.cursor\ai-model-shim"`）？
- [ ] **显示绝对路径**：启动脚本是否显示当前目录绝对路径，帮助用户定位文件位置？
- [ ] 是否依赖 `config.json` / `notion.env`？缺省时是否**从模板复制**或**明确提示**？
- [ ] 是否避免在日志中打印完整 Token？

## 拉取 / 初始化时：禁止覆盖本地已配置的密钥文件（强制）

当设备已完成初始化（即 `config.json` 中的 Key **不是** `YOUR_` 占位符、或 `notion.env` 中的 Token **不是**模板默认值）：

1. **`git pull` 不会覆盖**这些文件（它们已被 `.gitignore` 排除，不在仓库历史中）。
2. **Agent / 脚本 不得**在不经用户明确同意的情况下，将已填充真实密钥的 `config.json` 或 `notion.env` 回退为模板占位文件。具体：
   - `bootstrap-on-pull.cmd` 在生成占位文件前，必须检测文件内容是否为占位符；若已含真实 Key，必须 **SKIP** 并提示「已配置，跳过，避免覆盖」。
   - 任何对 `config.json` / `notion.env` 的写入或替换操作，若文件已存在且非占位符，必须**先向用户确认**。
3. 跨设备同步密钥请使用密码管理器或安全渠道单独传递，**不得**通过 Git。

## 提交 Git 前（Agent 自检）

- [ ] 是否误将 **`notion.env`**、**`config.json`** 等含密钥文件加入暂存区？（对照根目录 `.gitignore`）
- [ ] 新增「需本地配置」能力时，是否已同步模板与 **`bootstrap-on-pull.cmd` / 文档**？

## 与现有规则的关系

- **`提交git`**：先 Read `10-Topics/Gitee-Workspace-Git-Workflow.md`；与本条一并遵守。
- **脚本实质性修改**：仍遵守 `pre-edit-script-change-brief.mdc`。
