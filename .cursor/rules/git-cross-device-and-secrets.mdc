---
description: 跨设备路径与本地密钥基线（强制）：禁止硬编码路径 + 密钥不入库 + 模板兜底。Guard device_neutralizer 覆盖路径中性化，密钥安全仍由 Agent 防线
alwaysApply: true
severity: safety
task_triggers:
  - path_operation
  - secret_config_write
  - new_device_init
requires:
  - flow-high-risk-safety.mdc
glossary:
  layer: 横切
  purpose: 本地密钥安全 密钥不入版本库、模板占位、跨设备路径兼容（Guard device_neutralizer 补路径，Agent 守密钥）
  guard_implemented_in: "guard/src/p008/device_neutralizer.py (盘符/用户目录→~/workspace 抽象)"
guard_replaceable: partial
---

# 跨设备路径与本地密钥基线（强制）

## 目标

1. **跨设备路径差*：禁止硬编码绝对路径、盘符或固定父目录结构
   - **Guard 轨道**：`device_neutralizer.py` LLM 注入上下文中出现`D:\` / `C:\Users\` 等设备指纹替换为 `~/workspace` 等抽象，确保跨设备一致性
   - **Fallback 轨道**：Agent 自检脚本是否`%~dp0` / `__dirname` 等获取自身位置
2. **密钥安全**：真Token/API Key 永不入版本库；无密钥时降级提示，禁止静默失败
3. **「能用」与「功能全开」区*：占位配置可解析

## 同步方式

工作区通过网盘同步。`.gitignore` 同时作为未来云盘同步忽略规则的预留设计

## Notion Token

- Token 粘贴`.cursor/mcp/notion.env`；勿提交到版本库；跨设备用密码管理器传递

## 密钥模板

| 类型 | 要求 |
|:---|:---|
| 需本地密钥的目| 必须提供 `*example*` / `config.example.json` / `notion.env.example` 等模|

## Agent 自检（新修改脚本时）

| # | 检查项 | 执行方式 |
|:---:|:---|:---|
| 1 | 是否`%~dp0` / `__dirname` 等获取自身位置？ | Guard（device_neutralizer 覆盖注入上下文） |
| 2 | 批处`cd` 是否`/d` 参数| Agent 判断 |
| 3 | 路径引用是否用引号包裹？ | Agent 判断 |
| 4 | 是否依赖 `config.json` / `notion.env`？缺省时是否从模板复制？ | Agent 判断 |
| 5 | 是否避免在日志中打印完整 Token| Agent 判断 |

## 禁止覆盖本地已配置的密钥文件（强制）

- 设备已完成初始化时（`config.json` 非占位符、`notion.env` 非模板默认值），任何写入操作须先向用户确认
- 密钥不通过版本库传递

## 版本库操作前

- [ ] 是否误将 `notion.env` / `config.json` 加入暂存区？
- [ ] 新增「需本地配置」能力时，是否同步模板与文档
