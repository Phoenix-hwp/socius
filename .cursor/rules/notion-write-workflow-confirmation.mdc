---
description: Notion 对话内写入固定六步流程；目录编号菜单；正文预览后「确认写入」
alwaysApply: true
---

# Notion 写入与新建工作流确认

## 适用范围

凡会向 Notion **写入或新建** 的操作均适用：插件（MCP）、`run_notion_workflow.py` / `do_create_page`、其他脚本、REST **创建或更新落库**。

只读（查询、fetch、不涉及选定目录写库的 dry-run）仍须遵守「首条 Notion 操作」的 **网络确认**；本规则侧重 **写库前的交互与确认**。

**与 Git 区分**：工作区同步改为 **`提交git`**（见 **`git-workspace-commit.mdc`** 与 **`10-Topics/Gitee-Workspace-Git-Workflow.md`**）；Notion 写入默认仍在对话内完成，不要求为本任务单独打开系统终端。

---

## 对话内固定步骤（标准 · 后续写入一律按此执行）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。在未收到 **第 6 步** 的执行许可前，**不得**调用写入 API。

| 步骤 | 名称 | Agent 行为 | 用户典型回复 |
|:---:|:---|:---|:---|
| **1** | **网络** | 询问本条对话首次写入前网络是否可用（与 `notion-first-action-network-confirmation.mdc` 一致） | `Y` / `N` |
| **2** | **目录** | **`Read`** `.cursor/mcp/notion_cascader_directory_choices.json`，展示 **编号 + path + page/database** 表格；说明类型含义 | **单个数字**（1…n） |
| **3** | **标题策略** | 仅询问新建标题如何处理 | **`1`** 手动 / **`2`** 自动生成 |
| **4** | **标题落地** | 若 **1**：请用户 **只发一行标题**。若 **2**：先收集正文来源（Markdown 路径或已达成共识的正文素材），由 Agent **拟定标题并展示**，用户确认或改为手动 | 一行标题；或对拟题「确认 / 改成…」 |
| **5** | **正文预览** | 展示将写入的正文 **完整预览**（表格/列表可保留 Markdown 形态）；说明数据来源（用户文件 / 用户口述 / 检索摘要等）。**在用户发出固定口令前禁止写入** | 修改意见；或 **`确认写入`** |
| **6** | **执行与回执** | 调用 `do_create_page`（或等价 MCP），支持 **page** 与 **database** 父级；写入成功后回复 **页面链接**、块数等；删除本地临时 `.md`（若有） | （步骤 5 已授权） |

**固定口令**：对话内以用户发送 **`确认写入`**（四字，无歧义）作为第 5 步结束、进入第 6 步的唯一默认口令；用户改用同义明确句（如「可以，写入吧」）须在上下文中 **唯一表示同意当前预览**，否则 Agent 应简短确认后再执行。

---

## 对话内交互形态（强制 · 反对「代码式一行写完」）

1. **禁止**把多步叠成一段「清单 + `Y + … + notion.dir…` 拼接示例」当作默认引导。
2. **目录**：用户 **只回复编号**；Agent **内部**映射 `url`/`value`，**不得**要求用户抄写 `notion.dir.*` 长串（用户主动粘贴除外）。
3. **可选本机菜单**：与对话步骤语义对齐，见下节（**非默认**，不替代对话内六步）。

---

## 可选：本机 CMD 写入向导 & GUI 兜底

**默认仍以对话为准**：用户在 Cursor 里按 **步骤 1～6** 完成；Agent 在 **`确认写入`** 后调用 API。**不要求**用户为了写入 Notion 而去开系统终端。

**可选入口**（用户主动提出要用脚本/菜单时）：

- **CMD 逐步向导**：`.cursor/mcp/notion_write_menu.cmd` 或 `python notion_write_menu.py`（与对话六步同序；适合喜欢本机菜单的人）。
- **GUI 面板**（脚本兜底）：`.cursor/tools/notion_gui_menu.ps1`；由钩子关键词触发（见 `notion_gui_popup.*`）。

---

## 与网络确认的顺序

1. 每个对话中 **第一条** Notion **写入类**操作：须先取得 **`Y`**（`notion-first-action-network-confirmation.mdc`）。
2. **`Y` 之后**严格按上表 **步骤 2→6** 推进；不得跳过 **目录**、**标题策略**、**正文预览 + 确认口令**。

---

## 目录与标题（摘要）

- **目录**：数据文件 **`notion_cascader_directory_choices.json`**；缺失或过期时提示刷新 **`refresh_notion_directory_choices.cmd`**（刷新动作本身遵守网络确认）。详见 **`notion-directory-option-ids.mdc`**。
- **标题**：须在步骤 3 选定 **1 手动** 或 **2 自动**；自动须在落库前展示拟题。
- **禁止**：未确认目录即写入；未确认标题策略即自拟标题落库；未收到 **`确认写入`**（或等价且无歧义同意）即调用 API。

---

## 用户一句话合并授权（可选 · 非默认）

仅当用户 **主动**一次说清且无歧义时可跳过逐步追问，且须包含：**`Y`**、**目录（编号或 URL/ID）**、**标题策略与标题或自动生成说明**、**正文要点或路径**、以及 **`确认写入`** 或同等明确的立即执行意图。

**禁止**把本节当作默认 UX；**禁止**用代码式拼接模板教学。

---

## Agent 自检（写入前）

- [ ] 本条对话首次写入是否已取得 **`Y`**？  
- [ ] 是否已完成 **步骤 2 目录（编号）**？  
- [ ] 是否已完成 **步骤 3～4 标题策略与标题落地**？  
- [ ] 是否已展示 **步骤 5 正文预览**并已收到 **`确认写入`**（或无二义的同意）？  
- [ ] 是否避免默认父级、避免跳过预览？
