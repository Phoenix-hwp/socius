---
description: Git 提交流程 — 从 add 到 push 的具体步骤
glossary:
  layer: 3-工作流执行层
  module: Git
  depends: [mod-git-crud-framework]
---

# Git 提交流程

## 触发词

- 主指令：`提交git`
- 别名：`Git同步`、`推仓库`、`提交远端`

**多设备**：若变更涉及 `.cursor/rules`、别名表、Help Index，或拉取后即将新增同类对象，须先遵守 `post-git-pull-three-layer-awareness.mdc`。

---

## 执行步骤

### 步骤 1：环境确认

1. 确保工作目录在 **工作区根**
2. 执行 `git status -sb` 检查变更

### 步骤 2：变更分类展示

```
已变更文件：
- 新增：X 个（file1, file2...）
- 修改：Y 个（file3, file4...）
- 删除：Z 个（file5...）
```

### 步骤 3：轮次收束写入（提交前纳入行为日志）

在 `git add` 之前，确保当前轮次的行为快照已写入 `10-Topics/Round-Behavior-Log.md`（按 `flow-behavior-auto-receipt.mdc` 步骤 A 执行），以便本轮日志行随本次提交一并推送，避免 push 后立即出现待处理修改。

### 步骤 4：add 策略确认

**默认**：`git add .`（添加所有变更，含步骤 3 写入的日志文件）
**若用户指定范围**：按用户要求执行 `git add <指定文件>`

### 步骤 5：commit message 拟定与确认

1. Agent 根据变更内容拟定建议的 commit message
2. 回执给用户确认或修改
3. 用户确认后执行 `git commit -m "message"`

### 步骤 6：推送前拉取与推送

1. `git pull --rebase`
2. 若无冲突：`git push`
3. 推送成功后回执：远端分支 + 提交摘要

### 步骤 6 补充：拉取并入远端更新

当本次 git pull 实际合并入了远端新提交时，回执末尾须指向 `post-git-pull-three-layer-awareness.mdc`，并按其中「拉取完成后的立即动作 A/B」回执新增规则/脚本摘要。

---

## 冲突处理

若 `pull --rebase` 遇到冲突：

1. 暂停推送
2. 回执冲突文件清单
3. 询问用户：
   - `1`：协助解决冲突后继续
   - `2`：中止 rebase，改用 merge
   - `3`：取消本次推送

---

## 参考文档

- 首次绑定远程仓库：`10-Topics/Gitee-Workspace-Git-Workflow.md`
- 统一框架定义：`mod-git-crud-framework.mdc`
- 直接拉取（不提交）：`flow-git-pull.mdc`
- 审查式拉取：`flow-git-safe-pull.mdc`
- 拉取后三层架构对齐：`post-git-pull-three-layer-awareness.mdc`
