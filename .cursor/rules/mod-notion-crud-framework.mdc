---
description: Notion CRUD 统一框架 — 定义阶段结构，不含具体步骤
alwaysApply: true
glossary:
  layer: 2-模块执行层
  module: Notion
  depends: [mod-notion-precondition]
---

# Notion CRUD 统一框架

## 适用范围

凡通过 **Notion 插件（MCP）**、**`run_notion_workflow.py`**、**本地向导脚本** 等对 Notion 执行 **增 / 删 / 改 / 查**（调用官方 API 且依赖网络的读写）均适用。

**执行通道（默认）**：在已通过 **预览** 与口令许可的前提下，**优先**将参数映射为 **Notion MCP** 工具调用（**每次调用前读取对应工具 schema**）；**仅当**插件不可用、鉴权失败、用户明确要求脚本化、或命中批量/dry-run/审计等阈值时，改用 **`run_notion_workflow.py`** 等本地脚本，并在回执中简短注明 **兜底原因**。

**与 Git 区分**：仓库同步用 **`提交git`**；Notion 交互默认在对话内逐步完成，不要求为本任务单独打开终端。

---

## 抽象阶段结构（强制）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。高风险步骤不得跳过确认。

| 阶段 | 名称 | 核心职责 | 下层工作流引用 |
|:---:|:---|:---|:---|
| **A** | 网络前置 | 首条操作前确认网络可用 | `mod-notion-precondition.mdc` |
| **B** | 目录落点 | 选定 parent（页面或数据库）| 见下方「阶段B细则」 |
| **C** | 操作分型 | 按增/删/改/查分流到具体工作流 | 见下方「阶段C细则」 |

---

## 阶段 B — 目录落点（父级选择）

### 数据来源

- 扁平可选列表：`.cursor/mcp/notion_cascader_directory_choices.json`（字段含 `id`、`path`、`label`、`notionObjectType`、`nodeType`、`value`、`url`）
- 生成逻辑：**有子集则仅叶子**；**无子集则当前级**
- 叶子级判定：`nodeType` 为 `database` / `page` / `root` 且无子节点（实际可写入的落点）
- 刷新命令：`cmd /c ".cursor\mcp\refresh_notion_directory_choices.cmd"`
- 权威目录见：`10-Topics/script-option-ids.md`

---

### 默认模式：模糊匹配输入（高效路径）

**触发**：用户输入父级名（如"参考文摘"、"悦读笔记"）

**匹配逻辑**：
1. **大小写不敏感**：输入"参考文摘"匹配"参考文摘"
2. **字段优先级**：先 `label`（短名）→ 再 `path`（完整路径）
3. **叶子级优先**：多级目录时，优先匹配叶子节点（实际可写入的落点）

**结果分支**：

| 命中数 | 处理方式 |
|:---|:---|
| **零条** | 提示"未找到匹配目录，展示完整清单供选择" → 进入兜底模式 |
| **一条** | 回显确认："将在「参考文摘」下执行，确认？" → 用户确认后直接使用 |
| **多条（>1）** | 展示候选编号清单（1~n，含 `path` + `notionObjectType`），请用户 **只回复编号** 选定 |

---

### 兜底模式：完整清单选择

**触发**：
- 用户明确要求"展示目录"、"看清单"
- 模糊匹配零条命中
- 用户表示"不知道目录名"

**行为**：`Read` JSON → 展示 **完整** 编号清单（1～n，`path` + `notionObjectType`），请用户 **只回复编号**

---

### 快速通道：ID/URL 直达

**触发**：用户主动给出 `notion.dir.*`、完整 URL 或 32位 ID

**行为**：直接使用 `value` 或 `url` 作为父级输入，**跳过**匹配/选择流程

**约束**：**勿猜路径谐音**；若 ID 无效，回退到兜底模式

---

### 复用与更换

- **同一对话内复用**：若用户已在本次对话中完成阶段 B、选定 parent，且未表示更换目录、未改为全局搜索或「仅按已知页面 URL/ID」路径，则后续在同一父级下的 **查 / 改 / 删 / 增** 不必再次询问父级
- **更换目录**：用户明确要求"换目录"/"重新选父级"/"换一个库"等时，重新进入默认模式（模糊匹配输入）

---

**例外**：用户明确为 **全局搜索**、**仅按已知页面 URL/ID 操作**、且与「选定父级下的子内容」无关时，可 **跳过** 阶段 B，但阶段 A 仍适用。

---

## 阶段 C — 按操作分型

阶段 B 完成后，根据操作类型进入对应第三层工作流：

| 操作 | 核心步骤 | 执行前必须 | 工作流文件 |
|:---:|:---|:---|:---|
| **增（创建）** | 标题策略 → 标题落地 → 正文完整预览 → 口令确认 | 详见 `flow-notion-create.mdc` | `flow-notion-create.mdc` |
| **查（读取）** | 用户提供关键词 → 解析唯一对象 → 展示摘要 | 若命中多条，禁止在未选择前读取 | `flow-notion-query.mdc` |
| **改（更新）** | 定位 → 正文来源 → 更新策略选择 → 变更预览 → 口令确认 | 未确认不得调用更新 API；优先 MCP | `flow-notion-update.mdc` |
| **删（归档）** | 定位 → 二次确认 → 口令确认 | 默认使用归档（`archived: true`），与「清空块」区分 | `flow-notion-delete.mdc` |

---

## 定位子流程（删 / 改 / 查共用）

1. 请用户提供用于检索的 **标题或关键词**（可与页面标题 **部分匹配**，大小写不敏感）
2. 在阶段 B 选定的 **parent** 下解析：
   - **parent 为 database**：用数据库 **title 类型属性** 做 `contains` 查询
   - **parent 为 page**：枚举该页 **直接子页面**（`child_page`），用标题 **包含** 匹配
3. **零条**：提示收窄关键词或改用 URL/ID；不得臆造 ID
4. **一条**：进入该操作的后继步骤
5. **多条**：展示 **编号清单**（标题 + 可选 URL/ID 片段），请用户 **只回复编号** 选定唯一目标

**说明**：复杂嵌套（非直接子级、多库联动）可能无法仅靠「父级 + 标题」唯一定位；应请用户给出 **Notion URL** 或 **32 位 ID**。

---

## 固定口令（默认）

| 操作 | 默认口令 | 说明 |
|:---:|:---:|:---|
| 创建 | **`确认写入`** | 详见 `flow-notion-create.mdc` |
| 更新 | **`确认更新`** | 详见 `flow-notion-update.mdc` |
| 删除/归档 | **`确认删除`** | 须在二次确认后接收 |

---

## 对话内交互形态（强制）

1. **禁止**把多步叠成一段「清单 + 拼接示例」当作默认引导
2. **目录与定位**：用户 **只回复编号**；Agent **内部**映射，不得要求用户抄写长串 ID
3. **可选本机菜单**：`.cursor/mcp/notion_write_menu.cmd`（CRUD 向导）或 `.cursor/tools/notion_gui_menu.ps1`（面板兜底），**不替代**对话内逐步确认

---

## Agent 自检（任意 Notion API 前）

- [ ] 若为对话内 **首条** Notion 操作，是否已取得 **`Y`**（`mod-notion-precondition.mdc`）？
- [ ] 若适用阶段 B，是否已 **目录编号** 落点？
- [ ] **删 / 改 / 查** 是否已完成 **定位**且目标 **唯一**？
- [ ] 是否已确定进入正确的 **第三层工作流**？
