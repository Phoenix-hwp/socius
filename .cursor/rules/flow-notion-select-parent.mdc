---
description: Notion 阶段B：目录落点（父级选择）完整操作细则
alwaysApply: false
glossary:
  layer: 3-工作流执行层
  module: Notion
  operation: select_parent
  depends: [mod-notion-precondition, mod-notion-crud-framework]
guard_replaceable: false
---

# Notion 目录落点（父级选择）操作流

> 本文件包含阶B 的完整操作细则；抽象阶段定义`mod-notion-crud-framework.mdc` §阶段 B

## 数据来源

- 扁平可选列表：`.cursor/mcp/notion_cascader_directory_choices.json`（字段含 `id`、`path`、`label`、`notionObjectType`、`nodeType`、`value`、`url`
- 生成逻辑*有子集则仅叶**无子集则当前*
- 叶子级判定：`nodeType` `database` / `page` / `root` 且无子节点（实际可写入的落点
- 刷新命令：`cmd /c ".cursor\mcp\refresh_notion_directory_choices.cmd"`
- 权威目录见：`docs/script-option-ids.md`

---

## 默认模式：模糊匹配输入（高效路径

**触发**：用户输入父级名（如"参考文悦读笔记"

**匹配逻辑**
1. **大小写不敏感**：输参考文匹配"参考文
2. **字段优先*：先 `label`（短名）`path`（完整路径）
3. **叶子级优*：多级目录时，优先匹配叶子节点（实际可写入的落点

**结果分支**

| 命中| 处理方式 |
|:---|:---|
| **零条** | 提示"未找到匹配目录，展示完整清单供选择" 进入兜底模式 |
| **一* | 回显确认将在「参考文摘」下执行，确认？" 用户确认后直接使|
| **多条1* | 展示候选编号清单（1~n，含 `path` + `notionObjectType`），请用**只回复编* 选定 |

---

## 兜底模式：完整清单选择

**触发**
- 用户明确要求"展示目录"看清
- 模糊匹配零条命中
- 用户表示"不知道目录名"

**行为**：`Read` JSON 展示 **完整** 编号清单～n，`path` + `notionObjectType`），请用**只回复编*

---

## 快速通道：ID/URL 直达

**触发**：用户主动给`notion.dir.*`、完URL 。
32ID

**行为**：直接使`value` `url` 作为父级输入*跳过**匹配/选择流程

**约束***勿猜路径谐音**；若 ID 无效，回退到兜底模

---

## 复用与更

- **同一对话内复*：若用户已在本次对话中完成阶B、选定 parent，且未表示更换目录、未改为全局搜索或「仅按已知页URL/ID」路径，则后续在同一父级下的 **/ / / * 不必再次询问父级
- **更换目录**：用户明确要换目/"重新选父/"换一个库"等时，重新进入默认模式（模糊匹配输入

---

**例外**：用户明确为 **全局搜索***仅按已知页面 URL/ID 操作**、且与「选定父级下的子内容」无关时，可 **跳过** 阶段 B，但阶段 A 仍适用
