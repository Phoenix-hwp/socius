---
description: Notion「删」操作 — 归档页面的具体步骤
alwaysApply: false
glossary:
  layer: 3-具体工作流
  module: Notion
  operation: delete
  depends: [mod-notion-precondition, mod-notion-crud-framework]
---

# Notion 删除/归档页面工作流

## 适用范围

对 Notion 页面执行 **删除/归档** 操作。默认使用 **归档**（`archived: true`），与「清空块」区分。

**统一骨架**：网络（阶段A）→ 目录（阶段B）→ **本工作流（阶段C-删）**。全流程见 `mod-notion-crud-framework.mdc`。

---

## 前置要求

- 已完成 **阶段 A**（网络确认 **`Y`**）— 见 `mod-notion-precondition.mdc`
- 已完成 **阶段 B**（目录落点 parent 已选定）— 见 `flow-notion-select-parent.mdc`

---

## 步骤（固定4步）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。高风险步骤（删除）须 **二次确认 + 口令确认**。

| 步骤 | 名称 | Agent 行为 | 关键约束 |
|:---:|:---|:---|:---|
| **1** | **定位目标** | 用户提供 **标题或关键词** → 在 parent 下解析唯一对象 | 命中多条时展示编号清单 |
| **2** | **二次确认** | 明示目标 **标题 + URL 或 ID**，询问确认 | **必须**展示完整信息 |
| **3** | **口令确认** | 等待用户发送 **`确认删除`** | 无二义同意方可执行 |
| **4** | **执行归档** | 调用 API（`archived: true`），返回结果 | 默认归档，非物理删除 |

---

## 定位子流程（步骤1）

详见 `flow-notion-locate-target.mdc`：

1. 用户提供用于检索的 **标题或关键词**（部分匹配，大小写不敏感）
2. 在阶段 B 选定的 **parent** 下解析：
   - **parent 为 database**：用数据库 **title 类型属性** 做 `contains` 查询
   - **parent 为 page**：枚举该页 **直接子页面**（`child_page`），用标题 **包含** 匹配
3. **零条**：提示收窄关键词或改用 URL/ID；不得臆造 ID
4. **一条**：进入步骤2（二次确认）
5. **多条**：展示 **编号清单**（标题 + 可选 URL/ID 片段），请用户 **只回复编号** 选定唯一目标

**说明**：复杂嵌套（非直接子级、多库联动）无法仅靠「父级 + 标题」唯一定位时，应请用户给出 **Notion URL** 或 **32 位 ID**。

---

## 固定口令

- **`确认删除`** — 进入第4步的唯一许可
- 用户改用明确同意句（如「确定删除」）须在上下文中 **唯一表示同意当前目标**，否则 Agent 应简短确认后再执行

---

## 异常处理

| 异常场景 | 处理方式 |
|:---|:---|
| 零条命中 | 提示收窄关键词或改用 URL/ID |
| 多条命中 | 展示编号清单请用户选择（1/2/3...）|
| 用户未确认（步骤2/3）| 停留在确认阶段，等待明确指令 |
| API 归档失败 | 提示重试或转为仅做本地标记 |

---

## 与「清空块」的区别

| 操作 | API 行为 | 适用场景 |
|:---|:---|:---|
| **归档页面**（本工作流）| `archived: true` | 删除整个页面/数据库行 |
| **清空块** | 删除/归档页面的子块 | 保留页面，仅清空内容 |

若用户意图是「清空内容但保留页面」，应引导到 **更新** 工作流（`flow-notion-update.mdc`）策略1（清空后重写）。

---

## Agent 自检（删除前）

- [ ] 若为对话内 **首条** Notion 操作，是否已取得 **`Y`**（`mod-notion-precondition.mdc`）？
- [ ] 是否已完成 **阶段B 目录落点**（`mod-notion-crud-framework.mdc`）？
- [ ] 是否已完成 **定位**且目标 **唯一**？
- [ ] 是否已完成 **二次确认**（明示标题 + URL/ID）？
- [ ] 是否已收到 **`确认删除`**（或无二义的同意）？
