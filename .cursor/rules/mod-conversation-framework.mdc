---
description: 对话备份与恢复统一框架 — 定义阶段结构，不含具体步骤。备份落点按归属路由。
glossary:
  layer: 2-模块执行层
  module: Conversation
---

# 对话备份与恢复统一框架

## 适用范围

凡涉及 **对话备份、读取历史对话、恢复并续聊** 等操作均适用。

**执行通道**：默认使用 **Write** / **Read** 工具操作本地 Markdown 文件；不依赖网络。

**归属路由**：备份落点由 `mod-multi-round-framework.mdc` 的归属判断决定：
- 无归属 → `Daily-Backups/`（临时，15天自动清理）
- 有项目归属 → `<P>/对话备份/`（阶段，手动清理）

---

## 抽象阶段结构（强制）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。

| 阶段 | 名称 | 核心职责 | 下层工作流引用 |
|:---:|:---|:---|:---|
| **A** | 时间解析 | 解析日期、时段、年份省略 | 见下方「阶段A细则」 |
| **B** | 归属判断 | 确定对话归属（项目/日常） | `flow-conversation-routing.mdc` |
| **C** | 操作分型 | 按备份/读取/续聊分流到具体工作流 | 见下方「阶段C细则」 |

---

## 阶段 A — 时间解析

### 日期规则

- 完整格式：`X年X月X日`（如 2026年4月23日）
- 省略年份：同年份可省略，默认当前年份
- 示例：`4月23日下午` → 解析为当年 4月23日

### 时段规则（固定）

| 时段 | 时间范围 |
|:---|:---|
| 上午 | < 14:00 |
| 下午 | 14:00 - 19:59 |
| 晚上 | >= 20:00 |

### 边界时间确认

当前时间位于以下边界点正负 10 分钟内时，**必须询问用户确认**：

- 14:00（上午/下午分界）
- 20:00（下午/晚上分界）
- 00:00（跨日分界）

### 凌晨记录规则

当前时间处于 00:00-05:59 时，必须询问用户归属：
- 选项 1：记为"当前日期的上午"
- 选项 2：记为"前一日晚上"

---

## 阶段 B — 归属判断

> 完整操作细则见 `flow-conversation-routing.mdc`。
> 本节仅保留阶段定义与引用指针。

备份落点按归属分流：
- **无归属**：`Daily-Backups/` → 索引文件 `Daily-Backups/日常备份索引.md`
- **有项目目录**：`<P>/对话备份/` → 无独立索引，由 `Master_Control` 轮次日志替代

---

## 阶段 C — 按操作分型

阶段 B 完成后，根据操作类型进入对应第三层工作流：

| 操作 | 核心步骤 | 执行前必须 | 工作流文件 |
|:---:|:---|:---|:---|
| **备份** | 时间获取 → 归属判断 → 主题生成 → 内容摘要 → 落盘 | 边界时间须确认 | `flow-conversation-backup.mdc` |
| **读取** | 时间解析 → 索引检索 → 展示摘要 | 多条时须先选择 | `flow-conversation-read.mdc` |
| **续聊** | 时间解析 → 归属判断 → 定位主控 → 恢复上下文 | 多条时须先选择；多轮项目须先读主控 | `flow-conversation-resume.mdc` |
| **升级** | 归属判断 → 创建主控+备忘 → 迁移备份 → R0 | 用户确认 Y | `flow-multi-round-upgrade.mdc` |

---

## 备份文件规范

### 日常备份（无归属）

| 项目 | 规范 |
|:---|:---|
| 目录 | `Daily-Backups/` |
| 文件名 | `TMP_YYYY-MM-DD_时段_主题.md` |
| Lifecycle | `临时`（15天自动清理） |
| 索引 | `Daily-Backups/日常备份索引.md` |

### 项目归属备份

| 项目 | 规范 |
|:---|:---|
| 目录 | `<P>/对话备份/` |
| 文件名 | `YYYY-MM-DD_时段_主题.md` |
| Lifecycle | `阶段`（多轮完结后手动清理） |
| 索引 | 由 `<P>/<P>_Master_Control.md` 轮次日志替代 |

### 明细文件 frontmatter（必须）

```yaml
---
Lifecycle: 临时  # 或 阶段
date: YYYY-MM-DD
time: HH:MM
period: 上午/下午/晚上
topic: 简短主题
---
```

---

## 输出约定

- **备份成功**：回显归属 + 落点路径 + 主题
- **读取成功**：回显核心结论 + 下轮衔接点（如有）
- **续聊成功**：输出三段：`已恢复上下文`、`当前可继续事项`、`建议下一步`

---

## 三层架构速查

| 层级 | 文件 | 职责 |
|:---:|:---|:---|
| 第一层（网关） | `gateway-command-router.mdc` | 指令路由到本框架 |
| 第二层（框架） | `mod-conversation-framework.mdc` | 本文件，定义阶段结构 |
| 第二层（框架） | `mod-multi-round-framework.mdc` | 多轮三方架构与归属判断 |
| 第三层（工作流） | `flow-conversation-backup.mdc` | 备份流程具体步骤 |
| 第三层（工作流） | `flow-conversation-read.mdc` | 读取流程具体步骤 |
| 第三层（工作流） | `flow-conversation-resume.mdc` | 续聊流程具体步骤 |
| 第三层（工作流） | `flow-conversation-routing.mdc` | 归属判断操作细则 |
| 第三层（工作流） | `flow-multi-round-upgrade.mdc` | 多轮升级流程 |

---

## 规则索引

- **对话备份模块**：
  - 第二层：`mod-conversation-framework.mdc`（本文件，统一框架）
  - 第三层：`flow-conversation-backup.mdc`（备份流程）
  - 第三层：`flow-conversation-read.mdc`（读取流程）
  - 第三层：`flow-conversation-resume.mdc`（续聊流程）
- **多轮长记忆模块**：
  - 第二层：`mod-multi-round-framework.mdc`（三方独立架构）
  - 第三层：`flow-conversation-routing.mdc`（归属判断）
  - 第三层：`flow-multi-round-upgrade.mdc`（升级流程）
