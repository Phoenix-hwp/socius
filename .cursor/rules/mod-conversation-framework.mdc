---
description: 对话备份与恢复统一框架：定义阶段结构，不含具体步骤。备份落点按归属路由。
glossary:
  layer: 2-模块框架  module: Conversation
guard_replaceable: framework

> **Guard 轨道**：对话备读取/续聊是纯 Agent-文件系统交互，无对应 Guard 代码模块。对话内容的 State Persistence `state_persistence.py` 管理，断档恢复由 `recovery.py` 管理，但具体备份落点/路由/格式仍由本框+ flow-* 定义---

# 对话备份与恢复统一框架

## 适用范围

凡涉**对话备份、读取历史对话、恢复并续聊** 等操作均适用
**执行通道**：默认使**Write** / **Read** 工具操作本地 Markdown 文件；不依赖网络
**归属路由**：备份落点由 `mod-multi-round-framework.mdc` 的归属判断决定：
- 无归`Daily-Backups/`（临时，15天自动清理）
- 有项目归`<P>/对话备份/`（临时，15天自动清理）

---

## 抽象阶段结构（强制）

**原则**：每一Agent **只推进一*，每步只向用户索**当前步所需的一个答*
| 阶段 | 名称 | 核心职责 | 下层工作流引|
|:---:|:---|:---|:---|
| **A** | 时间解析 | 解析日期、时段、年份省| 见下方「阶段A细则|
| **B** | 归属判断 | 确定对话归属（项日常| `flow-conversation-routing.mdc` |
| **C** | 操作分型 | 按备读取/续聊分流到具体工作流 | 见下方「阶段C细则|

---

## 阶段 A 时间解析

### 日期规则

- 完整格式：`X年X月X日`（如 2026。
3日）
- 省略年份：同年份可省略，默认当前年份
- 示例：`4。
3日下午` 解析为当4。
3
### 时段规则（固定）

| 时段 | 时间范围 |
|:---|:---|
| 上午 | < 14:00 |
| 下午 | 14:00 - 19:59 |
| 晚上 | >= 20:00 |

### 边界时间确认

当前时间位于以下边界点正10 分钟内时*必须询问用户确认**
- 14:00（上下午分界- 20:00（下晚上分界- 00:00（跨日分界）

### 凌晨记录规则

当前时间处于 00:00-05:59 时，必须询问用户归属- 选项 1：记当前日期的上
- 选项 2：记前一日晚

---

## 阶段 B 归属判断

> 完整操作细则`flow-conversation-routing.mdc`> 本节仅保留阶段定义与引用指针
备份落点按归属分流：
- **无归*：`Daily-Backups/` 索引文件 `Daily-Backups/日常备份索引.md`
- **有项目目*：`<P>/对话备份/` 无独立索引，`Master_Control` 轮次日志替代

---

## 阶段 C 按操作分
阶段 B 完成后，根据操作类型进入对应第三层工作流
| 操作 | 核心步骤 | 执行前必| 工作流文|
|:---:|:---|:---|:---|
| **备份** | 时间获取 归属判断 主题生成 内容摘要 落盘 | 边界时间须确| `flow-conversation-backup.mdc` |
| **读取** | 时间解析 索引检展示摘要 | 多条时须先选择 | `flow-conversation-read.mdc` |
| **续聊** | 时间解析 归属判断 定位主控 恢复上下| 多条时须先选择；多轮项目须先读主控 | `flow-conversation-resume.mdc` |
| **升级** | 归属判断 创建主控+备忘 迁移备份 R0 | 用户确认 Y | `flow-multi-round-upgrade.mdc` |

---

## 备份文件规范

### 日常备份（无归属
| 项目 | 规范 |
|:---|:---|
| 目录 | `Daily-Backups/` |
| 文件| `TMP_YYYY-MM-DD_时段_主题.md` |
| Lifecycle | `临时`，5天自动清理） |
| 索引 | `Daily-Backups/日常备份索引.md` |

### 项目归属备份

| 项目 | 规范 |
|:---|:---|
| 目录 | `<P>/对话备份/` |
| 文件| `YYYY-MM-DD_时段_主题.md` |
| Lifecycle | `临时`，5 天自动清理） |
| 索引 | `<P>/<P>_Master_Control.md` 轮次日志替代 |

### 明细文件 frontmatter（必须）

```yaml
---
Lifecycle: 临时
date: YYYY-MM-DD
time: HH:MM
period: 上午/下午/晚上
topic: 简短主---
```

---

## 输出约定

- **备份成功**：回显归+ 落点路径 + 主题
- **读取成功**：回显核心结+ 下轮衔接点（如有- **续聊成功**：输出三段：`已恢复上下文`、`当前可继续事项`、`建议下一步`

---

## 规则文件内部分层（三层）

> 此表描述 `.cursor/rules/` 内部的文件组织约定，与系统四层能力架构（网关能力执行认知层）正交。四层架构定义见 `core/knowledge/framework.md` §一
| 层级 | 文件 | 职责 |
|:---:|:---|:---|
| 第一层（网关| `gateway-command-router.mdc` | 指令路由到本框架 |
| 第二层（框架| `mod-conversation-framework.mdc` | 本文件，定义阶段结构 |
| 第二层（框架| `mod-multi-round-framework.mdc` | 多轮三方架构与归属判|
| 第三层（工作流） | `flow-conversation-backup.mdc` | 备份流程具体步骤 |
| 第三层（工作流） | `flow-conversation-read.mdc` | 读取流程具体步骤 |
| 第三层（工作流） | `flow-conversation-resume.mdc` | 续聊流程具体步骤 |
| 第三层（工作流） | `flow-conversation-routing.mdc` | 归属判断操作细则 |
| 第三层（工作流） | `flow-multi-round-upgrade.mdc` | 多轮升级流程 |

---

## 规则索引

- **对话备份模块**  - 第二层：`mod-conversation-framework.mdc`（本文件，统一框架  - 第三层：`flow-conversation-backup.mdc`（备份流程）
  - 第三层：`flow-conversation-read.mdc`（读取流程）
  - 第三层：`flow-conversation-resume.mdc`（续聊流程）
- **多轮长记忆模*  - 第二层：`mod-multi-round-framework.mdc`（三方独立架构）
  - 第三层：`flow-conversation-routing.mdc`（归属判断）
  - 第三层：`flow-multi-round-upgrade.mdc`（升级流程）
