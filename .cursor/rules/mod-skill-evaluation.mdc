---
description: 技能评估体系 — 准入评分 + 表现评分 + 周期巡检 + 竞争替换 + 任务匹配。覆盖备选与已安装技能的全生命周期评估。
alwaysApply: false
Lifecycle: 长期
Created: 2026-05-12
related:
  - mod-skills-library-framework.mdc（Skills Library 框架，第 G 阶段引用本规则）
  - flow-skill-acquire.mdc（安全闸门引用本规则准入评分）
  - flow-behavior-auto-receipt.mdc（任务收束时嵌入表现评分）
  - Skills_Library/task-type-registry.md（任务类型注册表）
  - Skills_Library/skill-candidates.md（备选候选表）
  - Skills_Library/skill-registry.json（已安装技能注册表）
  - Skills_Library/skill-performance-log.jsonl（表现评分日志）
  - pre-change-impact-enumeration.mdc（涉及字段/结构变更时的引用搜索）
---

# 技能评估体系

## 适用范围

对 Skills Library 中所有技能的 **全生命周期评估**：
- **候选阶段**：新发现的 GitHub/本地技能 → 准入评分 → 决定是否进入候选表
- **表现阶段**：已安装技能每次完成任务 → 五维表现评分 → 累积性能画像
- **巡检阶段**：定期检查质量变化、风险变化、静默淘汰
- **替换阶段**：同一任务类型有多个候选时，按表现分竞争排序

---

## §1 任务类型锚点（中枢设计）

### 设计原则

**任务类型是锚点，技能反向声明自己覆盖哪些任务类型。**

```
任务类型（Task Type）←─ 技能声明 task_types ←─ 技能 A / 技能 B
    │
    ├── 触发关键词（可无限扩展，新说法自动追加）
    ├── 父类型（可选，支持层级）
    └── 结果期望（该类型任务完成的评判标准）
```

### 为什么反向设计

- 新技能注册时声明 `task_types: ["视频下载"]` 即可，关键词自动生效
- 新说法（如「扒个视频」）追加到对应任务类型的 `触发关键词`，所有覆盖该类型的技能全部受益
- 技能能做什么，读自己的 `task_types` 字段就知道

### 任务类型注册表

存放位置：`Skills_Library/task-type-registry.md`
每条记录包含：类型名、父类型、触发关键词、结果期望、覆盖技能列表。

---

## §2 候选准入评分（准入前）

### 触发条件

新发现一个技能仓库（GitHub / 本地），决定是否录入 `skill-candidates.md`。

### 六维评分

| 维度 | 权重 | 评分标准（0-5） |
|:---|:---|:---|
| **功能契合度** | 高 | 0=与现有体系无关；3=覆盖已知但未急迫需求；5=正好解决当前 Active 痛点 |
| **质量信号** | 高 | 0=无文档/无 Star/长期未更新；3=有维护但社区小；5=大牛/大厂维护 + 高 Star + 活跃 PR |
| **风险可控度** | 高 | 0=声明写入内核路径 + eval/exec + 外部平台强绑定；3=有风险但可隔离；5=纯流程/只读/无外部依赖 |
| **安装复杂度** | 中 | 0=需编译 + 系统级依赖 + root 权限；3=需多步安装但可自动化；5=npx 一行 / 单文件复制 |
| **体系兼容度** | 中 | 0=与已有 skill 路径重叠 + 功能冲突；3=有小重叠但可共存；5=完全独立 / 互补不重叠 |
| **可逆性** | 低 | 0=卸载需手动清理多处文件；3=可通过脚本卸载；5=git 一行回滚 + trash 目录隔离 |

满分 30。

### 准入阈值

| 分数 | 动作 |
|:---|:---|
| ≥ 24 | 推荐立即接入，录入候选表并标注「首推」 |
| 18-23 | 录入候选表，状态 = 候选 |
| 12-17 | 录入候选表，状态 = 观察（低分标注原因） |
| < 12 | 不入候选表（可记录到不入清单备查） |

### 准入评分记录

在 `skill-candidates.md` 中为该技能追加一行评估记录（日期、评分、各维度得分、决策）。

---

## §3 任务完成表现评分（安装后）

### 触发条件

已安装技能（`skill-registry.json` 中 `status: active`）完成任务后，在 `flow-behavior-auto-receipt.mdc` 的收束流程中嵌入评估步骤。

### 五维评分

| 维度 | 0分 | 3分 | 5分 | 检测方式 |
|:---|:---|:---|:---|:---|
| **正确性** | 完全失败，用户手动重来 | 1-2 处纠正 | 一次性完全正确，无纠正 | 用户后续纠正行为（见 §3.1） |
| **自主度** | 每步都需人工介入纠正 | 关键步骤需 1 次确认 | 全自动完成无需干预 | Agent 自评 + 用户终审 |
| **副作用** | 预期外文件/网络写入且未清理 | 有小副作用但已清理 | 零副作用 | `git diff --stat` 检查 |
| **效率（轮次）** | 超历史中位 2 倍+ | 接近中位 | 一轮完成 | 对话轮次计数 |
| **能耗（Token）** | 超历史中位 2 倍+，或离群值 | 接近中位 | 同类任务消耗最低 20% | Token 计数（收束时记录） |

满分 25。

### §3.1 正确性检测：用户后续纠正行为

不由 Agent 自我判断正确性，而是由用户完成后的行为来界定：

| 信号 | 判定 | 得分 |
|:---|:---|:---|
| 任务完成后用户直接进入下一个话题，无任何反馈 | 完全正确 | 5 |
| 用户说"再加一个 X"、"顺便也做下 Y" | 非纠正，是扩展需求 | 5 |
| 用户说"把 X 改成 Y"（单一纠正，Agent 立刻改对） | 1 处偏差 | 4 |
| 用户说"X 和 Y 都改一下"（两处纠正，Agent 都改对） | 2 处偏差 | 3 |
| 用户说"不对，应该……"后 Agent 重新执行了一整段 | 显著错误 | 2 |
| 用户说了 3+ 次纠正，或明显表达了不满 | 严重错误 | 1 |
| Agent 产出完全无法使用，用户手动重来 | 完全失败 | 0 |

### §3.2 效率基准：同任务类型历史中位数

```
效率分 = (轮次分 + Token分) / 2

轮次分 = 5 - min(5, (本轮轮次 / 该类型历史中位轮次 - 1) × 5)
Token分 = 5 - min(5, (本轮Token / 该类型历史中位Token - 1) × 5)
```

首次执行（该类型 < 3 条记录）：效率维度不参与评分（权重暂时为 0）。

### §3.3 技能表现分

```
技能表现分 = 最近 5 次任务得分的算术均值（< 5 次取全量）
```

### §3.4 表现评级

| 均值 | 评级 | 动作 |
|:---|:---|:---|
| ≥ 20 | ⭐ 优秀 | 保持，可升频 |
| 15-19 | ✅ 合格 | 保持 |
| 10-14 | ⚠ 关注 | 标注，下次评估重点看 |
| < 10 | ❌ 差 | 建议休眠 / 替换 |

### §3.5 评分日志

存放位置：`Skills_Library/skill-performance-log.jsonl`
每行一条 JSON：`{"date":"ISO","skill_id":"xxx","task_type":"xxx","correctness":5,"autonomy":4,"side_effects":5,"efficiency_turns":4,"efficiency_tokens":3,"total":21}`

---

## §4 任务匹配流程

### 决策链

```
用户任务 → 提取关键词 → 命中的任务类型（查 task-type-registry）
                            │
              ┌─────────────┼─────────────┐
              ↓             ↓             ↓
        内部技能有覆盖   内部无覆盖      内部无覆盖
        且 status=active  外部技能有覆盖  外部也无覆盖
              │             │             │
              ↓             ↓             ↓
         直接用内部       展示 Top 3      搜索（WebSearch
         技能执行         外部技能候选    + 备选列表）
                         AskQuestion     + 建议新建
                    [启用推荐] [Skip]
```

**优先级**：内部活跃技能 > 外部已安装技能 > 备选候选技能 > 全网搜索。

### 排序规则（多候选时）

同一任务类型有多个技能覆盖时，按以下优先级排序展示 Top 3：

1. `status = active` 优先于 `dormant` 优先于备选
2. 同状态下按 `表现分`（已安装）或 `准入分`（备选）降序
3. 分数相近（差值 < 3）时，按风险升序（低 > 中 > 高）

---

## §5 竞争替换

### 触发条件

同一任务类型存在 ≥ 2 个技能（1 活跃 + ≥ 1 备选或已安装）。

### 替换阈值

```
备选技能准入分 > 当前活跃技能表现分 + 4
  → AskQuestion [推荐替换] [暂缓]
```

阈值说明：准入分满分 30，表现分满分 25；+4 的差值确保备选在"未经验证的理论分"上显著高于当前"实测表现分"，防抖避免频繁切换。

### 替换后观察期

替换后新技能前 3 次任务表现分不计入替换判断（给新技能适应期），但正常记录到 `skill-performance-log.jsonl`。

---

## §6 周期性巡检

### 三级巡检机制

| 级别 | 触发条件 | 巡检内容 | 阻断 |
|:---|:---|:---|:---|
| **轻检** | 每次 sessionStart（与 kernel-runtime L3 闸门并行），或距上次轻检 > 7 天 | 检查 `skill-candidates.md` 中所有记录的最后评估日期；≥ 90 天的标注「待巡检」但不阻断 | 否（静默标注） |
| **中检** | 用户说「评估技能」/「技能巡检」，或距上次中检 > 30 天 | 逐技能检查：质量变化（Stars/更新）、风险变化（仓库状态）、表现评分趋势 | 否（仅报表） |
| **重检** | 用户说「技能大检」，或距上次重检 > 180 天 | 全部备选 + 活跃技能过一遍：准入分重算、静默淘汰判定、归档建议 | 是（AskQuestion 确认淘汰项） |

### 巡检记录

每次巡检后，在 `skill-candidates.md` 底部「巡检记录」区域追加一行（日期、级别、发现、动作）。

---

## §7 静默淘汰

### 触发条件（三者全部满足）

1. 最后评估距今天数 > 180 天
2. 期间无任务触发匹配（该技能覆盖的所有 `task_types` 在 `skill-performance-log.jsonl` 中记录数为 0）
3. GitHub 仓库无更新 / 仓库 404 / Stars 无增长

### 淘汰流程

1. 在重检中发现满足条件的技能
2. 在 `skill-candidates.md` 中标记为「静默待淘汰」
3. AskQuestion `[确认移除] [再观察 30 天]`
   - `[确认移除]`：从候选表中移除，追加到淘汰记录
   - `[再观察 30 天]`：重置最后评估日期，30 天后再判

---

## §8 评估数据流与嵌入点

### 在 flow-behavior-auto-receipt 中的嵌入

当任务完成自动收束（步骤 C）触发时，嵌入选填评估步骤（新增步骤 C+）：

1. 判断本轮是否使用了外部或内部技能完成任务
2. 若是 → 按 §3 五维标准自评正确性、自主度、副作用、效率、能耗
3. AskQuestion 轻确认：`[确认评估] [修改评估值]`（默认确认自评，用户可手动调整任意维度）
4. 写一行到 `Skills_Library/skill-performance-log.jsonl`
5. 更新 `skill-registry.json` 中该技能的 `last_used`、`performance_history` 和 `performance_score`

> 以上步骤落地为 `flow-behavior-auto-receipt.mdc` 步骤 C+（任务完成时触发）。具体执行顺序见该文件。

### 在 flow-skill-acquire 中的嵌入

在 §B 安全闸门通过后，嵌入准入评分步骤：

1. 计算六维准入分
2. 展示评分结果 + 建议（接入/候选/观察/不入）
3. 用户确认后，若 ≥ 18 分，写入 `skill-candidates.md`

---

## §9 字段规范

### skill-registry.json 新增字段

```json
{
  "task_types": ["视频下载", "媒体操作"],
  "performance_score": 17.5,
  "performance_history": [
    {"date": "2026-05-12", "task_type": "视频下载", "total": 20},
    {"date": "2026-05-11", "task_type": "视频下载", "total": 15}
  ]
}
```

### skill-candidates.md 新增字段

| 字段 | 说明 |
|:---|:---|
| task_types | 覆盖的任务类型列表 |
| 准入分 | 六维评分（满分 30） |
| 各维度得分 | 功能/质量/风险/安装/兼容/可逆 |
| 评估历史 | 每次重新评估的日期、分数变化、原因 |

---

## 异常边界

- **task-type-registry.md 不存在** → 降级为纯关键词匹配（无任务类型映射），提示用户创建注册表
- **skill-performance-log.jsonl 为空** → 效率维度和表现评级暂不可用，仅记录单次正确性/自主度/副作用
- **同一技能在候选表和注册表中均有记录** → 以注册表为准（已安装覆盖备选）
- **GitHub API 不可达** → 质量信号维度标记为「无法验证」，降权 1 分
