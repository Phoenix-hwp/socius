---
description: 第一层网关层——指令别名路由与全局异常兜底
Lifecycle: 长期
Created: 2026-05-12
Updated: 2026-05-23 (P008 条件唤醒 待办选中仿真/V012/意图级任务强制加
glossary:
  layer: 1-网关路由
  purpose: 指令解析、模块分发、跨模块协调、全局异常兜底
  downstream:
    - mod-notion-*
    - mod-project-*
    - mod-conversation-*
    - mod-multi-round-*
    - Skills_Library/scripts/speak.py
    - Skills_Library/scripts/todo-reminder.py
    - mod-decision-framework.mdc
alwaysApply: true
guard_replaceable: false
---

# 指令别名路由规则

## 适用范围

当用户输入短指令（尤其是 2-20 字的中文祈使句）时，先按下列步骤执行，再进入常规回答

## 路由流程

0. **强制 TIP 触发兜底（非纯文本产出物 必走 TIP*：在执行任何映射或操作之前，先对用户指令*产出物介质扫*——若指令同时满足以下两个条件，Agent **必须先执`task-init-protocol.mdc` 的完整流程（1.5*，不得跳过：

   | 条件 | 检测方|
   |:---|:---|
   | **产出物非纯文* | 指令中含 `SVG`/`PDF`/`PPT(P)`/`DOC(X)`/`图表`/`架构图`/`流程图`/`UML`/`Mermaid`/`时序图`/`视频`/`音频`/`图像` 等关键词，或显式提到了输出格|
   | **产出操作动词** | `输出`/`生成`/`写入`/`做`/`创建`/`画`/`绘制`/`转换`/`导出` 等创建型动词 + 产出物路径或文件|

   > **设计原因**：P008 依赖 TIP 作为上游触发入口。今天（2026-05-20）实测发Agent 可能在无别名匹配时跳TIP 直接执行，导P008 全链路断裂。此规则作为兜底——非纯文本产出物必然需要渲染管线（`RENDERER_RELIABILITY` 检查）和任务拆解（Step 4），因此优先触发 TIP

   > **跳过条件**：产出物为纯文本（Markdown/代码/JSON）且体积小（ 个文件的简单修改）标注 `纯文本简任务，跳TIP`

0. **方法可靠性预检（强制）**：在执行任何映射动作之前，先 Read `.cursor/method-reliability-registry.json`，筛选「当前任务类+ 当前设备(hostname)」的记录。若该方法的状态为 降权 🚫 禁用，且用户未显式指定该方法 跳过，取下一正常 的方法。若无正常方法可提示用户所有已知方法均被降禁用，请手动指定或等待恢复
0.2 **任务权限决策评估**：在 Step 0 预检完成后，若本轮对话涉及可执行任务（非纯查纯对话），调`mod-decision-framework.mdc` 评估当前任务Delegation Level（L0-L3）。评估时机由 `task-init-protocol.mdc` 统一调度——Step 4 拆解后逐子任务评估 P008，Step 3 提案时顺出评分

> **命令级别名路由与 P008 的条件唤*待办"本身属于命令级操作，不直接触P008。但待办选中的具体任务可能是意图级任务——若选中项满足以下任一条件，网关须在进入执行前 **Read `mod-decision-framework.mdc`** 再加载对应的 flow 规则
>   - ID 匹配 `V012-DRILL-*` `TG-*` `S2-*`
>   - 或对`Task-Type-Registry.json` 条目`typical_L 1`
>   GUI 点击动作（如"语音""说全）无决策空间，仍不触P008。命令级操作的防护依赖各flow 规则内的自检流程 + `flow-high-risk-safety.mdc` 红色警戒兜底

> **设计注记（远期）**：当 `core/data/Task-Type-Registry.json` 中某操作类型达到迁移门槛（连续成，状态稳定为 L0），Agent 将主动提出将该操作从别名路由迁移P008 意图评估链路。永久保留别名路由的操作：纯 GUI 点击动作（如"语音""说全）——无决策空间，不P008
0.3 **别名纠偏检索（经验层，轻量*：在别名匹配（Step 1）命中后、执行映射动作（Step 2）前，快速检索行为日志中的纠偏记录：
   - Read `core/data/Behavior-Fit-Log.jsonl` 最20 行（若不足则全读），搜索 `behavior` 字段含「纠正」「纠偏」「改读」「误读」「错误路径」等标记、且 `topic` 字段与当前命中别名同主题的记录
   - **若命*：在回复中标`📌 经验纠偏：上次同类操作曾纠正路径，本次按纠正后路径执行`，并按纠正记录调整执行路径，避免重蹈覆辙
   - **若未命中**：静默跳过，继续按别名映射原路执行
   - 本步为轻量检索（ Read + 文本匹配），不触L3 全量分析
0.1 **设备执行规则匹配（强制）**：在通过方法预检后、即将发Shell 命令前，Read `.cursor/device-execution-rules.json`，检测当前平台（`$env:OS` / `uname` + 当前 Shell 类型）。过`os` `shell` 字段均匹配的规则，对即将执行的命令文本逐条匹配 `condition`
   - `always`：无条件应用
   - `starts_with "<prefix>"`：命令以指定前缀开
   - `contains "<substr>"`：命令包含指定子
   - `regex "<pattern>"`：命令匹配正
   - 命中规则后按 `action` 执行：`replace`（自动替from→to）、`before`（在命令前追to）、`after`（在命令后追to）、`warn`（提示已有修正建议但不自动应用）、`block`（拒绝执行并说明原因
   - 应用修正后，在执行前简短标`已应用设备规<id>rationale>`，让用户感知
   - 跨设备兼容：本文件随 Git 同步；运行时仅匹配当OS+Shell 的规则，无关规则自动跳过，不产生副作
1. **先在 `adapters/cursor/aliases.md` 中查*完全匹配***高相似匹*（同义词、近义说法）
2. **若命中映*：必须先执行映射动作（通常是先 Read 指定文件并基于该文件继续处理），不得跳过到无关检
3. **若未命中映射**：才执行常规搜索；并可建议用户把该短指令加入别名清单

---

## 创建指令默认行为（强制）

- 当用户要创建指令"时，默认理解为：**创建一个执行级规则**（`.cursor/rules/*.mdc`），而不只是写在普通笔记里
- 创建时必须同步登记到 `adapters/cursor/aliases.md`，并给该指令配置 1 个或多个别名

## 别名长度约束（强制）

- 每条指令的别名长度必须为 **2-10 个字**（含 2 10
- 执行触发时，**完整指令***任一合规别名**均视为同一映射并执行同一动作
- 若用户给出的别名不满2-10 个字，应先提示并提供合规替代，再登记

## 别名映射

> 完整别名清单`adapters/cursor/aliases.md`。路由流Step 1 在匹配时 Read 该文件
> 帮助索引`Command-Help-Index.md`（`help` 指令入口）

---

## 跨模块复合指令（协调执行顺序

当用户输入涉及多个模块的指令时，按以下优先级顺序执行

| 场景示例 | 执行顺序 | 协调原则 |
|:---|:---|:---|
| "查询Notion后更 | Notion查询 Notion更新 | 同模块内衔接，parent复用 |

**协调原则**
1. **状态依赖优*：下游依赖上游结果时，上游先执行
2. **失败即停**：任一模块失败则暂停后续，询问是否继续
3. **统一回执**：多模块完成后给出汇总简

---

## 全局异常兜底

### 场景1：未知指令（未命中任何别名）

1. **模糊匹配推荐**：给出最接近的可选指
2. **询问澄清**您是想执[推荐指令] 吗？"
3. **引导帮助**：建议输`help` 查看完整指令清单，或将此指令登记到别名表

### 场景2：指令歧义（命中多个别名

1. 列出所有匹配的候选指
2. 请用户明确选择请输入数1/2/3 确认具体指令"

### 场景3：模块级失败（下游规则文件缺损坏

1. **检查新版映*：询问用户该指令是否有新版映
2. **尝试备用**：尝试使用备用规则或降级方案
3. **给出替代**：给出可继续的离线替代方

### 场景4：Guard 不可用（CLI 返回 exit_code 1

Guard 命令行工具（`guard.py`）不可用或执行失败时，Backend（Cursor Agent）须执行以下兜底

1. **自动降级为双Fallback 模式**：所有任务默认定**L1（告知执行）**，Agent 在执行前告知用户计划但不等待确认
2. **继续使用 .mdc 规则作为唯一安全*：`flow-high-risk-safety.mdc`、`data-governance-standards.mdc`、`script-coding-constraints.mdc` alwaysApply 安全规则继续全时段生
3. **不清空工具池**：不Guard 缺位而拦截任何工具调用；本场景的安全.mdc 规则单独负责
4. **提示用户**：Agent 回复中标`Guard 当前不可用，L1 + .mdc 规则执行`
5. **Guard 恢复*：下一轮任务自动切Guard 轨道（无需手动切换

> **RACI 的关*：Guard 缺位时，T1（命令拦截）退化为 .mdc 红色警戒流程，T4–T5（编一致性校验）退化为 Agent 自检清单

---

## 失败兜底（文件不存在时）

若用户指令映射到的关联文件不存在，不得直接失败；应先

1. **检查该指令是否已有新版映射**（询问用户）
2. **若无新版映射**，提示并协助修复映射到现存文
3. **再执行后续动*

---

## 删除保护（全局

> 细则`mod-project-memo-framework.mdc` §文件保护

---

## 规则文件内部分层（gateway/mod/flow

> 此表描述 `.cursor/rules/` 内部的文件组织约定，与系统四层能力架构（网关能力执行认知层）正交，互不替代。四层架构定义见 `core/knowledge/framework.md` §一

| 层级 | 前缀 | 职责 | 示例 |
|:---:|:---|:---|:---|
| 第一| `gateway-*.mdc` | 路由、分发、兜| 本文|
| 第二| `mod-*-*.mdc` | 模块通用规则、异常边| `mod-notion-crud-framework.mdc` |
| 第三| `flow-*-*.mdc` | 具体场景步骤 | `flow-notion-create.mdc` |

**横切规则**（不按上表前缀命名、不绑定单一业务模块）：`kernel-runtime.mdc`*内核运行：知识库约定 + 会话开收束 + 轮次确认 + 多轮长记*）、`pre-edit-script-change-brief.mdc`、`pre-change-impact-enumeration.mdc`*变更前搜全库引用**）、`git-cross-device-and-secrets.mdc`*跨设备路径兼容与本地密钥安全**）、`script-coding-constraints.mdc`*脚本编码约束：防耦合、防硬编码、保规则文件分层合规，含钩子双运行时规范**）等 与域模块 **松耦合**；业务步骤仍落在对应 `mod-*` / `flow-*`，避免在横切文件中重复写操作细则

---

## 规则索引

> 完整规则索引（严重级分类、模块分层清单）`Command-Help-Index.md`
> 路由流程中需要匹配模块规则时，通过别名映射Read 对应 `mod-*` / `flow-*` 文件
