---
description: 第一层网关 — 指令别名路由与全局异常兜底
glossary:
  layer: 1-网关路由层
  purpose: 指令解析、模块分发、跨模块协调、全局异常兜底
  downstream:
    - mod-notion-*
    - mod-git-*
    - mod-project-*
    - mod-conversation-*
    - mod-multi-round-*
alwaysApply: true
---

# 指令别名路由规则

## 适用范围

当用户输入短指令（尤其是 2-20 字的中文祈使句）时，先按下列步骤执行，再进入常规回答。

## 路由流程

0. **方法可靠性预检（强制）**：在执行任何映射动作之前，先 Read `.cursor/method-reliability-registry.json`，筛选「当前任务类型 + 当前设备(hostname)」的记录。若该方法的状态为 ⚠ 降权 或 🚫 禁用，且用户未显式指定该方法 → 跳过，取下一个 ✅ 正常 的方法。若无正常方法可用 → 提示用户所有已知方法均被降权/禁用，请手动指定或等待恢复。
0.1 **设备执行规则匹配（强制）**：在通过方法预检后、即将发出 Shell 命令前，Read `.cursor/device-execution-rules.json`，检测当前平台（`$env:OS` / `uname` + 当前 Shell 类型）。过滤 `os` 与 `shell` 字段均匹配的规则，对即将执行的命令文本逐条匹配 `condition`：
   - `always`：无条件应用
   - `starts_with "<prefix>"`：命令以指定前缀开头
   - `contains "<substr>"`：命令包含指定子串
   - `regex "<pattern>"`：命令匹配正则
   - 命中规则后按 `action` 执行：`replace`（自动替换 from→to）、`before`（在命令前追加 to）、`after`（在命令后追加 to）、`warn`（提示已有修正建议但不自动应用）、`block`（拒绝执行并说明原因）
   - 应用修正后，在执行前简短标注 `⚙ 已应用设备规则 <id>：<rationale>`，让用户感知
   - 跨设备兼容：本文件随 Git 同步；运行时仅匹配当前 OS+Shell 的规则，无关规则自动跳过，不产生副作用
1. **先在 `10-Topics/Cursor-command-aliases.md` 中查找**完全匹配**与**高相似匹配**（同义词、近义说法）
2. **若命中映射**：必须先执行映射动作（通常是先 Read 指定文件并基于该文件继续处理），不得跳过到无关检索
3. **若未命中映射**：才执行常规搜索；并可建议用户把该短指令加入别名清单

**拉取后 / 多设备对齐**：若本轮对话中完成了 `git pull`、或用户表示远端已更新别名/规则，在执行**任何**短指令前须先遵守 `post-git-pull-three-layer-awareness.mdc`（重新 Read 别名表与网关索引，避免映射过时）。

---

## 创建指令默认行为（强制）

- 当用户要求"创建指令"时，默认理解为：**创建一个执行级规则**（`.cursor/rules/*.mdc`），而不只是写在普通笔记里
- 创建时必须同步登记到 `10-Topics/Cursor-command-aliases.md`，并给该指令配置 1 个或多个别名

## 别名长度约束（强制）

- 每条指令的别名长度必须为 **2-10 个字**（含 2 与 10）
- 执行触发时，**完整指令**与**任一合规别名**均视为同一映射并执行同一动作
- 若用户给出的别名不满足 2-10 个字，应先提示并提供合规替代，再登记

---

## 当前关键别名（硬性优先）

| 别名 | 映射文件 | 执行动作 |
|:---|:---|:---|
| `help` / `查看帮助` | `Command-Help-Index.md` | 返回"指令 + 别名 + 用途"清单 |
| `项目备忘录` / `XX项目备忘录` | `flow-project-memo-read.mdc`（框架：`mod-project-memo-framework.mdc`） | 查看指定项目的待复核清单与备忘（支持多项目） |
| `记录备忘` / `记录XX备忘` | `flow-project-memo-append.mdc`（框架：`mod-project-memo-framework.mdc`） | 追加备忘到指定项目（支持多项目） |
| `改动预告` / `预期先行` | `pre-edit-script-change-brief.mdc` | 修改脚本前简述预期 |
| `提交git` / `Git同步` / `推仓库` / `提交远端` | `flow-git-commit.mdc`（框架：`mod-git-crud-framework.mdc`） | add → commit → pull --rebase → push；不含盲拉变体 |
| `拉取git` / `拉取远端` / `直接拉取` | `flow-git-pull.mdc`（框架：`mod-git-crud-framework.mdc`） | `git pull --rebase`，一句到位，不做审查 |
| `安全拉取` / `审查拉取` / `拉取审查` | `flow-git-safe-pull.mdc`（框架：`mod-git-crud-framework.mdc`） | `git fetch` 后展示远端差异，用户选 [1]覆盖 [2]合并 [3]保留 后再执行 |
| `新设备初始化` / `初始化` / `初始化设备` / `换机设置` / `设备初始化` | `git-cross-device-and-secrets.mdc`、`模型配置说明.md` | 在**工作区根**执行 `bootstrap-on-pull.cmd`（或由 Agent 等价调用），回执脚本摘要与仍须手动的配置项 |
| `git到新设备` / `克隆到新路径` / `同步到新位置` | `flow-git-clone-to-custom-path.mdc` | 确认后拉取 Git 到用户指定的自定义路径 |
| **Notion 相关**：| | |
| `Notion操作流程` / `Notion增删改查` | `mod-notion-crud-framework.mdc` | Read 统一 CRUD 框架 |
| `Notion创建` / `写入Notion` | `flow-notion-create.mdc` | 进入创建页面六步流程 |
| `Notion更新` / `更新Notion` | `flow-notion-update.mdc` | 更新页面内容 |
| `Notion删除` / `删除Notion` | `flow-notion-delete.mdc` | 归档页面 |
| `Notion查询` / `查询Notion` | `flow-notion-query.mdc` | 关键词检索并展示摘要 |
| **长记忆 / 多轮**：| | |
| `继续对话` / `继续XX项目` / `继续` | `flow-conversation-resume.mdc`（框架：`mod-multi-round-framework.mdc`） | 按归属路由：无归属按日常索引检索，有项目读取 Master_Control 轻确认 |
| `升级为多轮` / `创建项目主控` / `转为长记忆` | `flow-multi-round-upgrade.mdc`（框架：`mod-multi-round-framework.mdc`） | 创建 Master_Control + Project_Memo + 备份目录，R0 初始化 |
| `备份对话` / `备份当前对话` | `flow-conversation-backup.mdc`（框架：`mod-conversation-framework.mdc`） | 按归属落盘：无归属 → `Daily-Backups/`，有归属 → `<项目目录>/对话备份/` |
| `读取对话` / `查对话` | `flow-conversation-read.mdc`（框架：`mod-conversation-framework.mdc`） | 按日期+时段检索备份并展示摘要 |
| `总结本轮对话` / `本轮总结` | `kernel-runtime.mdc` §4.5 | 输出已完成/未完成/待确认/下轮起点 |
| `确认合并` / `执行合并` | `kernel-runtime.mdc` §4.6 | 一致性快检后合并写入主文档 |
| `执行策略` / `策略` / `风险策略` | `Command-Help-Index.md` | 返回当前执行策略 |
| `快速模式` / `自动执行` / `少确认` | `Command-Help-Index.md` | 低/中风险自动执行，高风险确认 |
| `谨慎模式` / `全确认` / `严格确认` | `Command-Help-Index.md` | 所有写入先确认 |
| **Earth Library**：| | |
| `存入图书馆` / `入馆` / `存知识` | `flow-library-ingest.mdc`（框架：`mod-earth-library-framework.mdc`） | 入库知识到 Earth Library |
| `启用图书馆` / `开馆` / `启用库` | `mod-earth-library-framework.mdc` | 开启补充参考模式 |
| `停用图书馆` / `闭馆` / `停用库` | `mod-earth-library-framework.mdc` | 关闭补充参考模式 |
| `图书馆巡检` / `巡检图书馆` / `库巡检` | `flow-library-review.mdc`（框架：`mod-earth-library-framework.mdc`） | 质量巡检并写入待处理队列 |
| `图书馆纠错` / `纠错图书馆` / `库纠错` | `flow-library-review.mdc`（框架：`mod-earth-library-framework.mdc`） | 对待处理项标记纠错建议 |
| `图书馆优化` / `优化图书馆` / `库优化` | `flow-library-review.mdc`（框架：`mod-earth-library-framework.mdc`） | 追加结构优化建议 |
| `更新图书馆标签` / `标签维护` / `更新标签` | `Earth_Library/Tag_Guide.md` | 维护标签词典 |
| **能力固化**：| | |
| `封装能力` / `固化流程` / `封装指令` | `flow-capability-encapsulate.mdc` | 提取步骤序列 → 复杂度判定产物 → 预览 → AskQuestion `[确认封装] [修改预览]` → 落盘 + 登记 skill-registry |
| **Skills Library**：| | |
| `获取技能` / `安装技能` / `接入能力` / `添加Skill` | `flow-skill-acquire.mdc`（框架：`mod-skills-library-framework.mdc`） | 外部技能获取 → 安全闸门（四维检查）→ 隔离试运行 → AskQuestion 确认部署 |
| `技能管理` / `技能开关` / `管理Skills` | `flow-skill-toggle.mdc`（框架：`mod-skills-library-framework.mdc`） | 查看/启闭/休眠/归档技能（AskQuestion 面板交互） |
| `技能全开` / `启用所有技能` | `flow-skill-toggle.mdc`（框架：`mod-skills-library-framework.mdc`） | 一键启闭所有外部技能（锁定技能除外） |
| `技能全关` / `禁用所有技能` | `flow-skill-toggle.mdc`（框架：`mod-skills-library-framework.mdc`） | 一键关闭所有外部技能，仅保留内核规则 |
| `执行技能` / `运行技能` / `调用技能` | `flow-skill-execute.mdc`（框架：`mod-skills-library-framework.mdc`） | 执行前快照 → 写路径限制 → 执行后差异审查 → AskQuestion 确认/回滚 |
| `评估技能` / `技能评估` / `技能巡检` | `mod-skill-evaluation.mdc`（框架：`mod-skills-library-framework.mdc`） | 中检：逐技能检查质量变化/风险变化/表现评分趋势 |

---

## 跨模块复合指令（协调执行顺序）

当用户输入涉及多个模块的指令时，按以下优先级顺序执行：

| 场景示例 | 执行顺序 | 协调原则 |
|:---|:---|:---|
| "提交到Notion然后Git备份" | Notion → Git | 先完成写入，再版本控制 |
| "Git提交后记录项目备忘" | Git → 项目备忘 | 先同步代码，再更新项目状态 |
| "查询Notion后更新" | Notion查询 → Notion更新 | 同模块内衔接，parent复用 |

**协调原则**：
1. **状态依赖优先**：下游依赖上游结果时，上游先执行
2. **失败即停**：任一模块失败则暂停后续，询问是否继续
3. **统一回执**：多模块完成后给出汇总简报

---

## 全局异常兜底

### 场景1：未知指令（未命中任何别名）

1. **模糊匹配推荐**：给出最接近的可选指令
2. **询问澄清**："您是想执行 [推荐指令] 吗？"
3. **引导帮助**：建议输入 `help` 查看完整指令清单，或将此指令登记到别名表

### 场景2：指令歧义（命中多个别名）

1. 列出所有匹配的候选指令
2. 请用户明确选择："请输入数字 1/2/3 确认具体指令"

### 场景3：模块级失败（下游规则文件缺失/损坏）

1. **检查新版映射**：询问用户该指令是否有新版映射
2. **尝试备用**：尝试使用备用规则或降级方案
3. **给出替代**：给出可继续的离线替代方案

---

## 失败兜底（文件不存在时）

若用户指令映射到的关联文件不存在，不得直接失败；应先：

1. **检查该指令是否已有新版映射**（询问用户）
2. **若无新版映射**，提示并协助修复映射到现存文件
3. **再执行后续动作**

---

## 删除保护（全局）

> 细则见 `mod-project-memo-framework.mdc` §文件保护。

---

## 三层架构速查

| 层级 | 前缀 | 职责 | 示例 |
|:---:|:---|:---|:---|
| 第一层 | `gateway-*.mdc` | 路由、分发、兜底 | 本文件 |
| 第二层 | `mod-*-*.mdc` | 模块通用规则、异常边界 | `mod-notion-crud-framework.mdc` |
| 第三层 | `flow-*-*.mdc` | 具体场景步骤 | `flow-notion-create.mdc` |

**横切规则**（不按上表前缀命名、不绑定单一业务模块）：如 `kernel-runtime.mdc`（**内核运行：知识库约定 + 会话开端/收束 + 轮次确认 + 多轮长记忆**）、`pre-edit-script-change-brief.mdc`、`pre-change-impact-enumeration.mdc`（**变更前搜全库引用**）、`git-cross-device-and-secrets.mdc`、`post-git-pull-three-layer-awareness.mdc`（**pull 并入远端更新后**重温三层架构与别名表，避免重复新增规则/脚本）、`script-coding-constraints.mdc`（**脚本编码约束：防耦合、防硬编码、保三层架构合规，含钩子双运行时规范**）等 — 与域模块 **松耦合**；业务步骤仍落在对应 `mod-*` / `flow-*`，避免在横切文件中重复写操作细则。

---

## 规则索引

- **Notion 模块**：
  - 第二层：`mod-notion-precondition.mdc`（阶段A网络确认）
  - 第二层：`mod-notion-crud-framework.mdc`（统一框架阶段B/C）
  - 第三层：`flow-notion-select-parent.mdc`（阶段B 目录落点操作细则）
  - 第三层：`flow-notion-locate-target.mdc`（共享定位子流程操作细则）
  - 第三层：`flow-notion-create.mdc`（增）
  - 第三层：`flow-notion-delete.mdc`（删）
  - 第三层：`flow-notion-update.mdc`（改）
  - 第三层：`flow-notion-query.mdc`（查）
- **Git 模块**：
  - 第二层：`mod-git-crud-framework.mdc`（统一框架阶段B/C）
  - 第三层：`flow-git-commit.mdc`（提交推送）
  - 第三层：`flow-git-pull.mdc`（直接拉取）
  - 第三层：`flow-git-safe-pull.mdc`（审查式拉取）
  - 第三层：`flow-git-clone-to-custom-path.mdc`（克隆到新路径）
- **对话备份模块**：
  - 第二层：`mod-conversation-framework.mdc`（统一框架阶段A/B/C，含归属路由）
  - 第三层：`flow-conversation-backup.mdc`（备份流程）
  - 第三层：`flow-conversation-read.mdc`（读取流程，支持双源）
  - 第三层：`flow-conversation-resume.mdc`（续聊流程）
- **多轮长记忆模块**：
  - 第二层：`mod-multi-round-framework.mdc`（三方独立架构）
  - 第三层：`flow-conversation-routing.mdc`（归属判断操作细则）
  - 第三层：`flow-multi-round-upgrade.mdc`（升级流程）
- **项目备忘录模块**：
  - 第二层：`mod-project-memo-framework.mdc`（统一框架）
  - 第三层：`flow-project-memo-read.mdc`（查看流程）
  - 第三层：`flow-project-memo-append.mdc`（追加流程）
- **Earth Library 模块**：
  - 第二层：`mod-earth-library-framework.mdc`（统一框架，知识补充层）
  - 第三层：`flow-library-ingest.mdc`（入库流程）
  - 第三层：`flow-library-ingest-notion-atomic.mdc`（Notion 原子入库：一页一卡）
  - 第三层：`flow-library-search.mdc`（检索流程）
  - 第三层：`flow-library-review.mdc`（巡检流程）
  - 架构文档：`Earth_Library/Earth_Library_Architecture.md`
- **行为偏好模块**：
  - 第二层：`mod-behavior-crud-framework.mdc`（统一框架：开端续接 / 快照 / 收束 / 手动收束）
  - 第三层：`flow-behavior-auto-receipt.mdc`（自动收束流程：步骤 A–E 全链路）
  - 注册表：`10-Topics/Behavior-Dimensions-Registry.md`（行为维度与值域）
  - 跟踪文件：`10-Topics/Active-Task-Tracker.json`（任务状态跟踪）
- **能力固化模块**：
  - 第三层：`flow-capability-encapsulate.mdc`（能力封装工作流：提取 → 判定 → 预览 → 落盘）
- **Skills Library 模块**：
  - 第二层：`mod-skills-library-framework.mdc`（统一框架：获取/部署/执行/开关/移除）
  - 第三层：`flow-skill-acquire.mdc`（技能获取：安全闸门 + 隔离试运行 + 落锚 + 清理安全约束 + 确认部署）
  - 第三层：`flow-skill-execute.mdc`（技能执行：快照 + 写路径限制 + 禁止操作清单 + 差异审查 + 确认/回滚）
  - 第三层：`flow-skill-toggle.mdc`（技能开关：单技能/全开全关/休眠/归档）
- **评估体系**：
  - 第二层：`mod-skill-evaluation.mdc`（准入评分 + 表现评分 + 巡检 + 替换）
  - 数据：`Skills_Library/task-type-registry.md`（任务类型锚点）
  - 数据：`Skills_Library/skill-performance-log.jsonl`（表现评分日志）
  - 数据：`Skills_Library/skill-candidates.md`（备选候选表）
- **横切**：`kernel-runtime.mdc`（内核运行：知识库 + 开端收束 + 轮次确认 + 长记忆）、`flow-high-risk-safety.mdc`（高风险操作防护：递归删除/强制杀进程约束）
