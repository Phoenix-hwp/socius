---
description: Earth Library 统一框架 — 作为项目知识补充层的阶段结构
glossary:
  layer: 2-模块框架层
  module: EarthLibrary
---

# Earth Library 统一框架

## 适用范围

凡涉及 **知识入库、检索、启停控制、质量巡检** 等 Earth Library 操作均适用。

## 存储约定

| 内容类型 | 存储位置 | 格式 |
|:---|:---|:---|
| 域概览卡 / 原子知识卡 | `Earth_Library/cards.jsonl` | JSONL |
| 问答模板 | `Earth_Library/Templates/` | `.md` |
| 卡片字段规范 | `Earth_Library/README_Card_Spec.md` | `.md` |
| 索引 | `Earth_Library/library_index.json` | JSON |
| 关联 | `Earth_Library/relations.jsonl` | JSONL |

## 角色与优先级

**回答优先级（域路由）**：

Agent 回答用户问题前，先判断问题域：

| 判断结果 | 主层（框架/方法论） | 补充层（数据/案例/最新信息） |
|---|---|---|
| **命中 Library 域概览卡** | Earth Library（域卡 → 原子卡） | 网络搜索 |
| **未命中** | 模型知识 + 网络实时信息 | Earth Library（被动补充检索） |

> 专业域的权威框架（如 BPMN、KANO、SWOT）优先使用 Library 知识；时效性数据（如竞品动态、行业数据）始终走网络搜索。两者融合回答。

**域划分参考**：`Earth_Library/Earth_Library_Memo.md` §5 领域划分表（当前 6 个域：商业战略与建模 / 流程建模与架构 / 产品方法论 / 用户研究 / B 端产品 / 支付金融）。

**启用前提**：`Earth_Library/System/library_switch.json` 的 `enabled` 为 `true` 时，才执行 Library 检索。

---

## 抽象阶段结构（强制）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。

| 阶段 | 名称 | 核心职责 | 下层工作流引用 |
|:---:|:---|:---|:---|
| **A** | 启停确认 | 检查 `library_switch.json` 状态 | 自动检测 |
| **B** | 操作分型 | 按入库/检索/启停/质检分流 | 见下方「阶段B细则」 |
| **C** | 执行与回执 | 执行具体操作，给出明确回执 | 见下方「阶段C细则」 |

---

## 阶段 A — 启停确认

### 自动检测

1. 读取 `Earth_Library/System/library_switch.json`
2. 检查 `enabled` 字段：
   - `true`：图书馆启用，回答时默认参考
   - `false`：图书馆停用，不主动参考（仅执行入库等写操作）

### 启停指令

| 指令 | 行为 |
|:---|:---|
| `启用图书馆` / `开馆` | 设置 `enabled: true` |
| `停用图书馆` / `闭馆` | 设置 `enabled: false` |

---

## 阶段 B — 操作分型

### B1. 入库操作

**触发词**：`存入图书馆` / `入馆` / `存知识`

**来源类型**：
- 对话沉淀（当前会话中的新增知识）
- 网络搜索精华
- 读书笔记
- Notion 拉取内容
- 用户直接提供的文本

> 入库字段 schema 见 `flow-library-ingest.mdc` §步骤2（包含必填/可选字段定义与自动推断规则）。

**进入工作流**：`flow-library-ingest.mdc`

---

### B2. 检索操作

**触发时机**：
- 用户主动查询：`搜索图书馆`、`查找知识`
- Agent 回答时自动补充（启用状态下）

**检索维度**：
- 关键词匹配（标题/内容/关键词字段）
- 标签筛选
- 置信度筛选
- 关联推荐（基于 `relations.jsonl`）

**进入工作流**：`flow-library-search.mdc`

---

### B3. 质检操作

**触发词**：
- `图书馆巡检` / `巡检图书馆` / `库巡检`
- `图书馆纠错` / `纠错图书馆` / `库纠错`
- `图书馆优化` / `优化图书馆` / `库优化`

**质检类型**：
| 类型 | 检测内容 | 输出 |
|:---|:---|:---|
| 巡检 | 疑似重复、关键词重合度高 | `Review_Queue.md` |
| 纠错 | 标记待复核项的非破坏式修正 | 卡片标注 |
| 优化 | 结构优化建议（主题聚合、来源补齐）| `Review_Queue.md` |

**进入工作流**：`flow-library-review.mdc`

---

### B4. 标签维护

**触发词**：`更新图书馆标签` / `更新标签` / `标签维护`

**维护对象**：`Earth_Library/System/tag_dictionary.json`

**支持操作**：
- 新增标签（name + category + triggers）
- 合并标签（迁移历史引用）
- 停用标签（标记 deprecated）

---

## 阶段 C — 执行与回执

### 成功回执格式（入库）

> 详见 `flow-library-ingest.mdc` §步骤6。

### 成功回执格式（检索）

> 详见 `flow-library-search.mdc` §步骤5。

### 成功回执格式（启停）

> 操作细则见下方 §阶段A — 启停指令。

---

## 与其他项目的集成

### 被动查询模式（域路由版，默认）

当图书馆**启用**时，回答用户问题分两条路径：

**路径 A：问题域命中 Library 域卡**
1. 匹配域概览卡（`cards.jsonl` 中 `type: 领域概览`）→ 确定 Library 覆盖该域
2. 通过 `search_library` 检索 `cards.jsonl` 中该域原子卡片 → 获取方法论/框架/边界。执行：
   `python Earth_Library/scripts/search_library.py --q <域关键词> --max_results 5`
3. 按域卡「域内缺口」标注，针对缺口维度进行网络搜索
4. 网络搜索补充实时数据/案例/最新研究
5. 融合：Library 框架 × 网络数据 = 回答

**路径 B：问题域未命中 Library 域卡**
1. 模型知识 + 网络搜索回答（主）
2. 可选：Library 关键词检索（被动补充）
3. 整合后给出完整回答

**问答模板**可进一步固化特定问题类型的执行步骤，参见 `Earth_Library/Templates/`。

### 主动推送模式（入库时）

入库新知识点时：
1. 提取关键词/标签
2. 匹配关注这些标签的项目
3. 在回执中提示："该知识可能与 [项目A]、[项目B] 相关"

### 项目订阅模式（规划中）

项目可在初始化时声明：
```yaml
# 在项目主控文件中
earth_library_subscription:
  tags: ["产品设计", "分佣"]
  keywords: ["盈钻", "订单"]
```

---

## 规则文件内部分层（三层）

> 此表描述 `.cursor/rules/` 内部的文件组织约定，与系统四层能力架构（网关层/能力层/执行层/认知层）正交。四层架构定义见 `Knowledge-Brain/framework.md` §一。

| 层级 | 文件 | 职责 |
|:---:|:---|:---|
| 第一层（网关） | `gateway-command-router.mdc` | 指令路由到本框架 |
| 第二层（框架） | `mod-earth-library-framework.mdc` | 本文件，定义阶段结构 |
| 第三层（工作流） | `flow-library-ingest.mdc` | 通用入库流程 |
| 第三层（工作流） | `flow-library-ingest-notion-atomic.mdc` | **Notion 原子入库（一页一卡）** |
| 第三层（工作流） | `flow-library-search.mdc` | 检索流程 |
| 第三层（工作流） | `flow-library-review.mdc` | 巡检流程 |

---

## 规则索引

- **Earth Library 模块**：
  - 第二层：`mod-earth-library-framework.mdc`（本文件，统一框架）
  - 第三层：`flow-library-ingest.mdc`（入库流程）
  - 第三层：`flow-library-ingest-notion-atomic.mdc`（Notion 原子入库流程）
  - 第三层：`flow-library-search.mdc`（检索流程）
  - 第三层：`flow-library-review.mdc`（巡检流程）
  - 架构文档：`Earth_Library/Earth_Library_Architecture.md`
  - 标签维护：`Earth_Library/Tag_Guide.md`
