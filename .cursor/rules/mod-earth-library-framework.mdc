---
description: Earth Library 统一框架 — 作为项目知识补充层的阶段结构
glossary:
  layer: 2-模块执行层
  module: EarthLibrary
---

# Earth Library 统一框架

## 适用范围

凡涉及 **知识入库、检索、启停控制、质量巡检** 等 Earth Library 操作均适用。

**角色定位**：为本工作区所有项目提供**知识参考补充层**，作为网络信息的辅助与沉淀。

**回答优先级**：
1. 模型知识与网络实时信息（主）
2. **Earth Library（补充）** ← 本框架管辖
3. 项目专属档案（背景）

**启用前提**：`Earth_Library/System/library_switch.json` 的 `enabled` 为 `true` 时，才执行第 2 步补充层。

---

## 抽象阶段结构（强制）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。

| 阶段 | 名称 | 核心职责 | 下层工作流引用 |
|:---:|:---|:---|:---|
| **A** | 启停确认 | 检查 `library_switch.json` 状态 | 自动检测 |
| **B** | 操作分型 | 按入库/检索/启停/质检分流 | 见下方「阶段B细则」 |
| **C** | 执行与回执 | 执行具体操作，给出明确回执 | 见下方「阶段C细则」 |

---

## 阶段 A — 启停确认

### 自动检测

1. 读取 `Earth_Library/System/library_switch.json`
2. 检查 `enabled` 字段：
   - `true`：图书馆启用，回答时默认参考
   - `false`：图书馆停用，不主动参考（仅执行入库等写操作）

### 启停指令

| 指令 | 行为 |
|:---|:---|
| `启用图书馆` / `开馆` | 设置 `enabled: true` |
| `停用图书馆` / `闭馆` | 设置 `enabled: false` |

---

## 阶段 B — 操作分型

### B1. 入库操作

**触发词**：`存入图书馆` / `入馆` / `存知识`

**来源类型**：
- 对话沉淀（当前会话中的新增知识）
- 网络搜索精华
- 读书笔记
- Notion 拉取内容
- 用户直接提供的文本

**统一字段**：
| 字段 | 必填 | 说明 |
|:---|:---:|:---|
| `title` | ✅ | 知识标题 |
| `type` | ✅ | 知识类型（方法论/概念定义/实操步骤/案例分析）|
| `source` | ✅ | 来源说明 |
| `source_mode` | ✅ | 来源模式（对话/网络/读书笔记/Notion）|
| `confidence` | ✅ | 置信度（高/中/低）|
| `keywords` | ✅ | 关键词（逗号分隔）|
| `content` | ✅ | 知识内容 |
| `source_url` | ❌ | 来源链接 |
| `source_path` | ❌ | 来源文件路径 |

**进入工作流**：`flow-library-ingest.mdc`

---

### B2. 检索操作

**触发时机**：
- 用户主动查询：`搜索图书馆`、`查找知识`
- Agent 回答时自动补充（启用状态下）

**检索维度**：
- 关键词匹配（标题/内容/关键词字段）
- 标签筛选
- 置信度筛选
- 关联推荐（基于 Relations_Index.md）

**进入工作流**：`flow-library-search.mdc`

---

### B3. 质检操作

**触发词**：
- `图书馆巡检` / `巡检图书馆` / `库巡检`
- `图书馆纠错` / `纠错图书馆` / `库纠错`
- `图书馆优化` / `优化图书馆` / `库优化`

**质检类型**：
| 类型 | 检测内容 | 输出 |
|:---|:---|:---|
| 巡检 | 疑似重复、关键词重合度高 | `Review_Queue.md` |
| 纠错 | 标记待复核项的非破坏式修正 | 卡片标注 |
| 优化 | 结构优化建议（主题聚合、来源补齐）| `Review_Queue.md` |

**进入工作流**：`flow-library-review.mdc`

---

### B4. 标签维护

**触发词**：`更新图书馆标签` / `更新标签` / `标签维护`

**维护对象**：`Earth_Library/System/tag_dictionary.json`

**支持操作**：
- 新增标签（name + category + triggers）
- 合并标签（迁移历史引用）
- 停用标签（标记 deprecated）

---

## 阶段 C — 执行与回执

### 成功回执格式（入库）

```
已存入 Earth Library：
- 卡片：Knowledge_Cards/YYYYMMDD-HHMMSS_标题.md
- 索引：Library_Index.md（已更新）
- 标签：[标签1, 标签2, 标签3]
- 关联：发现 2 条相关知识（Relations_Index.md 已更新）
```

### 成功回执格式（检索）

```
Earth Library 检索结果（启用状态）：
- 命中：N 条
- 相关卡片：
  1. 标题（置信度：高）- 摘要...
  2. 标题（置信度：中）- 摘要...
- 推荐：基于关联网络，还可参考...
```

### 成功回执格式（启停）

```
图书馆状态已更新：
- 当前状态：启用/停用
- 影响：回答时 将/不再 默认参考图书馆知识
```

---

## 与其他项目的集成

### 被动查询模式（默认）

当图书馆**启用**时，回答用户问题时：
1. 先用主层（模型知识+网络）回答
2. 搜索 Library 补充相关知识点
3. 整合后给出完整回答

### 主动推送模式（入库时）

入库新知识点时：
1. 提取关键词/标签
2. 匹配关注这些标签的项目
3. 在回执中提示："该知识可能与 [项目A]、[项目B] 相关"

### 项目订阅模式（规划中）

项目可在初始化时声明：
```yaml
# 在项目主控文件中
earth_library_subscription:
  tags: ["产品设计", "分佣"]
  keywords: ["赢钻", "订单"]
```

---

## 三层架构速查

| 层级 | 文件 | 职责 |
|:---:|:---|:---|
| 第一层（网关） | `gateway-command-router.mdc` | 指令路由到本框架 |
| 第二层（框架） | `mod-earth-library-framework.mdc` | 本文件，定义阶段结构 |
| 第三层（工作流） | `flow-library-ingest.mdc` | 入库流程具体步骤 |
| 第三层（工作流） | `flow-library-search.mdc` | 检索流程具体步骤 |
| 第三层（工作流） | `flow-library-review.mdc` | 巡检流程具体步骤 |

---

## 规则索引

- **Earth Library 模块**：
  - 第二层：`mod-earth-library-framework.mdc`（本文件，统一框架）
  - 第三层：`flow-library-ingest.mdc`（入库流程）
  - 第三层：`flow-library-search.mdc`（检索流程）
  - 第三层：`flow-library-review.mdc`（巡检流程）
  - 架构文档：`Earth_Library/Earth_Library_Architecture.md`
  - 标签维护：`Earth_Library/Tag_Guide.md`
