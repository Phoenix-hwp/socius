---
description: 权限决策框架（P008）— 7 维度任务评估 → 双轨制推导 L0-L3 Delegation Level → FSM 自动升级/降级。在任务启动协议（task-init-protocol.mdc）Step 4 拆解后、Step 5 执行中调用
glossary:
  layer: 2-模块框架层
  purpose: 按任务维度评估风险/创意/模糊度/影响范围/可逆性/外部暴露/价值冲突，自动推导 L0-L3 权限等级，决定 Agent 自主执行边界
  cross-layer-coupling: intentional  # 被 task-init-protocol.mdc 嵌入调用，双向引用为刻意设计
  trigger_points:
    - "task-init-protocol.mdc Step 4（拆解后逐子任务评估 P008）"
    - "task-init-protocol.mdc Step 3（提案时顺出 P008 评分，L≥L2 时展示维度明细）"

> **设计注记（远期）**：执行途中不需要 P008 中途升级检测——每个新用户指令 = 新任务边界 = 独立 P008。
> 远期当 Task-Type-Registry 对某操作类型积累足够成熟度后，别名路由将逐步收缩，收敛到 P008 意图评估链路。
> 永久保留别名路由的操作：纯 GUI 点击动作（如"播报""说全文"）——无决策空间，不走 P008。
  derivation:
    R_track: "max(S, Rev, A, E, Auth, V) → L_R"
    C_track: "C0→L0, C1→L1, C2→L1, C3→L2 → L_C"
    final: "L = max(L_R, L_C)"
  downstream:
    - 10-Topics/Decision-Log.jsonl (决策日志)
    - 10-Topics/User-Knowledge-Registry.jsonl (方案评估时查询)
    - .cursor/method-reliability-registry.json (M 维度消费者)
    - .cursor/rules/task-init-protocol.mdc (嵌入协议)
alwaysApply: false
trigger: 任务启动协议 Step 4 拆解后自动调用；Step 3 提案时顺出评分

> 命令级别名路由（"拉取git""提交git""待办"等）不触发 P008。
> P008 仅对意图级任务生效（原子/复合/大系统级）。
> 命令级操作的防护依赖各自 flow 规则内的自检流程 + 红色警戒兜底。
---

# 权限决策框架（Decision Framework）

## 设计原则

### 原则 1：一次触发升级（max() 一条线升维）

> 任何一个维度被拉到需要人为介入，就一定要人为接入。升维是 0→3 推上去，不是 3→0 降下来。

`max()` 的语义：不需要大部分维度都危险才需要确认——**一个维度的危险就足够**。

```
L = max(L_R, L_C)
L_R = max(map(S→L), map(Rev→L), map(A→L), map(E→L), map(Auth→L), map(V→L))
L_C = map(C→L)   // 独立轨道
```

### 原则 2：双轨制 — 物理风控 + 认知授权

| 轨道 | 测量 | 维度 | 聚合 |
|------|------|------|------|
| **R 轨**（Risk — 物理风控） | "搞砸了代价多大？" | S, Rev, A, E, Auth, V | max() |
| **C 轨**（Creativity — 认知授权） | "Agent 能不能自己做对？" | C（独立） | 独立映射 |

两者质不同。双轨制从根源上分开——物理风控和认知授权各跑各的，最后取更高者。

### 原则 3：默认保守，证据驱动升级

- S 维度：除非用户显式给了 success criteria，否则默认 S1
- C 维度：高创意 = 高不确定性，C3 → L2（确认而非直接执行）
- 升级需要连续 N 次同类任务成功（N ≥ 5），降级只需一次失败

### 原则 4：任务级评估 + 命令级兜底

任务级决定**默认行为**（L0-L3），命令级（`flow-high-risk-safety.mdc` 红色警戒）做**兜底阻断**。

---

## 一、7 维度定义

### S — 影响范围（Impact Scope）

**问**：搞砸了影响多大？

| 等级 | 定义 | 示例 | 映射 L |
|------|------|------|--------|
| **S0** | 单文件/单点 | 改一个 typo、单变量、单函数 | L0 |
| **S1** | 单模块/单服务 | 一个功能模块的全部文件（如 Notion 增删改查 4 个子能力的整合） | L0 |
| **S2** | 跨模块/跨服务 | 修改影响到多个独立模块间的交互 | L2 |
| **S3** | 跨系统/全局 | 全盘操作、跨仓库、外部系统（如 D 盘清空事故） | L3 |

---

### Rev — 可逆性（Reversibility）

**问**：执行后能不能恢复？

| 等级 | 定义 | 示例 | 映射 L |
|------|------|------|--------|
| **Rev0** | 完全可逆 | `git revert`、本地 diff 回退、快照恢复 | L0 |
| **Rev1** | 部分可逆 | 需要人工介入但可恢复 | L1 |
| **Rev2** | 难以回滚 | 跨模块状态耦合、需专门脚本恢复 | L2 |
| **Rev3** | 不可逆 | 删除、迁移、外部写入无回退机制 | L3 |

---

### A — 模糊度（Ambiguity）

**问**：目标清晰吗？Agent 能确定自己该做什么吗？

| 等级 | 定义 | 示例 | 映射 L |
|------|------|------|--------|
| **A0** | 目标明确、边界清晰 | "把这个文件的第 3 行改成 xxx" | L0 |
| **A1** | 方向清晰但细节不明 | "优化简历自我评价"——知道要优化，但尺度需试 | L1 |
| **A2** | 关键目标不明确，需要澄清 | "调整下架构"——不知范围/深度/约束 | L2 |
| **A3** | 任务未定义，需反向提问 | Agent 不能说"我打算这么做对吗"，而应**先总结讨论结论**，然后确认是否按此落地 | L2 |

> A3 特殊处理：若当前上下文中已有充分讨论的结论，Agent 应将所有讨论结论**逐条罗列**，然后问"是否按这些结论落地？"——用已建立的共识替代空白反问。

---

### C — 创意度（Creativity）— **独立轨道**

**问**：产出物的"正确答案"能被自动化验证吗？

| 等级 | 定义 | 示例 | 映射 L_C |
|------|------|------|----------|
| **C0** | 无创意：纯逻辑、纯分析、纯执行 | 从 Notion 提取内容形成知识卡、市场数据收集 | L0 |
| **C1** | 低创意：在既有框架内微调、润色、补全 | 简历措辞优化、代码注释补全 | L1 |
| **C2** | 中创意：需要构思，但边界由用户给定 | "我们架构应该支持多租户，你给个方案"——框在、人在框里发挥 | L1 |
| **C3** | 高创意：原创、发散、构建新结构或新概念 | "帮我设计一个新协议"、一句简单描述让 Agent 画一幅画 | L2 |

> C2 不升到 L2：因为边界是用户给的，偏了也是框内偏，告知后讨论即可。
> C3 升到 L2 不升 L3：C3 是"我能创作，但我需要你先看一眼方向"，不是你全不知道要什么。

**C 轨独立映射规则**：

| C | L_C | 行为 |
|---|-----|------|
| C0 | L0 | 无需动判断力，照指令执行 |
| C1 | L1 | 允许微调润色，改完告知 |
| C2 | L1 | 允许在用户给定边界内发挥 |
| C3 | L2 | 先出想法框架，确认方向再全力输出 |

---

### E — 外部可逆性（External Reversibility）

**问**：外部系统上的操作能不能撤销？

| 等级 | 定义 | 示例 | 映射 L |
|------|------|------|--------|
| **E0** | 无外部依赖 | 纯本地操作 | L0 |
| **E1** | 只读外部 | WebSearch、Fetch、读 Notion | L0 |
| **E2** | 可逆外部写入 | 写 Notion 页面（有版本历史/回收站）、调 API 创建测试资源 | L1 |
| **E3** | 不可逆外部写入 | 支付、强制删库、群发邮件、修生产配置 | L3 |

---

### Auth — 外部暴露（External Exposure）

**问**：给了外部什么钥匙？这把钥匙能收回来吗？

| 等级 | 定义 | 示例 | 映射 L |
|------|------|------|--------|
| **Auth0** | 零暴露 | 纯本地读写 | L0 |
| **Auth1** | 只读 | WebSearch、Fetch、读 MCP 资源 | L0 |
| **Auth2** | 可控写入 | 创建/编辑指定 Notion 页面、push feature 分支 | L1 |
| **Auth3** | 不可逆/广域写入 | 删 D 盘、force push 到 main、改生产配置、授权新 OAuth scope | L3 |

> Auth 和 E 正交但不重叠：Auth 问"给了多少权限"（范围），E 问"操作能不能撤销"（性质）。

---

### V — 价值冲突（Value Conflict）

**问**：任务是否涉及偏好选择、优先级 trade-off？

| 等级 | 定义 | 示例 | 映射 L |
|------|------|------|--------|
| **V0** | 无冲突，单目标 | 帮我查个资料 | L0 |
| **V1** | 轻微冲突，Agent 可给建议 | Agent 给建议，你说"可以，按你说的来" | L1 |
| **V2** | 中度冲突，需用户偏好 | 简历改了你的排版风格，你要求拉回偏好 | L2 |
| **V3** | 高度冲突（效率 vs 完整性、速度 vs 稳定性、使用者 vs 决策者利益） | PPT 内容完整性 vs 模板匹配度 trade-off | L3 |

> 未来演进：V 维度可能拆分为"任务目标维度"和"利益相关者维度"（老板优先 / 员工优先 / 客户优先 / 使用者优先）。

---

## 二、L0-L3 Delegation Level 映射

### 维度评估辅助输入（知识库查表）

在评估 S/A/C 维度时，Agent 应查询以下数据源，作为维度判断的辅助输入：

| 查什么 | 去哪查 | 影响哪个维度 |
|--------|--------|-------------|
| 领域方法论（B端产品链路、用例图规范等） | 关联 Notion 原文 + `Knowledge-Brain/framework.md` | A ↓（有方法论锚定，模糊度降低）、C ↓（有框架参考，创意自由度不需全开） |
| 操作经验/流程偏好 | `User-Knowledge-Registry.jsonl` | Rev ↑/↓（知道什么路径风险高） |
| 同类型任务历史 | `Task-Type-Registry.json` → P008 FSM + Decomposition-Log | A ↓（知道上次怎么拆的）、FSM 升级/降级判定 |
| 方法可靠性与已知参数 | `method-reliability-registry.json`（含 `known_issues` + `known_params`） | Rev ↑/↓（方法是否可靠）、C ↓（有参数模版可用） |

#### 知识脑预载入降噪（P008 联动）

> **触发时机**：`task-init-protocol.mdc` Step 2-KB 执行完毕后，`kb_protocols_activated` 字段已填入。

当 Agent 在执行 P008 评估前已通过 Step 2-KB 完成了知识脑前置查询（`kb_protocols_activated` 字段非空，且其中至少 1 条协议的 `validated_count ≥ 1`）：

| 条件 | 影响 | 原理 |
|------|------|------|
| `kb_protocols_activated` 中 ≥1 条 `validated_count ≥ 1` | A 降一级（A2→A1, A1→A0，不低于 A0） | 有已验证知识兜底，目标模糊度降低 |
| 同上 | C 降一级（C3→C2, C2→C1, C1→C0，不低于 C0） | 有框架/策略参考，创意不必全开 |
| `kb_protocols_activated` 为空或全部为 `validated_count = 0` | 不做降级 | 无已验证知识，认知风险不变 |

> **注意**：知识脑预载入**仅影响 A 和 C 维度**（认知层面）。物理风控维度（S/Rev/Auth/E/V）不受 KB 预载入影响——知道怎么做 ≠ 不会搞砸。降级后仍在 Step 3 提案中展示「原始评分 → KB 降级后评分」，让用户感知知识脑的影响。

**效果**：查到了 → 维度评估更准，L 可能降低（因有知识兜底）。查不到 → 不降 L，但不影响评估。A≥A2 且知识脑无覆盖时，在 L2 提案末尾追加「⚡ 知识脑缺口」提示。

| L | 行为 | 触发条件 |
|---|------|----------|
| **L0 — 全自动** | 直接执行，无需告知或确认 | L_R=0 且 L_C=0 |
| **L1 — 告知执行** | 执行前简述计划，执行后告知结果。不阻断 | max(L_R, L_C) = L1 |
| **L2 — 确认执行** | 出方案 → 等用户确认 → 执行。**L2 时必须展示各维度评分**，让用户知道哪个维度触发了升级，用户可手动降级覆盖 | max(L_R, L_C) = L2 |
| **L3 — 仅分析** | 只给分析、风险枚举、方案对比。不执行任何写操作 | max(L_R, L_C) = L3 |

**L2 展示格式**：

```
⚡ 决策框架评估：L2（确认执行）
维度：S0 Rev0 A0 C1 E0 Auth0 V2
触发项：V2（中度价值冲突 — 排版偏好需确认）
→ 方案如下，确认后执行：
```

---

## 三、升级/降级状态机（FSM）

### 升级规则（L2 → L1 → L0）

当满足以下全部条件时，同类型任务自动升级：

1. 连续 ≥ 5 次执行成功（用户未打断、结果符合预期）
2. 无越权行为
3. 任务模式一致（同类型任务）
4. 升级后首轮仍附带轻告知（不阻断），失败一次立即降级

### 降级规则

以下任一触发时立即降级：

1. 用户手动介入或中断
2. 结果不符合预期
3. 出现越权写入
4. 风险维度被重新评估为更高
5. 模糊度上升（如 A1 → A2）

### 强制 L3

以下情况无条件进入 L3：

- Rev3（不可逆风险）
- S3（跨系统）
- Auth3（不可逆广域写入）
- E3（不可逆外部写入）
- 涉及规则层（`.cursor/rules/*.mdc`）修改（需额外确认）

### FSM 知识联动（P1 — 规则占位，待数据积累后启用）

> **当前状态**：规则已写，不触发。待 `activation-log.jsonl` 中积累足够有效数据后再启用。详情见 `00-Inbox/存疑_知识脑与决策层联动_2026-05-17.md`。

**条件**：同 `task_type` 下，某协议 `activated_count ≥ 3` 且 `effective_count = 0`（所有激活均被纠正参与）。
**动作**：该协议在该 `task_type` 下的 P008 A/C 不再降级。`mod-decision-framework.mdc` §三 FSM 降级规则追加一条：「KB 协议在同类任务中连续未被有效使用」。
**启用门槛**：待 `activation-log.jsonl` 中该 `task_type` 累计 ≥5 条记录后首次启用。

---

## 四、任务判类（Step 1 触发）

| 类型 | 判定 | 流程 |
|------|------|------|
| **原子任务** | 单产出物、单领域、单工具链。如"改这份简历""读这个文件并总结" | Step 1 确认需求 → 直接跳到 Step 3 提案（跳过拆解），P008 在提案时顺出 |
| **复合任务** | 多产出物或多阶段。如"调研并输出 PPT""重构模块 + 写测试 + 更新文档" | 走完整协议，Step 4 拆解后对每个子任务独立评 P008 |
| **大系统级** | 跨系统、可能跨会话、涉及架构决策。如"把 Notion 集成改造成多租户" | 先进架构讨论对齐边界，降级后再进正常流程 |

> **判类只在 Step 1 做一次，子任务不重新判类。** 复合任务拆出来的子任务默认仍是可执行步骤。除非执行中发现**父任务判类时未被预见的新系统边界**，才触发升级。

---

## 五、大系统级：递归自相似协议

大系统级不是独立的协议——它是 `task-init-protocol.mdc` 的 **多层递归应用**。

```
Layer 0（任务判类）
  Step 1: 这是大系统吗？→ 是

Layer 1（架构层）── 对标协议的宏观应用
  ① Agent 复述理解 + 提出一级拆解方案
  ④ 拆解：列出关键决策点（≤5 个）
  ④-bis 材料对齐：已有架构文档 + 相关案例
  ② 结构探针 & 逐点讨论：每轮只攻一个决策点，清零再走
  ③ 提案：降级判断（见下方）
  ⑤ 执行落地

Layer 2（模块层）── 每个决策点独立走一轮微缩协议
  ① 理解 → ② 讨论 → ③ 定案 → 执行
```

**降级触发**：当架构讨论产出"可操作的任务清单"（谁做什么、产出物、验证标准）时 → 自动降级为复合任务 → 走正常拆解+执行流程。若仍高不确定性 → 提议 PoC（最小探路）→ PoC 出结果后回退再对齐。

**Agent 默认行为**：Agent 主动提出一级拆解方案。用户如有不同思路，在对话中直接碰撞调整。协议不预判用户是否有预设方案。

**防误判护栏**：
- "我不确定怎么做（策略模糊）≠ 大系统级" → 策略模糊用 Step 3（提案）解决
- 只有在"连做出来是什么都不完全清楚"时才升级为大系统级

---

## 六、决策日志（Decision-Log.jsonl）

每条决策一行 JSONL：

```json
{
  "date": "2026-05-15",
  "task_id": "T003-1",
  "dimensions": {"S":0,"Rev":0,"A":0,"C":1,"E":0,"Auth":0,"V":0},
  "L_R": 0,
  "L_C": 1,
  "L_final": 1,
  "decision": "L1 — 告知执行",
  "kb_protocols_activated": ["CP-003", "CP-012"],
  "kb_dimensions_adjusted": {"A": "1→0", "C": "1→0"},
  "kb_effective": true,
  "result": "success",
  "user_override": false
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `kb_protocols_activated` | `[string]` | Step 2-KB 预载入匹配的协议 ID 列表。空数组 = 无匹配 |
| `kb_dimensions_adjusted` | `{string: string}` | KB 降级效果：原始评级→降级后评级。如 `{"A":"1→0","C":"1→0"}`。仅当降级发生时填写 |
| `kb_effective` | `boolean` | 任务收束时由 `flow-behavior-auto-receipt.mdc` 回填。`true` = 激活协议本轮有效，`false` = 激活但被用户纠正，`null` = 无激活协议 |
| `kb_miscued` | `[string]` | 被用户纠正涉及的协议 ID 列表（收束时回填） |

> 方案评估时查询 `User-Knowledge-Registry.jsonl`，匹配同类任务的历史经验作为维度评估的辅助输入。
> 
> **知识脑缺口提示**：A≥A2 且知识脑无覆盖时，在 L2 提案末尾追加「⚡ 知识脑缺口：我尚未学习 [领域] 的方法论。如果你有相关笔记/文章/书籍，可以让我读一读。」不阻断任务。

---

## 七、与命令级兜底的嵌套

```
任务进入
  ↓
[任务级] 本框架做 7 维度 + 推导 L0-L3
  ↓
任务策略 = Lx，全任务统一行为
  ↓
[命令级] flow-high-risk-safety.mdc 红色警戒仍生效
  作为最后一道防线：即使任务评估为 L0
  但某条具体命令触发了 Rev3（递归删除）
  → 强制覆盖为 AskQuestion 确认 ⚠
```

---

## 八、未覆盖的补充维度（预留）

| 维度 | 来源 | 状态 |
|------|------|------|
| **M — 方法可靠性** | `method-reliability-registry.json` | 已有 FSM，降权 → 任务至少 L2 |
| **P — 路径敏感度** | `flow-high-risk-safety.mdc` 隐含 | `.cursor/rules/` → 强制 L3 |
| **I/O 结构约束** | `User-Knowledge-Registry.jsonl` | 有排版结构的输入 → C 压为 C0 |
