---
description: 权限决策框架（P008）——5 维度概念定义 + C偏差差额模型 + L0-L3 行为映射（V 维度 2026-05-22 移除，C 维度 2026-05-23 从L计算移除改为偏差检测）。实际决策评估与 FSM Guard（guard/src/p008/engine.py + fsm.py）代码执行Lifecycle: 长期
Created: 2026-05-14
Updated: 2026-05-23
glossary:
  layer: 2-模块框架  purpose: 提供维度概念定义L0-L3 行为解释，供人类阅读。运行时评估Guard engine.py 执行（L = max(L_R, L_K, L_from_C) + FSM 升级/降级）  cross-layer-coupling: intentional
  trigger_points:
    - "task-init-protocol.mdc Step 4（拆解后逐子任务评估 P008
    - "task-init-protocol.mdc Step 3（提案时顺出 P008 评分
alwaysApply: false
trigger: 任务启动协议 Step 4 拆解后自动调用；Step 3 提案时顺出评guard_replaceable: framework

> **Guard 轨道**：运行时决策评估维评+ C偏差 LFSM升级/降级/强制L3）由 `engine.py` + `fsm.py` 自动执行> **本文*：保留概念定义供人查阅；运行时行为以 Guard 代码为准---

# 权限决策框架（Decision Framework
## 设计原则

    20|
1. **L = max(L_R, L_K, L_from_C)**：R 轨测执行风险，K 轨测知识缺口，C 偏差测方案发散度超出任务类型的程度2. **三轨独立**   - **R *（S/Rev/A/E/Auth）测"搞砸代价"——物理风险，不可逆后   - **K *（知识覆盖度）测"不知道该怎么——方法缺   - **C 偏差**（方案发散度 vs 任务预期）测"agent 的方案是否超出了该任务类型应有的设计空间"——对齐问题，非风。
3. **默认保守，证据驱*：升级需连续次成功，降级一次失败即触发
4. **任务+ 命令级兜*：任务级定默认行为，命令级（Guard `safety_gate.py`）做最后阻
    30|---

## 一 维度定义（V 已于 2026-05-22 移除；C 已于 2026-05-23 改为任务类型元数据）

> **V**（方案选择代价）：原属 R 轨，2026-05-22 起不再影L 级计算。机会成本量化保留为远期辅助系统前置能力> **C**（创意度/方案发散度）026-05-23 起从 L 公式中移除。C 不再参与 `max(L_R, L_K)` 计算，改为独立的**差额模型**——超出任务类`expected_C` 的部分触发执行等级提升。C 的语义从「风险因子」重新定义为「方案发散度约束」
### R 轨维度：执行风险

#### 影响范围（S| 等级 | 定义 | 示例 |
|:---|:---|:---|
| 0 | 单文单点 | 改一typo、单函数 |
| 1 | 单模单服| 一个功能模块的全部文件 |
| 2 | 跨模跨服| 多个独立模块间交互变|
| 3 | 跨系全局 | 全盘操作、跨仓库、外部系|

#### 可逆性（Rev| 等级 | 定义 | 示例 |
|:---|:---|:---|
| 0 | 完全可| 本地快照回退、diff 恢复 |
| 1 | 部分可| 需人工介入但可恢复 |
| 2 | 难以回滚 | 跨模块状态耦合 |
| 3 | 不可| 删除、迁移、外部无回退机制 |

#### 模糊度（A| 等级 | 定义 | 示例 |
|:---|:---|:---|
| 0 | 目标明确、边界清| "把第3行改成xxx" |
| 1 | 方向清晰但细节不| "优化简历自我评 |
| 2 | 关键目标不明| "调整下架 |
| 3 | 任务未定| 需反向提问确认 |

#### 外部操作风险（E| 等级 | 定义 |
|:---|:---|
| 0 | 纯本地，不涉及外部系|
| 1 | 只读外部（搜查询|
| 2 | 可撤回的外部写入（如 Notion 页面有版本历史） |
| 3 | 不可撤回的外部写入（支付/删库/群发邮件|

#### 外部暴露（Auth| 等级 | 定义 |
|:---|:---|
| 0 | 零暴|
| 1 | 只读外部 |
| 2 | 可控写入 |
| 3 | 不可广域写入（force push main、删D盘） |

### K 轨维度：知识覆盖
    80|
| 等级 | 定义 | 行为 |
|:---|:---|:---|
| 0 | 全覆盖：域有匹配协议，≥1 已验| 照常执行 |
| 1 | 部分覆盖：有协议，全未验| 标注风险，正常执|
| 2 | 缺口覆盖：域已知但无特定协议 | **列出可行路径，确认方向后执行** |
| 3 | 零覆盖：全新| **列出可能方向，确认方向后执行** |

### C 维度：方案发散度（任务类型元数据，非风险维度
    90|
> C 是任务类型的固有属性，`Task-Type-Registry.json` `expected_C` 字段定义。每个任务类型有创意度天花板——写周报天然不允C3，写散文天然允许 C3
| C | 定义 | 典型任务类型 |
|:---:|:---|:---|
| **C0** | 纯执无设计空| git 操作、Notion 查询、TTS、部|
| **C1** | 在既有框架内微调 | 编码、规则创建、知识入库、Web 调研 |
| **C2** | 需要设计判| 图表绘制、富文档生成、代码审查、PRD 规格 |
| **C3** | 高度发散/原创构建 | 架构设计、知识脑消化 |

   100|
**C 偏差差额模型**
```
delta = C_agent - C_expected  （agent 方案的发散度 任务类型的预期天花板
如果 delta 0：C 在任务类型允许范围内 C 不干L 如果 delta = 1：L_from_C = 1 告知操作者：「方案比常规更发散如果 delta = 2：L_from_C = 2 确认：「方案非常规，确认方向？如果 delta = 3：L_from_C = 3 高风险分析：「方案严重偏离任务预期```

   110|
**最L = max(L_R, L_K, L_from_C)**。操作者可L1/L2/L3 面板中确认或覆盖 agent 的发散方案
---

## 二、L0-L3 行为

| L | 行为 | 触发 | AskQuestion | 确认面板 |
|:---:|:---|:---|:---:|:---|
| **L0** | 直接执行 | L_R=0 L_K=0 L_from_C=None | | |
| **L1** | 告知执行 | max(L_R, L_K, L_from_C)=1 | 是（轻量| 1 按钮：「开始执行|
| **L2** | 确认执行 | max(L_R, L_K, L_from_C)=2 | 是（完整| 2-3 按钮：「推荐方案「备选方案「我有想法|
| **L3** | 高风险分| max(L_R, L_K, L_from_C)=3 | 是（完整| 3-4 按钮：加「暂不执行|

   120|
**强制 L3**：Rev3 / S3 / Auth3 / E3 / 涉及规则文件修改 无条L3
### L1 告知执行（轻量确认）

```
📋 L1 告知执行

我要做的事：。
3 个模块的公共函数签名统一重命预期产出：dimensions.py / engine.py / context_builder.py 同步更新
风险提示：纯本地重命名，git checkout 可恢
   130|
📊 执行风险评估
  影响范围：跨模块影响 个文件）
  可逆性：完全可
[开始执行]
```

### L2 确认执行（方案对比）

```

   140|需要你确认执行方向

📋 推荐方案
  。
3 个模块的公共函数签名统一重命名（evaluate run_p008  涉及：dimensions.py / engine.py / context_builder.py
  优点：一步到位，后续调用方无需适配
  风险：若有未发现的隐式调用，运行时报
🔄 备选方  先加新函数保留旧接口，标DeprecationWarning 天后清理
  优点：零兼容性风   150|  代价：代码冗余持。
3 
📊 执行风险评估
  影响范围：跨模块影响 个文件）
  可逆性：完全可
[确认执行推荐方案]  [改用备选方案]  [我有其他想法，我来说]
```

### L3 高风险分析（决策面板
   160|
```
🚨 高风险任已分析，等你决定

📊 风险总览
  影响范围：跨系统（guard/src/ 全目录重组织  可逆性：不可逆（目录结构变动git log 会断裂）
  外部操作风险：纯本地

📋 方案 A（效率优先）
  直接创建 injection/ feedback/ 子目录，git mv 迁移文件保留历史
   170|  优点：git 历史完整
  代价：操作复杂，需手动处理每个文件

📋 方案 B（安全优先）
  先做镜像副本（git worktree），在副本上试跑 验证后合并回主仓  优点：零风险，随时可以扔掉副  代价：多 15 分钟操作时间

📋 回滚方案（适用于方A  git checkout . 恢复全部变更

   180|
[执行方案A]  [执行方案B]  [我有新思路]  [暂不执行，之后再说]
```

---

## 三、运行时行为（Guard 代码执行
以下全部`engine.py` + `fsm.py` + `dimensions.py` 自动执行，不在本文件中展开
- **5维度评分 L级推*：`dimensions.py` 定义，`engine.py` 聚合 `max(L_R, L_K, L_from_C)`
   190|- **C 偏差差额模型**：`compute_C_deviation_L(C_agent, C_expected)` delta L_from_C
- **FSM 升级/降级**：`fsm.py` 升级（连续≥5成功+无越理覆盖场景）；降级（失中断/越权 即时降级）；验证期（升级后首3次保留L1- **强制 L3 条件**：Rev3/S3/Auth3/E3/规则修改 自动阻断
- **KB 预载入降*：Step 2-KB 激活协A/C 降级 `context_builder.py` 注入
- **决策日志**：`decision_log.py` 写入 `Decision-Log.jsonl`
- **T 维度偏差*：actual/estimated FSM T维度查询
- **多次采样共识**：`ConsensusClassifier` 低共L2 升级
- **仿真数据纳入**：不区分仿真/真实 FSM 统计累积

> 详见：`guard/src/p008/engine.py`（评分引擎）、`guard/src/p008/dimensions.py`（维度定delta函数）、`guard/src/p008/fsm.py`（状态机）、`guard/guard.py --status`（状态检查）