---
description: Notion 模块全局前置条件 — 网络确认
alwaysApply: false
glossary:
  layer: 2-模块框架层
  module: Notion
  depends: []
  guard_coverage: "none — 网络确认是纯 Agent-user 交互流，无 Guard 模块可覆盖"
guard_replaceable: false
---

# Notion 模块前置条件

## 适用范围

所有 Notion API 调用前的统一前置检查。每个对话中第一条 Notion 相关操作前必须完成。

## 阶段 A — 网络确认

### 触发条件
- 每个对话中，准备执行第一条 Notion 相关操作（插件、MCP、脚本、API）之前。

### 强制流程

1. **询问用户**：是否开启网络环境（Y/N）？
2. **仅当用户明确回复 `Y`** 时，才执行 Notion 操作。
3. **若回复 `N`**：本次不执行 Notion 操作，并给出可继续的离线替代方案（如仅准备参数、生成待执行计划）。

### 后续流程

取得 **`Y`** 之后，进入 **阶段 B**（目录落点）与 **阶段 C**（操作分型）：
- 详见 `mod-notion-crud-framework.mdc`
- 新建页面细则：`flow-notion-create.mdc`
- 更新页面细则：`flow-notion-update.mdc`
- 删除/归档细则：`flow-notion-delete.mdc`
- 查询读取细则：`flow-notion-query.mdc`

## 约束

- 不得跳过该确认直接执行写入或读取。
- 若用户在当次消息中已明确授权"直接执行/无需再确认"，可按用户最新明确指令覆盖本规则。

## 一句话合并授权（可选）

仅当用户主动一次说清且无歧义时可跳过逐步追问，且须包含：**`Y`**、目录（编号或 URL/ID）、操作类型（增删改查）、以及对应 AskQuestion 选项组（`[确认写入]` / `[确认更新]` / `[确认删除]`）或同等明确执行意图。

**禁止**把本节当作默认 UX。
