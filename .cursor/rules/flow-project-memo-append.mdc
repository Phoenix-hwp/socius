---
description: 追加项目备忘流程：将信息追加到指定项目的备忘录
glossary:
  layer: 3-工作流执行层
  module: ProjectMemo
  depends: [mod-project-memo-framework]
guard_replaceable: false
---

# 追加项目备忘流程

## 触发

- 显式格式：`记录XX备忘`（如 `记录Demo备忘`
- 隐式格式：`记录备忘`（从上下文推断项目名
- 别名：`记下`

---

## 执行步骤（固定五步）

### 步骤 1：解析项目名

从指令中提取项目名：

| 指令 | 解析结果 |
|:---|:---|
| `记录Demo备忘` | `DemoProject` |
| `记录TestProject备忘` | `TestProject` |
| `记录备忘` | 从上下文推断，或询问用户 |

### 步骤 2：确保文件存

**文件路径**：`<Project_Name>/<Project_Name>_Project_Memo.md`

若文件不存在
- 若目录存在：自动创建标准骨架
- 若目录不存在：提示先执行 `创建XX项目索引`

### 步骤 3：解析备忘内

从用户消息中提取
- **备忘内容**（核心信息，必填
- **关联模块**（如 M03, M07, Notion, Git 等，可选）
- **来源**（默用户确认"

### 步骤 4：追加到记录

将信息追加到 `<Project_Name>_Project_Memo.md` §2「记录备忘（追加区）

格式
| 日期时间 | 备忘内容 | 关联模块 | 来源 |
|---|---|---|---|
| YYYY-MM-DD HH:MM | （用户提供的备忘内容| （模块，默认"-"| （来源，默认"用户确认"|

追加规则
- 按时间倒序（新的在上）
- 不覆盖历史记

### 步骤 5：更新清单状态（如适用

若备忘内容属当前待复核清的口径更新：
- 找到对应编号
- 更新状态字段（如：待确已确认）
- 可同步更当前暂定口径"字段

---

## 联动提示（如适用

若信息同时影响项目主背景，提示用户：

> "该备忘是否同步回`<Project_Name>_ProjectContext.md`

通过 AskQuestion `[同步回写] [仅备忘即可]` 弹出确认

---

## 成功回执格式

```
<Project_Name> 备忘已追加：
- 文件Project_Name>/<Project_Name>_Project_Memo.md
- 位置：记录备忘（追加区）
- 时间：YYYY-MM-DD HH:MM
- 内容摘要：（0..

[若更新状态]
待复核清单状态已更新：MEMO-XXX 已确

[若需联动]
提示：该备忘是否同步回写项目主背景？
```

---

## 参考文

- 统一框架定义：`mod-project-memo-framework.mdc`
- 多轮升级：`flow-multi-round-upgrade.mdc`（项目目录不存在时的兜底入口
