---
description: 长记忆项目每轮开场先确认上一轮检查点与本轮范围
alwaysApply: true
---

# 每轮开场检查点确认规则（轻确认版）

适用场景：长记忆、多轮推进、特殊项目（PRD 审阅、方案设计、长期复盘等）。

## 默认流程（下轮起点优先）

当用户要求“继续某项目/某备份/某长记忆主题”时，必须先执行：

1. 先读取主控文件或最近一条备份，提取：
   - 上一轮已处理到的位置
   - 上一轮未完成项
   - 下轮起点（若存在）
2. 若下轮起点清晰且无冲突，发起一句轻确认：
   - `按上轮起点继续 <章/阶段/环节>，是否直接开始？`
3. 用户确认后直接执行，不再重复完整询问。

## 完整询问触发条件（仅在歧义时）

仅在以下任一情况出现时，才使用完整询问：

- 同日期/时段命中多条备份且无法自动判定主题
- 主控中的下轮起点缺失、冲突或已过时
- 用户本轮目标与上轮起点明显不一致
- 章节/阶段/环节表述无法对齐

完整询问内容：
- 上一轮到第几章/第几阶段/第几环节？
- 本轮要继续哪一章/哪一阶段/哪一环节？

## 范围锁定（防止一轮扩散）

- 本轮只能处理用户确认的唯一范围。
- 范围外内容仅登记到“待处理池/下轮清单”，不得展开改写。

## 类型化字段（章节 / 阶段 / 环节）

- 主控文件建议包含 `推进类型` 字段，值允许：
  - `章节`
  - `阶段`
  - `环节`
- 若主控文件未声明类型，默认按 `章节` 询问；用户可当轮改为阶段或环节。

## 开场回执格式（固定三段）

- `已读取检查点`：回显上一轮位置与未完成项（1-3 条）
- `待你确认`：优先回显“下轮起点轻确认”；若触发歧义则改为完整询问
- `确认后执行`：说明将按确认范围继续并锁定边界
