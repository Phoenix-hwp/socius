---
description: Notion 增删改查统一流程 — 网络 → 目录 → 按操作分型（定位/预览/口令确认）
alwaysApply: true
---

# Notion 统一 CRUD 工作流（对话内）

## 适用范围

凡通过 **Notion 插件（MCP）、`run_notion_workflow.py`、本地向导脚本** 等对 Notion 执行 **增 / 删 / 改 / 查**（调用官方 API 且依赖网络的读写）均适用。

**执行通道（默认）**：在已通过 **预览** 与口令许可的前提下，**优先**将参数映射为 **Notion MCP** 工具调用（**每次调用前读取对应工具 schema**）；**仅当**插件不可用、鉴权失败、用户明确要求脚本化、或命中 **`10-Topics/Notion-统一入口规范.md`** §4.1 阈值（批量/dry-run/审计等）时，改用 **`run_notion_workflow.py`** 等本地脚本，并在回执中简短注明 **兜底原因**。

**与 Git 区分**：仓库同步用 **`提交git`**；Notion 交互默认在对话内逐步完成，不要求为本任务单独打开终端（可选脚本入口仍保留）。

---

## 抽象结构（强制）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。高风险步骤不得跳过确认。

### 阶段 A — 网络（首次 Notion 操作）

- 触发条件与口令：见 **`notion-first-action-network-confirmation.mdc`**（本条对话**第一条** Notion 相关 API 前须取得 **`Y`**）。
- 取得 **`Y`** 之后，同一对话内的后续 Notion 操作**不必**重复询问网络，除非用户明确表示网络状态已变化。

### 阶段 B — 目录（父级落点）

- **`Read`** `.cursor/mcp/notion_cascader_directory_choices.json`，展示 **编号 + path + notionObjectType**；用户 **只回复一个数字**。
- Agent 内部将编号映射为该项的 **`url` 或 `value`** 作为 **parent**（页面或数据库）；**不得**要求用户抄写 `notion.dir.*`（用户主动粘贴完整 URL/ID 除外）。
- 目录数据缺失或过期：提示执行 **`refresh_notion_directory_choices.cmd`**（刷新动作仍遵守首条网络确认）。详见 **`notion-directory-option-ids.mdc`**。
- **完整清单与复用**：**首次**需要用户选父级、或用户 **要求换目录** 时，须 **一次性** 给出 **完整** 1～n 表，**禁止**仅以「见 JSON」、半截表或分条补行代替。**同一对话内** `parent` 已选定且用户 **未**要求换目录时，后续操作 **不必**重复整张目录表（细则见 **`notion-directory-option-ids.mdc`** Agent 行为第 4、5 条）。

**例外**：用户明确为 **全局搜索**、**仅按已知页面 URL/ID 操作**、且与「选定父级下的子内容」无关时，可 **跳过** 阶段 B，但阶段 A 仍适用。

### 阶段 C — 按操作分型

| 操作 | 核心步骤（在阶段 B 之后） | 执行前必须 |
|:---:|:---|:---|
| **增（创建）** | 标题策略 **1** 手动 / **2** 自动 → 标题落地 → **正文完整预览** → 口令 **`确认写入`** | 详见 **`notion-write-workflow-confirmation.mdc`**（创建分支权威细则） |
| **查（读取）** | 用户提供 **定位标题**（或关键词）→ **解析唯一对象**（见下节「定位」）→ 展示摘要/正文要点 | 若命中多条，**禁止**在未选择前读取 |
| **改（更新）** | 定位 → 正文来源 → **更新策略 `1`/`2`**（见下节）→ **变更预览** → 口令 **`确认更新`** | 未确认不得调用更新 API；**优先 MCP**，脚本兜底须说明原因 |
| **删（归档）** | 定位 → **二次确认**（须明示目标标题 + URL 或 ID）→ 口令 **`确认删除`** | 默认使用 **归档**（`archived: true`），与「清空块」区分 |

---

## 定位子流程（删 / 改 / 查）

1. 请用户提供用于检索的 **标题或关键词**（可与页面标题 **部分匹配**，大小写不敏感）。
2. 在阶段 B 选定的 **parent** 下解析：
   - **parent 为 database**：用数据库 **title 类型属性** 做 `contains` 查询。
   - **parent 为 page**：枚举该页 **直接子页面**（`child_page`），用标题 **包含** 匹配。
3. **零条**：提示收窄关键词或改用 URL/ID；不得臆造 ID。
4. **一条**：进入该操作的后继步骤。
5. **多条**：展示 **编号清单**（标题 + 可选 URL/ID 片段），请用户 **只回复编号** 选定唯一目标。

**说明**：复杂嵌套（非直接子级、多库联动）可能无法仅靠「父级 + 标题」唯一定位；应请用户给出 **Notion URL** 或 **32 位 ID**。

---

## 改（更新）策略分支（强制）

在 **目标页面已唯一** 且 **正文来源已明确** 之后、展示最终预览之前，须请用户 **只选一**（一轮一问）：

| 选项 | 名称 | Agent 行为要点 |
|:---:|:---|:---|
| **`1`** | **清空后重写** | 等价于「先清空（归档）该页 **顶层正文块**，再写入预览中的正文」。**须显著提示风险**：Notion 上原有、但未包含在本次正文中的段落将 **不再保留**。实现上 **优先 MCP**（`fetch` → 按工具能力移除或归档子块 → 写入）；也可用脚本 `update_page` + `replace: true`。 |
| **`2`** | **局部合并更新** | **`fetch`/读取** Notion 当前正文（或块结构摘要），与本地既定正文 **对照 diff**，按 **`10-Topics/Behavior-Preferences-Sync-Playbook.md`**（错别字表 **对比合并**、章节口径等）及用户约定的 **对齐锚点** 生成 **合并稿**，再 **仅对应对块/段落** 追加或替换。**无稳定锚点**（如 `##` 章节标题、表格主键列、固定 Callout 区块）时：不得强行「局部」——须改为 **`1`**，或请用户指定锚点/分块范围。 |

**预览**：策略 **`1`** 展示将写入的全文（或等价结构）；策略 **`2`** 展示 **diff/合并说明 + 合并后关键片段**（足以判断未误删 Notion 独有条款）。用户口令仍为 **`确认更新`**。

**插件参数**：禁止臆测字段名；数据库写入须 **先 fetch schema**；块级更新须基于 **`fetch` 返回的块类型与 ID**（若工具支持）逐步提交。

---

## 固定口令（默认）

| 操作 | 默认口令 | 说明 |
|:---:|:---:|:---|
| 创建 | **`确认写入`** | 与既有写入规则一致 |
| 更新 | **`确认更新`** | 用户改用明确同意句时，Agent 须确认其指向当前预览 |
| 删除/归档 | **`确认删除`** | 须在二次确认后接收 |

---

## 对话内交互形态（强制）

1. **禁止**把多步叠成一段「清单 + 拼接示例」当作默认引导。
2. **目录与定位**：用户 **只回复编号**；Agent **内部**映射，不得要求用户抄写长串 ID。
3. **可选本机菜单**：`.cursor/mcp/notion_write_menu.cmd`（CRUD 向导）或 `.cursor/tools/notion_gui_menu.ps1`（面板兜底），**不替代**对话内逐步确认。

---

## 与既有规则的关系

- **网络**：**`notion-first-action-network-confirmation.mdc`**
- **目录选项**：**`notion-directory-option-ids.mdc`**
- **创建（增）专用六步表**：**`notion-write-workflow-confirmation.mdc`**
- **写入类**在取得 **`Y`** 后，创建路径仍须完整执行「标题 + 预览 + **`确认写入`**」。

---

## Agent 自检（任意 Notion API 前）

- [ ] 若为对话内 **首条** Notion 操作，是否已取得 **`Y`**？
- [ ] 若适用阶段 B，是否已 **目录编号** 落点？
- [ ] **删 / 改 / 查** 是否已完成 **定位**且目标 **唯一**？
- [ ] **改** 是否已选定 **`1` 清空后重写** 或 **`2` 局部合并**，且策略 **`1`** 已向用户提示 **正文丢失风险**（若适用）？
- [ ] **改** 是否 **优先 MCP**；若用脚本，是否在回执中注明 **兜底原因**？
- [ ] **增 / 改** 是否已 **预览** 且收到对应 **`确认写入`** / **`确认更新`**？
- [ ] **删** 是否已完成 **二次确认** 且收到 **`确认删除`**？

---

## 一句话合并授权（可选 · 非默认）

仅当用户 **主动**一次说清且无歧义时可跳过逐步追问，且须包含：**`Y`**、**目录（编号或 URL/ID）**、**操作类型（增删改查）**、**定位信息或标题策略与正文来源**、（若为 **改**）**更新策略 `1`/`2`**、以及对应 **`确认写入`** / **`确认更新`** / **`确认删除`** 或同等明确执行意图。

**禁止**把本节当作默认 UX。
