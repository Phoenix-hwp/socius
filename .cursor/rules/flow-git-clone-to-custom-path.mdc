---
description: Git 拉取到指定路径工作流 - 新设备首次初始化或同步
glossary:
  layer: 3-流程执行层
  purpose: 在新设备上将 Git 仓库拉取到用户指定的路径
  trigger: git到新设备、克隆到新路径、同步到新位置
alwaysApply: false
---

# Git 拉取到指定路径工作流

## 触发条件

用户输入以下任一指令时触发：
- `git到新设备`
- `克隆到新路径`
- `同步到新位置`
- `拉取git到新目录`

---

## 执行流程

### 阶段 A：确认与路径收集

1. **确认拉取意图**
   - 向用户说明："这将从远程仓库拉取最新内容到您指定的目录"
   - 询问："是否继续？（确认/取消）"
   - 若用户取消，结束流程

2. **收集目标路径**
   - 提示："请输入目标根目录完整路径（例如：`D:\Work\Cursor_Knowledge` 或 `C:\Users\xxx\Documents\Cursor_Knowledge`）"
   - 验证路径格式：
     - Windows 路径：支持 `C:\...` 或 `\\server\share\...`
     - 相对路径：相对于当前工作区根目录
   - 检查路径是否存在：
     - 存在 → 询问是否覆盖/合并/取消
     - 不存在 → 确认创建

3. **确认远程仓库**
   - 显示当前配置的远程仓库 URL
   - 若未配置远程，提示用户输入仓库地址

---

### 阶段 B：执行拉取

#### 场景 1：目标路径为空（全新克隆）

```bash
git clone <远程URL> <用户指定路径>
```

成功后：
- 提示："✅ 已成功克隆到 `<路径>`"
- 建议："请用 Cursor 打开新路径继续工作"

#### 场景 2：目标路径已存在 Git 仓库

```bash
cd <用户指定路径>
git status
git pull origin <分支名>
```

处理可能的冲突：
- 无冲突 → 提示成功
- 有冲突 → 进入冲突解决流程（见下方）

#### 场景 3：目标路径已存在非 Git 目录

询问用户：
1. **备份并覆盖** - 重命名现有目录，然后克隆
2. **合并内容** - 初始化 git，添加远程，拉取并合并
3. **取消操作**

---

### 阶段 C：后续配置（可选）

1. **询问是否切换工作区**
   - "是否要在 Cursor 中打开新路径？"
   - 若确认，提供指导或自动打开（如果支持）

2. **记录路径偏好**（可选）
   - 询问是否记录此路径为未来默认路径
   - 保存到配置文件中

---

## 冲突解决子流程

当 `git pull` 出现冲突时：

1. **显示冲突文件列表**
2. **询问策略**：
   - 选项 1：保留本地版本（`git checkout --ours`）
   - 选项 2：接受远程版本（`git checkout --theirs`）
   - 选项 3：手动解决（显示冲突文件路径）
   - 选项 4：中止合并（`git merge --abort`）

3. **解决后**：
   ```bash
   git add .
   git commit -m "合并远程更新"
   ```

---

## 边界情况处理

| 场景 | 处理策略 |
|:---|:---|
| 无网络连接 | 提示检查网络，提供离线使用方案 |
| 远程仓库不存在 | 提示检查仓库地址，提供手动输入 |
| 目标磁盘空间不足 | 检查可用空间，提示清理或更换位置 |
| 权限不足 | 提示以管理员身份运行或更换路径 |
| 路径包含特殊字符 | 自动转义或提示更换路径 |

---

## 失败兜底

若任一步骤失败：
1. 显示具体错误信息
2. 提供替代方案（如手动命令）
3. 记录失败日志到 `Daily-Backups/git-clone-errors.log`

---

## 快捷命令参考

用户可随时使用：
- `git remote -v` - 查看当前远程配置
- `git status` - 查看本地状态
- `git log --oneline -5` - 查看最近提交
