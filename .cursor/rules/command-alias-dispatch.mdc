---
description: 短指令别名分发，避免已约定指令漏执行
alwaysApply: true
---

# 指令别名分发规则

当用户输入短指令（尤其是 2-20 字的中文祈使句）时，先按下列步骤执行，再进入常规回答：

1. 先在 `10-Topics/Cursor-command-aliases.md` 中查找**完全匹配**与**高相似匹配**（同义词、近义说法）。
2. 若命中映射，必须先执行映射动作（通常是先 Read 指定文件并基于该文件继续处理），不得跳过到无关检索。
3. 若未命中映射，才执行常规搜索；并可建议用户把该短指令加入别名清单。

## 创建指令默认行为（强制）

- 当用户要求“创建指令”时，默认理解为：**创建一个执行级规则**（`.cursor/rules/*.mdc`），而不只是写在普通笔记里。
- 创建时必须同步登记到 `10-Topics/Cursor-command-aliases.md`，并给该指令配置 1 个或多个别名。

## 别名长度约束（强制）

- 每条指令的别名长度必须为 **2-10 个字**（含 2 与 10）。
- 执行触发时，**完整指令**与**任一合规别名**均视为同一映射并执行同一动作。
- 若用户给出的别名不满足 2-10 个字，应先提示并提供合规替代，再登记。

## 当前关键别名（硬性优先）

- `help` / `查看帮助`：
  - 先 Read `Command-Help-Index.md`
  - 返回“指令 + 别名 + 用途”清单
- `项目备忘录`：
  - 先 Read `469_Sports/469_Sports_Project_Memo.md`
  - 输出“当前待复核清单”与最近一条备忘记录
- `记录备忘`：
  - 将用户本条消息中的备忘信息追加写入 `469_Sports/469_Sports_Project_Memo.md`
  - 若命中待复核事项，更新对应事项状态
- `deepseek-pro4配置` / `代理配置备忘` / `换机代理步骤` / `pro4配置`：
  - 遵循 `.cursor/rules/deepseek-pro4-proxy-setup.mdc`：先 Read `10-Topics/TMP_DeepSeek-Cursor-Proxy-运行步骤.md`，再基于文档作答
- `改动预告` / `预期先行`（登记见 `Cursor-command-aliases.md`「脚本改动先说明」）：
  - Read `.cursor/rules/pre-edit-script-change-brief.mdc`，在本轮修改脚本/自动化入口时严格执行「落盘前先简述预期」
- `提交git` / `Git同步` / `推仓库` / `提交远端`：
  - Read `10-Topics/Gitee-Workspace-Git-Workflow.md`，遵循 `.cursor/rules/git-workspace-commit.mdc`，在工作区根协助 Git 提交与推送

## 失败兜底

若用户指令映射到的关联文件不存在，不得直接失败；应先：
1) 检查该指令是否已有新版映射；
2) 若无新版映射，提示并协助修复映射到现存文件；
3) 再执行后续动作。

## 删除保护（全局）

- 当用户提出“删除临时文件/草稿文件”但未逐个点名时，默认保留备忘录类文件（文件名含 `Memo` 或 `备忘录`）。
- 若用户明确点名删除某个备忘录文件，才执行删除。
