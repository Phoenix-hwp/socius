---
description: 变更前后双保险 — 落盘前搜全库引用防漏改，落盘后差异审查防非预期副作用
alwaysApply: true
Lifecycle: 长期
Created: 2026-05-12
glossary:
  layer: 横切
  purpose: 变更前后双保险 — 落盘前搜全库引用防漏改，落盘后差异审查防非预期副作用
  sequence: 1  # 三规则执行顺序：①影响枚举 → ②脚本预告 → ③编码自检
  downstream:
    - pre-edit-script-change-brief.mdc（脚本改前预告，sequence: 2）
    - script-coding-constraints.mdc（编码自检清单，sequence: 3）
    - .cursor/change-impact-checklist.json（影响面注册表，本规则的数据源）
---

# 变更前影响枚举

## 适用范围

凡对以下对象做**实质性变更**（重命名、删除、移动、字段名修改、路径常量变更、数据结构键名变更），须在**第一笔落盘**（`Write` / `StrReplace` / `Delete`）**之前**强制执行本规则。

**触发条件**（满足任一条即触发）：
- 重命名/删除/移动文件或目录
- 修改 JSON/JSONL 字段名
- 修改路径常量或引用（如 `card_path`、`library_index.json` 中路径字段）
- 修改 API/工具接口名、命令行参数名
- 修改数据结构的键名

**不触发**（以下变更可跳过影响枚举，直接进入常规自检）：
- 仅修改注释、文案
- 仅修改 `.md` 文档正文内容（不涉及路径/字段名/文件名）
- 仅新增文件（新建不算变更已有结构）
- 用户在本轮已明确说「跳过影响枚举」或等价意思

## 强制步骤

### 步骤 1：提取变更关键词

从本轮变更目标中提取 1-3 个核心关键词（即将被修改/删除/重命名的标识符）。

### 步骤 2：读取影响面注册表

Read `.cursor/change-impact-checklist.json`，获取「应搜索的文件类型列表」（`search_scopes` 数组）。

### 步骤 3：逐组搜索、列出待同步清单

对注册表中每个 group，用 **Grep**（内置工具）执行关键词搜索，输出格式：

```
## 变更影响枚举：<变更关键词>

| group | 文件数 | 需同步 | 无需改动 |
|:---|:---|:---|:---|
| Agent 规则 | 3 | 2 | 1 |
| ... | ... | ... | ... |

### 待同步清单
- [ ] .cursor/rules/xxx.mdc — 第 Y 行引用旧路径 "abc/old_path"
- [ ] 90-Templates/QnA_xxx.md — 第 Z 行引用旧字段 "card_path"
```

### 步骤 4：全打勾后落盘

- 待同步清单中所有 `[ ]` 变为 `[x]`（即修改完所有引用）后，才可执行本次变更的落盘。
- 若某个引用是**有意保留**（如变更日志、迭代记录中的历史记载），在清单中标注 `有意保留，不回改` 后跳过。
- 若搜索结果为 0（无引用），回执 `✅ 全库无引用，可安全变更`。

## 与已有规则的关系

| 规则 | 触发时机 | 分工 |
|:---|:---|:---|
| `pre-edit-script-change-brief.mdc` | 改脚本前 | 预告改后行为与验收 |
| `script-coding-constraints.mdc` | 落盘前 | 编码规范自检 |
| **本规则** | **变更前** | **搜全库引用，列出影响面** |

三者互不替代。脚本改动时三条都过。

## 例外与降级

- **Grep 不可用**：降级为 Glob 列出所有目标类型文件 → 逐个 Read 搜索关键词（虽然效率低，但不可跳过）
- **关键词模糊**（如只改了一个函数名但不确定影响面）：默认搜索该函数名在所有 `.py/.mjs/.mdc` 中的出现
- **紧急热修复**：用户明确说「跳过影响枚举、直接改」时，本轮跳过，但 Agent 仍须在回复中注记 `⚠ 已跳过影响枚举，可能遗漏引用同步`。

## §5 执行后差异审查（防非预期副作用）

> **定位**：本章是「变更前影响枚举」的对称补充——§1–§4 管的是**落盘前**防止引用遗漏，§5 管的是**落盘后**确认没有非预期副作用（文件被意外修改/污染）。

### 触发条件（满足任一条即触发）

- 本轮对话中对 ≥3 个文件执行了写入（`Write` / `StrReplace`）
- 执行了外部技能（Skills_Library 中的 skill）
- Earth Library 批量入库（≥3 条）
- 对 JSON/JSONL 数据文件的结构化写入
- 用户明确要求「检查本轮变更」

### 不触发

- 仅新增独立文件（不修改已有文件）
- 仅读取操作
- 仅修改注释/文案且涉及 ≤2 个文件

### 执行步骤

#### 步骤 5a：获取基线

在执行变更**之前**，记录当前 HEAD commit hash：

```
git rev-parse HEAD
```

#### 步骤 5b：执行差异审查

本轮所有落盘完成后，执行：

```
git diff --stat          # 文件级变更概览
git diff -- <关键路径>   # 对内核路径（.cursor/rules/, 10-Topics/）做详细 diff
```

#### 步骤 5c：分类展示

将变更分为三类：

| 分类 | 说明 | 动作 |
|:---|:---|:---|
| **预期内** | 本轮明确要改的文件 | 标注 ✅ 并简述变更内容 |
| **连锁影响** | 因引用同步而连带修改的文件 | 标注 🔗 并注明触发来源 |
| **非预期** | 不在本轮计划中的文件变更 | 标注 ⚠ 并提示审查 |

#### 步骤 5d：确认/回滚

- **无非预期变更** → 简短回执「✅ 变更审查通过，N 个文件均为预期/连锁变更」，不阻断
- **有非预期变更** → 列出 ⚠ 文件清单，AskQuestion `[确认保留全部变更] [回滚非预期文件] [回滚全部]`
  - `[确认保留全部]`：用户知情后放行
  - `[回滚非预期文件]`：`git checkout -- <非预期文件路径>`
  - `[回滚全部]`：`git reset --hard <基线commit>`（仅限本轮变更未提交前）

> **各模块具体流程**（Skills 执行 / Earth Library 批量入库 / 多轮项目改写）可在其第三层工作流中实现**更高防控等级**的差异审查（如 `git stash` 快照 + 写路径限制 + 逐文件 diff），本章仅定义**通用最低标准**。

## 与已有规则的关系

| 规则 | 触发时机 | 分工 |
|:---|:---|:---|
| `pre-edit-script-change-brief.mdc` | 改脚本前 | 预告改后行为与验收 |
| `script-coding-constraints.mdc` | 落盘前 | 编码规范自检 |
| **本规则 §1–§4** | **变更前** | **搜全库引用，列出影响面** |
| **本规则 §5** | **变更后** | **差异审查，防非预期副作用** |

四者互不替代。脚本改动时四条都过。

## 例外与降级

- **Grep 不可用**：降级为 Glob 列出所有目标类型文件 → 逐个 Read 搜索关键词（虽然效率低，但不可跳过）
- **关键词模糊**（如只改了一个函数名但不确定影响面）：默认搜索该函数名在所有 `.py/.mjs/.mdc` 中的出现
- **紧急热修复**：用户明确说「跳过影响枚举、直接改」时，本轮跳过，但 Agent 仍须在回复中注记 `⚠ 已跳过影响枚举，可能遗漏引用同步`。
- **跳过差异审查**：用户明确说「跳过审查」时，§5 本轮跳过，但 Agent 须注记 `⚠ 已跳过执行后审查`。

## 失败兜底

若本规则执行后仍有引用丢失（即第三层拉取后检查发现残留引用），则本轮影响枚举视为**不完整**，Agent 须在拉取后的回执中标注，并提示用户完善第二层影响面注册表（补充遗漏的 group 或 glob 模式）。
