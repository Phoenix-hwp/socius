---
description: Git CRUD 统一框架 — 定义阶段结构，不含具体步骤
glossary:
  layer: 2-模块执行层
  module: Git
---

# Git CRUD 统一框架

## 适用范围

凡对工作区执行 **提交、推送、拉取、分支管理** 等 Git 操作均适用。

**执行通道**：默认使用 **Shell** 工具直接执行 git 命令；高风险操作（`--force`, `reset --hard` 等）须先确认。

**与 Notion 区分**：Notion 写入用 `Notion操作流程`；Git 交互在工作区根执行，不要求为本任务单独打开终端。

---

## 抽象阶段结构（强制）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。高风险步骤不得跳过确认。

| 阶段 | 名称 | 核心职责 | 下层工作流引用 |
|:---:|:---|:---|:---|
| **A** | 环境确认 | 确认工作区根、Git 可用、Remote 配置 | 自动检测 |
| **B** | 状态检查 | `git status` 查看变更 | 见下方「阶段B细则」 |
| **C** | 操作分型 | 按「工作区同步 / 克隆到新路径」分流到第三层工作流 | 见下方「阶段C细则」 |

---

## 阶段 B — 状态检查

### 执行动作

1. 将 **当前工作目录** 落在 **工作区根**（与 `CURSOR_PROJECT_DIR` / 打开的 Cursor 仓库根一致）
2. 执行 `git status -sb` 获取变更摘要
3. 回执格式：
   - `已变更文件`：新增/修改/删除的文件分类统计
   - `待提交内容`：建议的提交范围
   - `下一步选项`：add / commit / push / pull 等

### 失败兜底

- 若 `.git` 目录不存在：提示"当前目录非 Git 仓库"
- 若 `remote` 未配置：提示用户参考 `10-Topics/Gitee-Workspace-Git-Workflow.md` §首次绑定
- 若认证失败：给出命令模板（不替用户保管 Token/密码）

---

## 阶段 C — 按操作分型

阶段 B 完成后，根据操作类型进入对应第三层工作流（**高内聚**：不拆多个同名 flow，变体在同一工作流内按用户意图子集执行）。

| 操作分型 | 典型意图 | 第三层工作流 | 说明 |
|:---:|:---|:---|:---|
| **工作区同步到远端** | `提交git` / 含 add+commit+pull+push 的同步；**仅拉取**、**仅推送**（无新 commit）等变体 | `flow-git-commit.mdc` | 步骤 5 已含 `pull --rebase` 与 `push`；无新提交时跳过 commit 相关步骤即可 |
| **克隆到新设备路径** | `git到新设备` 等 | `flow-git-clone-to-custom-path.mdc` | 与日常「当前仓库内提交」分流，避免混在一个 flow |

其他 Git 操作（分支、cherry-pick、`reset` 等）：先在本层明确风险与范围，再分步 Shell 执行或按需新增第三层 flow（**避免**为 push/pull 单独复制一套文档）。

---

## 高风险操作清单

以下操作**必须**先向用户确认：

| 操作 | 风险说明 |
|:---|:---|
| `git push --force` / `--force-with-lease` | 覆盖远端历史，可能导致协作丢失 |
| `git reset --hard` | 丢弃本地变更，不可恢复 |
| `git clean -fd` | 删除未跟踪文件，不可恢复 |
| 批量变更 `.gitignore` 后提交 | 可能误提交密钥路径 |
| 跳过 hook (`--no-verify`) | 绕过代码检查 |

---

## 输出约定

- **简报**：`status` 要点、拟提交文件类别、建议的 `commit message`
- **推送成功**：注明远端分支与提交摘要
- **失败回执**：明确失败原因 + 可执行的修复命令

---

## 三层架构速查

| 层级 | 文件 | 职责 |
|:---:|:---|:---|
| 第一层（网关） | `gateway-command-router.mdc` | 指令路由到本框架 |
| 第二层（框架） | `mod-git-crud-framework.mdc` | 本文件，定义阶段结构 |
| 第三层（工作流） | `flow-git-commit.mdc`、`flow-git-clone-to-custom-path.mdc` | 当前仓库同步；克隆到新路径 |

---

## 规则索引

- **Git 模块**：
  - 第二层：`mod-git-crud-framework.mdc`（本文件，统一框架）
  - 第三层：`flow-git-commit.mdc`（工作区同步：提交 / 拉取 / 推送变体统一入口）
  - 第三层：`flow-git-clone-to-custom-path.mdc`（新路径克隆）
