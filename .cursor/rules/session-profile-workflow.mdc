---
description: 会话开端读取档案；收束时分析契合度并写回同一份笔记
alwaysApply: true
---

# 个人档案会话工作流

- **档案路径**：优先 `10-Topics/Cursor-usage-profile-and-templates.md`；可用 `CURSOR_OBSIDIAN_PROFILE` 覆盖。
- **开端**：优先依赖 `.cursor/hooks.json` 的 `sessionStart` 注入。若上下文中未见到注入内容，则 Read 上述路径后再作答。
- **开端（续接）**：同时 Read `10-Topics/Active-Task-Tracker.md`。按活跃任务策略续接：匹配关键词自动续接、展示列表等轻确认、直接新需求不展示列表。细则见 `flow-behavior-auto-receipt.mdc`。
- **收束（自动）**：每轮对话结尾，Agent 按 `flow-behavior-auto-receipt.mdc` 执行轮级快照写入与纠偏检测。
- **收束（手动）**：触发语 `/收束`、`结束会话`、`结束对话`、`结束` → 按 `flow-behavior-auto-receipt.mdc` 步骤 E 执行完整收束。
- **禁止**：收束中删除 `Behavior-Fit-Log.md` 历史行；大段重写档案需用户明确要求。
