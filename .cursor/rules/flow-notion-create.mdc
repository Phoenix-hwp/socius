---
description: Notion「增」操作 — 创建页面/数据库行的具体步骤（6步标准流程）
Lifecycle: 长期
Created: 2026-05-12
alwaysApply: false
glossary:
  layer: 3-工作流执行层
  module: Notion
  operation: create
  depends: [mod-notion-precondition, mod-notion-crud-framework]
guard_replaceable: false
---

# Notion 创建页面工作流

## 适用范围

凡会向 Notion **写入或新建** 的操作均适用：插件（MCP）、`run_notion_workflow.py` / `do_create_page`、其他脚本、REST **创建页面或数据库行**。

**统一骨架**：网络（阶段A）→ 目录（阶段B）→ **本工作流（阶段C-增）**。全流程见 `mod-notion-crud-framework.mdc`。

只读（查询、fetch、不涉及选定目录写库的 dry-run）仍须遵守阶段A的网络确认；本工作流侧重 **新建落库前的交互与确认**。

---

## 前置要求

- 已完成 **阶段 A**（网络确认 **`Y`**）— 见 `mod-notion-precondition.mdc`
- 已完成 **阶段 B**（目录落点 parent 已选定）— 见 `flow-notion-select-parent.mdc`

---

## 步骤（固定六步）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。在未收到 **第 6 步** 的执行许可前，**不得**调用写入 API。

| 步骤 | 名称 | Agent 行为 | 用户典型回复 |
|:---:|:---|:---|:---|
| **1** | **标题策略** | 仅询问新建标题如何处理 | AskQuestion `[手动输入] [自动生成]` |
| **2** | **标题落地** | 若手动：请用户 **只发一行标题**。<br>若自动：先收集正文来源，由 Agent **拟定标题并展示**，用户确认或改为手动 | 一行标题；或对拟题「确认 / 改成…」 |
| **3** | **正文收集** | 收集正文来源（Markdown 路径、用户口述、检索摘要等） | （素材来源确认）|
| **4** | **正文预览** | 展示将写入的正文 **完整预览**（表格/列表可保留 Markdown 形态）；说明数据来源。**在收到确认前禁止写入** | 修改意见；或 AskQuestion `[确认写入] [修改预览]` |
| **5** | **执行与回执** | **自动通道选择**：<br>1. 判定内容长度（字符数 > 长文阈值 `chars` 或预估 blocks > 长文阈值 `blocks`）→ **长文**<br>2. **长文**：调用 upsert 脚本（默认 `notion_upsert_workflow.py`，启用查重 + 断点续传保护）<br>3. **短文**：优先 MCP `notion-create-pages`，失败时脚本兜底<br>4. 写入成功后回复 **页面链接**、块数、是否启用续传保护等 | （步骤4已授权）|
| **6** | **清理** | 删除本地临时 `.md`（若有）；长文模式完成续传后状态文件自动清理 | - |

**固定口令**：步骤4使用 **`AskQuestion`** 工具弹出 `[确认写入] [修改预览]` 选项（替代旧版文本口令「确认写入」），用户点击 `[确认写入]` 后方可进入步骤5。`[修改预览]` 表示用户有修改意见，停留在步骤4。

---

## 对话内交互形态（强制）

1. **禁止**把多步叠成一段「清单 + `Y + … + notion.dir…` 拼接示例」当作默认引导
2. **目录**：用户 **只回复编号**；Agent **内部**映射 `url`/`value`，**不得**要求用户抄写 `notion.dir.*` 长串（用户主动粘贴除外）
3. **可选本机菜单**：与对话步骤语义对齐（**非默认**，不替代对话内六步）：
   - CMD 逐步向导：`.cursor/mcp/notion_write_menu.cmd` 或 `python notion_write_menu.py`
   - GUI 面板（脚本兜底）：`.cursor/tools/notion_gui_menu.ps1`

---

## 目录与标题（摘要）

- 目录：数据文件 `notion_cascader_directory_choices.json`；缺失或过期时提示刷新 `refresh_notion_directory_choices.cmd`（刷新动作本身遵守网络确认）。详见 `flow-notion-select-parent.mdc`。
- 标题：须在步骤1选定 **1 手动** 或 **2 自动**；自动须在落库前展示拟题。
- 禁止：未确认目录即写入；未确认标题策略即自拟标题落库；未收到 AskQuestion `[确认写入]`（或等价且无歧义同意）即调用 API。

---

## 异常处理

| 异常场景 | 处理方式 |
|:---|:---|
| 用户未发确认口令 | 停留在预览阶段，询问修改意见 |
| API 写入失败（短文/MCP）| 保留预览，提示重试或转为本地保存 |
| 长文写入中断 | 自动保存状态到 `Daily-Backups/.notion_state/`，提示用户重跑即可续传 |
| 重复创建（同名已存在）| upsert 脚本自动识别 → 返回 `skipped` 或按 `on_conflict` 策略处理 |
| 标题策略选2但无法自动生成 | 回退到策略1，请用户提供标题 |

---

## Agent 自检（写入前）

- [ ] 本条对话首次写入是否已取得 **`Y`**（`mod-notion-precondition.mdc`）？
- [ ] 是否已完成 **阶段B 目录（编号）**（`mod-notion-crud-framework.mdc`）？
- [ ] 是否已完成 **步骤1～2 标题策略与标题落地**？
- [ ] 是否已展示 **步骤4 正文预览**并通过 AskQuestion `[确认写入]`（或无二义的同意）？
- [ ] 是否避免默认父级、避免跳过预览？

---

## 用户一句话合并授权（可选 · 非默认）

仅当用户 **主动**一次说清且无歧义时可跳过逐步追问，且须包含：**`Y`**、**目录（编号或 URL/ID）**、**标题策略与标题或自动生成说明**、**正文要点或路径**、以及明确执行意图。

**禁止**把本节当作默认 UX；**禁止**用代码式拼接模板教学。
