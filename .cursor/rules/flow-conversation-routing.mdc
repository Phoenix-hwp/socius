---
description: 对话归属判断操作细则 — 上下文推优先，无法判定时询问用户
glossary:
  layer: 3-工作流执行层
  module: MultiRound
  depends: [mod-multi-round-framework]
---

# 对话归属判断流程

## 触发条件

每当用户执行「备份对话」「继续对话」「继续XX项目」或会话即将结束时，Agent 须先完成归属判断。

---

## 执行步骤（三步）

### 步骤 1：上下文推（优先）

从当前会话上下文中提取项目线索：

| 线索类型 | 示例 | 权重 |
|:---|:---|:---|
| 项目目录路径 | `DemoProject/ProjectContext.md` | 高 |
| 项目主控文件 | `DemoProject/DemoProject_Master_Control.md` | 高 |
| 项目备忘录 | `DemoProject/DemoProject_Project_Memo.md` | 中 |
| 口头项目名 | 「Demo」「Earth Library」 | 中 |
| 主题词 | 「体育App」「店主端」 | 低 |

**判定规则**：
1. 若命中项目目录路径或主控文件 → **直接归属**，不询问
2. 若仅命中口头项目名 → 归属该项目，但回显一句「归属 <项目名>」
3. 未命中任何线索 → 进入步骤 2

### 步骤 2：用户问（兜底）

```
本轮对话涉及以下主题：<1-3个关键词>
是否需要归属到某个已知项目？或按日常对话处理？

已知项目（示例）：

输入编号或「日常」：
```

用户回复后映射：
- 数字 → 对应项目目录
- 「日常」/「无」/「不归属」→ 无归属

### 步骤 3：归属落定

| 用户回复 | 结果 | 后续流程 |
|:---|:---|:---|
| 有项目目录、无 `Master_Control` | 项目归属，但未升级 | 问是否「升级为多轮」 |
| 有项目目录、有 `Master_Control` | 多轮项目 | 读取主控 → 轮次日志 |
| 无归属 | 日常对话 | 按 `Daily-Backups/` 处理 |

---

## 无归属清理策略

| 归属 | Lifecycle | 清理策略 |
|:---|:---|:---|
| 无归属 | 临时 | 15天自动软删除（`Daily-Backups/TMP_*`） |
| 项目归属 | 阶段 | 多轮完结后手动确认删除 |

---

## 输出回执格式（无歧义时）

```
归属：<项目名>
落点：<P>/对话备份/
```

---

## 参考文档

- 统一框架定义：`mod-multi-round-framework.mdc`
- 备份流程：`flow-conversation-backup.mdc`
