---
description: 项目备忘录统一框架 — 支持多项目独立备忘和背景
glossary:
  layer: 2-模块执行层
  module: ProjectMemo
---

# 项目备忘录统一框架

## 适用范围

凡涉及 **查看项目备忘清单、追加备忘记录** 等操作均适用。

**执行通道**：默认使用 **Read** / **Write** / **StrReplace** 工具操作本地 Markdown 文件；不依赖网络。

**项目隔离**：每个项目的备忘录、背景信息完全独立存储，不混入同一路径。

---

## 抽象阶段结构（强制）

**原则**：每一轮 Agent **只推进一步**，根据用户指令分流。

| 阶段 | 名称 | 核心职责 | 下层工作流引用 |
|:---:|:---|:---|:---|
| **A** | 指令解析 | 解析项目名 + 区分查看/追加操作 | 见下方「阶段A细则」 |
| **B** | 文件定位 | 定位到项目专属备忘录文件 | 见下方「阶段B细则」 |
| **C** | 操作分型 | 按读取/追加分流到具体工作流 | 见下方「阶段C细则」 |

---

## 阶段 A — 指令解析

### 多项目指令格式

| 操作 | 指令格式 | 示例 |
|:---|:---|:---|
| **查看** | `XX项目备忘录` 或 `项目备忘录` | `469项目备忘录`、`项目备忘录` |
| **追加** | `记录XX备忘` 或 `记录备忘` | `记录469备忘`、`记录备忘` |

### 项目名解析规则

1. **明确指定**：指令中含项目名（如`469项目备忘录`→项目名`469_Sports`）
2. **上下文推断**：指令不含项目名时：
   - 检查当前对话是否在项目上下文中（读取最近的项目索引）
   - 若无法推断，询问用户"哪个项目的备忘录？"
3. **项目名规范化**：
   - 数字前缀自动补全：`469` → `469_Sports`
   - 全名直接使用：`469_Sports` → `469_Sports`

---

## 阶段 B — 文件定位

### 项目文件结构（每个项目独立）

```
<Project_Name>/
├── <Project_Name>_Project_Memo.md          # 项目备忘录（待复核清单 + 记录区）⭐
├── <Project_Name>_ProjectContext.md        # 项目主背景
├── <Project_Name>_Background_Pending.md    # 待处理背景
├── <Project_Name>_对话备份索引.md         # 对话备份索引
└── 对话备份/                                # 备份明细目录
```

### 主文件（动态路径）

- **路径模板**：`<Project_Name>/<Project_Name>_Project_Memo.md`
- **示例**：`469_Sports/469_Sports_Project_Memo.md`

### 文件初始化（不存在时）

若项目备忘录文件不存在，自动创建标准骨架：

```markdown
# <Project_Name> · 项目备忘录

> 用途：沉淀"已暂定但待复核"的项目背景信息，支持会话内随时追加与回看。

---

## 1) 当前待复核清单

| 编号 | 主题 | 当前暂定口径 | 待确认点 | 优先级 | 状态 |
|---|---|---|---|---|---|

---

## 2) 记录备忘（追加区）

> 规则：新信息按时间倒序追加，不覆盖历史。

| 日期时间 | 备忘内容 | 关联模块 | 来源 |
|---|---|---|---|
```

### 联动文件（可选）

当用户要求补全时，联动读取同项目下的：
- `<Project_Name>_ProjectContext.md`（项目主背景）
- `<Project_Name>_Background_Pending.md`（待处理背景）

---

## 阶段 C — 按操作分型

阶段 B 完成后，根据操作类型进入对应第三层工作流：

| 操作 | 核心步骤 | 执行前必须 | 工作流文件 |
|:---:|:---|:---|:---|
| **查看** | 读取主文件 → 展示待复核清单 → 展示最近记录 | 项目名已解析 | `flow-project-memo-read.mdc` |
| **追加** | 解析备忘内容 → 追加到 §2 → 更新清单状态（如适用） | 用户提供可落盘信息 + 项目名已解析 | `flow-project-memo-append.mdc` |

**注意**：工作流调用时必须传入 `project_name` 参数。

---

## 待复核清单结构

表格字段：

| 编号 | 主题 | 当前暂定口径 | 待确认点 | 优先级 | 状态 |
|:---:|:---|:---|:---|:---:|:---:|
| MEMO-001 | 示例主题 | 当前暂定口径说明 | 待确认的问题点 | 高 | 待确认 |

状态流转：
- 待确认 → 已确认（规则已生效）
- 追加备忘时可触发状态更新

---

## 别名约束

- 别名长度必须为 **2-10 个字**
- 用户提供别名不满足长度时，先提示并给出合规替代后再登记

---

## 文件保护（删除防护）

- 当用户要求"删除草稿/临时文件"时，默认**不删除**包含"备忘录"语义的文件（如 `*Memo*`、`*备忘录*`）
- 只有在用户**明确点名**要删除该备忘录文件时，才执行删除

---

## 与项目索引初始化的关系

**创建XX项目索引**指令（`project-index-bootstrap-commands.mdc`）：
- 初始化：`<XX>/<XX>_对话备份索引.md` + `<XX>/对话备份/`
- 备忘录文件**：**不自动创建**，首次使用 `XX项目备忘录` 时自动初始化

这样设计的原因：
- 不是所有项目都需要备忘录功能
- 首次使用时自动创建，保持项目目录整洁

---

## 三层架构速查

| 层级 | 文件 | 职责 |
|:---:|:---|:---|
| 第一层（网关） | `gateway-command-router.mdc` | 指令路由到本框架，解析项目名 |
| 第二层（框架） | `mod-project-memo-framework.mdc` | 本文件，定义阶段结构，动态文件定位 |
| 第三层（工作流） | `flow-project-memo-read.mdc` | 查看流程具体步骤（支持项目名参数） |
| 第三层（工作流） | `flow-project-memo-append.mdc` | 追加流程具体步骤（支持项目名参数） |

---

## 规则索引

- **项目备忘录模块**：
  - 第二层：`mod-project-memo-framework.mdc`（本文件，统一框架）
  - 第三层：`flow-project-memo-read.mdc`（查看流程）
  - 第三层：`flow-project-memo-append.mdc`（追加流程）
- **项目索引初始化**：`project-index-bootstrap-commands.mdc`（对话备份索引）
