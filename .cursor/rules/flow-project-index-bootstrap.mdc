---
title: 项目索引初始化流程（已废弃）
description: ⛔ 废弃于 2026-05-09，由 mod-multi-round-framework.mdc + flow-multi-round-upgrade.mdc 替代。原功能（创建项目目录+备份索引+主控文件）已收敛为多轮升级流程，不再使用独立的索引初始化入口。
glossary:
  layer: 已废弃
  deprecated: true
  replaced_by: [mod-multi-round-framework, flow-multi-round-upgrade]
alwaysApply: false
---

# ⛔ 本规则已废弃（2026-05-09）

> **替代**：`mod-multi-round-framework.mdc`（三方独立架构）+ `flow-multi-round-upgrade.mdc`（升级流程）。
> 原"创建XX项目索引"的初始化功能已收敛为多轮升级流程的一环，不再独立存在。

---

# 项目索引初始化流程

## 触发词

- 主指令：`创建XX项目索引`
- 别名：`创建项目索引`

---

## 执行步骤（三步）

### 步骤 1：解析项目名

1. 将 `XX` 解析为项目文件夹名（如 `DemoProject`）
2. 在工作区根目录检查是否存在 `<XX>/` 项目目录：
   - 若不存在 → 提示用户确认项目名或是否需要创建该目录
   - 若存在 → 继续

### 步骤 2：初始化结构

3. 初始化长记忆备份结构：
   - 索引文件：`<XX>/<XX>_对话备份索引.md`
   - 备份目录：`<XX>/对话备份/`
4. 若索引文件不存在则新建，使用以下标准骨架；若已存在则仅补齐缺失字段，不覆盖已有记录。

#### 索引骨架（标准）

- 标题：`# <XX> 对话备份索引`
- 说明区：
  - 用途：按"日期 + 时段 + 主题"检索历史对话备份
  - 时段规则：上午<14:00；下午14:00-20:00；晚上>=20:00
  - 边界提示：14:00/20:00/00:00 前后10分钟需人工确认
- 表头固定：`| 日期 | 时间 | 时段 | 主题 | 明细文件 |`

### 步骤 3：回执

初始化完成后回执：
- 已创建/已复用的索引文件路径
- 已创建/已复用的备份目录路径
- 一条可直接使用的示例指令：`备份当前对话`

---

## 失败兜底

- 若项目名包含非法路径字符或无法定位目录，先返回可修正建议（候选项目名），待用户确认后再创建。

---

## 脚本运行时约束（Python + Node.js）

若本指令后续实现为脚本化自动执行：

- 必须同时支持 Python 与 Node.js：Python 实现 `*.py`，Node.js 实现 `*.mjs` 或 `*.js`
- 推荐统一启动器优先 Python，失败后回退 Node.js（Windows：`cmd /c python ... || node ...`）
- 两个实现的输入参数、输出字段与行为必须一致
- 若本机缺少其中一个运行时，不得阻塞指令执行；应自动回退到可用运行时

---

## 与别名表的关系

- 别名映射见 `gateway-command-router.mdc`「当前关键别名」表
- 跨设备与路径硬编码约束见 `git-cross-device-and-secrets.mdc`
