---
description: 任务启动协议：强制网关，确保 Agent 在进入执行前完成边界对齐与结构探针。适用于所有非纯对话/非纯查询类任务。
  layer: 1-网关路由层 purpose: 任务理解（含Cynefin轻量判域）→拆解→材料清单对齐→读材料→提思路→确认，防止直接执行导致方向偏差
  sequence: 1.5-bis-KB（确认需求→交付物介质探针→拆解任务→材料清单对齐→知识脑前置查询→读材料→提思路→执行）
  pipeline_markers:
    rule: "Agent TIP *每一步完成后**必须在回复末尾输`[Step_ID]` 标记。required=true 的步骤（Step_2_KB 外）不可跳过——Guard 下次启动时自动扫描缺失标记并阻断
    close_check: "**收束前自检（强制）**：在最后一Step 5（执行与验证）收束前，Agent 必须回溯检查本8 步（Step_1/Step_1_5/Step_4/Step_4_bis/Step_2_KB/Step_2/Step_3/Step_5）是否均已输出完成标记或跳过标记。若有缺补充 `[Step_ID]（补标）` 后再结束。连2 次收束前未经 close_check Guard 启动时阻断
    format:
      done: "[Step_ID]"
      skip: "[Step_ID: reason]"
    definitions: ".cursor/workflow-definitions.json task_initiation"
  maturity:
    initial: 全量确认（Step 1-3 必做    mid: Step 1 必做，Step 4 Agent 自主，Step 3 仅复杂任    late: 高置信度跳过 Step 1，其Agent 自主
  downstream:
    - core/data/Decomposition-Log.jsonl (拆解决策记录)
    - core/data/User-Knowledge-Registry.jsonl (用户经验注册)
    - core/data/Active-Task-Tracker.json (任务parent/children/next_child)
    - core/data/Pending-Plan-Tracker.json (待办 source_task)
    - mod-decision-framework.mdc (决策框架 数据消费
alwaysApply: false
trigger: 用户发出非纯对话/非纯查询类的可执行任guard_replaceable: false
---
description: 任务启动协议：强制网关，确保 Agent 在进入执行前完成边界对齐与结构探针。适用于所有非纯对话/非纯查询类任务。

# 任务启动协议（Task Initiation Protocol
## 适用范围

当用户发出一*非纯对话、非纯查*的任务时（如"改这份简"根据模板PPT""调研并输出报），Agent 在进入实际执行前，按 **1.5-bis** 顺序走完。不可跳过
例外 1：用户明确说"不用确认直接"赶时间直接开时，可跳Step 1-3，但仍需执行 Step 4（拆解）Step 2（读材料），并在执行前简短标已跳过确认环节，直接执行"
例外 2：同会话内连续推进的同类子任务（试点消化 3 张卡"后的"继续消化第二），可跳Step 1（复述理解），但执行前须标注 P008 L 等级（如 `L1 告知执行，直接继续`）。若上下文发生实质变化（新约束、新产出要求、A≥A2），仍走完整 Step 1
---

## 协议步骤

### Step 1：复述理& 确认需
**目的**：确Agent 和用户对"要做什认知一致。需求理解偏= 结果必然偏差
**做法**1. **前置结构探针（CP-065 史蒂夫案例启发）**：在开始复述理解前，先对用户指令进行初步结构扫描，识别显式命令词、关键文件路径、意图转折信号，以搭建初步的「检索结构」。此步骤确保「结构先于内容」进行处理——这是记忆专家在处理海量信息前的共性策略2. **Cynefin 轻量判域（CP-101 启发*：判断该任务的因果可预测性——非完整五域分析，仅需判断   - **可预测（清晰/繁杂*：任务因果链明确，按协议执行即可
   - **不可预测（复混沌*：因果只能在事后回溯，需探路
   > 若判为「不可预测」：即使后续 P008 评估L0，亦建议「先做小范围探测再铺开」——域错了，再好的方法也反效果。
3. 用自然语言复述你对任务的理解，包含   - 产出物是什么（文件格式、交付标准）
   - 约束条件是什么（样式保留/内容优先/时间限制   - 可弹性空间是什么（哪些可以微调、哪些不能动4. `AskQuestion`（单选）确认，不要让用户打字确认
**示例**> "我理解这个任务是：把项目总结的内容写入简历，保留PDF 的双栏排版。对吗？还是说排版可以调整？"

**触发条件**：任务描述中存在一个以上的"可解读维。如果任务极简单（帮我读这个文），可跳过此步
---

### Step 1.5：交付物介质探针（Rendering Pipeline Probe
**目的**：在拆解任务之前，先判断最终交付物是否依赖外部渲染管线。LLM 胜任内容生成（SVG 设计、Mermaid DSL、布局逻辑），但渲染管线（编码、字体加载、PDF/PNG 转换）是 LLM 的脆弱点。此步骤确保在任务拆解前就明确渲染器选择，而非执行到一半才发现渲染不可靠
**为什么在 Step 1 之后、Step 4 之前**- Step 1 已确认最终交付物格式（如 PDF、PNG、SVG 文件）- Step 4 的拆解方案应该适配已确认的渲染器约束——如果某格式需要外部渲染引擎，子任务的工具选择和执行顺序都会受影响- 先知道渲染管线是否可靠，再拆解任务，避免拆出"在当前环境不可执的子任务
**做法**
1. **第一层：交付物介质判*（纯文本 vs 非纯文本
   基于 Step 1 确认的产出物格式，判断是否依赖外部渲染：

   | 交付物类| 是否需要渲染管| 处理方式 |
   |:---|:---|:---|
   | Markdown / 纯文/ 代码 | *** | LLM 原生输出，跳过后续探针，标注 `纯文本交付，无需渲染探针` |
   | JSON / JSONL / YAML | *** | Guard SchemaValidator 兜底，跳过探|
   | DOCX / PPTX | **是（轻量* | 内容LLM 生成，格式由 python-docx/pptx 封装；可靠degraded |
   | SVG / Mermaid / 架构| **是（重量* | 内容LLM 生成*渲染管线脆弱**（编码、字体、格式转换） |
   | PDF（从非文本源转换| *** | 不是 LLM 能力域，需外部转换|
   | 音频 / 视频 | *** | 需外部编码器，LLM 能力|

2. **第二层：渲染器可靠性检*（仅当交付物非纯文本时触发）

   若第一层判断需要渲染管线，Agent 需检`guard/src/p008/tool_selector.py` `RENDERER_RELIABILITY` 表（Guard 运行时返回的 `ToolSelectionResult`），判断该交付物格式在当前环境下是否*已验*的渲染器
   > **守则**：Agent 不自行评估渲染器的可靠性。可靠性由 Guard `RENDERER_RELIABILITY` 表（已验降权/不可未知）提供——Agent 只做「查询→照做」，不打分
   | 渲染器状| Agent 行为 |
   |:---|:---|
   | **verified**（已验证| 正常执行，使用该渲染器。在 Step 3 提案时标`渲染器已验证renderer_name>` |
   | **degraded**（降权） | 正常执行，但Step 3 提案时标`渲染器降权：<renderer_name> 可能存在格式漂移` |
   | **unreliable**（不可靠| **阻断**：告知用户该格式在当前环境下渲染不可靠，给出替代方案（外Skill / 手动渲染 / 降级为纯文本|
   | **unknown**（未知） | 标注 `渲染器状态未知：<renderer_name> 将尝试执行，如失败则按降级路径处理` |

3. **第三层：降级或接*（仅当渲染器不可靠或未知时触发）

   | 场景 | 决策 |
   |:---|:---|
   | 有已验证的外Skill 声明支持该格| **接管** Step 4 拆解时路由到Skill，子任务使用 Skill 的渲染管|
   | 无外Skill，但有已验证的兜底路| **降级** 走兜底路径（Edge 无头打印 SVG→PDF），标注 |
   | 无外Skill，也无兜底路| **阻断** 提示用户该格式在当前环境不可靠，建议手动处理或安装依|

   > **外部 Skill 路由**：Agent 检`Task-Type-Registry.json` 中该任务类型`skills.external` 字段。例`chart_diagram` 关联`skill-pretty-mermaid`——若当前平台 SVG 渲染不可靠，Agent 应在 Step 4 拆解时优先使用该 Skill
4. **探针结果记录**：在 Step 3 提案时，将探针结论纳入执行思路一并展示。格式：

   > `🔍 交付物介质探针：<交付物格 需要渲染管渲染<名称> 状<verified/degraded/unreliable> 决策=<正常执行/降级/阻断/接管>`

**跳过条件**：交付物为纯文本（Markdown/代码/JSON）→ 标注 `纯文本交付，无需渲染探针` 并跳过本步
**与下游联*- 探针结果决定 Step 4 的拆解方案——若需外部 Skill，在拆解时注明子任务由该 Skill 接管- 探针结果传给 Step 3 `mod-decision-framework.mdc` 评估——渲染管线不可靠可能提升 S 维度（降低自动化程度）
**设计原则**：此步骤不回答「LLM 能不能生成内容」——LLM 能生SVG/Mermaid/图表内容已经被验证。它只回答「生成的内容经过后续渲染管线后能不能稳定出」。LLM 的边界不发生在内容生成端，而发生在渲染管道端
---

### Step 4：拆解任务（先拆解，再读材料
**目的**：大任务拆成小步骤。拆解本身是对需求的第二次确认—我打算分这几步来
**为什么在 Step 2 之前**：知道子任务是什么，读材料才有目标性。先通读所有材料再拆任务容易陷入细节，注意力被无关内容稀释
**做法**1. 判断是简单任务还是复合任务：
   - 简单任务（改简历内）→ 单步骤，跳过拆解
   - 复合任务（如"调研并输PPT"）→ 拆成 2-3 个子任务，每个子任务有独立产出物
2. 子任务间若有依赖关系（A 产出B 输入），标注衔接。
3. 拆解结果向用户确认（`AskQuestion`4. 拆解后同步写入数据层   - `Active-Task-Tracker.json`：创建父任务条目，填`parent`/`children`/`next_child`
   - `Pending-Plan-Tracker.json`：每个子任务对应的待办填`source_task` 指向父任   - `Decomposition-Log.jsonl`：记录拆解决策（source=manual_review
**示例**> "我打算分两步> 市场调研（收集数分析 输出结构化结论）
> 模板套用（读模板结构 适配内容 PPT> 可以吗？"

### Step 4.1：拆解受阻时的策略（CP-065 瓶颈启发
若拆解后的某子任务在执行前评估为路径不可行：
- 不硬推，不增加复杂度——破解瓶颈的方法通常不是「更难」，而是「不同的方向」- 回到 Step 4，从另一个角度重新拆解该子任务的边界和路径- 参CP-065 的瓶颈策略：换编码模式、换工具链、换执行顺序，而非在同一方向上不断增加努力
---

### Step 4-bis：材料清单对
**目的**：拆解完任务后、实际读材料前，先列出需要读取的材料清单并确认。防止遗漏关键输入，也防止读了无关材料浪费注意力
**为什么在 Step 4 之后、Step 2 之前**：拆解后的每个子任务有明确的输入需求。材料清单应基于子任务需求反推，而非"把所有文件都读一
**做法**1. 基于 Step 4 的子任务拆解结果，列出每个子任务需要的输入材料
2. 标注每份材料的用途（用于哪个子任务的哪个环节。
3. `AskQuestion` 确认清单，格式：
   - 已知材料（用户已提供或可推断的）：列出并确认
   - 可能遗漏的材料（用户可能有但未主动给出的）：询问
4. 用户确认后，严格按照清单读取，不增不
**示例**> "基于拆解，我需要读取：
> **子任1（市场调研）**：用 WebSearch 收集公开数据
> **子任2（模板套用）**：你提供PPT 模板（结构探针）
> 
> 有没有其他材料需要我提前读的（如行业报告、数据源、竞品信息）

**跳过条件**：任务无输入材料（如WebSearch 任务）→ 标注"无材料依赖，跳过此步"
---

### Step 2-KB：知识脑前置查询（P008 预载入）

**目的**：在阅读任务材料之前，先用当前任务的 `task_type` + `domain` 查询知识脑，预加载相关协议到上下文。知识脑有覆降低 P008 评估A/C 维度，减少不必要的确认轮次
**执行时机**：Step 4-bis 材料清单对齐后、Step 2 读材料之前
**做法**1. 从当前任务提`task_type`（与 `Task-Type-Registry` `type_id` 对齐）和 `domain`（概念域，如 `BPMN` / `ProductManagement`）2. 调用 `core/knowledge/activation.md` *过滤1（自描述* 逻辑   - 粗筛：扫`protocols/` 中所`activation.task_types` 匹配的协   - 精筛：按 `concept_anchor` 概念空间碰撞
   - 快筛：按 `self_recital` 语义匹配，排除明显无   - 截断：单任务最多注5 条协。
3. 将匹配的协议 ID 列表填入该子任务对应P008 评估`kb_protocols_activated` 字段（供 `mod-decision-framework.mdc` 使用）4. 将协议的关键内容（`self_recital` + `decision_signal`）注入到当前上下文，供后Step 2/3/5 参考
**降级策略**- 知识脑未初始化或 `protocols/` 目录为空 跳过，标`KB 未就绪，跳过预载入`- 匹配结果0 记录 `kb_protocols_activated: []`（P008 不做 A/C 降级）- 匹配结果 > 0 记录协议 ID 列表，P008 评估A/C 各降一级（不低L0）
**与下游联*- Step 2-KB 产出`kb_protocols_activated` 传给 `mod-decision-framework.mdc` A/C 维度降级
- 任务收束`flow-behavior-auto-receipt.mdc` 对每个激活的协议执行四态有效性反
---

### Step 2：读取材& 结构探针

**目的**：在动手前彻底理解输入材料的结构，而不是边做边发现问题
**做法**1. 列出需要读取的材料清单（文件名 + 目的），向用户确认是否遗2. 读取材料时做"结构探针"   - PPT：检GROUP 嵌套层级、文本框是否独立、槽位数量与容量
   - DOCX：检测段表格/图片混合布局、分栏结   - PDF：检测排版结构（双栏/单栏、字体、字号）、文本提取质   - Markdown：检测标题层级、代码块、表格、图片引   - **I/O 约束扩展*：新任务类型（代配置/数据前端组件等）首次出现时，归纳对应的探针维度并补入本列表。不提前穷举，碰一个补一个。
3. 探针结果汇报——不等执行时才暴露问题：
   - 槽位不够 提前讨论微调方案
   - 结构复杂 提前讨论工具策略
   - 嵌套层深 提前讨论填原还是"新增文本框覆

**材料清单确认示例**> "我计划读取：A（用于子任务 1 的数据来源）、B（PPT 模板用于子任2 的结构探针）。有遗漏吗？"

---

### Step 3：提出执行思路 & 二次确认

**目的**：基于结构探针的结果，提出具体执行方案，确认细节后再动手
**做法**1. 基于 Step 2 的探针结果，提出每个子任务的执行思路   - 用什么工具（python-pptx? lxml? ZIP    - 模板如何适配（哪些页面对应哪些内容？有没有需要微复用/重排的？   - 预期会有哪些微调（如"模板槽位不够时会新增文本"某个版式需要复。
3 2. 确认原则*内容完整优先 > 模板严格匹配**。允许溢微调/页面重排/模板复用，但不允许内容错配或区域缺漏
3. 复杂任务 `AskQuestion` 逐条确认微调点。简单任一次性汇报思路后进入执
**跳过条件**：Step 2 未发现任何需要用户决策的歧义标注"无歧义，直接进入执行"并跳过此步
### Step 3-bis：SUCCES 轻量表达检查（方案提案后，执行前）

> 来源：CP-105 SUCCES 六原则。不是让方案徒增花哨，是让有逻辑的方案同时被记住
方案提案完成后、进入执行前，用以下三问快速自查（0 秒）
| # | 检查项 | SUCCES 原则 | 不通过|
|:---|:---|:---|:---|
| S | 方案的核心主张能*一句话**说清吗？ | Simple | 砍掉次要信息，补一今天想让你记住的是— |
| U | 15 秒有打破用户预期*钩子**吗？（反常识数据/惊讶问题/好奇缺口| Unexpected | 开头加一个反常识的事实或一个开放问|
| C | 用户能在脑中形成**画面**吗？（具体的数字/场景而非抽象概念| Concrete | 把抽象词替换为具体的时间/数字/动作 |

**不强制逐条通过**——但至少 1 条不通过时，标注 `📝 SUCCES 未达标：<S/U/C 哪条>`，让用户知道为什么方案可说了但没被记
**跳过条件**：纯执行任务（如"把这个值改42"）或无需说服用户的纯查询任务 跳过
---

### Step 5：执& 适配

**目的**：按前三步对齐的方案执行，不做额外假设
**做法**1. Step 4 的子任务顺序执行
2. 每完成一个子任务，验证产出物是否达标（内容完整、无错配、无缺漏。
3. 验证通过后再进入下一个子任务，不全部跑完再回头补"
4. 优先逐页验证再批量执行——先1 页确认逻辑正确，再铺开到其余页

---

## 与信任度 / 决策权的联动

| 信任度阶| Step 1 | Step 4 拆解 | Step 3 思路确认 |
|-----------|:---:|:---:|:---:|
| 初期（现在） | 必做 | 必做，须确认 | 复杂任务|
| 中期 | 必做 | Agent 自主拆解，仅确认歧义 | 跳过简单任|
| 远期 | 高置信度跳过 | Agent 自主 | 极少需|

此演进路径与决策框架（`mod-decision-framework.mdc`）的 L0→L3 对齐—*确认深度随能力成长递减，而非永远事无巨细**
### P008 决策框架嵌入
| 嵌入| 时机 | 动作 |
|--------|------|------|
| **Step 2-KB 预载* | Step 4 拆解后、Step 2 读材料前 | 调用 `activation.md` 过滤1 匹配协议 填入 `kb_protocols_activated` 字段。若有匹配项 `mod-decision-framework.mdc` 评估A/C 各降一级（不低L0|
| **Step 4 拆解* | 复合任务拆出子任务后 | 对每个子任务调用 `mod-decision-framework.mdc` 评估 7 维度 推导 L0-L3。L≥L2 时在 Step 3 提案中展示维度评分（KB 降级效果|
| **Step 5 执行* | 执行途中用户插话/修正/新指| **不需要中P008 重评** 每个新用户指= 新任务边界。Agent 终止或暂停当前任务，新指令独立走 Step 1→P008。原任务标记 paused + 残余待办 |
| **Step 1 判类** | 需求确认时 | 判定为原复合/大系统级。原子任务跳过拆解，P008 Step 3 提案时顺|

> 任务级评估决定默认行为（L0-L3），命令级兜底（`flow-high-risk-safety.mdc` 红色警戒）保持不变——即使任务评估为 L0，某条命令触Rev3（递归删除）时仍强制覆盖为确认
### 大系统级：递归自相
大系统级不是独立的协议——它是本协议.5-bis）的**多层递归应用**
```
Layer 0（任务判类）
  Step 1: 这是大系统吗？→ 
Layer 1（架构层）── 对标协议的宏观应  Agent 复述理解 + 主动提出一级拆解方  拆解：列出关键决策点（≤5 个）
  bis 材料对齐：已有架构文+ 相关案例
  结构探针 & 逐点讨论：每轮只攻一个决策点，清零再  提案 & 降级判断：可操作任务清单出现时降级为复合任务
  执行落地

Layer 2（模块层）── 每个决策点独立走一轮微缩协  理解 讨论 定案 执行
```

**降级触发**：当架构讨论产出"可操作的任务清单"（谁做什么、产出物、验证标准）自动降级为复合任走正常拆执行流程。若仍高不确定提议 PoC（最小探路）PoC 出结果后回退再对齐
**Agent 默认行为**：Agent 主动提出一级拆解方案。用户如有不同思路，在对话中直接碰撞调整。协议不预判用户是否有预设方案
**防误判护*- "我不确定怎么做（策略模糊）≠ 大系统级" 策略模糊Step 3（提案）解决
- 只有连做出来是什么都不完全清时才升级为大系统- **判类只在 Step 1 做一次，子任务不重新判类**——除非发现父任务判类时未被预见的新系统边
---

## 违规处理

- Agent Step 1 未执行的情况下直接进入执行，且用户后续纠正了需求理解偏视为此协议未遵守，应记录`known_issues`
- Agent 在未做结构探针（Step 2）的情况下选择工具，导致多轮迭代失视为"工具选择前置条件不满，写`method-reliability-registry` `known_issues` 字段
