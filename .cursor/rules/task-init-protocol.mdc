---
description: 任务启动协议 — 强制网关，确保 Agent 在进入执行前完成边界对齐与结构探针。适用于所有非纯对话/非纯查询类任务
glossary:
  layer: 1-网关路由层
  purpose: 任务理解→拆解→材料清单对齐→读材料→提思路→确认，防止直接执行导致方向偏差
  sequence: 1→4→4-bis→2→3→5（确认需求→拆解任务→材料清单对齐→读材料→提思路→执行）
  maturity:
    initial: 全量确认（Step 1-3 必做）
    mid: Step 1 必做，Step 4 Agent 自主，Step 3 仅复杂任务
    late: 高置信度跳过 Step 1，其余 Agent 自主
  downstream:
    - 10-Topics/Decomposition-Log.jsonl (⑤ 拆解决策记录)
    - 10-Topics/User-Knowledge-Registry.jsonl (⑥ 用户经验注册)
    - 10-Topics/Active-Task-Tracker.json (③ 任务树 parent/children/next_child)
    - 10-Topics/Pending-Plan-Tracker.json (④ 待办 source_task)
    - mod-decision-framework.mdc (① 决策框架 — 数据消费者)
alwaysApply: false
trigger: 用户发出非纯对话/非纯查询类的可执行任务
---

# 任务启动协议（Task Initiation Protocol）

## 适用范围

当用户发出一个**非纯对话、非纯查询**的任务时（如"改这份简历""根据模板做 PPT""调研并输出报告"），Agent 在进入实际执行前，按 **1→4→4-bis→2→3→5** 顺序走完。不可跳过。

例外：用户明确说"不用确认直接做""赶时间直接开始"时，可跳过 Step 1-3，但仍需执行 Step 4（拆解）和 Step 2（读材料），并在执行前简短标注"⚡ 已跳过确认环节，直接执行"。

---

## 协议步骤

### Step 1：复述理解 & 确认需求

**目的**：确保 Agent 和用户对"要做什么"认知一致。需求理解偏差 = 结果必然偏差。

**做法**：
1. 用自然语言复述你对任务的理解，包含：
   - 产出物是什么（文件格式、交付标准）
   - 约束条件是什么（样式保留/内容优先/时间限制）
   - 可弹性空间是什么（哪些可以微调、哪些不能动）
2. 用 `AskQuestion`（单选）确认，不要让用户打字确认。

**示例**：
> "我理解这个任务是：把项目总结的内容写入简历，保留原 PDF 的双栏排版。对吗？还是说排版可以调整？"

**触发条件**：任务描述中存在一个以上的"可解读维度"。如果任务极简单（如"帮我读这个文件"），可跳过此步。

---

### Step 4：拆解任务（先拆解，再读材料）

**目的**：大任务拆成小步骤。拆解本身是对需求的第二次确认——"我打算分这几步来做"。

**为什么在 Step 2 之前**：知道子任务是什么，读材料才有目标性。先通读所有材料再拆任务容易陷入细节，注意力被无关内容稀释。

**做法**：
1. 判断是简单任务还是复合任务：
   - 简单任务（如"改简历内容"）→ 单步骤，跳过拆解
   - 复合任务（如"调研并输出 PPT"）→ 拆成 2-3 个子任务，每个子任务有独立产出物
2. 子任务间若有依赖关系（A 产出是 B 输入），标注衔接点
3. 拆解结果向用户确认（`AskQuestion`）
4. 拆解后同步写入数据层：
   - `Active-Task-Tracker.json`：创建父任务条目，填充 `parent`/`children`/`next_child`
   - `Pending-Plan-Tracker.json`：每个子任务对应的待办填入 `source_task` 指向父任务
   - `Decomposition-Log.jsonl`：记录拆解决策（source=manual_review）

**示例**：
> "我打算分两步：
> ① 市场调研（收集数据 → 分析 → 输出结构化结论）
> ② 模板套用（读模板结构 → 适配内容 → 填 PPT）
> 可以吗？"

---

### Step 4-bis：材料清单对齐

**目的**：拆解完任务后、实际读材料前，先列出需要读取的材料清单并确认。防止遗漏关键输入，也防止读了无关材料浪费注意力。

**为什么在 Step 4 之后、Step 2 之前**：拆解后的每个子任务有明确的输入需求。材料清单应基于子任务需求反推，而非"把所有文件都读一遍"。

**做法**：
1. 基于 Step 4 的子任务拆解结果，列出每个子任务需要的输入材料
2. 标注每份材料的用途（用于哪个子任务的哪个环节）
3. 用 `AskQuestion` 确认清单，格式：
   - 已知材料（用户已提供或可推断的）：列出并确认
   - 可能遗漏的材料（用户可能有但未主动给出的）：询问
4. 用户确认后，严格按照清单读取，不增不减

**示例**：
> "基于拆解，我需要读取：
> **子任务 1（市场调研）**：用 WebSearch 收集公开数据
> **子任务 2（模板套用）**：你提供的 PPT 模板（结构探针）
> 
> 有没有其他材料需要我提前读的（如行业报告、数据源、竞品信息）？"

**跳过条件**：任务无输入材料（如纯 WebSearch 任务）→ 标注"⚡ 无材料依赖，跳过此步"。

---

### Step 2：读取材料 & 结构探针

**目的**：在动手前彻底理解输入材料的结构，而不是边做边发现问题。

**做法**：
1. 列出需要读取的材料清单（文件名 + 目的），向用户确认是否遗漏
2. 读取材料时做"结构探针"：
   - PPT：检测 GROUP 嵌套层级、文本框是否独立、槽位数量与容量
   - DOCX：检测段落/表格/图片混合布局、分栏结构
   - PDF：检测排版结构（双栏/单栏、字体、字号）、文本提取质量
   - Markdown：检测标题层级、代码块、表格、图片引用
   - **I/O 约束扩展点**：新任务类型（代码/配置/数据库/前端组件等）首次出现时，归纳对应的探针维度并补入本列表。不提前穷举，碰一个补一个。
3. 探针结果汇报——不等执行时才暴露问题：
   - 槽位不够 → 提前讨论微调方案
   - 结构复杂 → 提前讨论工具策略
   - 嵌套层深 → 提前讨论是"填原框"还是"新增文本框覆盖"

**材料清单确认示例**：
> "我计划读取：A（用于子任务 1 的数据来源）、B（PPT 模板用于子任务 2 的结构探针）。有遗漏吗？"

---

### Step 3：提出执行思路 & 二次确认

**目的**：基于结构探针的结果，提出具体执行方案，确认细节后再动手。

**做法**：
1. 基于 Step 2 的探针结果，提出每个子任务的执行思路：
   - 用什么工具（python-pptx? lxml? ZIP 层?）
   - 模板如何适配（哪些页面对应哪些内容？有没有需要微调/复用/重排的？）
   - 预期会有哪些微调（如"模板槽位不够时会新增文本框""某个版式需要复用 3 次"）
2. 确认原则：**内容完整优先 > 模板严格匹配**。允许溢出/微调/页面重排/模板复用，但不允许内容错配或区域缺漏
3. 复杂任务 → `AskQuestion` 逐条确认微调点。简单任务 → 一次性汇报思路后进入执行

**跳过条件**：Step 2 未发现任何需要用户决策的歧义点 → 标注"⚡ 无歧义，直接进入执行"并跳过此步。

---

### Step 5：执行 & 适配

**目的**：按前三步对齐的方案执行，不做额外假设。

**做法**：
1. 按 Step 4 的子任务顺序执行
2. 每完成一个子任务，验证产出物是否达标（内容完整、无错配、无缺漏）
3. 验证通过后再进入下一个子任务，不做"全部跑完再回头补"
4. 优先逐页验证再批量执行——先做 1 页确认逻辑正确，再铺开到其余页

---

## 与信任度 / 决策权的联动

| 信任度阶段 | Step 1 | Step 4 拆解 | Step 3 思路确认 |
|-----------|:---:|:---:|:---:|
| 初期（现在） | 必做 | 必做，须确认 | 复杂任务做 |
| 中期 | 必做 | Agent 自主拆解，仅确认歧义 | 跳过简单任务 |
| 远期 | 高置信度跳过 | Agent 自主 | 极少需要 |

此演进路径与决策框架（P008）的 L0→L3 对齐——**确认深度随能力成长递减，而非永远事无巨细**。

---

## 违规处理

- 若 Agent 在 Step 1 未执行的情况下直接进入执行，且用户后续纠正了需求理解偏差 → 视为此协议未遵守，应记录到 `known_issues`
- 若 Agent 在未做结构探针（Step 2）的情况下选择工具，导致多轮迭代失败 → 视为"工具选择前置条件不满足"，写入 `method-reliability-registry` 的 `known_issues` 字段
