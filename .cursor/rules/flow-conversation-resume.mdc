---
description: 恢复并续聊流程 — 按归属路由，多轮项目先读主控再轻确认
glossary:
  layer: 3-工作流执行层
  module: Conversation
  depends: [mod-conversation-framework, mod-multi-round-framework, flow-conversation-routing]
---

# 恢复并续聊流程

## 触发词

- 主指令：`继续对话`
- 带项目名：`继续XX项目` / `继续Demo` / `继续Earth Library`
- 口语兼容：`继续4月23日下午的对话` / `继续X月X日的对话`

---

## 执行步骤（固定六步）

### 步骤 1：时间解析

- 年份省略规则：省略年份默认当前年份
- 时段判定：（< 14:00 上午 / 14:00-19:59 下午 / >= 20:00 晚上）
- 缺失时段：先追问再继续

### 步骤 2：归属判断

按 `flow-conversation-routing.mdc` 判定：
1. 若用户带了项目名（`继续XX项目`）→ 直接归属该项目
2. 否则从上下文推 → 无法判定则询问

### 步骤 3：分流

| 归属结果 | 后续步骤 |
|:---|:---|
| 无归属 | 按日常流程：索引检索 → 恢复上下文 → 续聊引导（原步骤4-6） |
| 有项目目录、无 `Master_Control` | 通过 AskQuestion `[升级为多轮] [稍后再说]` 询问 → Y 执行 `flow-multi-round-upgrade.mdc` |
| 有项目目录、有 `Master_Control` | 进入步骤 4（多轮流程） |

### 步骤 4：读取主控（多轮专属）

1. Read `<P>/<P>_Master_Control.md`
2. 提取最近一轮轮次日志（§8 最末条目）
3. 回显：

```
已读取检查点：
- 上轮范围：<章节/阶段/环节>
- 未完成项：<1-3条>
- 下轮起点：<下轮起点内容>

待你确认：按上轮起点继续，是否直接开始？
```

### 步骤 5：轻确认 + 继续

- 用户确认 Y → 按主控下轮起点继续，锁定范围
- 用户指定新范围 → 以用户新范围为准
- 范围外内容仅登记到待处理池，不展开

### 步骤 6：继续交流

围绕确认的范围继续对话，每轮输出固定结构（问题清单 / 可执行方案 / 建议改写 / 本轮结论 / 下轮起点）。

---

## 多轮项目输出约定

```
=== 已恢复上下文 ===
（上轮结论摘要 + 最近轮次日志）

=== 当前可继续事项 ===
（来自上轮未完成项 + 下轮起点）

=== 建议下一步 ===
（基于下轮起点的具体建议）

开始吧。
```

---

## 与日常续聊的区别

| 对比项 | 日常续聊 | 多轮续聊 |
|:---|:---|:---|
| 索引来源 | `Daily-Backups/日常备份索引.md` | `Master_Control` 轮次日志 |
| 开场方式 | 展示摘要 → 用户选 | 读取主控 → 轻确认 |
| 范围锁定 | 无强制锁定 | 强制按主控范围 |
| 输出结构 | 三段式引导 | 固定四段（问题/方案/改写/结论+下轮） |

---

## 参考文档

- 统一框架：`mod-conversation-framework.mdc`
- 多轮框架：`mod-multi-round-framework.mdc`
- 归属判断：`flow-conversation-routing.mdc`
- 升级流程：`flow-multi-round-upgrade.mdc`
- 检查点确认：`kernel-runtime.mdc` §3
