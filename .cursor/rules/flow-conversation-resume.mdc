---
description: 恢复并续聊流按归属路由，多轮项目先读主控再轻确认
glossary:
  layer: 3-工作流执行层
  module: Conversation
  depends: [mod-conversation-framework, mod-multi-round-framework, flow-conversation-routing]
guard_replaceable: false
---

# 恢复并续聊流

## 触发

- 主指令：`继续对话`
- 带项目名：`继续XX项目` / `继续Demo` / `继续Earth Library`
- 口语兼容：`继续4。
3日下午的对话` / `继续X月X日的对话`

---

## 执行步骤（固定六步）

### 步骤 1：时间解

- 年份省略规则：省略年份默认当前年
- 时段判定：（< 14:00 上午 / 14:00-19:59 下午 / >= 20:00 晚上
- 缺失时段：先追问再继

### 步骤 2：归属判

`flow-conversation-routing.mdc` 判定
1. 若用户带了项目名（`继续XX项目`）→ 直接归属该项
2. 否则从上下文无法判定则询

### 步骤 3：分

| 归属结果 | 后续步骤 |
|:---|:---|
| 无归| 按日常流程：索引检恢复上下续聊引导（原步骤4-6|
| 有项目目录、无 `Master_Control` | 通过 AskQuestion `[升级为多轮] [稍后再说]` 询问 Y 执行 `flow-multi-round-upgrade.mdc` |
| 有项目目录、有 `Master_Control` | 进入步骤 4（多轮流程） |

### 步骤 4：读取主控（多轮专属

1. Read `<P>/<P>_Master_Control.md`
2. 提取最近一轮轮次日志（§8 最末条目）
3. 回显

```
已读取检查点
- 上轮范围章节/阶段/环节>
- 未完成项1-3
- 下轮起点下轮起点内容>

待你确认：按上轮起点继续，是否直接开始？
```

### 步骤 5：轻确认 + 继续

- 用户确认 Y 按主控下轮起点继续，锁定范围
- 用户指定新范以用户新范围为准
- 范围外内容仅登记到待处理池，不展开

### 步骤 6：继续交

围绕确认的范围继续对话，每轮输出固定结构（问题清/ 可执行方/ 建议改写 / 本轮结论 / 下轮起点）

---

## 多轮项目输出约定

```
=== 已恢复上下文 ===
（上轮结论摘+ 最近轮次日志）

=== 当前可继续事===
（来自上轮未完成+ 下轮起点

=== 建议下一===
（基于下轮起点的具体建议

开始吧
```

---

## 与日常续聊的区别

| 对比| 日常续聊 | 多轮续聊 |
|:---|:---|:---|
| 索引来源 | `Daily-Backups/日常备份索引.md` | `Master_Control` 轮次日志 |
| 开场方| 展示摘要 用户| 读取主控 轻确|
| 范围锁定 | 无强制锁| 强制按主控范|
| 输出结构 | 三段式引| 固定四段（问方案/改写/结论+下轮|

---

## 参考文

- 统一框架：`mod-conversation-framework.mdc`
- 多轮框架：`mod-multi-round-framework.mdc`
- 归属判断：`flow-conversation-routing.mdc`
- 升级流程：`flow-multi-round-upgrade.mdc`
- 检查点确认：`kernel-runtime.mdc` §3
