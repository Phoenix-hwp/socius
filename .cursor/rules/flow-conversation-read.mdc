---
description: 读取历史对话流程 — 按日期时段检索并展示摘要
glossary:
  layer: 3-工作流执行层
  module: Conversation
  depends: [mod-conversation-framework]
---

# 读取历史对话流程

## 触发词

- 主指令：`读取 X年X月X日，上午/下午/晚上的对话`
- 别名：`查对话`

口语兼容：
- `读取4月23日下午的对话`（同年份可省略年份）
- `继续X月X日的对话`（缺少时段时先追问）

---

## 执行步骤（固定四步）

### 步骤 1：时间解析

1. **年份处理**：若未写年份，默认当前年份（仅同年可省略）
2. **时段处理**：
   - 上午：< 14:00
   - 下午：14:00 - 19:59
   - 晚上：>= 20:00
3. **缺失追问**：若口语指令缺少时段，先追问"上午、下午还是晚上？"

### 步骤 2：索引检索

在 `Daily-Backups/日常备份索引.md` 中按日期+时段匹配：

- **命中 1 条**：直接进入步骤 3
- **命中多条**：展示主题列表，请用户回复编号选择
- **未命中**：明确告知，给出相邻时段建议

### 步骤 3：明细读取

读取选中的明细文件，提取：
- 核心结论
- 未完成事项
- 待确认事项
- 下轮衔接点（如有）

### 步骤 4：摘要展示

回执格式（三段）：

```
【主题】XXX
【时间】YYYY-MM-DD 时段

=== 核心结论 ===
...

=== 未完成/待确认 ===
...

=== 下轮衔接点 ===
...
```

---

## 多条命中处理

当同日期同时段命中多条记录时：

1. 展示编号清单（1~n）
2. 每行显示：编号 + 主题 + 时间
3. 请用户只回复数字编号
4. 选定后进入步骤 3

---

## 未命中处理

明确告知：
- "未找到 YYYY-MM-DD 时段 的备份记录"
- 给出建议：相邻时段 / 相邻日期

---

## 参考文档

- 统一框架定义：`mod-conversation-framework.mdc`
