---
description: Earth Library 检索流程 — 知识查询的具体步骤
glossary:
  layer: 3-工作流执行层
  module: EarthLibrary
  depends: [mod-earth-library-framework]
---

# 检索流程

## 触发时机

1. **主动查询**：用户输入 `搜索图书馆`、`查找知识` + 关键词
2. **自动补充**：图书馆启用时，回答前自动检索相关知识点

---

## 执行步骤（固定五步）

### 步骤 1：解析查询条件

**关键词提取**：
- 从用户问题中提取核心关键词
- 支持多关键词（逗号或空格分隔）

**可选筛选条件**：
| 条件 | 示例 |
|:---|:---|
| 标签筛选 | `--tag "产品设计" --tag "高置信度"` |
| 置信度阈值 | `--confidence_threshold "中"` |
| 时间范围 | `--start_date 2026-04-01 --end_date 2026-05-01` |
| 最大结果数 | `--max_results 5` |

### 步骤 2：执行检索

**调用脚本**：
```bash
cmd /c "python Earth_Library/scripts/search_library.py --q \"关键词\" --max_results 5 || node Earth_Library/scripts/search_library.mjs --q \"关键词\" --max_results 5"
```

**检索维度**（按优先级）：
1. 标题匹配（library_index.json 的 title 字段）
2. 关键词字段匹配（library_index.json 的 keywords 字段）
3. 正文匹配（cards.jsonl 的 body_md 字段）
4. 标签匹配（cards.jsonl 的 tags 字段）

### 步骤 3：关联推荐（可选）

若检索结果较少，执行关联推荐：
```bash
cmd /c "python Earth_Library/scripts/get_related.py --card_id \"xxx\" || node ..."
```

**关联类型**：
- 关键词相交
- 标签相交
- 引用关联

### 步骤 4：结果排序与过滤

**排序规则**：
1. 置信度（高 > 中 > 低）
2. 匹配度（标题匹配 > 关键词匹配 > 内容匹配）
3. 时间（新 > 旧）

**过滤规则**：
- 默认排除低置信度（除非用户明确要求）
- 自动去重（基于 `relations.jsonl` 的疑似重复标记）

### 步骤 5：结果展示

**回执格式（命中时）**：
```
Earth Library 检索结果：
- 查询：关键词
- 命中：N 条（高置信度 M 条）

相关卡片：
1. 【标题】（置信度：高 | 标签：xx, yy）
   摘要：...
   ID：cards.jsonl 中 id 字段

2. 【标题】（置信度：中 | 标签：aa, bb）
   摘要：...
   ID：cards.jsonl 中 id 字段

推荐关联：基于知识网络，还可参考...
- xxx（关键词相交）
- yyy（标签相交） — 来自 relations.jsonl
```

**回执格式（未命中时）**：
```
Earth Library 检索结果：
- 查询：关键词
- 命中：0 条

建议：
- 尝试其他关键词（如：xxx, yyy）
- 该知识可能尚未入库，可执行「存入图书馆」
```

---

## 与其他项目集成

### 在回答中引用

当图书馆启用时，回答流程：
1. 先用模型知识+网络信息回答
2. 检索 Library 补充知识点
3. 引用格式："根据此前沉淀的知识（Library/xxx），..."

### 主动推送提示

入库新知识点时，若检测到与某项目相关，回执提示：
```
该知识可能与以下项目相关：
- `<YourProject>`（标签匹配示例：领域关键词）
- 可考虑更新项目备忘录
```

---

## 参考文档

- 统一框架定义：`mod-earth-library-framework.mdc`
- 架构设计：`Earth_Library/Earth_Library_Architecture.md`
