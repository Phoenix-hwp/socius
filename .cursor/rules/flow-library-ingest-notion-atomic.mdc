---
description: Notion 原子入库流程 — 将 Notion 页面树拆分为独立卡片（一页一卡）
glossary:
  layer: 3-工作流执行层
  module: EarthLibrary
  depends: [mod-earth-library-framework]
---

# Notion 原子入库流程

## 适用范围

当用户希望将 **Notion 中的书籍/课程/知识体系** 按章节/主题拆分为 **独立卡片** 时使用。

**与常规入库的区别**：

| 模式 | 粒度 | 内容策略 | 网状结构 |
|:---:|:---|:---|:---|
| **原子入库（本流程）** | 一页一卡 | 概括+摘录+链接 | 章节/主题间独立连边 |
| **归纳入库（旧模式）** | 整树一卡 | 全文汇总 | 以整本书为单一节点 |

**选择原则**：
- 若需 **拆解书中知识点、建立概念级关联** → 使用本流程（原子入库）
- 若需 **保留整书概览、快速归档** → 使用 `notion_drill_earth_library.py`（归纳入库）

---

## 触发词

- **主指令**：`Notion原子入库` / `原子入馆`
- **自然语言**：`把 Notion 这本书拆成一张张卡片入库`

---

## 执行步骤（固定五步）

### 步骤 1：参数确认

**必须提供**：

| 参数 | 说明 | 示例 |
|:---:|:---|:---|
| `page-id` | Notion 根页面 ID 或 URL | `11f299d0-5ba8-8347-81f3-81f070cb0b5a` |
| `book-title` | 书籍/主题名称（用于上下文） | `《商业模式新生代》` |

**可选参数**：
- `--depth`: 钻取深度（默认 2，即根→子页面→孙页面）
- `--dry-run`: 预览不实际入库

### 步骤 2：页面树拉取

调用 `notion_drill` 遍历页面树，获取：
- 每个页面的标题、URL、摘要
- 页面层级结构（depth）
- 父子关系

### 步骤 3：逐页生成原子卡片

**每张卡片模板结构**：

```markdown
---
Lifecycle: 阶段
Title: {页面标题}
Type: {书籍/章节/知识点}
Source: Notion 知识库 - {书名}
SourceMode: notion_page
SourceURL: {Notion URL}
Confidence: 高
Keywords: {自动提取}
Tags: {自动标签}
NotionPageId: {ID}
---

# Summary
{页面首句摘要或标题}

# Details
<!-- earth-library:notion-atomic -->

## 核心要点
（待补充：提炼3-5点核心结论或方法论）

## 关键摘录
（待补充：少量原文摘录，注明页码或段落位置）

## 溯源
- Notion原文: [{标题}]({URL})
- 来源: {书名}
- 层级: 书籍 ＞ 章节

<!-- /earth-library:notion-atomic -->

## 本地补充
（在此添加关联思考、实践案例、后续待办等）

# Related
- (待关联：其他相关卡片)
```

**关键设计**：
- `<!-- earth-library:notion-atomic -->` 包裹区：**机器写入、用户不直接编辑**
- `## 本地补充` 区：**用户自由编辑，下次同步时保留**
- **不存储全文**，只保留链接和待补充的概括区

### 步骤 4：结构化关系记录

自动写入 `Relations_Index.md`：
- 每张卡片的入库记录（便于追溯）
- 可选：父子关系标注（人工后续补充强语义边）

### 步骤 5：执行与回执

**调用脚本**：

```bash
python Earth_Library/scripts/notion_atomic_ingest.py \
  --page-id "{id}" \
  --book-title "《书名》" \
  --depth 2
```

**成功回执格式**：

```
Notion 原子入库完成：
- 书籍/主题: 《书名》
- 总页面数: N
- 成功入库: N 张卡片
- 卡片路径示例:
  1. Knowledge_Cards/YYYYMMDD-HHMMSS_第一章.md
  2. Knowledge_Cards/YYYYMMDD-HHMMSS_第二节.md
  ...
- 关系索引: Relations_Index.md（已更新）

下一步建议：
1. 逐张卡片补充「核心要点」和「关键摘录」
2. 在 Relations_Index.md 中添加章节间的强语义关联
3. 使用「搜索图书馆」验证关联检索效果
```

---

## 卡片生命周期建议

### 初始状态（机器生成）
- Summary: 自动提取的首句或标题
- 核心要点/关键摘录: 占位提示，待人工补充
- Notion 链接: 已填充

### 完善状态（人工补充后）
- 核心要点: 用户提炼的 3-5 点结论
- 关键摘录: 精选原文+位置
- 本地补充: 关联思考、实践案例

### 关联强化（可选）
在 `Relations_Index.md` 中人工添加：
```markdown
| 日期 | 来源条目 | 目标条目 | 关系类型 | 关系说明 |
|---|---|---|---|---|
| 2026-05-08 | `Knowledge_Cards/...第一章.md` | `Knowledge_Cards/...第二章.md` | precedes | 章节顺序 |
| 2026-05-08 | `Knowledge_Cards/...商业模式.md` | `Knowledge_Cards/...价值主张.md` | prerequisite | 概念依赖 |
```

---

## 约束与最佳实践

### 空间效率
- **单卡建议 < 2KB**（不含本地补充区）
- 大量原文留在 Notion，本地只留**入口+概括**

### 检索效率
- 卡片轻量 → 全文检索更快
- 结构化关系 → 关联推荐更准

### 与 Notion 的协同
- Notion 作为 **「完整档案库」**
- Earth Library 作为 **「知识提炼层+网状索引」**
- 需要深入时：点击卡片中的 Notion 链接跳转

---

## 故障排查

| 现象 | 原因 | 解决 |
|:---|:---|:---|
| 子页面未生成卡片 | depth 设置不足 | 增加 `--depth` |
| 卡片内容为空 | Notion 页面本身无文本块 | 检查源页面 |
| 关系未建立 | 首次入库无既有卡片可关联 | 正常，后续卡片会逐步建立 |

---

## 相关文件

- 执行脚本: `Earth_Library/scripts/notion_atomic_ingest.py`
- 架构文档: `Earth_Library/Earth_Library_Architecture.md` §「两种入库模式对比」
- 父框架: `.cursor/rules/mod-earth-library-framework.mdc`
- 归纳入库（对比）: `.cursor/mcp/notion_drill_earth_library.py`
