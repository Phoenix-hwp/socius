---
description: Notion「改」操作 — 更新页面内容的具体步骤
alwaysApply: false
glossary:
  layer: 3-工作流执行层
  module: Notion
  operation: update
  depends: [mod-notion-precondition, mod-notion-crud-framework]
guard_replaceable: false
---

# Notion 更新页面工作流

## 适用范围

对 Notion 现有页面执行 **内容更新** 操作。支持两种更新策略：**清空后重写** 或 **局部合并更新**。

**统一骨架**：网络（阶段A）→ 目录（阶段B）→ **本工作流（阶段C-改）**。全流程见 `mod-notion-crud-framework.mdc`。

---

## 前置要求

- 已完成 **阶段 A**（网络确认 **`Y`**）— 见 `mod-notion-precondition.mdc`
- 已完成 **阶段 B**（目录落点 parent 已选定）— 见 `flow-notion-select-parent.mdc`

---

## 步骤（固定6步）

**原则**：每一轮 Agent **只推进一步**，每步只向用户索要 **当前步所需的一个答案**。高风险步骤（更新）须 **策略选择 + 变更预览 + 口令确认**。

| 步骤 | 名称 | Agent 行为 | 关键约束 |
|:---:|:---|:---|:---|
| **1** | **定位目标** | 用户提供 **标题或关键词** → 在 parent 下解析唯一对象 | 命中多条时展示编号清单 |
| **2** | **正文来源** | 确认更新内容的来源 | 本地文件 / 用户口述 / 检索摘要 |
| **3** | **更新策略** | 通过 AskQuestion 让用户选择：`[清空后重写] [局部合并]` | 清空后重写须显著提示风险 |
| **4** | **变更预览** | 策略1：展示将写入的**全文**；策略2：展示 **diff/合并说明 + 关键片段** | 须足以判断未误删独有条款 |
| **5** | **口令确认** | 通过 AskQuestion 弹出 `[确认更新] [修改预览]` | 无同意方可执行 |
| **6** | **执行更新** | **自动通道选择**：<br>1. 判定内容长度（字符数 > 2000 或预估 blocks > 10）→ **长文**<br>2. **长文**：调用 `notion_upsert_workflow.py`（断点续传保护，支持策略1/2）<br>3. **短文**：优先 MCP；脚本兜底须注明原因<br>4. 写入成功后回执 **页面链接**、块数、策略、续传状态等 | 按选定策略执行 |

---

## 定位子流程（步骤1）

详见 `flow-notion-locate-target.mdc`：

1. 用户提供用于检索的 **标题或关键词**（部分匹配，大小写不敏感）
2. 在阶段 B 选定的 **parent** 下解析：
   - **parent 为 database**：用数据库 **title 类型属性** 做 `contains` 查询
   - **parent 为 page**：枚举该页 **直接子页面**（`child_page`），用标题 **包含** 匹配
3. **零条**：提示收窄关键词或改用 URL/ID；不得臆造 ID
4. **一条**：进入步骤2（正文来源）
5. **多条**：展示 **编号清单**（标题 + 可选 URL/ID 片段），请用户 **只回复编号** 选定唯一目标

**说明**：复杂嵌套（非直接子级、多库联动）无法仅靠「父级 + 标题」唯一定位时，应请用户给出 **Notion URL** 或 **32 位 ID**。

---

## 更新策略分支（步骤3）

在 **目标页面已唯一** 且 **正文来源已明确** 之后、展示最终预览之前，须请用户 **只选一**（一轮一问）：

| 选项 | 名称 | Agent 行为要点 | 风险说明 |
|:---:|:---|:---|:---|
| **`1`** | **清空后重写** | 等价于「先清空（归档）该页 **顶层正文块**，再写入预览中的正文」| ⚠️ **显著提示风险**：Notion 上原有、但未包含在本次正文中的段落将 **不再保留** |
| **`2`** | **局部合并更新** | `fetch`/读取 Notion 当前正文，与本地正文 **对照 diff**，按对齐锚点生成合并稿，仅对应对块/段落追加或替换 | 无稳定锚点时须改为策略1或请用户指定锚点/分块范围 |

### 策略实现

- **策略1（清空后重写）**：
  - 优先 MCP：`fetch` → 按工具能力移除或归档子块 → 写入
  - 也可用脚本 `update_page` + `replace: true`

- **策略2（局部合并）**：
  - 依赖 **稳定锚点**：如 `##` 章节标题、表格主键列、固定 Callout 区块
  - 参考：`10-Topics/Behavior-Preferences-Sync-Playbook.md`（错别字表对比合并、章节口径等）
  - **无稳定锚点**时：不得强行「局部」——须改为 **`1`**，或请用户指定锚点/分块范围

---

## 固定口令

- **`确认更新`** — 步骤5通过 AskQuestion `[确认更新] [修改预览]` 获取许可
- 用户改用明确同意句（如「确定更新」）须在上下文中 **唯一表示同意当前预览**，否则 Agent 应简短确认后再执行

---

## 异常处理

| 异常场景 | 处理方式 |
|:---|:---|
| 零条命中（定位）| 提示收窄关键词或改用 URL/ID |
| 多条命中（定位）| 展示编号清单请用户选择（1/2/3...）|
| 策略2但无稳定锚点 | 请求用户指定锚点/分块范围，或改为策略1 |
| 用户未确认（步骤5）| 停留在预览阶段，询问修改意见 |
| API 更新失败（短文/MCP）| 保留预览，提示重试或转为本地保存 |
| 长文更新中断（策略1归档后/写入中）| 自动保存状态到 `Daily-Backups/.notion_state/`，重跑即可从断点续传 |
| 追加中断（策略2）| 同上，状态文件跟踪已追加块数 |

---

## 插件参数约束

- 禁止臆测字段名；数据库写入须 **先 fetch schema**
- 块级更新须基于 **`fetch` 返回的块类型与 ID**（若工具支持）逐步提交
- **优先 MCP**；若用脚本，须在回执中注明 **兜底原因**

---

## Agent 自检（更新前）

- [ ] 若为对话内 **首条** Notion 操作，是否已取得 **`Y`**（`mod-notion-precondition.mdc`）？
- [ ] 是否已完成 **阶段B 目录落点**（`mod-notion-crud-framework.mdc`）？
- [ ] **改** 是否已完成 **定位**且目标 **唯一**？
- [ ] **改** 是否已选定 AskQuestion 的更新策略（清空后重写 / 局部合并），且清空后重写已向用户提示 **正文丢失风险**（若适用）？
- [ ] **改** 是否 **优先 MCP**；若用脚本，是否将在回执中注明 **兜底原因**？
- [ ] 是否已展示 **变更预览** 且已通过 AskQuestion `[确认更新]`（或无二义的同意）？
