---
description: 系统审计框架 — 全系统架构/数据/编码/技能健康检查，输出结构化报告
Lifecycle: 长期
Created: 2026-05-16
Updated: 2026-05-23
glossary:
  layer: 2-模块框架层
  purpose: 周期审计系统整体健康度——覆盖架构规范、数据治理、注册表对齐、编码规范、过渡方案检查、代码质量（外部技能对标）。输出结构化报告供讨论决策
  trigger_points:
    - "用户指令：系统检查 / 自检 / 审计 / 巡检系统 / 健康检查"
    - "过渡方案登记文件中阈值触发项拉取后自动提示"
  downstream:
    - .cursor/rules/script-coding-constraints.mdc（编码规范参考）
    - data-governance-standards.mdc（数据治理宪法）
    - 10-Topics/Task-Type-Registry.json（注册表对齐）
    - Skills_Library/skill-registry.json（技能健康检查）
    - Transition-Plan-Registry.json（过渡方案检查）
    - .cursor/skills/addyosmani/security-and-hardening/SKILL.md（D6-S 安全对标清单）
    - .cursor/skills/addyosmani/performance-optimization/SKILL.md（D6-P 性能对标清单）
    - .cursor/skills/addyosmani/code-review-and-quality/SKILL.md（D6-C 五轴审查对标清单）
    - .cursor/skills/addyosmani/code-simplification/SKILL.md（D6-M 可维护性对标清单）
alwaysApply: false
trigger: 用户输入 系统检查 / 自检 / 审计 时触发
guard_replaceable: framework
---

# 系统审计框架

## 触发词

- 主指令：`系统检查`
- 别名：`自检`、`审计`、`巡检系统`、`健康检查`

## 意图

对仓库进行一次全系统健康扫描，覆盖架构规范、数据治理、注册表对齐、编码规范、过渡方案、代码质量（外部技能对标）六大维度，输出结构化报告与待处理项清单。

---

## 审计维度

### D1：架构规范

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| layer 字段覆盖 | 所有规则文件是否声明 `layer`（横切规则同样需要） | `.cursor/rules/*.mdc` |
| layer 命名一致 | L1/L2/L3 命名是否统一 | `.cursor/rules/*.mdc` |
| 跨层耦合标注 | 双向引用的规则是否标注 `cross-layer-coupling: intentional` | `.cursor/rules/*.mdc` frontmatter |
| frontmatter 完整性 | `Lifecycle` / `Created` / `glossary.layer` 等必填字段 | `.cursor/rules/*.mdc` |

### D1b：系统四层能力架构

> 与 D1（规则文件内部分层 gateway/mod/flow）正交。四层定义见 `Knowledge-Brain/framework.md` §一。

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| 四层文件覆盖 | 每层必须有一个以上落地文件 | 四层定义文件 |
| 网关层入口 | `gateway-command-router.mdc` + `kernel-runtime.mdc` 存在 | 文件存在性检查 |
| 能力层入口 | `task-type-registry.md` + `skill-registry.json` + `mod-decision-framework.mdc` 存在 | 文件存在性检查 |
| 执行层入口 | `Skills_Library/` 非空 + `skill-registry.json` 有已注册技能 | 目录 + 注册表 |
| 认知层入口 | `Knowledge-Brain/` 八大子文件存在（classifier / extract-templates / template-generator / concept-anchor / activation / concept-tree.json / synthesizer / activation-log.jsonl） | 文件存在性检查 |
| 认知层接口链路 | 能力层引用认知层的链路是否断裂（`activation.md` ↔ `task-init-protocol.mdc` Step 2-KB 预载入 ↔ `mod-decision-framework.mdc` A/C 降级 ↔ `flow-behavior-auto-receipt.mdc` §C#13 反馈） | 跨文件关键词搜索 |
| 认知层 Step S | `classifier.md` §六 Step S 的描述是否存在（全域巡检维度 S1-S6） | Read 文件——确认从「管线内审」升级为「全域知识驱动巡检」 |
| 层命名一致性 | 四层命名是否统一（网关层/能力层/执行层/认知层） | `Knowledge-Brain/framework.md` |

### D2：数据治理

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| JSONL schema 统一 | 同一文件内所有行共享同一字段集合（可选字段用 null） | 所有 `.jsonl` |
| JSON 长文本 | 字符串字段 >50 字是否外迁到 `.md` | 所有 `.json` |
| 权威源唯一 | 同一数据是否存在双写副本 | `library_index.json` ↔ `cards.jsonl` 等 |

### D3：注册表对齐

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| Task-Type 双表对齐 | JSON 与 MD 的 type_id 是否一一对应 | `Task-Type-Registry.json` ↔ `task-type-registry.md` |
| skill task_types | 技能声明的 task_type 是否在注册表中存在 | `skill-registry.json` ↔ 双表 |
| type_id 孤儿 | 注册表中 type_id 有无技能覆盖（uncovered 标记） | 双表 `skills.uncovered` |

### D4：编码规范

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| 硬编码路径 | 脚本内是否有绝对路径/固定盘符 | `.py`、`.mjs`、`.sh`、脚本文件 |
| 跨目录导入 | 是否存在 `sys.path.insert` 动态跨目录导入 | `.py` |
| 阈值硬编码 | 脚本内是否写死业务阈值（2000/10/50 等） | `.py`、`.mjs` |

### D5：过渡方案

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| 阈值触发 | 过渡方案中 `current >= value` 的项是否已触发 | `Transition-Plan-Registry.json` |
| stale 检测 | 长时间（>30天）未触发的过渡方案是否需要移除/调整 | `Transition-Plan-Registry.json` |

### D6：代码质量（外部技能对标）

> **设计意图**：D1-D5 面向本仓库规则体系健康度，D6 扩展为**通用代码质量维度**——对标 Addy Osmani 22 技能套件中的安全/性能/可审查/可维护四项清单。审计时 Agent 逐项对照清单检查仓库代码，但不执行技能脚本（轻量清单式对标）。对标基准文件见 frontmatter `downstream`。

#### D6-S：安全（对标 `security-and-hardening`）

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| OWASP Top 10 覆盖 | 是否存在已知注入点（SQL/NoSQL/OS 命令拼接）、XSS 未转义输出、越权访问 | `.py` / `.js` / `.ts` 文件中搜索 `execute(.*\+` / `innerHTML` / `eval(` 等模式 |
| 密钥安全 | 是否存在硬编码密钥/Token/密码在脚本中（非 `notion.env` / `.env.example`） | Grep 搜索 `password\|secret\|api_key\|token.*=` 并排除模板文件 |
| 认证与授权 | API/脚本入口是否有认证校验（Notion Token 除外） | `.py` / `.js` 文件搜索认证中间件/校验逻辑 |
| 输入校验 | 外部输入（用户参数/API 响应/文件内容）是否在边界处校验 | `.py` 文件搜索 `sys.argv` / `input()` / `request.get` 后是否有校验 |
| 速率限制 | 对外暴露的接口是否有速率限制 | `notion_drill.py` / `run_notion_mcp.py` 等对外脚本 |
| 依赖审计 | `requirements.txt` / `package.json` 中依赖是否有已知漏洞 | `npm audit` / `pip check`（需本机运行，若不可用则标记跳过） |

#### D6-P：性能（对标 `performance-optimization`）

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| N+1 查询 | 循环内是否存在数据库/API 独立查询 | `.py` 文件中搜索 `for.*in` 内嵌 `fetch` / `query` / `search` |
| 无分页列表 | 全量返回列表的接口/函数是否缺少分页参数 | `.py` / `.mjs` 文件搜索 `return.*list` / `find_all` 无 `limit` 约束 |
| 热路径大对象 | 高频路径上是否创建了大量临时对象或重复 I/O | 审计时审视核心脚本（如 `notion_drill.py`）的主循环 |
| 同步阻塞 | Python 脚本中是否存在同步 I/O 阻塞（无 async/await 的长时间操作） | `.py` 文件搜索 `time.sleep` / 同步文件读写 / `requests.get` 无超时 |
| 缓存策略 | 重复计算/查询是否有缓存（如 Knowledge-Brain 的 activation 机制） | `Knowledge-Brain/activation.md` / 脚本中的缓存装饰器 |

#### D6-C：可审查性（对标 `code-review-and-quality`）

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| 变更大小 | 近期 git diff 中单次变更是否超过 ~300 行（超过则建议拆分） | `git log --oneline -5` + `git diff --stat HEAD~5` |
| 五轴覆盖 | 最近 PR/变更是否包含正确性/可读性/架构/安全/性能五项审查记录 | `Decision-Log.jsonl` / `Behavior-Fit-Log.jsonl` 中审查类记录 |
| 死代码 | 是否存在未引用的函数/类/模块、注释掉的代码块、过时的兼容 shim | `.py` / `.mjs` 文件搜索 `# TODO: remove` / `# deprecated` / `@deprecated` |
| 命名规范 | 是否存在无语义变量名（`temp` / `data` / `result` / `obj`） | `.py` 文件搜索模式 |
| 审查记录 | 最近 N 次变更是否有对应的审查决策日志（无记录 = 审查缺失信号） | `Decision-Log.jsonl` 最近 20 条 |

#### D6-M：可维护性（对标 `code-simplification`）

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| 函数体过大 | 是否存在超过 200 行的单个函数/方法 | `.py` 文件按函数边界统计行数 |
| 重复代码 | 是否存在超过 10 行高度相似的代码块（跨文件或同文件内） | 审计时 Agent 审视近期修改的文件 |
| 魔力数字 | 是否存在语义不明的硬编码常量（非配置常量） | `.py` 文件中排除已声明常量的 magic number |
| 嵌套深度 | 是否存在超过 4 层的条件/循环嵌套 | `.py` 文件审视 |
| 依赖膨胀 | `requirements.txt` / `package.json` 是否有未使用的依赖 | `pip check` / `npx depcheck`（需本机运行，若不可用则标记跳过） |

#### D6 审计执行细则

1. **加载对标清单**：审计启动时 Read `D6-S` / `D6-P` / `D6-C` / `D6-M` 四个对标技能各自的 §Verification / §Checklist / §Red Flags 章节获取最新检查项（不硬编码于此框架中，确保技能更新自动生效）
2. **执行方式**：Grep + Glob 静态搜索为主，可在输出报告后提示用户运行 `npm audit` / `pip check` 等需本机环境的命令（此类检查项标记为 `🔧本机`）
3. **输出格式**：D6 的 4 个子维度在审计报告中分别占一行
4. **范围控制**：D6 仅对本仓库代码（`.cursor/mcp/*.py`、`.cursor/hooks/*.py`、`Skills_Library/scripts/*.py`、`phoenix-agent/`）执行，不扩展到用户引入的外部项目

### D7：认知自省与三层追问

> **来源**：CP-074（认知心理学四种互补视角）。当系统遇到复杂问题或设计决策时，按三层追问避免分析层次混淆。

| 检查项 | 说明 | 数据源 |
|:---|:---|:---|
| 功能层覆盖 | 系统的每个主要组件是否有明确的"功能目的"说明（为什么存在——进化视角） | `Knowledge-Brain/framework.md` / `.cursor/rules/*.mdc` frontmatter |
| 算法层覆盖 | 系统的每个主要操作是否有明确的"怎么做"说明（流程/步骤——信息加工+计算机视角） | `Skills_Library/skill-registry.json` / `flow-*.mdc` |
| 物质层覆盖 | 系统的物理依赖是否有明确声明（文件路径/磁盘/网络——神经科学视角） | `config.json` / `device-execution-rules.json` |
| 层次混淆检测 | 是否存在用"物质层"解释"功能层"的现象（如"这个组件设计成串行是因为文件 I/O 的限制"——本质上是功能设计而非物理约束） | 审计时按三层框架审视关键模块的描述 |

**三层追问标准格式**（遇到任何复杂问题时）：

```
为什么有这个组件/流程？（功能目的）
  ↓
这个组件/流程怎么运作？（算法/步骤）
  ↓
这个组件/流程用什么支撑？（物理实现）

---

## 输出格式

```
## 系统审计报告（<日期>）

| 维度 | 检查项数 | 通过 | 问题 | 严重 |
|:---|:---|:---|:---|:---|
| D1 架构规范 | N | N | N | N |
| D1b 四层架构 | N | N | N | N |
| D2 数据治理 | N | N | N | N |
| D3 注册表对齐 | N | N | N | N |
| D4 编码规范 | N | N | N | N |
| D5 过渡方案 | N | N | N | N |
| D6-S 安全 | N | N | N | N |
| D6-P 性能 | N | N | N | N |
| D6-C 可审查 | N | N | N | N |
| D6-M 可维护 | N | N | N | N |
| D7 认知自省 | N | N | N | N |

### 严重项（阻断）
- [ ] <问题描述> — <位置> — <修复建议>

### 问题项
- [ ] <问题描述> — <位置> — <修复建议>

### 过渡方案触发
- [ ] T01 — 阈值已满足（当前: N >= 3），建议讨论升级
```

---

## 执行约束

- 每次审计后输出结构化报告 + 待处理清单
- 发现阻断级问题（如关键文件缺失、数据双写）立即提示，不自动修复
- 非阻断问题由用户决策是否本轮修复
