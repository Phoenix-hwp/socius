---
description: 将本 Obsidian 库作为 Cursor 长期记忆与知识上下文来源
alwaysApply: true
---

# Cursor_Knowledge（Obsidian 长记忆库）

- **库根路径**：将本 Obsidian 库作为文件夹在 Cursor 中打开时，**工作区根目录**即知识库根（与 `CURSOR_PROJECT_DIR` 一致）；勿写死某台机器上的盘符路径。
- **用途**：存放个人/项目的长期上下文、决策记录、术语表、流程与复盘，供 Cursor Agent 在回答时检索与引用。

## Agent 行为约定

1. **何时查库**：当问题涉及「之前约定过的事」「项目背景」「个人偏好」「持久化事实」或用户显式提到知识库/笔记时，应优先在本仓库内用搜索/阅读工具查找相关 Markdown（及少量文本附件），再作答。
2. **如何引用**：引用知识库内容时给出 vault 内相对路径或完整路径（如 `Topics/xxx.md`），并区分「笔记原文」与「推断」。
3. **如何更新**：若用户要求把结论写入知识库，在合适目录新建或编辑 `.md` 文件，使用清晰标题与必要 frontmatter；避免无故改写历史笔记，重大变更可另建附录或新条目。
4. **Obsidian 语义**：理解 `[[双向链接]]`、标签与 frontmatter；未解析链接可结合文件名与上下文推断。

## 建议目录（可随用随建）

- `00-Inbox/`：临时捕获
- `10-Topics/`：主题笔记
- `20-Projects/`：项目长期上下文
- `90-Templates/`：模板

若某路径尚不存在，可按上述结构创建后再写入。